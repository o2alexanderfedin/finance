---
context: default
phase: between 15 (complete) and 16 (not started)
task: 6
total_tasks: 6
status: phase_complete_clean_boundary
last_updated: 2026-08-12T05:32:32.627Z
---

# Where this stops

**Nothing is mid-flight.** Phase 15 is complete, verified 5/5, code-reviewed with every finding
fixed, and **merged to `develop`**. The working tree is clean, there are no open PRs, and the
suite is green. This is a boundary between phases, not a pause inside one.

**The next step is a decision, not a continuation.** See "Human Actions Pending".

> Deliberately placed at `.planning/.continue-here.md`, not inside `15-realism-polish-and-upstream/`.
> A handoff sitting in a completed phase's directory reads as unfinished work — that exact
> misdirection is why STATE.md records the stale phase-09 and phase-10 handoffs being deleted.

## Critical Anti-Patterns

Every row below was discovered through an actual failure in this session, not predicted.

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| **Vacuous proof** | A proof that is green and *permanently unable to fail*. Happened **twice**: 15-02's year-genericity gate regex could not match its own required positive control (`[A-Za-z_$][\w$]*[Yy]ear` needs ≥5 chars, so a bare `year` never matched); 15-03's never-executes proof called a decomposed helper instead of the composed export, so mutating `fjsCheck` to *actually run the guest program* left the unit suite green at 3110/3110. | **blocking** | Mutate the **production body** the proof guards and watch that specific leaf go red, then restore byte-identical. Mutating the fixture is not the same test. AGENTS.md already requires this; the failures came from skipping it. |
| **Trusting the plan's own literals** | Plan text contained values that could not produce their own stated expectations. 15-04's plan used `'100000'`/`'150000'` where `centsFromString` has no implied cents scaling, so its own expected columns were unreachable. 15-06's hand-typed dialect count predated 15-02 adding a dialect. | **blocking** | Before implementing, verify each literal against the function that will consume it. Every one of the six Phase 15 plans contained at least one real defect. |
| **Grepping for a runtime surface** | The MCP tool count was reported wrong three times (7, then 8, then 12; the answer is **13**) because finance-local `toolEntry(` declarations miss the upstream `casToolRegistry`/`evoToolRegistry` spread into `financeMcpHandlers`. This produced a *false headline claim* — "no document-write tool is registered" — when `evo_add` has always been there. | **blocking** | For anything the server advertises, **start `node index.js` and ask it** (`initialize` → `notifications/initialized` → `tools/list`). `cas-refresh-cross-process.test.js` has the spawn-and-speak pattern to copy. Do not derive it from source. |
| **Two sources of truth drifting** | `REQUIREMENTS.md`'s traceability table read `Pending` for PROV-06/PROV-08 while their own checkboxes read `[x]`. This is the **identical defect Phase 13 closed out with**, recurring one phase later. | advisory | When marking any requirement complete, update the checkbox **and** the table row, then sweep *all* complete requirements rather than only the ones you noticed. |
| **Double-counted proof metric** | `node --test 2>&1 \| grep -c '^✔ import("./fjs/'` inflates by exactly 2× whenever the `functionalscript` submodule is initialized — its own `all.test.js` re-scans the same tree. Reports 1690 where the truth is 845. | advisory | Always de-duplicate: `... \| sed 's/ ([0-9.]*ms)$//' \| sort -u \| wc -l`. STATE.md's "Test metrics" section carries the corrected command. |
| **`npx` in a real-process test** | `npx functionalscript` can never resolve — the package's bin is named `fjs` — so it fell through to npm's registry-touching path, 84% of one test's wall clock, and made `npm test` red under parallel load. Pre-existing since Phase 5. | advisory | Spawn `node` against an absolute path into `node_modules`. See `fjsCliPath` in `cas-refresh-cross-process.test.js`. Fixed in `e36ef1a`; do not reintroduce. |

<current_state>
Phase 15 (Realism Polish and Upstream) is **done and merged**.

- Branch `develop` @ `c54bfa7`, in sync with origin, working tree clean
- `npm test`: **exit 0, 6296/6296**, 907 de-duplicated project-local proofs
- Open PRs: **0**. Worktrees: **1** (the main checkout; two stale ones were removed after
  verifying they were clean)
- Requirements: **82 of 93 complete (88.2%)**
- Phases: **15 of 19 complete**
</current_state>

<completed_work>

This session, in order:

- **Resumed** at Phase 13 complete but entirely unpushed (70 commits, local-only)
- **Found `npm test` RED** and fixed the root cause — a pre-existing Phase 5 defect where
  `npx functionalscript` could not resolve and reached the network (`e36ef1a`)
- **Shipped Phase 13** — PR #62, later merged
- **Ran Phase 15 end to end**: smart discuss (16 decisions) → research → validation strategy →
  6 plans across 2 waves → execution → code review → 3 fixes → verification 5/5
- **Merged both** — PR #62 (`3f45a57`) and PR #63 (`c54bfa7`) into `develop`
- **Cleaned up** — removed two stale worktrees after confirming they were clean; verified both
  straggler branches' commits were already in `develop` and `main`
- **Added `.planning/CAPABILITIES.md`** — a snapshot of what the system can actually do today,
  measured from a live `tools/list` and a real `npm test`

Phase 15 delivered all five of its requirements: **PROV-08, TAX-17, MCP-09, PROV-06, DOC-16**.
Proofs went 845 → 907.

**Phase 15 added zero new `human_needed` debt** — the verifier fetched the live IRS
`i1040sd.pdf` and closed the phase's only manual-only item rather than deferring it. Phases 10
and 13 both failed to do that.
</completed_work>

<remaining_work>

- **Phase 16 — The Orphan Ingestion Island** (MAINT-01). Blocked on an owner decision.
- **Phase 17 — Documentation Truth Pass** (MAINT-02…05). Depends on nothing. Ready to plan.
- **Phase 18 — Dependency and Duplication Debt** (MAINT-06…08). Depends on nothing.

All three are T3 backlog, unordered, and independent of each other.
</remaining_work>

<decisions_made>

- **Phase 14 skipped, Phase 15 run instead** (owner, 2026-08-11). Phase 15's declared
  `Depends on: Phase 14` was **waived, not satisfied**.
- **No TY2024 IRS figures transcribed.** Research fetched IRS `i1040sd.pdf` (2025 rev.) p9: the
  Capital Loss Carryover Worksheet consumes four prior-year *result* figures and **zero** tax
  parameters, so the "minimal TY2024 subset" the phase was originally sized around does not
  exist. Year-genericity is proven structurally instead — which also avoided stacking a second
  batch of unverified IRS constants on the batch Phase 13 already left open.
- **An absent carryover document is a legitimate `0n`, not a refusal.** Only a document that
  *exists* with a missing or inconsistent field refuses, naming the field. The strict reading
  would have made every first-year filer unable to file.
- **DOC-16 was local adoption, not an upstream contribution** — the registry already shipped in
  the pinned 0.43.1. No cross-repo PR was opened or needed.
- **Stop at `develop`; do not merge to `main`** (owner, 2026-08-12).
</decisions_made>

<blockers>
- **Phase 16 needs an owner decision** — see Human Actions Pending. Not a technical blocker;
  Phases 17 and 18 can proceed without it.
- **EXEC-13 / PROV-04 / PROV-05 have no owner** — orphaned by the Phase 14 skip.
</blockers>

## Human Actions Pending

1. **Decide Phase 16 (blocking for that phase only).** Binary: either wire
   `fjs/document/1099int/from_ocr` so an import graph from `index.js` reaches it, **or** delete it
   along with `fjs/document/ocr_amount` and `fjs/document/subject`. ROADMAP.md is explicit:
   *"Research: No — the decision needs the author's intent, not investigation."* Guessing wrong
   means deleting working tested code, or wiring up something you wanted gone.
   *If wired*, TEST-03 requires a real-process integration call. *If removed*, `todo/plan.md`'s
   Track B must be amended so the next reader does not rebuild it.

2. **Decide the fate of EXEC-13 / PROV-04 / PROV-05.** These are the **last non-MAINT
   requirements** in the milestone and all three belong to skipped Phase 14. When 16–18 finish,
   v1 will have every T3 item done and these three T2 items homeless. A write-path-only phase
   would not need your filed return — that was the part of Phase 14 that genuinely required you.

3. **Decide release timing.** `develop` is **110 commits ahead of `main`**. A release means
   bumping `package.json` from `0.12.0`, writing CHANGELOG entries for Phases 13 and 15, merging
   to `main`, and tagging. Note an existing test asserts `serverInfo.version` equals
   `package.json`'s version, so the bump is a code change, not just metadata.

4. **Standing verification debt** (unchanged, no scheduled owner):
   - Phases **05, 06, 07** have **no VERIFICATION.md** under any name
   - Phase **10** — Tax Computation Worksheet: cent-exact or whole-dollar?
   - Phase **13** — TY2025 figures never checked against the printed IRS PDFs

   Items 2 and 3 were both to be resolved by Phase 14's acceptance run.

## Required Reading (in order)

1. **`AGENTS.md`** — the single source of truth for conventions, layout, style, and testing.
   `CLAUDE.md` only points here. Read it in full before writing anything.
2. **The anti-pattern table above** — particularly the vacuous-proof row. Two shipped this
   session.
3. **`.planning/CAPABILITIES.md`** — what the system can actually do today, measured rather than
   asserted. Read this before answering any "can it…?" question.
4. **`.planning/STATE.md`** — especially "Test metrics", which carries the corrected
   proof-counting command.
5. **`.planning/phases/15-realism-polish-and-upstream/15-REVIEW.md`** — the CR-01 finding is a
   good model of the defect class that survives six agents reporting success.

## Infrastructure State

- **No background processes.** No servers, watchers, or agents running.
- **git**: `develop` @ `c54bfa7`, in sync with origin, tree clean, 0 open PRs, 1 worktree.
- **Submodule**: `functionalscript` @ `cc93a3ca` (`heads/main-27-gcc93a3ca`), consistent with
  `package-lock.json` and the pinned `0.43.1`.
- **Suite runtime**: full `npm test` is ~8 minutes (481s, 6296 tests). Use targeted
  `node --test <file>` per task; reserve the full run for gates.

<context>
The through-line of this session was that **green does not mean verified**. Nine real defects
surfaced in Phase 15, and not one was caught by reading code. They were caught by mutating
production and watching a specific test go red, by re-deriving a plan's literals against the
function that consumes them, and — in the case of my own capability report — by finally starting
the server and asking it instead of grepping for the answer.

Two of those defects were proofs that could never fail. That is worse than having no test,
because it reads as coverage in every summary and every review that follows.

The machinery works when it is actually run: research falsified a phase premise before any code
was written, the plan-checker caught two warnings, the code reviewer found a critical that six
executors missed and reproduced it twice, and the verifier mutated production three times and
closed a manual item by fetching the source PDF. Keep using it that way.
</context>

<next_action>
Answer the Phase 16 wire-or-delete question, then run `/gsd-plan-phase 16`.

If you would rather not decide now, run `/gsd-plan-phase 17` instead — **Phase 17 (Documentation
Truth Pass) depends on nothing, needs no owner decision, and already has six concrete success
criteria written.** It is the highest-value unblocked work available, and several of its criteria
target exactly the kind of stale-claim drift this session kept tripping over.
</next_action>
