---
context: default
phase: none — milestone v2 complete, v1.0.0 released
task: 0
total_tasks: 0
status: released
last_updated: 2026-08-17T20:55:00.000Z
---

# Where this stops

**v1.0.0 is released. All 120 requirements are complete. Nothing is in flight.**

```
develop  5f75fcd  ═  origin/develop
main     66c0873  ═  origin/main        tag v1.0.0 pushed
2242 tests · 0 fail · 2218 project-local proof leaves · tsc clean
2 branches · 1 worktree · 0 open PRs · 0 stashes · nothing unpushed
```

Re-derive rather than trusting the numbers above:

```sh
npm test 2>&1 | grep -c '^✔ import("./fjs/'        # 2218 — the ONLY stable count
grep -cE '^- \[x\] \*\*[A-Z]+-[0-9]+' .planning/REQUIREMENTS.md   # 120
grep -cE '^- \[ \] \*\*[A-Z]+-[0-9]+' .planning/REQUIREMENTS.md   # 0
```

> **Never gate on `npm test`'s total** (AGENTS.md line 127). It includes ~2,100 vendored submodule
> proofs and moves with submodule state. The submodule is deliberately **de-initialised**; if you
> find it checked out, someone left it that way and every total you read is inflated. That happened
> in this session and inflated every figure reported for hours.

<current_state>

- All four reference taxpayers in `.planning/PERSONA-COVERAGE.md` compute a full Form 1040 through
  the product path — stored program, sandboxed execution, every figure citing its document box,
  byte-identical reproduction.
- **13 MCP tools, unchanged through twelve phases.** A `finance_compute_1040` tool would let the
  agent stop authoring programs, which is what the architecture exists to prevent. `tools/list`
  returning 13 is the check.
- `functionalscript` pinned at **0.43.1** — deliberately; see below.
</current_state>

## What is genuinely still open — none of it a requirement

Four `fjs/todo/` notes, each with a **tested** reason and a recipe, not a shrug:

| Note | Why it is open |
|---|---|
| `upstream-mjs-migration.md` | 0.44/0.45 dropped the `.js` emit. Migrating all 396 files was *measured* on a throwaway snapshot and still leaves **288 errors**; every mechanical fix is a cast, an `any`, or a redeclared type — all three forbidden. The one-line-per-type upstream fix is specified with its retirement condition. |
| `tax-return-report-source-k1-routes-unexercised.md` | The stored program's text and its function twin are parallel implementations. Three route lines in the *text* are executed by no fixture. The cheap version of the fixture is a **fake pass** — the reducer's fallthrough returns unchanged — and the real one moves four hand-typed figures including a Tax Table band lookup. |
| `schedule-b-omits-k1-interest-and-dividends.md` | Newly reachable: K-1 interest can push 1040 line 2b past Schedule B's $1,500 threshold while its own line 4 (1099-INT only) stays below it. Not fixed because printed Part I is a list of payers, not a total. |
| `upstream-mcp-protocol-version-negotiation.md`, `-node-spawn-effect`, `-total-match-dispatch` | Re-verified still open against upstream 0.45.0. |

## Critical anti-patterns — every row is an actual failure

| Pattern | What happened | Prevention |
|---|---|---|
| **`git push` succeeding while doing nothing** | An agent run **un-isolated in the main checkout** did `git checkout -b` and left HEAD on its branch. Three merges landed there; three `git push origin develop` were silent no-ops. Caught only because **main's test count came back 2185 instead of 2242** — a number, not a ref check. | Never run an agent un-isolated in the main checkout. After any merge, verify `git rev-parse develop` equals what you expect. |
| **A workaround that works is the most expensive bug** | The proof suite ran **twice** for months, recorded as a "double-counted *metric*, severity advisory, remedy: de-duplicate". It was double-*run* — `node --test` discovered the vendored submodule's `all.test.ts`. ~53s per run. | Test the diagnosis, not just the symptom. A documented workaround removes all pressure to look again. |
| **A total cannot see a swap** | A two-field transposition and a passive/non-passive column swap each left whole suites green — `(d)+(f)` commutes. | Assert the columns and the `boxPath`, never only the sum. |
| **A count true of the part, false of the whole** | Eleven occurrences. Two registries both 23 long and not the same 23; a hand-typed list three short; the proof floor stale by 1108. | Only a **comparison** stops it. Naming the hazard in a comment failed three times; `theHandTypedListNamesEveryModeledKind` and `dialect_parity` each caught a real omission within a day. |
| **A stale worktree** | Four agents were handed worktrees cut **265 commits** stale and burned most of their budget discovering it. | Make base verification the first instruction: `git checkout -B "$(git rev-parse --abbrev-ref HEAD)" origin/develop && npm ci`. |
| **Context % in hook output is the PARENT's** | An agent stopped after 18 tool calls believing it was at 72%. | Tell subagents to ignore it. |
| **A tick needing a paragraph of caveats** | Phase 25 checked TAX-27 while its own prose said the credit did not compute. | If the caveat is needed, leave it unchecked. |

## Required reading, in order

1. **`AGENTS.md`** — in full. Its mutation-recipe section was corrected twice this week; read it
   rather than recalling it. Line 127 is the gating rule.
2. The anti-pattern table above.
3. **`.planning/CAPABILITIES.md`** — measured from a live server, not asserted.
4. `.planning/PERSONA-COVERAGE.md`.

## Infrastructure

- No background processes. Nothing running.
- `npm test` ~30s · `npm run test:proofs` · `npm run test:integration`.
- `node --test <a file under fjs/>` is a **FAKE PASS**. Root-level `*.test.js` are the documented
  exception, which is why `npm test` is pinned to `node --test *.test.js`.
- `type-level/` is untracked and belongs to another session. Leave it.

<next_action>
Nothing is required. The milestone is closed and released. If work resumes, the four `fjs/todo/`
notes are the candidate list — each already carries its own recipe and retirement condition.
</next_action>
