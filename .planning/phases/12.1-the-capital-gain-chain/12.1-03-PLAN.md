---
phase: 12.1
plan: 03
type: execute
wave: 2
depends_on: ["12.1-01"]
files_modified:
  - fjs/schedule/d/module.f.js
autonomous: true
requirements: [TAX-11]

must_haves:
  truths:
    - "A net short-term gain and a net long-term gain combine into Schedule D line 16, and that gain routes to Form 1040 line 7a."
    - "A net loss year computes Schedule D line 21 as the LESSER of the loss and $3,000 ($1,500 if married filing separately), and that capped figure — not line 16 itself — is what routes to Form 1040 line 7a."
    - "A zero-net year routes exactly $0 to Form 1040 line 7a."
    - "1099-DIV box 2a (capital gain distributions) feeds Schedule D line 13, already inclusive of boxes 2b/2c/2d — none of those three boxes is separately added again."
    - "A short-term gain and a long-term gain of the same size never collapse into an indistinguishable total: Schedule D lines 7 and 15 are asserted separately, not just line 16."
    - "Form 8949's absent-basis/unsupported-adjustment refusal threads through Schedule D as the SAME shaped refusal, not a new fjs/return/scope kind."
  artifacts:
    - path: "fjs/schedule/d/module.f.js"
      provides: "Schedule D lines 1a-21 (the loss-cap three-way branch), the 28% Rate Gain and Unrecaptured Section 1250 Gain worksheets (dialect-reachable lines only), over Form 8949 category totals and 1099-DIV boxes"
      min_lines: 300
      exports: ["scheduleD"]
  key_links:
    - from: "fjs/schedule/d/module.f.js"
      to: "fjs/form8949/module.f.js"
      via: "form8949(brokerageForms) supplies categoryA/B/D/E totals for Schedule D lines 1b/2/8b/9"
      pattern: "form8949\\("
    - from: "fjs/schedule/d/module.f.js"
      to: "fjs/document/1099div/module.f.js"
      via: "reads box2aTotalCapitalGainDistr/box2bUnrecapSec1250Gain/box2dCollectibles28PercentGain directly"
      pattern: "box2aTotalCapitalGainDistr"
---

<objective>
Build `fjs/schedule/d/module.f.js`: Schedule D lines 1a through 21 (the full schedule this
phase needs — lines 6/14's multi-year carryover excepted, per CONTEXT.md Decision 1.5 and
ROADMAP's own carve-out to TAX-17/Phase 15), including the loss-cap three-way branch that Form
1040 line 7a needs on every net-loss return, and the two sub-worksheets (28% Rate Gain,
Unrecaptured Section 1250 Gain) at exactly the lines this project's dialects can populate
(CONTEXT.md Decision 2.5).

Purpose: this is the module that turns Form 8949's category totals and 1099-DIV's boxes into
the `scheduleD15Cents`/`scheduleD16Cents`/`scheduleD18Cents`/`scheduleD19Cents` figures
`dispatchLine16` already accepts as inputs, and the three-way-routed figure Form 1040 line 7a
needs. Depends on Plan 12.1-01 (`fjs/form8949`) for category totals; independent of Plan
12.1-02 (`fjs/tax/line16/sdtw`), which this module's output feeds but does not call.
Output: `fjs/schedule/d/module.f.js`, exporting `scheduleD` and its `ScheduleDInputs`/
`ScheduleDOutcome` types — the contract Plan 12.1-04 (Wave 3) wires into Form 1040.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-the-capital-gain-chain/12.1-CONTEXT.md
@.planning/phases/12.1-the-capital-gain-chain/12.1-RESEARCH.md
@.planning/phases/12.1-the-capital-gain-chain/12.1-PATTERNS.md
@fjs/form8949/module.f.js
@fjs/schedule/b/module.f.js
@fjs/document/1099div/module.f.js
@fjs/tax/params/module.f.js

<form_face>
<!-- Schedule D (Form 1040) 2025, fetched and read directly THIS session (f1040sd.pdf,
     "Created 10/6/25") — the real printed line numbers and text, verified rather than
     recalled, superseding any paraphrase elsewhere in this phase's planning documents where
     they might disagree. -->

**Part I — Short-Term.** 1a: totals for ST transactions on 1099-B/1099-DA, basis reported to
IRS, NO adjustments (the "skip Form 8949 entirely" shortcut — leave blank if you choose to
report everything on 8949; THIS PROJECT'S dialect always routes through Form 8949, so line 1a
is a documented, permanent 0). 1b: totals from Form(s) 8949 with Box A (or G, digital-asset,
out of scope) checked. 2: Box B/H. 3: Box C/I (no 1099-B — out of scope). 4: ST gain from Form
6252 and Forms 4684/6781/8824 (documented 0, no dialect). 5: net ST gain/loss from
partnerships/S-corps/estates/trusts via Schedule K-1 (documented 0, no dialect). 6: ST capital
loss carryover, entered as a NEGATIVE number (documented 0 — multi-year, TAX-17/Phase 15,
CONTEXT.md 1.5). 7: **net ST gain/loss = combine lines 1a through 6.**

**Part II — Long-Term.** 8a: same shortcut as 1a, long-term (documented 0). 8b: Box D/J. 9: Box
E/K. 10: Box F/L (out of scope). 11: gain from Form 4797 Part I; LT gain from Forms 2439/6252;
LT gain/loss from Forms 4684/6781/8824 (documented 0, no dialect). 12: K-1 net LT (documented
0, no dialect). 13: **capital gain distributions** ("see the instructions" — this is where
1099-DIV box 2a lands; box 2a is already inclusive of 2b/2c/2d's sub-components, per
RESEARCH.md's Finding 1 — do NOT add 2b/2c/2d again here). 14: LT capital loss carryover,
entered NEGATIVE (documented 0, multi-year, TAX-17/Phase 15). 15: **net LT gain/loss = combine
lines 8a through 14.**

**Part III — Summary.** 16: combine lines 7 and 15 -> if a GAIN, this is Form 1040 line 7a and
continue to 17; if a LOSS, skip 17-20, go to 21 (and complete 22); if ZERO, skip 17-21, enter
-0- on Form 1040 line 7a, go to 22. 17: are lines 15 AND 16 both gains? Yes -> 18; No -> skip
18-20, go to 22 (this is the SAME condition `fjs/tax/line16/module.f.js` already computes as
`scheduleDLinesFifteenAndSixteenAreBothGains` — do not reimplement it a second time in this
module; AGENTS.md "one rule, one place"). 18: 28% Rate Gain Worksheet line 7, if required. 19:
Unrecaptured Section 1250 Gain Worksheet line 18, if required. 20: routing between QDCGT and
the Schedule D Tax Worksheet — already `dispatchLine16`'s own job; this module does not
reimplement it. 21: **if line 16 is a LOSS**, enter the SMALLER of (the loss on line 16) or
($3,000, or $1,500 if married filing separately) — "treat both amounts as positive numbers"
when comparing, then the result carries the loss's sign onto Form 1040 line 7a. 22: do you have
qualified dividends — already Form 1040 line 3a's own concern, not this module's.
</form_face>

<sub_worksheets>
<!-- Per CONTEXT.md Decision 2.5 (LOCKED): model ONLY the one line each sub-worksheet's dialect
     can populate; every other line hardcoded to its zero-equivalent, each documented as a
     deliberate boundary in this module's own docstring (mirrors fjs/schedule/b's Form 8815
     boundary). Do not attempt to derive any of the "every other line" figures from data this
     module happens to have on hand (e.g. this module's own line 7) even where it looks
     possible — Decision 2.5 is locked as a blanket boundary, not a per-line judgment call. -->

**28% Rate Gain Worksheet — Line 18** (i1040sd.pdf p.11). Line 4 (the ONLY populated line): "any
collectibles gain reported to you on Form 1099-DIV box 2d." Lines 1, 2, 3, 5, 6 hardcoded 0n,
documented. Line 7 (this worksheet's own final line, "combine lines 1-6, floor 0") therefore
reduces to `max(box2dSum, 0n)` in this project — implement it as the full combine anyway, per
TAX-15's "one printed line, one const," so a later phase widening the dialect can see exactly
which lines to revisit.

**Unrecaptured Section 1250 Gain Worksheet — Line 19** (i1040sd.pdf pp.13-14). Line 11 (the
ONLY populated line): "amounts reported to you as unrecaptured section 1250 gain on a Schedule
K-1, Form 1099-DIV, or Form 2439... or in connection with Form 1099-R" — this project reads
only the 1099-DIV term, `box2bUnrecapSec1250Gain`. Lines 1-10, 12, 14, 15, 16 hardcoded 0n,
documented (including line 14, "§1202/collectibles total from the 28% Rate Gain Worksheet
lines 1-4" — even though line 4 above is non-zero, Decision 2.5 is a blanket boundary; hardcode
line 14 to 0n and document why, rather than cross-referencing the other worksheet's line 4).
Line 13 = "add lines 9 through 12" = line 11 alone (9,10,12 are 0). Line 17 = "combine lines
14-16, if zero or a gain enter -0-" = 0n (all three inputs are 0n). Line 18 = "line 13 minus
line 17, floor 0" = `max(line11 - 0n, 0n)`.
</sub_worksheets>

<contract>
```
ScheduleDInputs  = { readonly status: IndividualFilingStatus,
                      readonly brokerageForms: readonly Stored<OneZeroNineNineB>[],
                      readonly dividendForms: readonly Stored<OneZeroNineNineDiv>[] }
ScheduleDOk      = { readonly kind: 'ok',
                      readonly line1a: bigint, readonly line1b: bigint, readonly line2: bigint,
                      readonly line3: bigint, readonly line4: bigint, readonly line5: bigint,
                      readonly line6: bigint, readonly line7: bigint,
                      readonly line8a: bigint, readonly line8b: bigint, readonly line9: bigint,
                      readonly line10: bigint, readonly line11: bigint, readonly line12: bigint,
                      readonly line13: bigint, readonly line14: bigint, readonly line15: bigint,
                      readonly line16: bigint, readonly line18: bigint, readonly line19: bigint,
                      readonly line21: bigint,
                      readonly line7aCapitalGainOrLoss: bigint,  // the 3-way routed figure
                      readonly sources: readonly Source[] }
ScheduleDError   = { readonly kind: 'error', readonly message: string }
ScheduleDOutcome = ScheduleDOk | ScheduleDError
export const scheduleD: (inputs: ScheduleDInputs) => ScheduleDOutcome
```
`line7aCapitalGainOrLoss` is this module's OWN routing of lines 16/21 (gain -> line16; zero ->
0n; loss -> line21, already signed negative) — Form 1040 line 7a in Plan 12.1-04 reads this
field directly rather than re-deriving the three-way branch a second time.
</contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Schedule D lines 1a-21, the loss-cap branch, and the two bounded sub-worksheets</name>
  <files>fjs/schedule/d/module.f.js</files>
  <read_first>
    fjs/form8949/module.f.js (as built by Plan 12.1-01 — its exact `Form8949Outcome` contract)
    fjs/schedule/b/module.f.js (full — docstring/boundary-paragraph shape, the reimplemented
    aggregation idiom)
    fjs/document/1099div/module.f.js:96-185 (the exact box names: box2aTotalCapitalGainDistr,
    box2bUnrecapSec1250Gain, box2cSection1202Gain — read but NOT separately added,
    box2dCollectibles28PercentGain)
    .planning/phases/12.1-the-capital-gain-chain/12.1-RESEARCH.md, "The 28% Rate Gain
    Worksheet" and "The Unrecaptured Section 1250 Gain Worksheet" sections (full transcriptions)
  </read_first>
  <behavior>
    - Category totals from `form8949`: `categoryA -> line1b`, `categoryB -> line2`,
      `categoryD -> line8b`, `categoryE -> line9`. If `form8949(brokerageForms)` returns
      `{ kind: 'error' }`, `scheduleD` returns the SAME shaped `{ kind: 'error', message }`
      immediately — do not compute any line.
    - `line7 = line1a + line1b + line2 + line3 + line4 + line5 + line6` with 1a/3/4/5/6 always
      0n this phase (each its own named documented-zero const, not a bare literal inline).
    - `line13 = sum(box2aTotalCapitalGainDistr over dividendForms)`, reading absent boxes as
      absent (never `'0.00'`), reimplementing the `sumBoxOverDocuments` idiom locally.
    - `line15 = line8a + line8b + line9 + line10 + line11 + line12 + line13 + line14`.
    - `line16 = line7 + line15`.
    - `line18 = max(sum(box2dCollectibles28PercentGain over dividendForms), 0n)`.
    - `line19 = max(sum(box2bUnrecapSec1250Gain over dividendForms) - 0n, 0n)` (line17 of that
      worksheet is always 0n this phase, per `<sub_worksheets>`).
    - Loss cap: `capCents = status === 'marriedFilingSeparately' ? 150000n : 300000n`;
      `line21 = line16 < 0n ? -min(-line16, capCents) : 0n` (line21 is populated ONLY when
      line16 is a loss; 0n otherwise, matching the printed form's own "skip 21" instruction for
      a gain or zero year).
    - `line7aCapitalGainOrLoss = line16 > 0n ? line16 : (line16 === 0n ? 0n : line21)`.
    - A fixture with a $6,000.00 short-term loss and NO long-term activity, single filer:
      `line7 = -600000n`, `line16 = -600000n`, `line21 = -300000n` (capped at $3,000.00), and
      `line7aCapitalGainOrLoss === line21`.
    - The SAME fixture with `status: 'marriedFilingSeparately'`: `line21 = -150000n`.
    - A fixture with a $6,000.00 short-term GAIN and a $2,000.00 long-term LOSS (net gain
      $4,000.00): `line7 = 600000n`, `line15 = -200000n` — asserted SEPARATELY, not just
      `line16 = 400000n` — this is Mutation Gate M2's control shape (Task 2).
    - A fixture with `box1eCostOrOtherBasis` genuinely absent on one brokerage document:
      `scheduleD(...)` returns `{ kind: 'error', ... }`, propagated verbatim from `form8949`.
  </behavior>
  <action>
    Create `fjs/schedule/d/module.f.js`. Docstring: cite `f1040sd.pdf` ("Created 10/6/25") and
    `i1040sd.pdf` pp.11,13-14 for the sub-worksheets, state the module is STANDALONE (imports
    nothing at runtime from `fjs/tax/`, `fjs/return/`, `fjs/form1040/`), and give Decision 2.5's
    sub-worksheet boundary and the "no prior-year carryover" boundary (lines 6/14, always 0)
    each their own `## ...` docstring paragraph, mirroring `fjs/schedule/b`'s Form 8815
    precedent. Export the exact contract in `<contract>` and implement per `<form_face>` and
    `<sub_worksheets>` above and per the `behavior` block. Reuse
    `fjs/schedule/b/module.f.js:98-158`'s `sumBoxOverDocuments`/`addBoxSums` idiom, reimplemented
    locally and privately (not imported). `status` is read ONLY for the loss-cap threshold — do
    not thread it anywhere else.
  </action>
  <acceptance_criteria>
    - `scheduleD` is exported with the exact contract above.
    - The four fixtures in `behavior` (single-filer loss cap, MFS loss cap, mixed gain/loss with
      lines 7 and 15 asserted separately, propagated form8949 error) each pass.
    - A clean-gain fixture (both ST and LT gains, both categories populated) produces
      `line7aCapitalGainOrLoss === line16` exactly.
    - A zero-net fixture produces `line7aCapitalGainOrLoss === 0n` and `line21 === 0n`.
    - Every documented-zero line (`line1a`, `line3`, `line4`, `line5`, `line6`, `line8a`,
      `line10`, `line11`, `line12`, `line14`) is a named `const` bound to `0n` with a docstring
      comment stating why, not inlined as a bare `0n` in an arithmetic expression.
  </acceptance_criteria>
  <verify>
    <automated>node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>
    `fjs/schedule/d/module.f.js` exists, exports the contract, and its `proof` covers every
    behavior above.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mutation Gate M2 — the short/long-term equivalent-mutant trap</name>
  <files>fjs/schedule/d/module.f.js, fjs/form8949/module.f.js</files>
  <read_first>
    fjs/form8949/module.f.js (the exact categorization branch — the mutation target)
    .planning/phases/12.1-the-capital-gain-chain/12.1-VALIDATION.md, row M2
    AGENTS.md, section "The equivalent mutant: a mutation a neighbouring operation absorbs"
  </read_first>
  <action>
    In `fjs/form8949/module.f.js`'s category-derivation function (Plan 12.1-01), swap the
    boolean feeding the short-term/long-term split — mutate so a document with
    `box2ShortTermGainOrLoss: true` is categorized as if it were long-term (Category D/E instead
    of A/B), or vice versa. Using this plan's own mixed-gain/loss fixture ($6,000.00 short-term
    GAIN + $2,000.00 long-term LOSS, net $4,000.00), confirm that a proof asserting `line16`
    ALONE stays green (the total is unaffected by which part is "short" vs "long" when nothing
    else reads the split) while the proof asserting `line7` AND `line15` SEPARATELY (already
    written in Task 1) turns RED, because the $6,000.00 and the $2,000.00 have moved to the
    wrong Part of Schedule D. Record which leaf and which specific assertion (line7's value,
    line15's value, or both) reddened. Revert the mutation exactly and confirm the suite is
    green again with the proof count restored.
  </action>
  <acceptance_criteria>
    - The mutation compiles.
    - A total-only assertion (`line16`) does NOT catch the mutation — confirm this explicitly by
      temporarily checking that a `line16`-only proof would stay green under the mutation
      (demonstrate the equivalent-mutant trap is real, not merely asserted).
    - The `line7`-and-`line15`-separately proof from Task 1 DOES catch it, and the SUMMARY names
      the exact leaf and which of line7/line15 (or both) reddened, with the wrong values shown.
    - After reverting, `npm test` is green and the proof count matches Task 1's post-build count.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>
    Mutation Gate M2 watched failing (with the equivalent-mutant trap explicitly demonstrated
    and then defeated by the line7/line15-separate assertion), reverted cleanly, suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form 8949 category totals -> Schedule D lines | Trusted contract from Plan 12.1-01; this module does not re-validate the documents themselves. |
| 1099-DIV boxes -> Schedule D line 13/18/19 | Same trust level as every other document read in this codebase — already validated by the dialect's own `validate`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-01 | Tampering (a guard weakened into decoration) | Form 8949's error propagation through `scheduleD` | mitigate | Task 1's fixture asserts the error arm propagates verbatim, not silently swallowed into a zero. |
| T-12.1-02 | Tampering (the half-change) | N/A for this plan | accept | `scheduleD` is not wired into Form 1040 yet — that atomic step is Plan 12.1-04. |
</threat_model>

<verification>
`npm test` green; Mutation Gate M2 watched failing and reverted; the four required fixtures in
Task 1 all pass; `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly greater than Plan
12.1-01's post-build count.
</verification>

<success_criteria>
`fjs/schedule/d/module.f.js` exists, computes the loss-cap three-way branch correctly for gain/
zero/loss years, keeps short-term and long-term totals independently observable, and threads
Form 8949's refusal verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-the-capital-gain-chain/12.1-03-SUMMARY.md`
</output>
