---
phase: 12.1
plan: 05
type: execute
wave: 4
depends_on: ["12.1-04"]
files_modified:
  - demo/steps/04-refusal.js
autonomous: true
requirements: [TAX-11]

must_haves:
  truths:
    - "The demo builds under tsc after the scope reclassification (the offered array no longer names a now-modeled kind)."
    - "The demo's own prose no longer claims a stale entry count for the refusal table."
    - "The demo's general claim ('declare something unmodeled and the whole return refuses') remains demonstrably true after this phase."
  artifacts:
    - path: "demo/steps/04-refusal.js"
      provides: "Updated offered[] toggle list and corrected source-footer label/anchor"
  key_links:
    - from: "demo/steps/04-refusal.js"
      to: "fjs/return/scope/module.f.js"
      via: "unmodeledKindRefusals/modeledKinds/kindVocabulary read live from the engine at render time"
      pattern: "unmodeledKindRefusals"
---

<objective>
Fix the two stale artifacts CONTEXT.md Decision 3.3 and PATTERNS.md's own reading of this file
identify: `offered`'s two departing kind names (a `tsc`-forced fix) and the hardcoded "44-entry"
label in `sourceFooter` (NOT `tsc`-forced — a silent prose drift PATTERNS.md flags as a separate
finding this phase should also fix).

Purpose: `demo/steps/04-refusal.js` is the "declare something unmodeled and the whole return
refuses" showcase. After Plan 12.1-04 lands, `qualifiedDividends` and `capitalGainsOrLosses`
have left the `UnmodeledKind` union the demo's `offered` array is typed against, so the build
fails until this plan replaces them; separately, the refusal table shrinks from 44 to 38
entries, which the page's live-read summary line already handles automatically but a single
hand-written prose label does not.
Output: `demo/steps/04-refusal.js`, compiling and narrating correctly against the post-Phase
12.1 engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-the-capital-gain-chain/12.1-CONTEXT.md
@.planning/phases/12.1-the-capital-gain-chain/12.1-PATTERNS.md
@demo/steps/04-refusal.js
@fjs/return/scope/module.f.js

<interfaces>
`demo/steps/04-refusal.js:34-41`, the array to fix:
```
const offered = [
    'qualifiedDividends',
    'capitalGainsOrLosses',
    'socialSecurityBenefits',
    'iraDistributions',
    'itemizedDeductions',
    'childTaxCreditOrOtherDependents',
]
```
`'qualifiedDividends'` and `'capitalGainsOrLosses'` have left `UnmodeledKind` (Plan 12.1-04) —
`tsc` fails at this literal array (typed `readonly UnmodeledKind[]`) until they are replaced.
Replace with two kinds still in the 38 that refuse after Plan 12.1-04 — e.g.
`pensionsAndAnnuities` and `scheduleOneAdditionalIncome`, or any other two still-refusing kinds
(check the live `unmodeledKindRefusals` table in `fjs/return/scope/module.f.js` rather than
trusting this suggestion, since the exact remaining set is what Plan 12.1-04 actually ships).

`demo/steps/04-refusal.js:171`, the stale label:
```
{ label: 'fjs/return/scope — the 44-entry refusal table', path: 'fjs/return/scope/module.f.js', line: 126 },
```
The count is now 38, and Plan 12.1-04's edits to `fjs/return/scope/module.f.js` (the docstring
rewrite at lines 41-54, plus the table edit itself) may have moved which line the
`unmodeledKindRefusals` array literally starts on — re-resolve the line number against the
FILE AS IT EXISTS after Plan 12.1-04, do not assume it is still 126.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the two departing kinds and correct the stale count/anchor</name>
  <files>demo/steps/04-refusal.js</files>
  <read_first>
    demo/steps/04-refusal.js (full — both stale sites, and how `offered`/`refusalFor`/
    `sourceFooter` are used elsewhere in the file, to confirm no other reference needs touching)
    fjs/return/scope/module.f.js (as it exists AFTER Plan 12.1-04 — the live 38-entry
    `unmodeledKindRefusals` table and its current line number)
  </read_first>
  <action>
    In `demo/steps/04-refusal.js`, replace `'qualifiedDividends'` and `'capitalGainsOrLosses'`
    in the `offered` array with two kinds confirmed present in the post-Phase-12.1
    `unmodeledKindRefusals` table (read the live table; do not hand-guess). Update the
    `sourceFooter` entry at line 171: change `'the 44-entry refusal table'` to
    `'the 38-entry refusal table'`, and re-resolve the `line:` value to wherever
    `unmodeledKindRefusals`'s declaration actually starts in the post-Plan-12.1-04 file (grep
    for `export const unmodeledKindRefusals` in the current file and use that line number, do
    not assume 126 is unchanged). Re-read the render function's narration (the `why`/`vocabulary`
    /`closing` sections) and confirm no other sentence assumes the old 44/6/50 split — the
    live-read `${unmodeledKindRefusals.length}`/`${modeledKinds.length}`/`${kindVocabulary.length}`
    interpolations already self-update and need no manual change.
  </action>
  <acceptance_criteria>
    - `offered` contains no kind that is in `modeledKinds` after Plan 12.1-04 (cross-check
      against the live table, not a hand-typed list).
    - `npx tsc --noEmit` passes with the demo included in the type-check scope (confirm via the
      project's normal `npm test` / `tsc` invocation, which already covers `demo/`).
    - The `sourceFooter` label reads "38-entry," not "44-entry," and its `line:` value points at
      the actual current declaration line of `unmodeledKindRefusals`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `demo/steps/04-refusal.js` compiles, its toggle list only offers kinds that still refuse,
    and its hand-written prose matches the shipped 12/38/50 split.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo narration -> stakeholder trust | The demo is a showcase artifact; a stale claim here (wrong count, offering an already-modeled kind) undermines exactly the "the engine is honest about what it knows" story the phase's own TAX-16 work tells. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-01 | Tampering (a guard weakened into decoration) | N/A — this plan touches only demo narration, no guard logic | accept | No production refusal logic is touched here. |
| T-12.1-02 | Tampering (the half-change) | N/A — this plan runs strictly after Plan 12.1-04 lands, per its `depends_on` | accept | The dependency wave ordering guarantees this plan never runs against a half-changed engine. |
</threat_model>

<verification>
`npx tsc --noEmit` passes; `npm test` remains green (no proof leaves live in this file, so the
project-local proof count is unaffected by this plan).
</verification>

<success_criteria>
The demo compiles and narrates correctly against the shipped Phase 12.1 engine, with no stale
kind name and no stale count.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-the-capital-gain-chain/12.1-05-SUMMARY.md`
</output>
