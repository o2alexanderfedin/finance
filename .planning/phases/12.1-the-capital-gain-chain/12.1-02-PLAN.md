---
phase: 12.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fjs/tax/line16/sdtw/module.f.js
autonomous: true
requirements: [TAX-11, TAX-15]

must_haves:
  truths:
    - "A return with unrecaptured section 1250 gain on Schedule D line 19 computes a real Form 1040 line 16 figure that taxes the §1250 slice at up to 25%, matching the printed Schedule D Tax Worksheet exactly, line by line."
    - "A return with 28% rate (collectibles) gain on Schedule D line 18 computes a real figure that taxes that slice at 28%, matching the printed worksheet, line by line."
    - "On a degenerate input where Schedule D lines 18 and 19 are both zero, the Schedule D Tax Worksheet and the already-shipped QDCGT worksheet produce the IDENTICAL final cents figure."
    - "No variable in this module is named magi."
  artifacts:
    - path: "fjs/tax/line16/sdtw/module.f.js"
      provides: "The 47-line Schedule D Tax Worksheet as one pure function, IRS line order, cent-exact"
      min_lines: 500
      exports: ["sdtw"]
  key_links:
    - from: "fjs/tax/line16/sdtw/module.f.js"
      to: "fjs/tax/table/module.f.js"
      via: "two INDEPENDENT baseTaxForAmount calls, lines 44 and 46"
      pattern: "baseTaxForAmount"
    - from: "fjs/tax/line16/sdtw/module.f.js"
      to: "fjs/tax/params/module.f.js"
      via: "ordinaryBrackets[status] 24%-bracket ceiling reused for line 19's breakpoint, never hand-typed"
      pattern: "ordinaryBrackets"
---

<objective>
Build `fjs/tax/line16/sdtw/module.f.js`: the 47-line Schedule D Tax Worksheet, transcribed
line by line from the printed TY2025 worksheet, mirroring `fjs/tax/line16/qdcgt/module.f.js`'s
shape exactly (SDTW is QDCGT plus two spliced-in terms and a 25%/28% add-on, per
RESEARCH.md's own line-correspondence table). This is a standalone, self-contained pure
function — it takes plain `bigint` inputs, exactly like `qdcgt`, and has no dependency on
Form 8949 or Schedule D's own modules.

Purpose: this worksheet is what makes `dispatchLine16`'s branch 2a COMPUTE instead of refusing
(Plan 12.1-04, Wave 3) — the central deliverable ROADMAP criterion 2 names. Building it
standalone, in its own wave, lets it be verified independently against the two worked examples
before anything else in the phase depends on it.
Output: `fjs/tax/line16/sdtw/module.f.js`, exporting `sdtw` and its `SdtwInput`/`Sdtw` types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-the-capital-gain-chain/12.1-CONTEXT.md
@.planning/phases/12.1-the-capital-gain-chain/12.1-RESEARCH.md
@.planning/phases/12.1-the-capital-gain-chain/12.1-PATTERNS.md
@fjs/tax/line16/qdcgt/module.f.js
@fjs/tax/table/module.f.js
@fjs/tax/params/module.f.js

<interfaces>
<!-- Extracted this session. -->

`qdcgt`'s own contract to mirror, from `fjs/tax/line16/qdcgt/module.f.js:86-123`:
`QdcgtInput = { status, line1Cents, line2Cents, filingScheduleD, scheduleD15Cents,
scheduleD16Cents, line7aCents }`. Output `Qdcgt` is a flat record `line1..line25` plus
`method22`/`method24: Line16BaseMethod`. SDTW's own output must be the analogous flat record
`line1..line47` plus `method44`/`method46: Line16BaseMethod` (the two independent
`baseTaxForAmount` dispatch tags, at the two lines that call it — NOT `method22`/`method24`).

`baseTaxForAmount`, from `fjs/tax/table/module.f.js:376-378`:
`(taxParamSet: TaxParamSet) => (status: IndividualFilingStatus) => (incomeCents: bigint) =>
{ readonly method: 'taxTable' | 'taxComputationWorksheet', readonly cents: bigint }`. Called
TWICE, independently, once for the amount on SDTW line 21 (-> line 44) and once for the amount
on line 1 (-> line 46) — never decided once and reused (QDCGT's own Pitfall 1, restated for
this worksheet by RESEARCH.md).

`ordinaryBrackets`, from `fjs/tax/params/module.f.js:233-306`: `Record<IndividualFilingStatus,
{ readonly brackets: readonly { readonly ratePercent: number, readonly ceiling: string |
undefined }[] }>`. Line 19's breakpoint ($197,300 / $394,600 / $197,300 / $197,300) is exactly
`ordinaryBrackets[status].brackets.find(b => b.ratePercent === 24)?.ceiling`, narrowed with
`assert` (never a cast or `!`) exactly as `fjs/tax/line16/qdcgt/module.f.js` narrows
`taxParams2025` at module scope.

Fractional-cent rounding idiom, from `fjs/tax/line16/qdcgt/module.f.js:217`:
`halfUp(multiply(of(line17)(1n))(of(15n)(100n)))` for a 15% multiply. SDTW's four rate-multiply
lines (31: 15%, 34: 20%, 40: 25%, 43: 28%) use the identical `of`/`multiply`/`halfUp` idiom with
the SDTW's own rates. Imports: `of`, `multiply`, `halfUp` from
`../../../types/rational/module.f.js`; `centsFromString` from `../../../exact/module.f.js`
(same relative depth as `qdcgt`, since `sdtw` sits at the same `fjs/tax/line16/<name>/` depth).
</interfaces>

<worksheet>
<!-- The 47-line SDTW, transcribed from i1040sd.pdf pp.15-16 (2025 rev., Dec 11 2025). The
     line-by-line INSTRUCTIONS are reproduced below (safe to copy — they are short and
     mechanical). The WORKED-EXAMPLE NUMBERS are deliberately NOT re-transcribed into this
     plan: compressing 47 hand-typed dollar figures into prose here would itself become the
     "planning text contradicting shipped code" failure mode (AGENTS.md, CONTEXT.md's
     "verified defect" note) if this plan introduced even one transcription slip of its own —
     which is exactly what an earlier draft of this file did (an off-by-one in Example 1's
     line-25 vicinity), caught only by re-deriving the numbers from RESEARCH.md's own table
     during planning. -->

1. Taxable income (Form 1040 line 15). 2. Qualified dividends (line 3a). 3. Form 4952 line 4g.
4. Form 4952 line 4e (or the smaller dotted-line amount). 5. line3 − line4, floor 0.
6. line2 − line5, floor 0. 7. smaller of Sch D line 15 or 16. 8. smaller of line3 or line4.
9. line7 − line8, floor 0. 10. line6 + line9. 11. Sch D line18 + line19. 12. smaller of line9
or line11. 13. line10 − line12. 14. line1 − line13, floor 0. 15. $48,350 single/MFS; $96,700
MFJ/QSS; $64,750 HoH. 16. smaller of line1 or line15. 17. smaller of line14 or line16.
18. line1 − line10, floor 0. 19. smaller of line1 or ($197,300 single/MFS/HoH; $394,600 MFJ/QSS).
20. smaller of line14 or line19. 21. larger of line18 or line20. 22. line16 − line17 (taxed at
0%; no printed floor — assert non-negative, matching QDCGT's own no-floor idiom). [If line1 ==
line16, skip 23-43, go to 44.] 23. smaller of line1 or line13. 24. line22 (or 0 if blank).
25. line23 − line24, floor 0. 26. $533,400 single; $300,000 MFS; $600,050 MFJ/QSS; $566,700 HoH.
27. smaller of line1 or line26. 28. line21 + line22. 29. line27 − line28, floor 0. 30. smaller of
line25 or line29. 31. line30 × 15%. 32. line24 + line30. [If line1 == line32, skip 33-43, go to
44.] 33. line23 − line32. 34. line33 × 20%. [If Sch D line19 == 0, skip 35-40, go to 41.]
35. smaller of line9 or Sch D line19. 36. line10 + line21. 37. line1. 38. line36 − line37, floor
0. 39. line35 − line38, floor 0. 40. line39 × 25%. [If Sch D line18 == 0, skip 41-43, go to 44.]
41. line21 + line22 + line30 + line33 + line39. 42. line1 − line41. 43. line42 × 28%.
44. `baseTaxForAmount` on line21. 45. line31 + line34 + line40 + line43 + line44. 46.
`baseTaxForAmount` on line1 (independently). 47. smaller of line45 or line46 -> Form 1040 line16.

**The two worked examples' full 47-line tables live ONLY in
`.planning/phases/12.1-the-capital-gain-chain/12.1-RESEARCH.md`, section "Worked Examples —
Two Cases That Route to the SDTW" — read that section directly and use ITS table as the
hand-typed expected values. Do not use any other transcription, including this plan's prose.**

For orientation only (re-derive these from RESEARCH.md's own table before writing the proof,
per CONTEXT.md Decision 3.4 — do not copy these two lines as a substitute for reading the full
table):
- Example 1 (MFJ, taxable income $97,000.00, qualified dividends $300.00, Sch D 15/16 =
  $40,000.00 gain each, Sch D 18 = $0.00, Sch D 19 = $10,000.00): final line47 = $7,575.00,
  both `method44` and `method46` are `taxTable`.
- Example 2 (Single, taxable income $700,000.00, qualified dividends $0.00, Sch D 15/16 =
  $50,000.00 gain each, Sch D 18 = $50,000.00, Sch D 19 = $0.00): final line47 = $211,520.25,
  both `method44` and `method46` are `taxComputationWorksheet`.
</worksheet>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transcribe the 47-line Schedule D Tax Worksheet as one pure function</name>
  <files>fjs/tax/line16/sdtw/module.f.js</files>
  <read_first>
    fjs/tax/line16/qdcgt/module.f.js (full — the exact shape, docstring structure, no-floor
    assert idiom, and independent-dispatch idiom to mirror)
    fjs/tax/table/module.f.js:340-391 (`baseTaxForAmount`, `Line16BaseMethod`)
    fjs/tax/params/module.f.js:225-320 (`ordinaryBrackets`, `capitalGainsBreakpoints`)
    .planning/phases/12.1-the-capital-gain-chain/12.1-RESEARCH.md, "Code Examples" section (the
    full 47-line transcription and the `ordinaryBrackets` reuse snippet)
  </read_first>
  <behavior>
    - Every printed line 1-47 exists as its own named `const lineN`, in order, each preceded by
      a comment quoting or closely paraphrasing the printed instruction (TAX-15).
    - Lines with a printed "-0-" floor (5, 6, 9, 14, 16, 18, 20, 21, 25, 29, 38, 39) use the
      ternary floor form QDCGT uses at its own line 5; lines with NO printed floor that cannot
      legitimately go negative (22, per QDCGT's own line-9 precedent) use an `assert`, not a
      defensive clamp. Verify each line's floor/no-floor status against `i1040sd.pdf` pp.15-16
      directly rather than trusting this list, per CONTEXT.md 3.4.
    - Lines 35-40 (the 25%-capped unrecaptured §1250 add-on) and 41-43 (the 28%-capped
      collectibles add-on) are each gated on Schedule D line 19 / line 18 being non-zero
      respectively, per the printed page's own bracketed skip instructions — implement the skip
      as "these lines equal their floor/zero value" rather than as an early return, so the
      function still returns all 47 fields on every input (mirrors QDCGT returning all 25 fields
      even when some lines are trivially zero).
    - Line 19's breakpoint is derived from `ordinaryBrackets[status]`'s 24%-rate bracket
      ceiling, narrowed with `assert`, never a fresh hand-typed literal.
    - `method44` and `method46` can legitimately differ within one execution (neither worked
      example alone exercises the independent-dispatch property this worksheet shares with
      QDCGT — add a THIRD proof case combining Example 1's line21-producing inputs with
      Example 2's line1-producing inputs, analogous to QDCGT's own `splitDispatchInput`).
  </behavior>
  <action>
    Create `fjs/tax/line16/sdtw/module.f.js`. Docstring: cite `i1040sd.pdf` pp.15-16 (2025 rev.,
    printed `Dec 11, 2025`), state this is one of Form 1040 line 16's methods (paired with
    `fjs/tax/line16/qdcgt`), name the defect class this file guards against (omitting either of
    lines 44/46's independent `baseTaxForAmount` dispatch — QDCGT's Pitfall 1, restated here),
    and state the record-not-array rationale (`noUncheckedIndexedAccess`) exactly as QDCGT's
    docstring does. Export `SdtwInput` (fields: `status`, `line1Cents`, `line2Cents`,
    `form4952Line4gCents`, `form4952Line4eCents`, `scheduleD15Cents`, `scheduleD16Cents`,
    `scheduleD18Cents`, `scheduleD19Cents` — no `filingScheduleD`/`line7aCents` fork the way
    QDCGT has one, since branch 2a's guard already establishes Schedule D is being filed with
    both lines 15/16 as gains before this worksheet is ever called) and `Sdtw` (flat record
    `line1..line47` plus `method44`, `method46: Line16BaseMethod`). Implement `sdtw` per the
    `<worksheet>` block above and RESEARCH.md's full transcription, calling `baseTaxForAmount`
    independently at lines 44 and 46.
  </action>
  <acceptance_criteria>
    - `sdtw` is exported with the contract above; every one of lines 1-47 is a distinct named
      `const` (verified by the grep gate in Task 2).
    - No identifier anywhere in the file is `magi` (TAX-15).
    - Line 19's breakpoint traces to `ordinaryBrackets`, not a fresh literal (grep the file for
      a bare `'197300.00'`/`'394600.00'` string literal — it must NOT appear; only
      `ordinaryBrackets` lookups produce that value).
  </acceptance_criteria>
  <verify>
    <automated>node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>
    `fjs/tax/line16/sdtw/module.f.js` exists with all 47 lines implemented and exported.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove both worked examples line-by-line, the degenerate QDCGT equivalence, and the TAX-15 structural gates</name>
  <files>fjs/tax/line16/sdtw/module.f.js</files>
  <read_first>
    fjs/tax/line16/qdcgt/module.f.js:395-454 (the per-line regression-pair proof idiom to
    mirror — every line asserted individually, not just the final figure)
    fjs/tax/line16/module.f.js:1066-1088 ("What this phase deliberately does NOT prove, and
    where it moves" — the degenerate-equivalence differential this task now delivers)
    .planning/phases/12.1-the-capital-gain-chain/12.1-RESEARCH.md, "Worked Examples — Two Cases
    That Route to the SDTW" (the ONLY source for the two examples' full line values)
    .planning/phases/12.1-the-capital-gain-chain/12.1-VALIDATION.md, rows for "SDTW reproduces
    both hand-transcribed worked examples," "degenerate equivalence," and "No SDTW line
    skipped... TAX-15"
  </read_first>
  <behavior>
    - Example 1's all 47 lines assert individually against RESEARCH.md's own table (re-derived
      independently by the executor before trusting, per CONTEXT.md 3.4) — `method44 ===
      'taxTable'`, `method46 === 'taxTable'`.
    - Example 2's all 47 lines assert individually against RESEARCH.md's table —
      `method44 === 'taxComputationWorksheet'`, `method46 === 'taxComputationWorksheet'`; lines
      35-40 assert to their skip-value (0) since Schedule D line 19 is 0 on this example.
    - The split-dispatch fixture (Task 1's third behavior case) asserts `method44 !==
      method46`.
    - Degenerate equivalence: on an input with `scheduleD18Cents: 0n`, `scheduleD19Cents: 0n`,
      `form4952Line4gCents: 0n` (so branch 2b's guard would also be false), `sdtw(...).line47
      === qdcgt(...).line25` for the SAME underlying return (same status, line1Cents, line2Cents,
      scheduleD15/16Cents, and QDCGT's own `filingScheduleD: true`/`line7aCents: 0n` fork since
      Schedule D is being filed). Import `qdcgt` for this ONE differential proof only — this is
      the "10-RESEARCH.md degenerate-equivalence differential" the roadmap has deferred since
      Phase 10, and it validates both transcriptions against each other.
  </behavior>
  <action>
    Add `proof` leaves to `fjs/tax/line16/sdtw/module.f.js`: `regressionExampleOneAllFortySevenLines`,
    `regressionExampleTwoAllFortySevenLines`, `splitDispatchLinesFortyFourAndFortySixDisagree`,
    `degenerateEquivalenceWithQdcgtWhenScheduleD18And19AreZero`.
  </action>
  <acceptance_criteria>
    - `assertEq(r.line47, ...)` on both worked examples matches RESEARCH.md's hand-typed cents
      figures exactly (line47 = 757500n for Example 1, 21152025n for Example 2).
    - The degenerate-equivalence leaf asserts `sdtw(...).line47 === qdcgt(...).line25` and ALSO
      that the shared cents value is non-trivial (not both zero), so the equivalence is a real
      check rather than a vacuous 0 === 0.
    - `grep -c "// [0-9]*\." fjs/tax/line16/sdtw/module.f.js` reports at least 47.
    - `! grep -rn "\bmagi\b" fjs/tax/line16/sdtw` reports nothing (exit code 1 / empty output).
  </acceptance_criteria>
  <verify>
    <automated>node --test 2>&1 | grep -c '^✔ import("./fjs/' &amp;&amp; grep -c "// [0-9]*\." fjs/tax/line16/sdtw/module.f.js</automated>
  </verify>
  <done>
    Both worked examples pass line-by-line, the degenerate-equivalence proof against `qdcgt`
    passes, and the TAX-15 structural grep gates both pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worksheet input -> tax figure | This module is pure computation over already-trusted `bigint` inputs; no document or external data crosses here directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-01 | Tampering (a guard weakened into decoration) | N/A for this plan | accept | This plan builds the WORKSHEET only; branch 2b's Form 4952 refusal and the ordering guard live in `fjs/tax/line16/module.f.js`, mitigated in Plan 12.1-04. |
| T-12.1-02 | Tampering (the half-change) | N/A for this plan | accept | This module is not wired into the dispatcher or Form 1040 yet — that atomic wiring is Plan 12.1-04's job. Building this worksheet alone changes no observable behavior (nothing calls it yet). |
</threat_model>

<verification>
`npm test` green; both worked examples and the degenerate-equivalence proof pass;
`node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly greater than 629.
</verification>

<success_criteria>
`fjs/tax/line16/sdtw/module.f.js` exists, computes both worked examples exactly, agrees with
`qdcgt` on the degenerate case, and satisfies TAX-15's structural requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-the-capital-gain-chain/12.1-02-SUMMARY.md`
</output>
