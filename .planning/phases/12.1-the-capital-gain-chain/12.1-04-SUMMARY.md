---
phase: 12.1-the-capital-gain-chain
plan: 04
subsystem: tax-engine
tags: [form1040, dividends, capital-gains, schedule-d-tax-worksheet, scope-guard, functionalscript, mutation-testing]

# Dependency graph
requires:
  - phase: 12.1-the-capital-gain-chain
    provides: "fjs/form8949/module.f.js (Plan 12.1-01), fjs/tax/line16/sdtw/module.f.js (Plan 12.1-02), fjs/schedule/d/module.f.js (Plan 12.1-03) — the three standalone modules this plan wires together"
provides:
  - "Form 1040 lines 3a/3b/7a compute for real from stored 1099-DIV/1099-B documents, replacing declaredZero placeholders"
  - "fjs/return/scope: six kinds reclassified modeled (12 total); 38 remain refused; section1202Gain and investmentInterestForm4952 stay refused deliberately"
  - "fjs/tax/line16 dispatcher branch 2a (Schedule D Tax Worksheet) computes instead of refusing; branch 2b (Form 4952) unchanged"
  - "The atomic scope-reclassification + 1040-wiring transition, landed as two commits engineered so neither is ever externally observable as a wrong answer"
affects: [13-the-65-plus-profile-and-schedules, 15-realism-polish-and-upstream]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Reversed task order as a git-history-level atomicity guarantee: wiring lands FIRST while the scope guard still refuses all six kinds (making the new code unreachable-or-correct), reclassification lands LAST as the single atomic transition commit — never merely documented as an ordering convention"
    - "A second, earlier error-arm short-circuit in form1040IncomeLines (Schedule D's absent-basis refusal), returned before form1040TaxAndPaymentLines is ever called, mirroring the existing line16Outcome error-arm pattern one function earlier"
    - "Hand-typed field-name allowlist (incomeLineFieldNames) paired with a hand-typed count, used in place of Object.entries(...) once a record gains bookkeeping fields alongside its printed ReportLine fields"

key-files:
  created: []
  modified:
    - fjs/form1040/core/module.f.js
    - fjs/return/scope/module.f.js
    - fjs/tax/line16/module.f.js
    - demo/lib/fixtures.js
    - demo/steps/02-line16.js
    - demo/steps/03-return.js
    - demo/steps/04-refusal.js
    - demo/steps/05-exactness.js
    - demo/steps/00-about.js

key-decisions:
  - "Task 1 (Form 1040 wiring) lands BEFORE Task 2 (scope reclassification), the reverse of the natural reading order — this is the plan's own load-bearing instruction, verified correct by construction: the six kinds stay in unmodeledKindRefusals throughout Task 1, so the new line3a/3b/7a/dispatch code is reachable only for declarations the scope guard already lets through (dividends without Schedule D, non-declaring taxpayers with real documents)."
  - "filingScheduleD is read verbatim from declaredKinds.includes('capitalGainsOrLosses'), never from document presence (12.1-CONTEXT.md Decision 1.6) — status is now computed once near the top of form1040IncomeLines (deduplicating a second computation previously only done at line 12e) because Schedule D's loss-cap threshold needs it too."
  - "form1040IncomeLines's return type widened to a union with Form1040Error, tagged kind: 'ok' on the existing flat-record shape plus four new dispatcher-input bookkeeping fields (filingScheduleD, scheduleD1{5,6,8,9}Cents) — every one of the ~15 test call sites in this file that read lines directly now narrows via a new expectIncomeOk helper, mirroring fjs/schedule/d's own expectOk idiom."
  - "Line 7a, when filing Schedule D, is built via the SAME documentLine/BoxSum idiom every other document-sourced line uses (not a bespoke ReportLine literal) — this correctly falls back to a profile-declared zero on the structurally possible case of a declared-but-documentless Schedule D filer, rather than risking an empty non-empty-tuple violation."
  - "The mixed-dividends fixture Mutation Gate M5 targets uses hand-picked wages ($50,000.00) chosen so taxable income lands at exactly $19,500.00 — verified independently by hand AND by a one-off script run of the already-proven qdcgt/sdtw functions before being written down, never generated from the wiring code under test."
  - "Mutation Gate M4's literal instruction ('remove one entry from modeledKinds') does not compile on its own — doing so breaks _EveryKindIsEitherModeledOrRefused (TS2344), a stronger and correctly-caught defect, not a coverage gap. The semantically-equivalent compiling mutation (migrate the kind to unmodeledKindRefusals without updating expectedModeledKindCount) is what was actually run, per AGENTS.md's 'rewrite in a form that still typechecks' rule."
  - "TAX-11/TAX-15 marked complete via gsd-sdk's requirements.mark-complete verb, which updates the checkbox lines only — the traceability table's per-requirement status column was left as the tool produced it (already inconsistent with other completed Phase-12 requirements in that same table), not hand-edited."

patterns-established:
  - "Pattern: when a record type gains bookkeeping fields alongside its printed-line fields, replace Object.entries(record) iteration with a hand-typed field-name allowlist typed as a keyof-Exclude, paired with the existing hand-typed count — Object.entries would otherwise crash on the non-ReportLine-shaped bookkeeping fields."

requirements-completed: [TAX-11, TAX-15]

# Metrics
duration: 70min
completed: 2026-08-09
---

# Phase 12.1 Plan 04: Form 1040 Wiring — The Atomic Transition Summary

**Dividend income and Schedule D capital gains compute for real on Form 1040 lines 3a/3b/7a, the Schedule D Tax Worksheet dispatcher branch computes instead of refusing, and the six-kind scope reclassification landed as the single atomic transition commit — with all three mutation gates (ordering, count-guard, atomicity) watched failing on the exact predicted leaf and value, then reverted.**

## Performance

- **Duration:** ~70 min
- **Completed:** 2026-08-09T01:00:17Z
- **Tasks:** 3 (Task 1: Form 1040 wiring, lands first; Task 2: scope reclassification + branch-2a computation + demo fix, the atomic transition; Task 3: mutation gates M3/M4/M5, verification-only)
- **Files modified:** 9 (1 core engine file created no new files; 3 engine files + 5 demo files modified — see key-files)

## Accomplishments

- `fjs/form1040/core/module.f.js`: `Form1040Inputs` widened with `dividendForms`/`brokerageForms`; lines 3a/3b now read 1099-DIV boxes unconditionally (matching 1a/2a/2b's existing pattern); line 7a routes through Schedule D's three-way gain/zero/capped-loss figure when `capitalGainsOrLosses` is declared, or capital gain distributions otherwise; a new, earlier error-arm guard stops the whole report before any line is built when Schedule D's absent-basis refusal fires.
- `fjs/return/scope/module.f.js`: `modeledKinds` 6 → 12 (qualifiedDividends, ordinaryDividends, capitalGainDistributions, capitalGainsOrLosses, unrecaptured1250Gain, collectibles28RateGain added); `unmodeledKindRefusals` 44 → 38; the 12+38=50 invariant re-asserted; `section1202Gain` and `investmentInterestForm4952` re-verified still refusing, by name, for two independently-stated reasons.
- `fjs/tax/line16/module.f.js`: branch 2a now calls a new `sdtwOutcome()` closure (mirroring `qdcgtOutcome`'s shape) instead of building a refusal — the guard condition itself is byte-identical to before. Branch 2b (Form 4952) is completely untouched. Four proof leaves rewritten from "asserts a refusal" to "asserts a computed figure": two reuse 12.1-RESEARCH.md's Worked Example 1 ($7,575.00, `757500n`); two others were hand-computed fresh from the printed worksheet (28%-rate-gain collectibles-only case: $6,975.00; both-nonzero case: $8,175.00; no-qualified-dividends case: $7,611.00), cross-checked at lines 41-47 by hand and confirmed against the already-proven `sdtw` function before being written down.
- `demo/steps/04-refusal.js`: `offered[]` no longer names a now-modeled kind (`qualifiedDividends`/`capitalGainsOrLosses` replaced with `pensionsAndAnnuities`/`scheduleOneAdditionalIncome`); the stale "44-entry" label/anchor corrected to "38-entry" at the table's new declaration line.
- Four other demo files (`00-about.js`, `02-line16.js`, `03-return.js`, `05-exactness.js`) needed mechanical fixes this task's edits forced: a new required `Line16Inputs` field (`form4952Line4eCents`), a widened `Form1040Inputs` (`dividendForms`/`brokerageForms`), and — since this plan's docstring rewrites moved code — re-resolved `sourceFooter` line numbers, plus two now-false UI claims corrected ("Schedule D — refuses" preset and its blurb now describe a return that computes).
- All three Mutation Gates watched failing with the leaf, the assertion, and the wrong value recorded (see dedicated section below), then reverted to byte-identical prior state — confirmed via `git diff HEAD` returning empty after each.
- The phase-wide "no `magi`" structural gate re-confirmed clean across all three of this phase's new module directories (`fjs/form8949`, `fjs/schedule/d`, `fjs/tax/line16/sdtw`) — the first point in the phase all three exist simultaneously.
- Project-local honest proof count: **659 → 663** (4 new leaves, all added in Task 1; Task 2 rewrote existing leaves without adding new ones; Task 3 added none).
- `npx tsc --noEmit` clean at **every** commit boundary, including Task 1's — confirmed the task-order safety argument holds in practice, not just in the plan's prose.
- TAX-11 and TAX-15 marked complete in `.planning/REQUIREMENTS.md` — this is the fourth and final plan of Phase 12.1, closing both requirements which span all four plans.

## Task Commits

1. **Task 1: Wire Form 1040 lines 3a/3b/7a and the dispatch call site (lands FIRST, safe while the scope guard still refuses)** - `31e6a75` (feat)
2. **Task 2: Reclassify six kinds as modeled, make branch 2a compute, and fix the demo (lands LAST — the atomic transition)** - `fe54496` (feat)
3. **Task 3: Watch Mutation Gates M3, M4, M5 fail, then revert each; phase-wide structural gate** - no commit (verification-only; each mutation was applied, observed, and reverted before any commit, leaving all three files byte-identical to their prior commits — confirmed via `git diff HEAD` returning empty after each)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified

- `fjs/form1040/core/module.f.js` - Lines 3a/3b/7a wired to real documents/Schedule D; `Form1040Inputs`/`Form1040IncomeLines`/`inputsOf` widened; a new earlier error-arm guard; the `dispatchLine16` call site now passes real facts. 2,491 lines, 4 new proof leaves.
- `fjs/return/scope/module.f.js` - `modeledKinds` 6→12, `unmodeledKindRefusals` 44→38, docstring rewritten to state the new dividend/capital-gain boundary.
- `fjs/tax/line16/module.f.js` - Branch 2a computes via the new `sdtw` import; `Line16Inputs` gains `form4952Line4eCents`; four proof leaves rewritten with hand-computed figures; `expectedRefusingArmCount` 5→4.
- `demo/lib/fixtures.js` - `dividendForms: []`/`brokerageForms: []` added to the sample return's `inputs` export.
- `demo/steps/02-line16.js`, `03-return.js`, `05-exactness.js`, `00-about.js` - `form4952Line4eCents`/widened-input fixes `tsc` forced; source-footer line numbers re-resolved; two stale "Schedule D refuses" UI claims corrected.
- `demo/steps/04-refusal.js` - `offered[]` and the refusal-table label/anchor corrected.

## Decisions Made

See `key-decisions` in frontmatter. In addition:

- **This plan closes TAX-11 and TAX-15**, marked complete via `gsd-sdk query requirements.mark-complete TAX-11 TAX-15` — both requirements span all four plans of Phase 12.1 (Plans 12.1-01/02/03 each correctly deferred them, per their own SUMMARY notes), and this plan is where Form 8949, Schedule D, and the Schedule D Tax Worksheet are actually wired into a real, computing Form 1040.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `demo/steps/05-exactness.js` needed the widened `Form1040Inputs`**
- **Found during:** Task 1, `npx tsc --noEmit` after widening `Form1040Inputs`
- **Issue:** This demo file constructs `Form1040Inputs` object literals directly (not via the shared `inputs` fixture in `demo/lib/fixtures.js`), so widening the type surfaced two more call sites needing `dividendForms: []`/`brokerageForms: []` beyond the ones the plan's own interfaces section named.
- **Fix:** Added the two empty-array fields to the `withElection` object literal (its sibling `withoutElection` spreads it, so one fix covers both).
- **Files modified:** `demo/steps/05-exactness.js`
- **Verification:** `npx tsc --noEmit` clean.
- **Committed in:** `31e6a75` (Task 1)

**2. [Rule 3 - Blocking] `demo/steps/02-line16.js` needed the new `Line16Inputs` field**
- **Found during:** Task 2, `npx tsc --noEmit` after widening `Line16Inputs` with `form4952Line4eCents`
- **Issue:** This demo file constructs a `Line16Inputs` fixture (`neutral`) directly; the new required field was missing.
- **Fix:** Added `form4952Line4eCents: 0n` to `neutral`.
- **Files modified:** `demo/steps/02-line16.js`
- **Verification:** `npx tsc --noEmit` clean.
- **Committed in:** `fe54496` (Task 2)

**3. [Rule 1 - Bug] Two demo UI claims became actively false as a direct consequence of Task 2's own change**
- **Found during:** Task 2, reading `demo/steps/02-line16.js` while fixing its `tsc` error (deviation 2, above)
- **Issue:** The "Schedule D — refuses" preset button and its `why` tooltip, plus the page copy "Three of them compute; the fourth is selected correctly and then refuses" and the `scheduleDTaxWorksheet` method blurb, all stated that the Schedule D Tax Worksheet branch refuses — which stopped being true the moment this same commit's `fjs/tax/line16` edit landed. Left unfixed, the shipped demo would have shown a real, computed dollar figure under a button and page copy both claiming a refusal.
- **Fix:** Renamed the preset to "Schedule D Tax Worksheet" with an accurate `why`; updated the page copy to "All four compute"; rewrote the method blurb to describe what the worksheet actually does (25%/28% capped add-ons) instead of "correctly selected, and refused."
- **Files modified:** `demo/steps/02-line16.js`
- **Verification:** Manual read-through; no automated check exists for demo prose (out of scope for this project's proof discipline, which covers `fjs/**` only).
- **Committed in:** `fe54496` (Task 2)

**4. [Rule 1 - Bug] Five `sourceFooter` line-number references across four demo files went stale**
- **Found during:** Task 2, after this plan's own docstring rewrites shifted line numbers in `fjs/return/scope/module.f.js`, `fjs/tax/line16/module.f.js`, and `fjs/form1040/core/module.f.js`
- **Issue:** `demo/steps/00-about.js`, `02-line16.js`, `03-return.js`, and `05-exactness.js` each carry hardcoded `{ line, proofLine }` pairs pointing at specific lines in those three files, for GitHub "source ↗"/"proof ↗" links. None of these are checked by any automated anchor audit (a known, previously-deferred gap — `demo/todo/showcase-demo.md`), so they silently point at the wrong line once the referenced file's line count changes.
- **Fix:** Re-grepped each referenced function/export's current line number and updated all five entries (`fjs/tax/line16 — the dispatch`, `fjs/return/scope — the one place a refusal is constructed`, `fjs/return/scope — the six modeled and forty-four refused kinds` — also renamed to "twelve modeled and thirty-eight refused" — `fjs/form1040/core — the whole-return entry point` ×3, `fjs/tax/line16 — which method priced line 16`).
- **Files modified:** `demo/steps/00-about.js`, `02-line16.js`, `03-return.js`, `05-exactness.js`
- **Verification:** Manual re-grep of each target line against the fixed content; not automatable within this plan's scope (no anchor-audit tool exists yet).
- **Committed in:** `fe54496` (Task 2)

---

**Total deviations:** 4 auto-fixed (2 blocking, 2 bugs)
**Impact on plan:** All four fixes were mechanical, direct consequences of this plan's own required edits (type widenings, docstring rewrites) surfacing in files outside the plan's stated `files_modified` list. None represent scope creep — each is exactly the kind of `tsc`-forced or correctness-forced fix AGENTS.md and this plan's own deviation rules call for, and each was verified before being folded into the same task's commit.

## Mutation Gates — Watched Failing (12.1-VALIDATION.md rows M3, M4, M5)

### M3 — the ordering gate

**Mutation applied** (Task 3, in `fjs/tax/line16/module.f.js`'s `dispatchLine16`): moved branch 2c's `if (qualifiedDividendsCents > 0n) { return qdcgtOutcome() }` block to run BEFORE branch 2a's guard, leaving every other line unchanged.

**Which leaf reddened:** `import("./fjs/tax/line16/module.f.js").proof.ordering.scheduleDConditionsOutrankQualifiedDividends()` — the exact leaf named in the plan, plus five others as blast radius (`branches.scheduleDLineNineteenComputesNamingUnrecapturedSection1250Gain`, `branches.scheduleDLineEighteenComputesNamingTwentyEightPercentRateGain`, `branches.formFourNineFiveTwoWithLineFourGRefusesNamingTheElection`, `exhaustiveness.dispatchIsExhaustiveOverTheFourTaxThreeBranches`, `exhaustiveness.everyRefusingArmNamesWhatIsUnmodeled`) — 6 of 663 project-local leaves failed, matching the mutation's actual reach (anything relying on 2a/2b preceding 2c).

**Which assertion reddened:** `assertEq(outcome.method, 'scheduleDTaxWorksheet', [...])` on the target leaf — the method assertion fires before the `cents` assertion is ever reached (`assertEq` throws on first mismatch), so a direct invocation was used to observe both values together: `{ kind: 'ok', method: 'qdcgt', cents: 637500n }` instead of `{ kind: 'ok', method: 'scheduleDTaxWorksheet', cents: 757500n }`.

**The two competing cents values:** correct (2a-before-2c) = `757500n` ($7,575.00, Worked Example 1's own answer); swapped (2c-before-2a) = `637500n` ($6,375.00, the SAME figure `controlSameInputsWithZeroEighteenAndNineteenSelectQdcgt` independently hand-verifies for the QDCGT route on this taxpayer's ordinary facts) — a genuine VALUE disagreement, not merely a tag disagreement, exactly the strengthening Plan 12.1-04's own objective describes.

**Reverted:** file restored from a pre-mutation copy; `git diff HEAD -- fjs/tax/line16/module.f.js` returns empty. `npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'` shows **663**; full `npm test` green (2901 pass, 0 fail).

### M4 — the count-guard

**Mutation applied** (Task 3, in `fjs/return/scope/module.f.js`): the plan's literal instruction — deleting `collectibles28RateGain` from `modeledKinds` alone, leaving `expectedModeledKindCount` at 12 — was tried FIRST and does **not compile**: `tsc` reports `TS2344: Type 'false' does not satisfy the constraint 'true'` at `_EveryKindIsEitherModeledOrRefused`, because the removed kind is then classified nowhere. This is itself a correct and stronger finding (AGENTS.md: "a mutation must still typecheck... or it measures the compiler, not the suite"), recorded here rather than silently worked around. The semantically-equivalent COMPILING mutation was then run instead: `collectibles28RateGain` moved from `modeledKinds` into `unmodeledKindRefusals` (as a real second entry, restoring the type-level partition) while `expectedModeledKindCount` stayed at `12` — i.e., "one entry silently migrated back to unmodeled without correcting the modeled count," a real, reachable regression shape.

**Which leaf reddened:** `import("./fjs/return/scope/module.f.js").proof.partition.modeledKindsIsExactlyTwelve()` — the exact leaf the plan names (post-Task-2 rename from `modeledKindsIsExactlySix`) — plus three others as blast radius (`unmodeledRefusalsIsExactlyThirtyEight`, `refusalTableFollowsKindVocabularyOrder`, `scope.allTwelveModeledKindsDeclaredTogetherAreInScope`) — 4 of 663 leaves failed.

**Which assertion reddened:** `assertEq(modeledKinds.length, expectedModeledKindCount)` — failure output `[ 11, 12 ]`, exactly the `[11, 12]` pair the plan predicted.

**Reverted:** file restored from a pre-mutation copy; `git diff HEAD -- fjs/return/scope/module.f.js` returns empty. `npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'` shows **663**; full `npm test` green (2901 pass, 0 fail).

### M5 — the atomicity constraint

**Mutation applied** (Task 3, in `fjs/form1040/core/module.f.js`): reverted `line3a` from `fromDocuments('1040 line 3a')(sumBoxOverDocuments(...))` back to `declaredZero('1040 line 3a')` — Task 1's own wiring, undone — while leaving `fjs/return/scope`'s six-kind reclassification (Task 2) fully in place. Compiles clean (both idioms are `ReportLine`-producing calls of the same shape).

**Which leaf reddened:** `import("./fjs/form1040/core/module.f.js").proof.dividendsAndScheduleDBeforeTheScopeReclassificationLands.mixedDividendsComputeRealLinesThreeAThreeBSevenAAndANonZeroLineSixteen()` — 1 of 663 leaves failed (the narrowest blast radius of the three gates, because only this one leaf asserts line 3a's real value on a dividend-bearing fixture).

**Which assertion reddened:** `assertEq(lineRuled(outcome.lines)('1040 line 3a').value, 50000n, '$500.00 qualified dividends')` — failure output `[ 0n, 50000n, '$500.00 qualified dividends' ]` — `line3a.value === 0n`, the exact "confident zero" this phase's entire task-ordering strategy exists to prevent from ever landing for real, now caught by a real proof the instant it is reintroduced.

**Reverted:** file restored from a pre-mutation copy; `git diff HEAD -- fjs/form1040/core/module.f.js` returns empty. `npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'` shows **663**; full `npm test` green (2901 pass, 0 fail).

### Phase-wide structural gate

`grep -rn "\bmagi\b" fjs/form8949 fjs/schedule/d fjs/tax/line16/sdtw` reports nothing (empty output) — confirmed at the point all three of this phase's new module directories exist simultaneously for the first time.

## Task 2 acceptance criterion verified without a new committed proof leaf

Task 2's own acceptance criteria ask that "Task 1's clean-gain and absent-basis `capitalGainsOrLosses` fixtures both now pass through the FULL `form1040Report(...)` entry point... confirming the scope guard no longer masks either" — but Task 2's file list restricts `fjs/form1040/core/module.f.js` to exactly ONE line (`form4952Line4eCents: 0n`). This was verified as a one-off script run (not committed, mirroring how Plans 12.1-01/03's own mutation-gate tasks are verification-only):

- **Clean-gain fixture** (declared `capitalGainsOrLosses`, one 1099-B with both proceeds and basis present) through `form1040Report(...)`: now returns `{ kind: 'ok', lines: [...], ... }` — the scope guard no longer masks it.
- **Absent-basis fixture** (same declared kind, one 1099-B with proceeds present and basis genuinely absent) through `form1040Report(...)`: still returns `{ kind: 'error', ... }`, but now via the NEW mechanism — `message: "Form 8949: document sha256-b-absent-basis has box1dProceeds present but box1eCostOrOtherBasis is genuinely absent..."`, `unmodeled: []` — not the OLD scope-refusal message shape (`"scope refusal: this return declares..."`, `unmodeled: [...]`). The mechanism shift Task 1's own leaf flagged as "not yet distinguishable" is now directly observable.

## Carried-forward item, still open (not resolved by this plan)

Plan 12.1-02 flagged, and 12.1-03 did not close: **SDTW lines 22 and 32** carry printed "skip" instructions reasoned to be safe no-ops (the surrounding mins/floors make the shortcut and the straight-through arithmetic agree), but no worked example exercises either path directly. This plan's own mutation sweep — M3, M4, M5 — was named as this gap's designated fallback owner. **None of the three gates, nor any of this plan's own hand-computed fixtures (Worked Example 1 reused; the 28%-rate-gain collectibles-only, both-nonzero, and no-dividends cases), happen to construct an input where `line1 === line16` or `line1 === line32`** (every fixture used here has taxable income strictly above the zero-rate breakpoint with a nonzero ordinary-rate slice). This remains a reasoned-but-unobserved inference, now flagged a second time. Closing it would require a fixture with taxable income small enough to land entirely within the SDTW's own 0%-rate treatment — a genuinely new fixture, out of this plan's scope (`fjs/tax/line16/sdtw/module.f.js` is not in this plan's `files_modified` list). Recommend a future phase's own mutation sweep (or a dedicated micro-plan) construct this fixture explicitly rather than let it evaporate a third time.

## Issues Encountered

- Mutation Gate M4's literal instruction as written in `12.1-VALIDATION.md` does not compile on its own (see the M4 section above) — resolved by running the semantically-equivalent compiling form, per AGENTS.md's own documented precedent for this exact situation.
- No other issues. `npx tsc --noEmit` was green at both Task 1's and Task 2's commit boundaries on the first attempt after each task's edits settled (after fixing the `tsc`-surfaced call sites the plan itself predicted would need it).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 12.1 (The Capital-Gain Chain) is now COMPLETE: all four plans landed (12.1-01 Form 8949, 12.1-02 the Schedule D Tax Worksheet, 12.1-03 Schedule D, 12.1-04 this plan's Form 1040 wiring and atomic scope transition).
- TAX-11 and TAX-15 marked complete in REQUIREMENTS.md — both were the last two Pending requirements this phase owned.
- `section1202Gain` and `investmentInterestForm4952` remain unmodeled by design, as does the multi-year capital-loss-carryover (TAX-17, Phase 15) and the wash-sale/accrued-market-discount adjustment (Decision 1.4, Phase 15 candidate) — none of these are blockers for the approved autonomous run's next phase.
- The carried-forward SDTW lines 22/32 skip-instruction inference (above) remains open and should be visible to whichever future work next touches `fjs/tax/line16/sdtw`.
- Per the phase owner's approved autonomous run (STATE.md, 2026-08-07): next is **Phase 13**, with Phase 14 (Acceptance) skipped as NOT AUTONOMOUS-EXECUTABLE.

## Self-Check: PASSED

- FOUND: `fjs/form1040/core/module.f.js`, `fjs/return/scope/module.f.js`, `fjs/tax/line16/module.f.js` (all modified, present)
- FOUND: commit `31e6a75`, FOUND: commit `fe54496` (`git log --oneline --all | grep -E '31e6a75|fe54496'`)
- Re-measured at write time: `npx tsc --noEmit` clean; `node --test 2>&1 | grep -c '^✔ import("./fjs/'` = 663 (659 baseline + 4 Task-1 leaves); `npm test` green (2901 pass, 0 fail); `git status --short` clean (all three mutation-gate files confirmed byte-identical to their last commit via `git diff HEAD` returning empty, immediately after each revert).

---
*Phase: 12.1-the-capital-gain-chain*
*Completed: 2026-08-09*
