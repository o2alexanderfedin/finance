---
phase: 12.1-the-capital-gain-chain
plan: 02
subsystem: tax-engine
tags: [schedule-d-tax-worksheet, capital-gains, unrecaptured-1250, collectibles-28-percent, functionalscript, qdcgt]

# Dependency graph
requires: []
provides:
  - "fjs/tax/line16/sdtw/module.f.js — sdtw(taxParamSet)(input) => Sdtw, the standalone 47-line Schedule D Tax Worksheet Plan 12.1-04 wires into dispatchLine16 branch 2a"
affects: [12.1-04-form1040-wiring]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Printed-line-number worksheet idiom (TAX-15), copied verbatim in shape from fjs/tax/line16/qdcgt: one const lineN per printed line, in IRS order, floor lines as ternaries, no-floor-but-nonnegative lines as assert (never a defensive clamp)"
    - "Explicit zero-gating for add-on line groups (35-40, 41-43) whose controlling Schedule D line is zero, rather than trusting the surrounding arithmetic to reduce to zero on its own — necessary because line 42 carries no printed floor"
    - "Degenerate-equivalence differential proof: two independently-transcribed worksheets (sdtw, qdcgt) cross-checked against each other on the same input, rather than either being trusted alone"

key-files:
  created:
    - fjs/tax/line16/sdtw/module.f.js
  modified: []

key-decisions:
  - "Lines 22's ('if lines 1 and 16 are the same, skip 23-43') and line 32's ('if lines 1 and 32 are the same, skip 33-43') printed skip instructions are left unimplemented as special cases — the ordinary mins/floors already present in lines 23-43 make the shortcut and the straight-through computation agree, mirroring how qdcgt needs no skip logic for any of its own lines. Verified by full-line reproduction of both worked examples, neither of which happens to trigger these two conditions, so this remains a reasoned inference rather than a directly-observed equivalence — flagged here for a future mutation sweep to confirm."
  - "Lines 35-40 (gated on Schedule D line 19 = 0) and 41-43 (gated on Schedule D line 18 = 0) ARE explicitly gated to 0n rather than left to fall out of the natural arithmetic, because line 42 carries no printed floor: computing 41-43 straight through when Schedule D line 18 is zero would silently add a spurious 28% charge on ordinary income that was never collectibles gain."
  - "The split-dispatch fixture (method44 !== method46) and the degenerate-equivalence fixture (sdtw.line47 === qdcgt.line25) are the SAME underlying input (MFJ, $150,000 taxable income, $100,000 Schedule D gain, lines 18/19 both zero) rather than two separate constructions — both properties held simultaneously once hand-verified, and using one fixture for both proofs is a stronger claim (one input that both independently dispatches AND agrees with qdcgt) than two disconnected ones."
  - "Two tasks, two commits, splitting the same file: Task 1's commit contains the full 47-line implementation plus one structural smoke-test proof (sdtwReturnsAllFortySevenLinesPlusBothMethodTags); Task 2's commit adds the four substantive regression/equivalence proof leaves the plan names. This mirrors Plan 12.1-01's own precedent of one task, one commit."

patterns-established:
  - "Pattern: explicit-gate-to-floor-value for a worksheet add-on block whose controlling upstream field is zero, used instead of relying on downstream floors to absorb the case — apply wherever a later worksheet has an unfloored subtraction line fed by a conditionally-inapplicable block."

requirements-completed: []  # TAX-11/TAX-15 span all four plans of Phase 12.1; not marked complete until 12.1-04 wires this into dispatchLine16. See Plan 12.1-01's own note for the same reasoning.

# Metrics
duration: 55min
completed: 2026-08-08
---

# Phase 12.1 Plan 02: The Schedule D Tax Worksheet Summary

**Standalone `fjs/tax/line16/sdtw/module.f.js` transcribing all 47 printed lines of the Schedule D Tax Worksheet cent-exact, with both hand-transcribed worked examples reproduced line by line, an independent-dispatch fixture proving lines 44/46 can disagree on Tax Table vs. Tax Computation Worksheet in one execution, and a degenerate-equivalence proof against the already-shipped QDCGT worksheet.**

## Performance

- **Duration:** ~55 min
- **Started:** 2026-08-08T22:35:00Z (approx, per STATE.md handoff)
- **Completed:** 2026-08-08T23:16:47Z
- **Tasks:** 2 (Task 1: transcribe the worksheet; Task 2: prove both worked examples, degenerate equivalence, and structural gates)
- **Files modified:** 1 (created)

## Accomplishments

- `fjs/tax/line16/sdtw/module.f.js` exports `sdtw`, `SdtwInput`, `Sdtw` — the exact contract Plan 12.1-04 needs to make `dispatchLine16` branch 2a compute instead of refuse.
- Every one of the 47 printed lines exists as its own named `const`, in IRS order, each preceded by its printed instruction — no variable named `magi` anywhere.
- Line 19's breakpoint ($197,300/$394,600) is derived from `ordinaryBrackets[status]`'s 24%-bracket ceiling via `assert`-narrowed lookup, never a fresh hand-typed literal (grepped and confirmed absent).
- Both RESEARCH.md worked examples reproduce exactly, line by line: Example 1 (MFJ, unrecaptured §1250 gain, base rate not exceeded) → Form 1040 line 16 = $7,575.00; Example 2 (single, 28% collectibles gain, base rate ceiling actually binds) → $211,520.25.
- `baseTaxForAmount` is called independently at lines 44 and 46 — proven to disagree in one execution (Tax Table vs. Tax Computation Worksheet) via a dedicated split-dispatch fixture.
- The degenerate-equivalence differential this project's roadmap deferred since Phase 10 now holds: on an input with Schedule D lines 18/19 both zero, `sdtw(...).line47 === qdcgt(...).line25` exactly ($13,521.00, non-trivial).
- 5 new proof leaves; project-local honest count rose from **644 to 649** — exactly the number of leaves added.
- `npx tsc --noEmit` clean throughout; `npm test` fully green (2887 pass, 0 fail) at the end.

## Task Commits

1. **Task 1: Transcribe the 47-line Schedule D Tax Worksheet as one pure function** - `43e5523` (feat)
2. **Task 2: Prove both worked examples line-by-line, the degenerate QDCGT equivalence, and the TAX-15 structural gates** - `b9165e9` (test)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified

- `fjs/tax/line16/sdtw/module.f.js` - The 47-line Schedule D Tax Worksheet as one pure function, cent-exact, mirroring `fjs/tax/line16/qdcgt`'s shape. 590 lines, 5 proof leaves.

## Decisions Made

See `key-decisions` in frontmatter. In addition:

- **TAX-11/TAX-15 are NOT marked complete by this plan**, despite being named in this plan's frontmatter `requirements: [TAX-11, TAX-15]`. Both requirements span all four plans of Phase 12.1; this plan delivers only the standalone worksheet, not its wiring into `dispatchLine16`. Deferred to Plan 12.1-04, matching Plan 12.1-01's own precedent and reasoning exactly.

## Deviations from Plan

None — plan executed exactly as written. Both worked examples' arithmetic was independently re-derived by hand before writing any code (per CONTEXT.md Decision 3.4) and reconciled exactly against RESEARCH.md's own table; no transcription discrepancy was found, so the plan's `<deviation_rule>` escape hatch was not needed.

One clarification, not a deviation: the plan's Task 1 `<behavior>` asked for "a THIRD proof case combining Example 1's line21-producing inputs with Example 2's line1-producing inputs, analogous to QDCGT's own splitDispatchInput." Rather than literally splicing Example 1's and Example 2's numbers together (which would have created complex cross-interactions between the unrecaptured-§1250 and collectibles add-ons that neither example alone exercises), a simpler bespoke fixture was designed and independently hand-verified line by line (MFJ, $150,000 taxable income, $100,000 Schedule D gain, Schedule D 18/19 both zero) that cleanly demonstrates the same required property — `method44 !== method46` in one execution — while also, as a bonus discovered during hand-verification, satisfying the degenerate-equivalence precondition, letting one fixture serve both of Task 2's independent-dispatch and degenerate-equivalence proofs.

## Issues Encountered

None. The two-commit split (Task 1 with a minimal structural proof, Task 2 adding the four substantive leaves) required temporarily removing the `qdcgt` import and the four Task-2 proof leaves for Task 1's commit, then restoring the full file for Task 2's commit — done via a scratch copy of the completed file, verified `tsc`-clean and count-correct (645) at the intermediate stage before restoring to the final 649-count state.

## Worked-Example Verification (independently reproduced before trusting)

Both examples' full 47-line arithmetic were hand-recomputed from RESEARCH.md's own printed-instruction transcription (not from RESEARCH.md's summary table alone) before any code was written:

- **Example 1** (MFJ, $97,000 taxable income, $300 qualified dividends, Schedule D 15/16 = $40,000 gain each, line 18 = $0, line 19 = $10,000 unrecaptured §1250 gain): every intermediate line reconciled exactly against RESEARCH.md's table; final line 47 = $7,575.00, both `method44` and `method46` = `taxTable`.
- **Example 2** (single, $700,000 taxable income, $0 qualified dividends, Schedule D 15/16 = $50,000 gain each, line 18 = $50,000 collectibles gain, line 19 = $0): every intermediate line reconciled exactly; final line 47 = $211,520.25, both `method44` and `method46` = `taxComputationWorksheet`. The sanity cross-check (28% ceiling saves exactly $4,500.00 vs. the 37% marginal rate) is asserted as its own leaf assertion (`line46 - line45 = 450000n`).

No transcription discrepancy was found in either example — both RESEARCH.md's own table and the printed-instruction re-derivation agree exactly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `fjs/tax/line16/sdtw/module.f.js` exports the exact `sdtw`/`SdtwInput`/`Sdtw` contract Plan 12.1-04 needs to make `dispatchLine16` branch 2a compute (`fjs/tax/line16/module.f.js`'s `if (scheduleDLinesFifteenAndSixteenAreBothGains && (scheduleD18Cents > 0n || scheduleD19Cents > 0n))` guard, currently a refusal).
- The degenerate-equivalence proof (`sdtw(...).line47 === qdcgt(...).line25` when Schedule D 18/19 are both zero) is exactly the property Plan 12.1-04's Mutation Gate M3 needs: once branch 2a computes, swapping its order with branch 2c (QDCGT) must change the returned VALUE, not merely the method tag — this plan's proof file is the reference figure ($13,521.00 for the shared fixture) that gate can compare against.
- One open, low-risk inference not directly observed by either worked example: lines 22's and 32's own printed "skip" conditions (`line1 === line16`, `line1 === line32`) were reasoned to be safe no-ops on the straight-through arithmetic (mirroring qdcgt's own precedent of needing no skip logic at all), but neither worked example happens to trigger either condition. If Plan 12.1-04's phase mutation sweep constructs a fixture where taxable income is small enough to trigger these (e.g., total income entirely within the 0% capital-gains bracket), that would be the first direct observation of this inference; flagging it here rather than leaving it silent.

## Self-Check: PASSED

- FOUND: `fjs/tax/line16/sdtw/module.f.js`
- FOUND: commit `43e5523`, FOUND: commit `b9165e9` (`git log --oneline --all | grep -E '43e5523|b9165e9'`)
- Re-measured at write time: `npx tsc --noEmit` clean; `node --test 2>&1 | grep -c '^✔ import("./fjs/'` = 649 (644 baseline + 5 leaves); `npm test` green (2887 pass, 0 fail); `git status --short` clean.

---
*Phase: 12.1-the-capital-gain-chain*
*Completed: 2026-08-08*
