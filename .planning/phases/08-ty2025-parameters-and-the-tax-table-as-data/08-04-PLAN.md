---
phase: 08-ty2025-parameters-and-the-tax-table-as-data
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified: [fjs/server/finance_tax_params/module.f.js, fjs/server/module.f.js, fjs-run-integration.test.js]
autonomous: true
requirements: [MCP-07]
tags: [mcp-tool, finance_tax_params, integration, phase-8]

must_haves:
  truths:
    - "finance_tax_params(2025) returns the parameter set, each entry carrying its own Rev. Proc. citation, so the agent reads parameters rather than recalling them"
    - "An unsupported year returns an errorResult naming the supported years — never a null, an empty set, or a silent fallback"
    - "The response carries the parameters and the Tax Table's band structure, never the generated rows, and stays comfortably inside the 64KB size guard"
    - "The tool is reachable through a real stdio JSON-RPC session, not only through the virtual/unit-level proof"
  artifacts:
    - path: "fjs/server/finance_tax_params/module.f.js"
      provides: "The finance_tax_params MCP tool, mirroring finance_schema's shape exactly"
      contains: "financeTaxParamsTool"
    - path: "fjs/server/module.f.js"
      provides: "financeMcpHandlers registers financeTaxParamsTool"
      contains: "financeTaxParamsTool"
    - path: "fjs-run-integration.test.js"
      provides: "A real-process call('finance_tax_params', { year: 2025 }) in the same session the existing calls run in"
      contains: "finance_tax_params"
  key_links:
    - from: "fjs/server/module.f.js financeMcpHandlers"
      to: "fjs/server/finance_tax_params/module.f.js financeTaxParamsTool"
      via: "array concatenation into fromRegistry([...])"
      pattern: "financeTaxParamsTool"
    - from: "fjs-run-integration.test.js"
      to: "fjs/server/finance_tax_params/module.f.js"
      via: "call('finance_tax_params', { year: 2025 }) in the same real session that derives advertisedTools/toolsCalled at runtime"
      pattern: "finance_tax_params"
---

<objective>
Build `finance_tax_params(year)`, the MCP tool that hands the agent Plans 08-01/08-02's stored
parameter set and Tax Table band structure — mirroring `finance_schema`'s already-shipped shape
exactly — register it in `financeMcpHandlers`, and exercise it through the real stdio integration
session in the SAME task the registry entry lands in.

Purpose: MCP-07 requires the agent to read TY2025 parameters through a tool rather than recalling
them. Per 08-VALIDATION.md's explicit ordering note, registering a fifth tool in
`financeMcpHandlers` without a matching `call(...)` in `fjs-run-integration.test.js` breaks
`npm test` immediately, because that file derives its advertised/called tool sets from a live
`tools/list` response at runtime — this is the mechanism working as designed, and the two edits
must land together.
Output: `fjs/server/finance_tax_params/module.f.js` (new tool), one new entry in
`fjs/server/module.f.js`'s `financeMcpHandlers`, and one new real-process call in
`fjs-run-integration.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/08-ty2025-parameters-and-the-tax-table-as-data/08-CONTEXT.md
@.planning/phases/08-ty2025-parameters-and-the-tax-table-as-data/08-RESEARCH.md
@.planning/phases/08-ty2025-parameters-and-the-tax-table-as-data/08-VALIDATION.md
@.planning/phases/08-ty2025-parameters-and-the-tax-table-as-data/08-01-SUMMARY.md
@.planning/phases/08-ty2025-parameters-and-the-tax-table-as-data/08-02-SUMMARY.md
</context>

<interfaces>
<!-- fjs/server/finance_schema/module.f.js — the precedent this plan mirrors, already read in
     full during planning -->
export const financeSchemaTool: ToolEntry<never>
// dialectSchemas typed { readonly [dialect: string]: Type }, errorResult message shape:
// `unknown dialect: ${args.dialect}; known: ${knownDialects.join(', ')}`

<!-- fjs/server/module.f.js — existing, already read in full during planning -->
export const financeMcpHandlers = home => cacheKey => fromRegistry([
    ...casToolRegistry(home)(cacheKey),
    ...evoToolRegistry(evo(fileCas(sha256)(home))(cacheKey)),
    casRefreshTool(fileCas(sha256)(home))(cacheKey),
    financeSchemaTool,
    fjsRunTool(home)(fileCas(sha256)(home))(evo(fileCas(sha256)(home))(cacheKey)),
])

<!-- fjs-run-integration.test.js — existing, already read in full during planning -->
const call = async (name, args) => { ... toolsCalled.add(name) ... }
// end-of-file self-enforcing check: [...toolsCalled].sort().join(',') === [...advertisedTools].sort().join(',')

<!-- fjs/tax/params/module.f.js (Plan 08-01), fjs/tax/table/module.f.js (Plan 08-02) — the two
     data sources this tool composes, field names confirmed in their SUMMARYs -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: finance_tax_params tool, mirroring finance_schema exactly</name>
  <files>fjs/server/finance_tax_params/module.f.js</files>
  <read_first>
    fjs/server/finance_schema/module.f.js (the exact precedent to mirror — `toolEntry`
    construction, the open-keyed-map lesson, the `errorResult` refusal message shape, the
    five-leaf proof pattern), fjs/tax/params/module.f.js (every field this tool re-exposes),
    fjs/tax/table/module.f.js (`taxTableBandStructure` only — never the generated rows, per
    08-CONTEXT.md's explicit decision that the response would otherwise exceed the 64KB size
    guard).
  </read_first>
  <action>
    Create `fjs/server/finance_tax_params/module.f.js`. Import `taxParamsByYear` from
    `../../tax/params/module.f.js` and `taxTableBandStructure` from `../../tax/table/module.f.js`.

    First, narrow `taxParamsByYear[2025]` exactly ONCE, at module scope, before building anything
    from it: `const taxParams2025 = taxParamsByYear[2025]` immediately followed by
    `assert(taxParams2025 !== undefined, 'expected TY2025 parameters to be present in
    taxParamsByYear')` (`assert` from `functionalscript/fjs/asserts/module.f.js`). This project's
    `tsconfig.json` sets `noUncheckedIndexedAccess: true`, so indexing `taxParamsByYear[2025]`
    yields `TaxParamSet | undefined` even for the literal key `2025` — the open index-signature
    type `fjs/tax/params/module.f.js` declares does not narrow away on its own. Do NOT use a
    non-null assertion (`taxParamsByYear[2025]!`) or a type cast to silence the compiler here —
    both are banned by AGENTS.md's hard rules; `assert` is the only compliant narrowing path, and
    it throws a bare string, never an `Error` — nothing downstream may branch on
    `instanceof Error`.

    Using the narrowed `taxParams2025`, build and export a named constant `response2025 =
    { taxYear: 2025, standardDeduction: taxParams2025.standardDeduction, agedOrBlindAdditional:
    taxParams2025.agedOrBlindAdditional, dependentStandardDeductionCap:
    taxParams2025.dependentStandardDeductionCap, ordinaryBrackets: taxParams2025.ordinaryBrackets,
    capitalGainsBreakpoints: taxParams2025.capitalGainsBreakpoints, taxTableBandStructure }` —
    explicitly NOT the generated rows. Exporting this named constant (rather than only assembling
    it inline inside the lookup map below) gives Task 2's proof something to compare against
    directly, without a second index into any open-keyed map.

    Build `taxParamsResponses`, a lookup map keyed by year, typed as an open numeric-keyed map
    `{ readonly [year: number]: TaxParamsResponse }` (mirroring `finance_schema`'s own
    open-string-keyed-map lesson, applied here to a numeric key), with exactly one entry today:
    `taxParamsResponses = { 2025: response2025 }`. Export `knownYears =
    Object.keys(taxParamsResponses).map(Number)`.

    Export `financeTaxParamsTool`, built via `toolEntry` from
    `functionalscript/fjs/protocol/mcp/module.f.js`, named `'finance_tax_params'`, with a
    description mirroring `finance_schema`'s own phrasing but for tax-year parameters (e.g. "Given
    a tax year, returns that year's TY parameter set — standard deduction, aged/blind additional
    amounts, ordinary rate brackets, capital-gains breakpoints, and the Tax Table's band structure,
    each carrying its own Rev. Proc. citation — read this before authoring a program that consults
    a tax figure, so parameters are read, never recalled."), input RTTI `{ year: number }`, and a
    handler that looks `args.year` up in `taxParamsResponses`: if `undefined`, returns
    `pure(errorResult(`unknown tax year: ${args.year}; known: ${knownYears.join(', ')}`))`
    (mirroring `finance_schema`'s unknown-dialect message verbatim in spirit) — this lookup is
    ALREADY the correct narrowing pattern for an arbitrary, untrusted `args.year`, exactly like
    `finance_schema`'s own `dialectSchemas[args.dialect]` check, and needs no further change;
    otherwise returns `pure(okResult(JSON.stringify(response)))`.
  </action>
  <verify>
    <automated>npx tsc</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc` exits 0 with no output
    - `grep -c "finance_tax_params" fjs/server/finance_tax_params/module.f.js` is at least 2
      (the tool name string and the description referencing it)
    - `grep -c "errorResult" fjs/server/finance_tax_params/module.f.js` is at least 1
    - `grep -n "taxParamsByYear\[2025\].*Rows\|generateRow\|lookupTaxTable" fjs/server/finance_tax_params/module.f.js` returns no output — the response never includes generated rows
    - `grep -n "const taxParams2025 = taxParamsByYear\[2025\]" fjs/server/finance_tax_params/module.f.js` shows the narrowed constant defined
    - `grep -c "taxParamsByYear\[2025\]" fjs/server/finance_tax_params/module.f.js` equals 1 —
      only the single narrowing site indexes the map directly
    - `grep -n "export const response2025" fjs/server/finance_tax_params/module.f.js` shows the
      named constant Task 2's proof compares against
  </acceptance_criteria>
  <done>The tool exists, mirrors finance_schema's shape, excludes generated rows from its
  response, and narrows `taxParamsByYear[2025]` exactly once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit proof, registry wiring, and the integration-test call — one task, per the ordering rule</name>
  <files>fjs/server/finance_tax_params/module.f.js, fjs/server/module.f.js, fjs-run-integration.test.js</files>
  <read_first>
    fjs/server/module.f.js (the `financeMcpHandlers` array and the
    `toolsListEnumeratesComposedRegistry` proof leaf to extend), fjs-run-integration.test.js (the
    `call`/`waitForId` pattern and the end-of-file
    `toolsCalled`/`advertisedTools` self-enforcing coverage assertion),
    .planning/phases/08-ty2025-parameters-and-the-tax-table-as-data/08-VALIDATION.md's "Ordering
    note the executor must not get wrong" section.
  </read_first>
  <behavior>
    - `year2025Resolves`: calls `financeTaxParamsTool.handle({ year: 2025 })` via `runPure`,
      asserts `isError` is `undefined`, and asserts the JSON-parsed response text deep-equals
      `response2025` — Task 1's own exported, already-narrowed constant — never a second,
      hand-typed literal, and never a fresh `taxParamsResponses[2025]` index (which would be
      `TaxParamsResponse | undefined` again under this project's `noUncheckedIndexedAccess`).
    - `responseStaysUnderSizeGuard`: asserts the returned text's `.length` is strictly less than
      `65536` (64KB), demonstrating the no-generated-rows design holds in practice, not only in
      intent.
    - `unknownYearRefused`: calls with `{ year: 2024 }`, asserts `isError` is `true`, and asserts
      the message includes both `'2024'` (the offending year) and `'2025'` (the known set) —
      mirroring `finance_schema`'s `unknownDialectRefused` leaf.
  </behavior>
  <action>
    Add the `proof` export described above to `fjs/server/finance_tax_params/module.f.js`, using
    `runPure` from `functionalscript/fjs/effects/module.f.js` exactly as
    `fjs/server/finance_schema/module.f.js`'s own `call`/`textOf` helpers do.

    In `fjs/server/module.f.js`: import `financeTaxParamsTool` from
    `./finance_tax_params/module.f.js` and add it as an entry in `financeMcpHandlers`'s
    `fromRegistry([...])` array, alongside `financeSchemaTool`/`fjsRunTool`. Extend the existing
    `toolsListEnumeratesComposedRegistry` proof leaf with one more assertion:
    `assert(tools.some(t => t.name === 'finance_tax_params'))`.

    In `fjs-run-integration.test.js`: add exactly one `await call('finance_tax_params', { year:
    2025 })` in the same real session the existing calls already run in (after the existing
    `finance_schema` call is a natural place, but any point in the same session satisfies the
    coverage requirement), asserting the response is not an error and that its text includes
    `'31500.00'` (the MFJ standard deduction amount) and `'2025-32'` (its citation) — both
    confirming the tool is reachable for real and that the response actually carries the cited
    parameter data, not a stub. These three edits are ONE task and belong in ONE commit: per
    08-VALIDATION.md, registering the tool in `financeMcpHandlers` without this integration-test
    call breaks `npm test` immediately, because that test's own coverage assertion compares the
    set of tools called against the set `tools/list` advertises at runtime.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and reports 0 fail
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` is strictly greater than 185 (the
      pre-phase baseline), accounting for every leaf added across all four of this phase's plans
    - `npm run test:integration` exits 0
    - `grep -n "financeTaxParamsTool" fjs/server/module.f.js` shows it present in the
      `financeMcpHandlers` registry array
    - `grep -n "call('finance_tax_params'" fjs-run-integration.test.js` shows the call is present
    - `git diff --stat` for this task's commit touches all three files listed above — the
      registry entry and the integration call are not split across separate commits
  </acceptance_criteria>
  <done>finance_tax_params is registered, callable through the real stdio session, stays inside
  the size guard, and the integration suite's self-enforcing coverage check passes without
  needing a follow-up plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → finance_tax_params | The `{ year: number }` argument is untrusted client input; the response is our own compiled-in data, not re-parsed from anywhere untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure / silent regression (a tool shipped but never proven reachable, repeating Phase 7's mocked-seam defect) | `finance_tax_params`'s reachability through the real transport | mitigate | The real-process integration call in Task 2 is required, not optional; the integration test's own runtime-derived coverage assertion fails the whole suite if this tool is advertised but never called |
| — | Denial of Service (malformed `year` argument) | `financeTaxParamsTool`'s input handling | mitigate | `toolEntry`'s own RTTI validation on `{ year: number }` refuses a malformed argument via `errorResult` before the handler ever runs — the same generic mechanism every existing tool in this registry already relies on |
</threat_model>

<verification>
`npm test` exits 0 with `tsc` clean, and the project-local proof count strictly greater than 185.
`npm run test:integration` exits 0. Manual: `git status --porcelain` is empty after the plan
completes; `grep -n "financeTaxParamsTool" fjs/server/module.f.js` and `grep -n
"call('finance_tax_params'" fjs-run-integration.test.js` both show the wiring landed together.
</verification>

<success_criteria>
`finance_tax_params(2025)` is registered, callable through the real stdio session, refuses an
unsupported year actionably, and stays comfortably inside the response size guard. MCP-07 is
satisfied, and every one of Phase 8's four requirements (TAX-01, TAX-02, TAX-04, MCP-07) is now
covered across this phase's four plans.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ty2025-parameters-and-the-tax-table-as-data/08-04-SUMMARY.md`
</output>
