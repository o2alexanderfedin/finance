---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 12
type: execute
wave: 5
depends_on: ["13-11"]
files_modified: [fjs/form1040/core/module.f.js]
autonomous: true
requirements: [TAX-14]

must_haves:
  truths:
    - "1040 lines 8, 10, 17, 20, 23 and 31 cite Schedule 1/2/3's own per-line documented zeros instead of an opaque inline declaredZero call"
    - "A single return combining every wave's feature (retirement income, Social Security, the senior deduction, itemizing, dependents) computes correctly end to end for the first time in this phase"
    - "modeledKinds and unmodeledKindRefusals are unchanged by this plan (still 20/30) — the five Schedule-1/2/3-adjacent coarse kinds stay refused, per Plan 13-11's documented finding"
  artifacts:
    - path: "fjs/form1040/core/module.f.js"
      provides: "lines 8/10/17/20/23/31 wired to scheduleOne/scheduleTwo/scheduleThree"
      contains: "scheduleOne"
---

<objective>
Wire Schedule 1/2/3's own per-line documented zeros into Form 1040 lines 8, 10, 17, 20, 23 and
31 — replacing five opaque inline `declaredZero(...)` calls with real, traceable module calls —
and prove, for the first time in this phase, that one return combining retirement/Social
Security income (Wave 1), the senior deduction (Wave 2), itemizing (Wave 3) and dependents
(Wave 4) computes correctly end to end.

Purpose: ROADMAP.md criterion 4's "carry every line the profile actually reaches" is satisfied
by real per-line citation, not by a single catch-all zero; the combined end-to-end proof is
this phase's first genuine full-profile computation.
Output: `fjs/form1040/core/module.f.js` wired to the three new schedule modules; no kind
reclassification in this plan (see Plan 13-11's objective for why).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
From Plan 13-11: `scheduleOne(profile)`, `scheduleTwo(profile)`, `scheduleThree(profile)`, each
returning a record of documented-zero `ReportLine`s keyed by printed line name, plus a
`line10`/`line26` (Schedule 1), `line3`/`line21` (Schedule 2), `line8`/`line15` (Schedule 3)
total.

From `fjs/form1040/core/module.f.js` (current — all six fields below ALREADY EXIST as
`declaredZero` placeholders; no typedef/orderedLines/count-constant change is needed, per this
phase's recurring finding that every 1a-37 field was already declared in Phase 10):
```
const line8 = declaredZero('1040 line 8')     // additional income, Schedule 1
const line10 = declaredZero('1040 line 10')   // adjustments, Schedule 1
const line17 = declaredZero('1040 line 17')   // Schedule 2, part I
const line20 = declaredZero('1040 line 20')   // Schedule 3, part I
const line23 = declaredZero('1040 line 23')   // other taxes, Schedule 2 part II
const line31 = declaredZero('1040 line 31')   // Schedule 3, part II
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire lines 8, 10, 17, 20, 23 and 31 through the three schedule modules</name>
  <files>fjs/form1040/core/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js (all six placeholders' exact current locations — line8/line10
    in `form1040IncomeLines`, line17/line20/line23/line31 in `form1040TaxAndPaymentLines`)
  </read_first>
  <action>
    Import `scheduleOne` from `../../schedule/1/module.f.js`, `scheduleTwo` from
    `../../schedule/2/module.f.js`, `scheduleThree` from `../../schedule/3/module.f.js`. In
    `form1040IncomeLines`, compute `const scheduleOneResult = scheduleOne(profile)` once, and
    replace `line8`/`line10`'s value expressions with `{ value: scheduleOneResult.line10.value,
    sources: scheduleOneResult.line10.sources, rule: '1040 line 8' }` /
    `{ ..., rule: '1040 line 10' }` respectively (line8 reads Schedule 1's Part I total,
    line10 reads Part II's total — do not conflate the two). In
    `form1040TaxAndPaymentLines`, compute `scheduleTwoResult = scheduleTwo(profile)` and
    `scheduleThreeResult = scheduleThree(profile)` once each, and wire line17 ← Schedule 2's
    Part I total, line23 ← Schedule 2's Part II total, line20 ← Schedule 3's Part I total,
    line31 ← Schedule 3's Part II total, each preserving the schedule module's own `sources`
    (still citing `declaredKinds`, unchanged in VALUE, upgraded in CITATION GRANULARITY).
  </action>
  <acceptance_criteria>
    - Behavior: for a profile with no declared Schedule-1/2/3 kind, `line8`/`line10`/`line17`/
      `line20`/`line23`/`line31` all remain `0n` (value unchanged from before this plan) but
      their `sources` now come from the respective schedule module's own construction.
    - Source: `grep -c "scheduleOne\|scheduleTwo\|scheduleThree" fjs/form1040/core/module.f.js`
      shows all three imported and called.
    - Test: `npm test` clean; `expectedIncomeLineCount === 31` and
      `expectedWholeReportLineCount === 56` both STILL PASS UNCHANGED (no field was added).
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; `expectedIncomeLineCount === 31` and `expectedWholeReportLineCount === 56` both STILL PASS UNCHANGED (no field was added).</automated>
  </verify>
  <done>for a profile with no declared Schedule-1/2/3 kind, `line8`/`line10`/`line17`/ `line20`/`line23`/`line31` all remain `0n` (value unchanged from before this plan) but their `sources` now come from the respective schedule module's own cons...</done>
</task>

<task type="auto">
  <name>Task 2: The full-profile end-to-end proof — every prior wave's feature, one return</name>
  <files>fjs/form1040/core/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js (Waves 1-4's own `wave1.../wave2.../wave3.../wave4...` proof
    blocks — this task composes a fixture that exercises ALL of them together for the first
    time)
  </read_first>
  <action>
    Add a proof leaf (e.g. `wave5FullProfile`) calling `form1040Report` for a single 65+ filer
    declaring `['wages', 'taxableInterest', 'socialSecurityBenefits', 'iraDistributions',
    'pensionsAndAnnuities', 'federalTaxWithheldOnOther1099', 'seniorAndOtherScheduleOneADeductions',
    'itemizedDeductions', 'childTaxCreditOrOtherDependents', 'additionalChildTaxCredit']` with:
    a W-2, an SSA-1099, a 1099-R, a `vnd.fjs.itemized_deductions` document whose total exceeds
    the standard deduction, and a `dependents` array with one qualifying child — assert
    `outcome.kind === 'ok'`, and that `line6b`, `line13b`, `line12e`, `line19` are ALL non-zero
    simultaneously, each matching a hand-computed figure independent of the others (so a
    regression in one wave's wiring cannot hide behind another wave's correct output). This is
    the first proof in the codebase to combine every one of this phase's five slices in a
    single computed return.
  </action>
  <acceptance_criteria>
    - Behavior: `outcome.kind === 'ok'`; `line6b.value`, `line13b.value`, `line12e.value`,
      `line19.value` are all non-zero, each independently matching a hand-computed cents value.
    - Test: `npm test` clean; `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises above
      Wave 4's ending count.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises above Wave 4's ending count.</automated>
  </verify>
  <done>`outcome.kind === 'ok'`; `line6b.value`, `line13b.value`, `line12e.value`, `line19.value` are all non-zero, each independently matching a hand-computed cents value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Schedule 1/2/3 module output → 1040 wiring | Pure computation, no new untrusted input introduced by this wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-16 | Tampering | wiring five lines from three modules without touching typedef/count constants | mitigate | `expectedIncomeLineCount`/`expectedWholeReportLineCount` re-asserted unchanged in Task 1's acceptance criteria, so a miscount is caught immediately |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Plan 13-11's ending count.
</verification>

<success_criteria>
Lines 8/10/17/20/23/31 cite real Schedule 1/2/3 module output. A single return exercising every
one of this phase's five slices together computes correctly, proven end to end for the first
time.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-12-SUMMARY.md`
</output>
