---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 03
type: execute
wave: 2
depends_on: ["13-02"]
files_modified: [fjs/tax/params/module.f.js, fjs/schedule/1a/module.f.js]
autonomous: true
requirements: [TAX-09]

must_haves:
  truths:
    - "Schedule 1-A Part V computes the $6,000 senior deduction with a CONTINUOUS 6% phase-out over $75k/$150k MAGI, floored at $0"
    - "A married-filing-separately filer gets $0 unconditionally, at any income, via a short-circuit BEFORE the phase-out arithmetic runs"
    - "Part I's MAGI equals AGI exactly, since every add-back this engine could apply is always zero (Form 2555/4563/Puerto Rico all unmodeled)"
    - "Part VI totals Parts II+III+IV+V (documented zeros for II/III/IV) to line 38"
  artifacts:
    - path: "fjs/tax/params/module.f.js"
      provides: "seniorDeduction parameter cited kind:'publicLaw', publicLaw:'119-21', section:'§70103'"
      contains: "'119-21'"
    - path: "fjs/schedule/1a/module.f.js"
      provides: "Schedule 1-A Parts I/V/VI as named line functions, MFS short-circuit, boundary proofs"
      min_lines: 120
  key_links:
    - from: "fjs/schedule/1a/module.f.js"
      to: "fjs/tax/params/module.f.js"
      via: "taxParamSet.seniorDeduction"
      pattern: "seniorDeduction"
---

<objective>
Build Wave 2's foundation for the senior-deduction slice (TAX-09): store the OBBBA senior
deduction's parameters ($6,000, 6%, $75k/$150k) with an honest Public Law citation, and
implement Schedule 1-A Parts I (MAGI), V (Enhanced Deduction for Seniors) and VI (total) as
pure, named, printed-order functions — including the MFS short-circuit and the continuous
(not $1,000-stepped) phase-out. This plan produces the module; Plan 13-04 wires line 13b and
reclassifies the kind.

Purpose: a 65+ TY2025 return without the senior deduction is structurally wrong, not merely
incomplete (ROADMAP.md criterion 1) — and getting the phase-out's arithmetic SHAPE wrong
(continuous vs. stepped) is the single most likely silent-wrong-number failure in this phase
(CONTEXT.md Decision 5.5).
Output: `fjs/tax/params/module.f.js` (new `seniorDeduction` parameter) and
`fjs/schedule/1a/module.f.js` (new module, Parts I/V/VI, boundary and MFS proofs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-RESEARCH.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
`Citation` is already the widened discriminated union (Plan 13-01):
`{ kind: 'revProc', ... } | { kind: 'publicLaw', publicLaw, section, effectiveDate } | { kind: 'code', ... }`.

From `fjs/tax/line16/qdcgt/module.f.js` (worksheet-as-named-functions idiom, unchanged, the
model to copy) and `fjs/tax/deduction/module.f.js`'s `standardDeductionCents` (the
"exceptions return zero before any increment is added" ordering discipline — the exact
precedent for the MFS short-circuit: `if (spouseItemizes || dualStatusAlien) { return 0n }`
runs BEFORE any chart lookup).

13-RESEARCH.md's own transcription (§"Code Examples"), the arithmetic to implement exactly:
```
const line31 = scheduleOneAMagiCents           // = Part I line 3, == AGI here
const line32 = mfj ? 15000000n : 7500000n      // $150,000 / $75,000, in cents
const line33 = line31 > line32 ? line31 - line32 : 0n
const line34 = halfUp(of(line33 * 6n)(100n))    // 6% -- CONTINUOUS, no $1,000 stepping
const line35 = line34 < 600000n ? 600000n - line34 : 0n
const line36a = taxpayerHasValidSsnAndBornBefore1961Jan2 ? line35 : 0n
const line36b = mfj && spouseHasValidSsnAndBornBefore1961Jan2 ? line35 : 0n
const line37 = line36a + line36b
```
`filingStatus === 'marriedFilingSeparately'` must short-circuit the WHOLE part to `0n` BEFORE
line31 runs (Decision 5.4) — the printed caution is a filing-status gate, not a consequence of
the arithmetic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the senior deduction's OBBBA parameters</name>
  <files>fjs/tax/params/module.f.js</files>
  <read_first>
    fjs/tax/params/module.f.js (the `standardDeduction`/`agedOrBlindAdditional` shape to
    mirror, and the widened `Citation` union from Plan 13-01)
  </read_first>
  <action>
    Add exported constant `seniorDeduction` shaped `{ readonly amount: AmountWithCitation,
    readonly phaseoutRatePercent: number, readonly phaseoutThreshold: { readonly single:
    AmountWithCitation, readonly marriedFilingJointly: AmountWithCitation, readonly
    headOfHousehold: AmountWithCitation, readonly qualifyingSurvivingSpouse: AmountWithCitation
    } }` with `amount.amount = '6000.00'`, `phaseoutRatePercent = 6`,
    `phaseoutThreshold.marriedFilingJointly.amount = '150000.00'`, and single/HoH/QSS all
    `'75000.00'` (13-RESEARCH.md §1 — the threshold is NOT MFJ-vs-everyone-else in the sense of
    a two-way split; it is exactly $75,000 for every non-MFJ individual status, MFS included
    even though MFS gets $0 by the caption gate, not by this threshold). Every entry's citation
    is `{ kind: 'publicLaw', publicLaw: '119-21', section: '§70103', effectiveDate:
    '2025-01-01' }` (13-RESEARCH.md Pitfall 5, assumption A1 — MEDIUM confidence on the exact
    section number, HIGH confidence on the dollar figures; do not invent a Rev. Proc. citation
    for this figure). Extend `taxParamsByYear[2025]` and `everyDollarStringField`'s round-trip
    list with the five new `amount` fields (base $6,000 plus four threshold entries).
  </action>
  <acceptance_criteria>
    - Source: `seniorDeduction.amount.amount === '6000.00'`, `.phaseoutRatePercent === 6`,
      `.phaseoutThreshold.marriedFilingJointly.amount === '150000.00'`,
      `.phaseoutThreshold.single.amount === '75000.00'`.
    - Behavior: a new proof leaf asserts every `seniorDeduction` citation's `kind ===
      'publicLaw'` and `publicLaw === '119-21'`, field-by-field (never a deep-equality
      assertion against a second copy of the object).
    - Test: `npm test` clean; project-local proof count rises.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises.</automated>
  </verify>
  <done>`seniorDeduction.amount.amount === '6000.00'`, `.phaseoutRatePercent === 6`, `.phaseoutThreshold.marriedFilingJointly.amount === '150000.00'`, `.phaseoutThreshold.single.amount === '75000.00'`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Schedule 1-A Parts I, V and VI</name>
  <files>fjs/schedule/1a/module.f.js</files>
  <read_first>
    fjs/tax/line16/qdcgt/module.f.js (record-shape idiom, no-floor assert idiom, proof
    structure)
    fjs/schedule/b/module.f.js (the documented-zero-with-boundary-in-docstring idiom, to copy
    for Parts II/III/IV; the standalone-callable, not-wired-into-scope-guard docstring shape)
    fjs/tax/deduction/module.f.js (lines ~196-251: `standardDeductionCents`'s "exceptions
    return zero before any increment" ordering discipline — the model for the MFS
    short-circuit)
  </read_first>
  <behavior>
    Test 1 (continuous phase-out, no stepping): MAGI exactly at $75,000 → line34 = 0, line35 =
    $6,000; MAGI $80,000 (single) → line33 = $5,000, line34 = $300.00 (6% of $5,000), line35 =
    $5,700.00 — assert the CENT-EXACT value, not merely "less than $6,000", to catch a
    $1,000-stepped implementation that would wrongly floor to a round number.
    Test 2 (floor at zero): MAGI $175,000 exactly (single) → line35 = $0 (75000 + 6000/0.06 =
    175000); MAGI $174,999.99 → line35 = $0.06 (one cent above zero); MAGI $175,000.01 → line35
    = $0. Same shape for MFJ at $250,000.
    Test 3 (MFS short-circuit): an MFS filer with MAGI = $0 gets line37 = $0 (proves the gate
    is a short-circuit, not a consequence — at $0 MAGI the ordinary arithmetic would ALSO give
    $0, so this alone is not decisive); a SECOND MFS fixture with MAGI = $10,000 (well under the
    $75,000 threshold, where the ordinary arithmetic would give the full $6,000 if the MFS gate
    were absent) also gets line37 = $0 — THIS is the decisive proof (Decision 5.4/Pitfall 3).
    Test 4 (per-spouse, not per-return): an MFJ couple where BOTH spouses qualify (valid SSN,
    born before 1961-01-02) gets line37 = 2 × line35 (up to $12,000 combined) — the SAME
    phased-out line35 granted to each spouse independently, not halved.
    Test 5 (Parts II/III/IV documented zero): line38 = 0 (tips) + 0 (overtime) + 0 (car loan) +
    line37, each citing the profile's `declaredKinds` box as a documented zero, with the
    module's own docstring stating the boundary (mirroring `schedule/b`'s Form 8815 paragraph).
  </behavior>
  <action>
    Create `fjs/schedule/1a/module.f.js` exporting `scheduleOneA` typed
    `(taxParamSet: TaxParamSet) => (input: ScheduleOneAInput) => ScheduleOneA`, where
    `ScheduleOneAInput` carries `status: IndividualFilingStatus`, `agiCents: bigint` (the
    caller-computed 1040 line 11b), `taxpayerHasValidSsnAndBornBefore1961Jan2: boolean`,
    `spouseHasValidSsnAndBornBefore1961Jan2: boolean`. Compute Part I (`line1 = agiCents`,
    `line2a`-`line2d = 0n` each — Puerto Rico/Form 2555 x2/Form 4563, all unmodeled and always
    zero — `line2e = 0n`, `line3 = line1 + line2e`), then export the named function
    `seniorDeductionPhaseoutIncome`, typed `(agiCents: bigint) => bigint`, computing exactly
    `line3` (TAX-15/Decision 5.6 — this is one of the four required income functions; its OWN
    docstring states its add-back list, currently always zero, and that it deliberately does
    NOT share code or a name with the SALT-cap or CTC/ODC phase-out's own functions even though
    all three currently equal bare AGI). Part V per the `<interfaces>` transcription above,
    with the MFS short-circuit as the FIRST statement in the function body:
    `if (status === 'marriedFilingSeparately') { return { ...zeroed Part V lines... } }` —
    return before `line31` is even assigned. Use `halfUp`/`of` from
    `fjs/types/rational/module.f.js` for the 6% multiplication (never a hand-rolled
    `* 6n / 100n` without half-up rounding). Part VI: `line13`/`line21`/`line30` are documented
    zeros (Parts II/III/IV — `profileDeclaredZeroLine`-shaped, reimplemented locally per
    `schedule/b`'s pattern, citing the profile's `declaredKinds`), `line38 = line13 + line21 +
    line30 + line37`. Add the module docstring's boundary paragraph for Parts II/III/IV,
    mirroring `schedule/b`'s Form 8815 shape, and add a `threshold − 1¢/threshold/threshold +
    1¢` hand-written trio (not merely the generic `fjs/tax/boundary` entry Plan 13-08 or a
    later task may add) at both the $75k/$150k start and the $175k/$250k floor point, per
    13-PATTERNS.md's boundary-probe idiom (both levels required).
  </action>
  <acceptance_criteria>
    - Behavior: single filer, MAGI $80,000 → `line35 === 570000n` (cents), exact.
    - Behavior: MFS filer, MAGI $10,000 → `line37 === 0n` (the decisive short-circuit proof).
    - Behavior: MFJ, both spouses qualifying, MAGI $0 → `line37 === 1200000n` (2 × $6,000).
    - Behavior: `line38` for a profile with no declared tips/overtime/car-loan kinds equals
      `line37` exactly, with `line13`/`line21`/`line30` each citing `declaredKinds`.
    - Test: `npm test` clean; project-local proof count rises further.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises further.</automated>
  </verify>
  <done>single filer, MAGI $80,000 → `line35 === 570000n` (cents), exact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| caller-computed AGI → Schedule 1-A | `agiCents` is a trusted `bigint` from an already-computed `ReportLine`, not re-derived or re-validated here |
| filing status → MFS eligibility gate | Read verbatim off the already-validated `ReturnProfile`, same trust level as every other filing-status-driven branch in this codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-05 | Tampering | continuous vs. stepped phase-out arithmetic | mitigate | Cent-exact boundary proofs at both the phase-out start and the zero-floor point, per TAX-04's convention, specifically chosen to distinguish continuous from $1,000-stepped arithmetic |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Wave 1's ending count.
</verification>

<success_criteria>
`fjs/schedule/1a/module.f.js` computes Parts I/V/VI exactly as the 2025 form prints them,
including the MFS short-circuit and the continuous phase-out, cent-exact at every boundary
research names, ready for Plan 13-04 to wire into 1040 line 13b.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-03-SUMMARY.md`
</output>
