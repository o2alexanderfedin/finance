---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 07
type: execute
wave: 3
depends_on: ["13-06"]
files_modified: [fjs/tax/deduction/module.f.js, fjs/form1040/core/module.f.js, fjs/return/scope/module.f.js]
autonomous: true
requirements: [TAX-13]

must_haves:
  truths:
    - "deductionChoice compares the standard deduction against Schedule A's total and picks correctly in both directions"
    - "A return above $15,750/$31,500 in itemized deductions can still lose to the standard deduction"
    - "1040 line 12e cites BOTH the standard and itemized figures, regardless of which won"
    - "itemizedDeductions is a modeled kind, not a refused one"
  artifacts:
    - path: "fjs/tax/deduction/module.f.js"
      provides: "deductionChoice, a discriminated union { chosen, standard, itemized }"
      contains: "deductionChoice"
    - path: "fjs/return/scope/module.f.js"
      provides: "modeledKinds grown from 17 to 18; unmodeledKindRefusals shrunk from 33 to 32; verified that no hand-typed proof fixture references itemizedDeductions, and that the two-kind fixture Plan 13-04 re-pointed still passes unchanged"
      contains: "itemizedDeductions"
  key_links:
    - from: "fjs/form1040/core/module.f.js"
      to: "fjs/tax/deduction/module.f.js"
      via: "deductionChoice(taxParamSet)(input)"
      pattern: "deductionChoice"
---

<objective>
Complete Wave 3 (TAX-13): add `deductionChoice` to `fjs/tax/deduction`, wire it into 1040 line
12e (citing both figures), reclassify `itemizedDeductions` atomically, and prove — in BOTH
directions — that itemizing is a genuine choice, not an automatic win.

Purpose: the stated failure mode is an engine where itemizing automatically wins above the
standard deduction; a case above $15,750/$31,500 where the standard deduction still wins is the
load-bearing proof (CONTEXT.md Decision 2.4, ROADMAP.md criterion 3).
Output: a return that itemizes computes, and the standard-vs-itemized comparison decides
correctly either way; `itemizedDeductions` moves to `modeledKinds` (18 total; 32 refused).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
From `fjs/tax/deduction/module.f.js` (current, unchanged by this plan except the addition):
```
export const standardDeductionCents = taxParamSet => input => { ... returns bigint ... }
// StandardDeductionInput: { status, agedOrBlindBoxes, claimedAsDependent, spouseItemizes, dualStatusAlien, earnedIncomeCents }
```
From Plan 13-06's `fjs/schedule/a/module.f.js`: `scheduleA(taxParamSet)(input)` returns a
record whose `line17` is the itemized total (bigint cents) and `line18` is the taxpayer's
itemize-anyway election (boolean).

From `fjs/form1040/core/module.f.js` (current — `line12e` is an EXISTING field, computed today
purely from `standardDeductionCents`; this plan widens its computation, not its typedef slot):
```
const line12e = { value: standardDeductionCents(taxParamSet)({...}), sources: twelveESources, rule: '1040 line 12e' }
```

**Note on this wave's scope reclassification and the phase's other proof-fixture repoints.**
Unlike Wave 1 (Plan 13-02, `socialSecurityBenefits`) and Wave 2 (Plan 13-04,
`seniorAndOtherScheduleOneADeductions`/`childTaxCreditOrOtherDependents`), no pre-existing
hand-typed proof leaf in `fjs/return/scope/module.f.js` uses `itemizedDeductions` as its example
"still refused" fixture — a full-file read confirms the only occurrences of `itemizedDeductions`
are its own table row and the neighboring `netQualifiedDisasterLoss` row. Task 2 must still
VERIFY this explicitly (never assume it), and must also confirm that Plan 13-04's `Wave 2`
repoint (`householdEmployeeWages`/`unreportedTips`) still passes unchanged after this wave's
reclassification, since both of those kinds remain refused through this wave too.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deductionChoice to fjs/tax/deduction</name>
  <files>fjs/tax/deduction/module.f.js</files>
  <read_first>
    fjs/tax/deduction/module.f.js (the full `standardDeductionCents` function and its
    "ordered steps as the rule" docstring discipline — the model to extend, not replace)
  </read_first>
  <behavior>
    Test 1 (itemizing wins): standard deduction $15,750 (single), itemized total $20,000 →
    `chosen === 'itemized'`, `itemized === 2000000n`.
    Test 2 (standard wins ABOVE the threshold — the load-bearing case): itemized total
    $16,000 but the SAME filer also has `earnedIncomeCents`/box combination raising their
    standard deduction to $17,750 (two boxes checked, 65+ and blind) → `chosen === 'standard'`
    even though itemized ($16,000) exceeds the single filer's BASE $15,750 — proves the engine
    is not comparing against the wrong baseline.
    Test 3 (line 18 election, itemizing when it loses): itemized $10,000 < standard $15,750,
    but `itemizeEvenThoughLessThanStandardDeduction === true` → `chosen === 'itemized'`
    regardless (Decision 2.5's whole purpose).
    Test 4 (both figures always present): regardless of which arm wins, the returned record
    carries BOTH `standard` and `itemized` as bigints — never only the winner.
  </behavior>
  <action>
    Add `export const deductionChoice`, typed `(taxParamSet: TaxParamSet) =>
    (input: StandardDeductionInput & { readonly itemizedCents: bigint, readonly
    itemizeEvenThoughLessThanStandardDeduction: boolean }) => { readonly chosen: 'standard' |
    'itemized', readonly standard: bigint, readonly itemized: bigint }`. Compute `standard =
    standardDeductionCents(taxParamSet)(input)` and take `itemized = input.itemizedCents`
    directly (Schedule A has already computed it — this function does not call `scheduleA`
    itself, since it has no document inputs, matching Decision 2.4's "the comparison lives
    beside the figure it compares against, not the schedule that computes the other side").
    `chosen = itemizeEvenThoughLessThanStandardDeduction || itemized > standard ? 'itemized' :
    'standard'` — copy `standardDeductionCents`'s "compute both figures, then decide" ordering
    discipline into this function's own docstring. Add the generated-leaf-per-combination proof
    idiom (`Object.fromEntries`, with an independently hand-typed expected count) for a small
    matrix of (standard-wins / itemized-wins / election-overrides) × (a couple of filing
    statuses), per `chartCombinations`'s own precedent.
  </action>
  <acceptance_criteria>
    - Behavior: Test 1-4 all pass as specified.
    - Test: `npm test` clean; project-local proof count rises.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises.</automated>
  </verify>
  <done>Test 1-4 all pass as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Wire line 12e through deductionChoice, then reclassify itemizedDeductions atomically</name>
  <files>fjs/form1040/core/module.f.js, fjs/return/scope/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js (line12e's current computation and `twelveESources`)
    fjs/return/scope/module.f.js (modeledKinds/unmodeledKindRefusals, now at 17/33 — read the
    WHOLE file, specifically confirming `itemizedDeductions` never appears as a hand-typed
    proof fixture, and re-reading Plan 13-04's `twoUnmodeledKindsRefuseNamingBothInFormOrder`/
    `unmodeledFollowsFormOrderNotDeclarationOrder` leaves, which use `householdEmployeeWages`/
    `unreportedTips` — neither is touched by this wave)
  </read_first>
  <action>
    In `fjs/form1040/core/module.f.js`: import `deductionChoice` and `scheduleA`. Before
    `line12e` is built, compute `const scheduleAResult = scheduleA(taxParamSet)({ status,
    agiCents: line11b.value, itemizedEntries: ..., medicalExpenseEntries: ..., profile })`
    (reading the profile's declared `vnd.fjs.itemized_deductions`/`vnd.fjs.medical_expenses`
    documents — widen `Form1040Inputs` with `itemizedDeductionForms` and
    `medicalExpenseForms: readonly Stored<...>[]`, and widen `inputsOf` 7→9 curried parameters,
    per the same widening discipline Plan 13-02 used). Replace `line12e`'s value computation
    with `deductionChoice(taxParamSet)({ status, agedOrBlindBoxes: ..., claimedAsDependent:
    ..., spouseItemizes: ..., dualStatusAlien: ..., earnedIncomeCents: ...,
    itemizedCents: scheduleAResult.line17, itemizeEvenThoughLessThanStandardDeduction:
    profile.value.itemizeEvenThoughLessThanStandardDeduction === true }).<chosen === 'itemized'
    ? 'itemized' : 'standard'>` — i.e., take the WINNING figure's cents value. Widen
    `twelveESources` to cite BOTH figures' sources: the existing filing-status/12d-box sources
    PLUS the itemized-deductions document's hash (when present) — line12e cites what was
    COMPARED, not only what won, per Decision 2.4. In the SAME commit, in
    `fjs/return/scope/module.f.js`: delete `itemizedDeductions`'s entry from
    `unmodeledKindRefusals`, append it to `modeledKinds` (in `kindVocabulary` order). Bump
    `expectedModeledKindCount` 17 → 18 and `expectedUnmodeledKindCount` 33 → 32. Note:
    `netQualifiedDisasterLoss` (Schedule A's line 15 exception) stays REFUSED per Decision 1.4
    — do not reclassify it; only correct its remedy string later, in Wave 5.

    Add a mechanical verification step (not a code change unless it finds a problem): confirm,
    via `grep -n "itemizedDeductions" fjs/return/scope/module.f.js`, that the only two
    occurrences remaining after this edit are `modeledKinds`'s own new entry and the
    docstring's reclassification-history paragraph — i.e., no hand-typed proof leaf anywhere in
    the file used `itemizedDeductions` as a "still refused" fixture (there was none to begin
    with; this step makes that fact explicit and checked rather than assumed). Also re-run
    `twoUnmodeledKindsRefuseNamingBothInFormOrder` and
    `unmodeledFollowsFormOrderNotDeclarationOrder` (Plan 13-04's repoint) and confirm both still
    pass unmodified — `householdEmployeeWages` and `unreportedTips` are untouched by this wave's
    reclassification.
  </action>
  <acceptance_criteria>
    - Behavior: a fixture with itemized total exceeding the standard deduction produces
      `line12e.value` equal to the itemized figure, with `sources` including the itemized
      document's hash.
    - Behavior: a fixture with itemized total below the standard deduction (and no line-18
      election) produces `line12e.value` equal to the standard figure.
    - Source: `modeledKinds.length === 18`, `unmodeledKindRefusals.length === 32`.
    - Source: `grep -n "itemizedDeductions" fjs/return/scope/module.f.js | wc -l` equals exactly
      2 (the `modeledKinds` entry and the docstring history paragraph) — no orphaned proof
      fixture referencing it as a refused kind.
    - Behavior: `twoUnmodeledKindsRefuseNamingBothInFormOrder` and
      `unmodeledFollowsFormOrderNotDeclarationOrder` (Plan 13-04's repoint) still pass, byte-for-
      byte unchanged, after this wave's reclassification.
    - Type: `tsc --noEmit` clean.
    - Test: `npm test` clean; project-local proof count rises.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises.</automated>
  </verify>
  <done>a fixture with itemized total exceeding the standard deduction produces `line12e.value` equal to the itemized figure, with `sources` including the itemized document's hash.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end proof — both directions, at the load-bearing scale</name>
  <files>fjs/form1040/core/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js (Wave 1/2's `wave1.../wave2...` proof blocks, the style to
    extend)
  </read_first>
  <action>
    Add a proof leaf (e.g. `wave3Itemizing`) calling `form1040Report` for a single filer
    declaring `['wages', 'itemizedDeductions']` with a `vnd.fjs.itemized_deductions` document
    whose total exceeds $15,750 — assert `line12e` equals the itemized total and `line15`
    (taxable income) reflects it. Add a SECOND leaf, the load-bearing one per criterion 3: the
    SAME filer with itemized total above $15,750 but LESS than a higher standard deduction the
    filer is entitled to (e.g., a 65+-and-blind filer with two age/blindness boxes, standard
    deduction $19,750, itemized $17,000) — assert `chosen === 'standard'` reaches line12e,
    proving itemizing does NOT automatically win above the base $15,750/$31,500 figure.
  </action>
  <acceptance_criteria>
    - Behavior: the itemizing-wins leaf's `line12e.value` equals the itemized total.
    - Behavior: the standard-still-wins leaf's `line12e.value` equals the (higher) standard
      figure, even though itemized total exceeds the BASE standard deduction.
    - Test: `npm test` clean; project-local proof count rises above Wave 2's ending count.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises above Wave 2's ending count.</automated>
  </verify>
  <done>the itemizing-wins leaf's `line12e.value` equals the itemized total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| taxpayer-asserted itemized total vs. standard deduction | Both figures are already-computed, already-validated bigints; the comparison itself introduces no new untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-10 | Repudiation | scope reclassification | mitigate | `_EveryKindIsEitherModeledOrRefused` type-checks the partition |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Task 2's ending count.
</verification>

<success_criteria>
`deductionChoice` picks correctly in both directions, including the load-bearing above-threshold
case where the standard deduction still wins. 1040 line 12e cites both figures. `modeledKinds`
is 18; `unmodeledKindRefusals` is 32; both sum to 50. Verified: no proof fixture references
`itemizedDeductions`, and Wave 2's `householdEmployeeWages`/`unreportedTips` repoint still
passes unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-07-SUMMARY.md`
</output>
</output>
