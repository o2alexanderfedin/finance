---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 04
type: execute
wave: 2
depends_on: ["13-03"]
files_modified: [fjs/form1040/core/module.f.js, fjs/return/scope/module.f.js]
autonomous: true
requirements: [TAX-09]

must_haves:
  truths:
    - "A 65+ single filer's 1040 line 13b is a real, non-zero Schedule 1-A figure, not a declaredZero placeholder"
    - "An MFS 65+ filer's line 13b is $0 regardless of income"
    - "seniorAndOtherScheduleOneADeductions is a modeled kind, not a refused one"
  artifacts:
    - path: "fjs/form1040/core/module.f.js"
      provides: "line13b computed from fjs/schedule/1a's scheduleOneA output"
      contains: "scheduleOneA"
    - path: "fjs/return/scope/module.f.js"
      provides: "modeledKinds grown from 16 to 17; unmodeledKindRefusals shrunk from 34 to 33; the two-kind 65+-profile proof fixture re-pointed at a pair that stays refused through the rest of this phase"
      contains: "seniorAndOtherScheduleOneADeductions"
  key_links:
    - from: "fjs/form1040/core/module.f.js"
      to: "fjs/schedule/1a/module.f.js"
      via: "scheduleOneA(taxParamSet)({ status, agiCents: line11b.value, ... })"
      pattern: "scheduleOneA"
---

<objective>
Complete Wave 2 (TAX-09): wire Schedule 1-A's Part VI total into Form 1040 line 13b, reclassify
`seniorAndOtherScheduleOneADeductions` atomically, and prove end to end that a 65+ return's
line 13b — and therefore its taxable income and tax — moves for the first time.

Purpose: this is the slice's vertical cut. A 65+ TY2025 return stops being structurally wrong
the moment this plan lands (ROADMAP.md criterion 1).
Output: line 13b reads Schedule 1-A's real output; `seniorAndOtherScheduleOneADeductions`
moves to `modeledKinds` (17 total; 33 refused).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
From Plan 13-03's `fjs/schedule/1a/module.f.js`:
```
export const scheduleOneA = taxParamSet => input => ({ line1, line2a..line2e, line3, line13, line21, line30, line31..line37, line38 })
// input: { status, agiCents, taxpayerHasValidSsnAndBornBefore1961Jan2, spouseHasValidSsnAndBornBefore1961Jan2 }
```
From `fjs/form1040/core/module.f.js` (current, this plan's edit target — `line13b` is an
EXISTING `declaredZero` field, no typedef/orderedLines/count-constant change needed):
```
const line13b = declaredZero('1040 line 13b')
```
`taxpayerHasValidSsnAndBornBefore1961Jan2`/`spouseHasValidSsnAndBornBefore1961Jan2` are NOT yet
profile fields — derive them from the EXISTING `taxpayerBornBeforeJan2_1961`/
`spouseBornBeforeJan2_1961` age boxes (already on `vnd.fjs.return_profile`, used by line 12e's
standard-deduction computation) — a valid SSN is assumed true for a declared filer (no SSN
validity field exists on the primary taxpayer/spouse; Schedule 8812's `dependents` array is the
first place SSN validity becomes a per-person fact, Wave 4). Do not add a new profile field for
this; state the assumption in the wiring's own comment.

**A pre-existing proof leaf that hard-codes `seniorAndOtherScheduleOneADeductions` as HALF of a
two-kind "still refused" fixture — Task 2 MUST adapt it, never delete it:**
```
// line 553: theSixtyFivePlusProfileRefusesNamingBothUnmodeledKinds: () => {
//   classifyScope(['wages','taxableInterest','seniorAndOtherScheduleOneADeductions','childTaxCreditOrOtherDependents'])
//   assertEq(outcome.unmodeled.length, 2, ...)
//   assertEq(outcome.unmodeled[0], 'seniorAndOtherScheduleOneADeductions', ...)
//   assertEq(outcome.unmodeled[1], 'childTaxCreditOrOtherDependents', ...)
//   assert(outcome.message.includes('1040 line 13b'), ...); assert(outcome.message.includes('1040 line 19'), ...)
// }
// line 575: controlTheSixtyFivePlusProfileWithoutThoseTwoKindsIsInScope (its own body — classifyScope(['wages','taxableInterest']) — is unaffected)
// line 583: unmodeledFollowsFormOrderNotDeclarationOrder: () => {
//   declares the SAME two kinds in both orders; asserts `unmodeled.join(',') ===
//   'seniorAndOtherScheduleOneADeductions,childTaxCreditOrOtherDependents'` and that both
//   orderings produce byte-identical messages
// }
```
Once `seniorAndOtherScheduleOneADeductions` moves to `modeledKinds` this wave,
`outcome.unmodeled.length` drops to 1 (only `childTaxCreditOrOtherDependents` remains), so
`theSixtyFivePlusProfileRefusesNamingBothUnmodeledKinds`'s `=== 2` assertion goes red, and
`unmodeledFollowsFormOrderNotDeclarationOrder`'s comma-joined string no longer matches. Fixing
this by simply dropping `seniorAndOtherScheduleOneADeductions` from the declared list and
keeping `childTaxCreditOrOtherDependents` alone would only defer the break to Wave 4 (Plan
13-10 also reclassifies `childTaxCreditOrOtherDependents`) — so the replacement pair chosen here
must be two kinds that stay refused for the REST of this phase, not merely past this wave.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire line 13b (kind still refused — inert)</name>
  <files>fjs/form1040/core/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js (line13b's current placeholder, and line12e's neighboring
    computation for the age-box-reading idiom to copy)
  </read_first>
  <action>
    Import `scheduleOneA` from `../../schedule/1a/module.f.js`. In `form1040IncomeLines`,
    replace `const line13b = declaredZero('1040 line 13b')` with a call to `scheduleOneA(taxParamSet)({
    status, agiCents: line11b.value, taxpayerHasValidSsnAndBornBefore1961Jan2:
    profile.value.taxpayerBornBeforeJan2_1961 === true, spouseHasValidSsnAndBornBefore1961Jan2:
    profile.value.spouseBornBeforeJan2_1961 === true })`, then
    `line13b = { value: result.line38, sources: unionSources([line11b]), rule: '1040 line 13b'
    }` — cites AGI (line11b), since Schedule 1-A's whole computation is a pure function of it
    for this engine's modeled scope. Leave `line14`'s existing `totalLine('1040 line 14')([line12e,
    line13a, line13b])` untouched (line14 already sums line13b, so it automatically picks up
    the new value with no further wiring).
  </action>
  <acceptance_criteria>
    - Behavior: a single 65+ filer with AGI $80,000 produces `line13b.value === 570000n` (the
      Schedule 1-A figure from Plan 13-03's own fixture), and `line14.value` includes it.
    - Behavior: with `seniorAndOtherScheduleOneADeductions` still in `unmodeledKindRefusals`,
      a profile DECLARING that kind still refuses at `classifyScope`, before this wiring is
      ever reached — one control fixture proves it (inert until Task 2).
    - Test: `npm test` clean; project-local proof count rises.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises.</automated>
  </verify>
  <done>a single 65+ filer with AGI $80,000 produces `line13b.value === 570000n` (the Schedule 1-A figure from Plan 13-03's own fixture), and `line14.value` includes it.</done>
</task>

<task type="auto">
  <name>Task 2: The atomic transition — reclassify seniorAndOtherScheduleOneADeductions, and adapt the two-kind proof fixture</name>
  <files>fjs/return/scope/module.f.js</files>
  <read_first>
    fjs/return/scope/module.f.js (modeledKinds/unmodeledKindRefusals, the two hand-typed count
    constants, the historical-precedent docstring paragraph — the same file Plan 13-02 already
    touched, now at 16/34). Specifically read, before editing,
    `theSixtyFivePlusProfileRefusesNamingBothUnmodeledKinds` (line 553),
    `controlTheSixtyFivePlusProfileWithoutThoseTwoKindsIsInScope` (line 575), and
    `unmodeledFollowsFormOrderNotDeclarationOrder` (line 583) — see this plan's `<interfaces>`
    for why all three need attention this wave.
  </read_first>
  <action>
    In one commit: delete `seniorAndOtherScheduleOneADeductions`'s entry from
    `unmodeledKindRefusals`; append it to `modeledKinds` (in `kindVocabulary` order, after
    `qualifiedBusinessIncomeDeduction`'s position, matching where the kind sits in the
    vocabulary). Bump `expectedModeledKindCount` 16 → 17 and `expectedUnmodeledKindCount`
    34 → 33 in the same change. Extend the Wave-1 docstring paragraph (or add a new one) noting
    this fifth reclassification.

    In the SAME commit, re-point `theSixtyFivePlusProfileRefusesNamingBothUnmodeledKinds` and
    `unmodeledFollowsFormOrderNotDeclarationOrder` at a replacement pair that stays refused for
    the rest of this phase — use `householdEmployeeWages` (1040 line 1b) and `unreportedTips`
    (1040 line 1c), in that `kindVocabulary` order (both are named in Decision 1.4 as staying
    refused through Wave 5; neither is reclassified by any later plan in this phase). Rename
    `theSixtyFivePlusProfileRefusesNamingBothUnmodeledKinds` to
    `twoUnmodeledKindsRefuseNamingBothInFormOrder` (its original name narrated "the 65+ profile"
    specifically, but the two kinds it now exercises are a generic control pair, not the
    profile's own actual remaining gaps — leaving the old name would misdescribe what the leaf
    tests, the same drift AGENTS.md's docstring discipline forbids). Update its body: declare
    `['wages', 'taxableInterest', 'householdEmployeeWages', 'unreportedTips']`; keep
    `outcome.unmodeled.length === 2`; `outcome.unmodeled[0] === 'householdEmployeeWages'`,
    `outcome.unmodeled[1] === 'unreportedTips'`; message assertions become
    `outcome.message.includes('1040 line 1b')` and `outcome.message.includes('1040 line 1c')`.
    Rename `controlTheSixtyFivePlusProfileWithoutThoseTwoKindsIsInScope` to
    `controlTheSameDeclarationWithoutThoseTwoKindsIsInScope` (its own assertion —
    `classifyScope(['wages', 'taxableInterest'])` returns `{ kind: 'ok' }` — is unchanged; only
    the name and its neighboring "65+ profile" comment need to stop claiming a pairing that no
    longer holds). Update `unmodeledFollowsFormOrderNotDeclarationOrder` to declare
    `['unreportedTips', 'householdEmployeeWages']` one way and
    `['householdEmployeeWages', 'unreportedTips']` the other, asserting
    `declaredOneWay.unmodeled.join(',') === 'householdEmployeeWages,unreportedTips'` (form
    order) and that both orderings still produce byte-identical messages — the exact property
    this leaf exists to prove, now proved against a pair that survives the rest of the phase.
  </action>
  <acceptance_criteria>
    - Source: `modeledKinds.length === 17`, `unmodeledKindRefusals.length === 33`, both against
      the bumped hand-typed constants.
    - Type: `tsc --noEmit` clean.
    - Behavior: `classifyScope(['seniorAndOtherScheduleOneADeductions'])` returns `{ kind: 'ok' }`.
    - Behavior: `twoUnmodeledKindsRefuseNamingBothInFormOrder` asserts
      `outcome.unmodeled.length === 2`, `outcome.unmodeled[0] === 'householdEmployeeWages'`,
      `outcome.unmodeled[1] === 'unreportedTips'`, and the message names `'1040 line 1b'` and
      `'1040 line 1c'` — the identical two-kinds-named-in-form-order property the deleted
      `seniorAndOtherScheduleOneADeductions`/`childTaxCreditOrOtherDependents` version proved.
    - Behavior: `unmodeledFollowsFormOrderNotDeclarationOrder` asserts
      `declaredOneWay.unmodeled.join(',') === 'householdEmployeeWages,unreportedTips'`
      regardless of declaration order, and that both orderings' messages are identical.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>`modeledKinds.length === 17`, `unmodeledKindRefusals.length === 33`, both against the bumped hand-typed constants; the two-kind proof fixture re-pointed at `householdEmployeeWages`/`unreportedTips` and green.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end proof — a 65+ return's line 13b moves, and MFS stays at $0</name>
  <files>fjs/form1040/core/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js (Wave 1's `wave1RetirementAndSocialSecurity` proof block, the
    style to extend)
  </read_first>
  <action>
    Add a proof leaf (e.g. `wave2SeniorDeduction`) calling `form1040Report` for a single 65+
    filer declaring `['wages', 'seniorAndOtherScheduleOneADeductions']` with AGI in the phase-out
    range — assert `line13b`'s value is non-zero and matches Schedule 1-A's own hand-computed
    figure, and that `line15` (taxable income) is correspondingly lower than it would be with
    `line13b = 0`. Add a control leaf: the same profile as MFS returns `line13b.value === 0n`
    even though it declares the kind and is otherwise identical.
  </action>
  <acceptance_criteria>
    - Behavior: the single-filer proof's `outcome.kind === 'ok'` and `line13b` is non-zero.
    - Behavior: the MFS control's `line13b.value === 0n`.
    - Test: `npm test` clean; project-local proof count rises above Wave 1's ending count.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises above Wave 1's ending count.</automated>
  </verify>
  <done>the single-filer proof's `outcome.kind === 'ok'` and `line13b` is non-zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| filing status + age boxes → MFS eligibility gate | Read verbatim off the already-validated profile, no new untrusted input introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-06 | Repudiation | scope reclassification | mitigate | `_EveryKindIsEitherModeledOrRefused` type-checks the partition after every reclassification |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Wave 2 Plan 13-03's ending count.
</verification>

<success_criteria>
A 65+ single filer's 1040 line 13b computes a real Schedule 1-A senior deduction; an MFS filer's
line 13b is $0. `modeledKinds` is 17; `unmodeledKindRefusals` is 33; both sum to 50. The two-kind
65+-profile proof fixture is re-pointed at `householdEmployeeWages`/`unreportedTips`, a pair
stable through the rest of the phase, preserving every property it proved.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-04-SUMMARY.md`
</output>
</output>
