---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 09
type: execute
wave: 4
depends_on: ["13-08"]
files_modified: [fjs/form8812/module.f.js]
autonomous: true
requirements: [TAX-12]

must_haves:
  truths:
    - "Schedule 8812 classifies each dependent as CTC (under 17 + SSN valid) or ODC (otherwise) using only the two facts the printed form itself keys on"
    - "The CTC/ODC phase-out is STEPPED — 5% per $1,000 rounded UP, a true cliff, unlike the senior deduction's continuous curve"
    - "Both Part I (line 19) and Part II-A (line 28/ACTC) are computed from one function execution, sharing internal intermediates, never as independently-callable exports"
  artifacts:
    - path: "fjs/form8812/module.f.js"
      provides: "Schedule 8812 Parts I and II-A, roundUpToNextThousandDollars, childTaxCreditPhaseoutIncome"
      min_lines: 150
  key_links:
    - from: "fjs/form8812/module.f.js"
      to: "fjs/tax/params/module.f.js"
      via: "taxParamSet.childTaxCredit"
      pattern: "childTaxCredit"
---

<objective>
Build Schedule 8812, both parts, in one module: Part I's nonrefundable CTC/ODC (line 19) and
Part II-A's Additional Child Tax Credit (line 28), sharing internal intermediates, plus the new
`roundUpToNextThousandDollars` arithmetic primitive and the `childTaxCreditPhaseoutIncome`
named function.

Purpose: `roundUpToNextThousandDollars` has no precedent anywhere in this codebase and is the
highest-risk new arithmetic primitive this phase introduces (CONTEXT.md non-negotiable #5) —
getting the stepped-vs-continuous distinction backwards against Wave 2's senior deduction is
the phase's most likely silent-wrong-number failure.
Output: `fjs/form8812/module.f.js` (new module, both parts, boundary proofs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-RESEARCH.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
13-RESEARCH.md §4's full transcription (Part I lines 1-14, Part II-A lines 15-20/27) — the
exact arithmetic to implement; Part II-B (3+ qualifying children / Puerto Rico residents,
lines 21-26) is OUT OF SCOPE this phase — model it as a documented boundary in the module's own
docstring (research confirms this profile never reaches it: `line16b < $5,100` skips it, and
$1,700 × 3 = $5,100 is the exact crossover — a taxpayer with 3+ qualifying children under 17
COULD reach it; state this honestly as an unmodeled boundary the module refuses on, per TAX-16's
own discipline, rather than silently computing a wrong Part II-A result).

Research's own transcription, exact:
```
const line9 = mfj ? 40000000n : 20000000n      // $400,000 / $200,000, cents
const excess = line3 > line9 ? line3 - line9 : 0n
const line10 = excess === 0n ? 0n : roundUpToNextThousandDollars(excess)
const line11 = halfUp(of(line10 * 5n)(100n))    // 5%
```
`Credit Limit Worksheet A` (line13): `line1 = 1040 line18` (tax after Schedule 2 Part I);
`line2 = sum of several Schedule 3 credits, all documented zero for this profile`; `line3 =
line1 - line2` — model line2 explicitly as a documented zero (not hard-coded away), per
`qdcgt`'s "every printed line exists, computed, even when it is provably zero today" discipline.

From Plan 13-08's `fjs/return/profile/module.f.js`: `dependents: option(array({ relationship,
ssnValidForEmployment, ageAtYearEnd, livedWithTaxpayer }))`. From
`fjs/tax/params/module.f.js`: `childTaxCredit` (Plan 13-08).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Schedule 8812 — dependent classification, Part I, Part II-A</name>
  <files>fjs/form8812/module.f.js</files>
  <read_first>
    fjs/schedule/d/module.f.js (role/shape reference only — a multi-part schedule with a
    phase-out and downstream dispatch, read for structure, not excerpted line-by-line)
    fjs/tax/line16/qdcgt/module.f.js (the record-shape/no-floor-assert/proof-structure idioms,
    reused here for the "one function, both parts" discipline)
    fjs/schedule/b/module.f.js (the "reimplement helpers locally" precedent, if this module
    needs a document-reading helper — Schedule 8812 has no document inputs beyond the profile's
    `dependents` array, so this is mostly for the profile-field-reading idiom)
  </read_first>
  <behavior>
    Test 1 (classification): a dependent aged 10 with `ssnValidForEmployment: true` counts
    toward line4 (CTC); a dependent aged 20 counts toward line6 (ODC) regardless of SSN
    validity; a dependent aged 10 WITHOUT `ssnValidForEmployment` counts toward line6, not
    line4 (the printed form's own two-fact test, per 13-RESEARCH.md §4 — age under 17 at year
    end AND a valid SSN).
    Test 2 (stepped cliff, the highest-risk boundary): MFJ, line3 (modified AGI) exactly
    $400,000.00 → line10 = $0, line11 = $0; line3 = $400,000.01 → excess = $0.01 →
    `roundUpToNextThousandDollars($0.01) = $1,000.00` → line11 = $50.00 (5% of $1,000) — a
    single CENT over the threshold costs the full $50 immediately (Decision 5.5's own
    boundary). Contrast with Wave 2's senior-deduction fixture at its own threshold, where one
    cent over costs a proportionally tiny amount — cite that contrast in a code comment.
    Test 3 (roundUpToNextThousandDollars, standalone): $0.01 → $1,000.00; $1,000.00 (already
    an exact multiple) → $1,000.00 (per the printed instruction "If more than zero and NOT a
    multiple of $1,000, enter the NEXT multiple" — an exact multiple stays itself, it does not
    round up again); $1,000.01 → $2,000.00.
    Test 4 (Part I → Part II-A sharing intermediates): a fixture where `line12 <= line11`
    (line 12's own STOP — no CTC/ODC/ACTC at all) short-circuits BOTH parts from one function
    call, never leaving Part II-A independently computable on stale Part I state.
    Test 5 (ACTC, line16b < $5,100, Part II-B skipped): 2 qualifying children ($1,700 × 2 =
    $3,400 < $5,100) → `line27 = min(line17, line20)` directly, Part II-B never entered.
    Test 6 (Part II-B boundary, documented refusal): 3+ qualifying children under 17
    (`line16b >= $5,100`) is REFUSED with a named remedy citing "Part II-B (3+ qualifying
    children or Puerto Rico residents)", never silently computed with Part II-B's own lines
    treated as zero.
  </behavior>
  <action>
    Create `fjs/form8812/module.f.js` exporting `scheduleEightEightOneTwo` (or `form8812`,
    pick one clear name and use it consistently) typed `(taxParamSet: TaxParamSet) =>
    (input: Form8812Input) => Form8812Outcome`, where `Form8812Input` carries `status:
    IndividualFilingStatus`, `agiCents: bigint`, `dependents: readonly { relationship: string,
    ssnValidForEmployment: boolean, ageAtYearEnd: number, livedWithTaxpayer: boolean }[]`,
    `line18Cents: bigint` (1040 tax after Schedule 2 Part I, the caller-computed input Credit
    Limit Worksheet A needs), `earnedIncomeCents: bigint`, `nontaxableCombatPayCents: bigint`.
    `Form8812Outcome` is a discriminated union `{ kind: 'ok', line1..line14, line16a, line16b,
    line17, line18a, line18b, line19, line20, line27 } | { kind: 'error', message: string,
    unmodeled: readonly [] }` (Part II-B's refusal, a document-data-sufficiency refusal per the
    error-arm threading pattern, `unmodeled: []` since it is not a `fjs/return/scope` kind).
    Classify dependents: `qualifyingChildren = dependents.filter(d => d.ageAtYearEnd < 17 &&
    d.ssnValidForEmployment)`, `otherDependents = dependents.filter(d => !(d.ageAtYearEnd < 17
    && d.ssnValidForEmployment))` — a pure filter/reduce, no shared precedent to copy structurally
    (13-PATTERNS.md's "No Analog Found" table). Part I per research's transcription: line1 =
    agiCents; line2a-2d = 0 (foreign add-backs, unmodeled); line3 = line1+line2d; export
    `childTaxCreditPhaseoutIncome`, typed `(agiCents: bigint) => bigint`, computing exactly
    line3 (TAX-15/Decision 5.6's fourth function, its own docstring stating its own add-back
    list, independently stored despite currently equaling
    `seniorDeductionPhaseoutIncome`/`saltCapPhasedownIncome`); line4 = qualifyingChildren.length;
    line5 = line4 × `taxParamSet.childTaxCredit.ctcAmount`; line6 = otherDependents.length;
    line7 = line6 × `taxParamSet.childTaxCredit.odcAmount`; line8 = line5+line7; line9 =
    status === 'marriedFilingJointly' ? `phaseoutThreshold.marriedFilingJointly` :
    `phaseoutThreshold.other`; write `roundUpToNextThousandDollars`, typed `(cents: bigint) =>
    bigint`, as a MODULE-LOCAL helper (no existing precedent to extend, per 13-PATTERNS.md) —
    `cents === 0n ? 0n : ((cents - 1n) / 100000n + 1n) * 100000n` (ceiling-divide to the next
    whole $1,000 in cents; verify against Test 3's three cases exactly, including the
    already-a-multiple case, which must NOT round up again); line10 = excess === 0n ? 0n :
    roundUpToNextThousandDollars(excess); line11 = halfUp(line10 × 5 / 100); line12 = line8 <=
    line11 ? (STOP, everything downstream 0) : line8 - line11; line13 = Credit Limit Worksheet
    A's own line3, computed HERE as `min(0n, ...)`-free explicit arithmetic: `clw.line1 =
    input.line18Cents`, `clw.line2 = 0n` (documented zero — every Schedule 3 credit this
    profile could have is unmodeled, per 13-RESEARCH.md §4), `clw.line3 = clw.line1 -
    clw.line2`; line14 = min(line12, line13). Part II-A: line16a = line12 - line14 (0 if 0,
    STOP); line16b = qualifyingChildren.length × actcCap; if line16b < 510000n (i.e. $5,100 in
    cents), proceed with Part II-B skipped: line17 = min(line16a, line16b); line18a =
    earnedIncomeCents; line18b = nontaxableCombatPayCents; line19 = line18a > 250000n ?
    line18a - 250000n : 0n; line20 = halfUp(line19 × 15 / 100); line27 = min(line17, line20).
    If line16b >= 510000n, return the `{ kind: 'error', ... }` outcome naming Part II-B as
    unmodeled, before line17 is computed (this is Decision-1.4-shaped scope honesty: 3+
    qualifying children is a REAL case this engine cannot yet finish, not a documented zero).
  </action>
  <acceptance_criteria>
    - Behavior: Test 1's three classification fixtures each land in the predicted line4/line6
      bucket.
    - Behavior: Test 2's cent-boundary fixture: `line11 === 0n` at exactly $400,000.00,
      `line11 === 5000n` at $400,000.01 (cents; $50.00 = 5000 cents).
    - Behavior: Test 3's three `roundUpToNextThousandDollars` cases match exactly, including
      the already-a-multiple case NOT rounding again.
    - Behavior: Test 6's 3+-qualifying-children fixture returns `kind: 'error'` naming Part
      II-B, never a silently-wrong `line27`.
    - Test: `npm test` clean; project-local proof count rises.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises.</automated>
  </verify>
  <done>Test 1's three classification fixtures each land in the predicted line4/line6 bucket.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `dependents` array → CTC/ODC classification | Taxpayer-declared facts, trusted verbatim; the citizenship/resident-alien test is an accepted, documented trust boundary (Decision 5.7), stated in this module's own docstring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-12 | Tampering | `roundUpToNextThousandDollars` off-by-one at an exact multiple | mitigate | Explicit boundary test at exactly $1,000.00 (must NOT round again), plus the $0.01/$1,000.01 flanking cases |
| T-13-13 | Information Disclosure | Part II-B refusal message | mitigate | Names only "Part II-B (3+ qualifying children or Puerto Rico residents)" — no taxpayer amount or dependent identity |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Plan 13-08's ending count.
</verification>

<success_criteria>
Schedule 8812 correctly classifies dependents, applies the STEPPED phase-out cliff exactly, and
computes both Part I and Part II-A from one function execution, refusing honestly on the
unmodeled Part II-B case.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-09-SUMMARY.md`
</output>
