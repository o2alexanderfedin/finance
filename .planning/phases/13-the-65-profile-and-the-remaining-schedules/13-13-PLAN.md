---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 13
type: execute
wave: 5
depends_on: ["13-12"]
files_modified: [magi-gate.test.js, fjs/return/scope/module.f.js, .planning/REQUIREMENTS.md, .planning/ROADMAP.md]
autonomous: true
requirements: [TAX-14]

must_haves:
  truths:
    - "grep -rn 'magi' fjs/ returns nothing, enforced by a mechanical test rather than remembered"
    - "No remedy string in fjs/return/scope promises Phase 13 for a kind this phase does not actually make computable"
    - "REQUIREMENTS.md and ROADMAP.md both state the Social Security Benefits Worksheet has 18 lines, not 19"
  artifacts:
    - path: "magi-gate.test.js"
      provides: "a real-process mechanical gate: grep -rn 'magi' fjs/ must be empty"
      contains: "magi"
    - path: "fjs/return/scope/module.f.js"
      provides: "ten corrected remedy strings (Decision 1.4's five, plus five more this phase's own research found)"
      contains: "no phase yet"
  key_links:
    - from: "magi-gate.test.js"
      to: "fjs/"
      via: "a recursive filesystem walk asserting no file contains the lowercase substring 'magi' outside this gate's own module"
      pattern: "readdirSync"
---

<objective>
Close the phase: build the mechanical MAGI-token gate (criterion 5), correct every stale
remedy string this phase's own work has made false — Decision 1.4's original five, plus five
more that Plan 13-11's research finding surfaced (the Schedule 1/2/3-adjacent coarse kinds,
which this phase ships modules for but never makes computable) — and fix REQUIREMENTS.md's and
ROADMAP.md's stale "19-line" Social Security Benefits Worksheet count.

Purpose: a rule nobody has to remember is a rule that decays (AGENTS.md's own recurring lesson,
applied here to TAX-15's "no shared MAGI" principle); and Phase 1 exists precisely so no later
phase is planned against text that dissolves on contact — leaving a known-false line count for
Phase 17 to find would be that failure committed knowingly (Decision 5.3).
Output: `magi-gate.test.js` (new, root-level, real-process gate), `fjs/return/scope/module.f.js`
(ten corrected remedy strings), `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` (18-line
correction).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-RESEARCH.md
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
`cas-refresh-cross-process.test.js` (root-level, `@ts-nocheck`, real-process precedent — the
exact template for a new root-level impure `.test.js` file, since a mechanical tree-walk needs
`node:fs`, which no `.f.js` module may import per AGENTS.md's purity rule, and no existing
FunctionalScript proof precedent exists for file-content scanning per 13-PATTERNS.md's own "No
Analog Found" table):
```js
// @ts-nocheck
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readdirSync, readFileSync, statSync } from 'node:fs'
import { join } from 'node:path'
```
`npm test` runs bare `node --test`, which discovers every `*.test.js` at the repo root
automatically (no registration needed, per the existing `fjs-run-integration.test.js`/
`cas-refresh-cross-process.test.js` precedent) — a new `magi-gate.test.js` needs no wiring into
`all.test.js` or `package.json`.

Decision 1.4's five remedy strings (current text, `fjs/return/scope/module.f.js`):
```
{ kind: 'householdEmployeeWages', ..., remedy: 'no dialect models it (Phase 13)' },
{ kind: 'medicaidWaiverPayments', ..., remedy: 'no dialect models it (Phase 13)' },
{ kind: 'otherEarnedIncome', ..., remedy: 'no dialect models it (Phase 13)' },
{ kind: 'federalTaxWithheldOnOtherForms', ..., remedy: 'no dialect models it (Phase 13)' },
{ kind: 'netQualifiedDisasterLoss', ..., remedy: 'requires Schedule A (TAX-13, Phase 13)' },
```
Plan 13-11's own finding, five MORE remedy strings this phase's shipped work makes false the
same way (currently naming "Phase 13" as though declaring the kind will compute after this
phase ships, when in fact Schedule 1/2/3 exist but the coarse kind can never resolve to a real
figure):
```
{ kind: 'scheduleOneAdditionalIncome', ..., remedy: 'requires Schedule 1 (TAX-14, Phase 13)' },
{ kind: 'scheduleOneAdjustments', ..., remedy: 'requires Schedule 1 (TAX-14, Phase 13)' },
{ kind: 'scheduleTwoTaxes', ..., remedy: 'requires Schedule 2 (TAX-14, Phase 13)' },
{ kind: 'scheduleThreeNonrefundableCredits', ..., remedy: 'requires Schedule 3 (TAX-14, Phase 13)' },
{ kind: 'scheduleThreeRefundableCredits', ..., remedy: 'requires Schedule 3 (TAX-14, Phase 13)' },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The mechanical MAGI gate</name>
  <files>magi-gate.test.js</files>
  <read_first>
    cas-refresh-cross-process.test.js (the root-level impure-JS template: `@ts-nocheck` header
    with its justifying comment, `node:test`/`node:assert/strict` imports)
  </read_first>
  <action>
    Create `magi-gate.test.js` at the repo root, `@ts-nocheck` (same two justifications as
    `cas-refresh-cross-process.test.js`: `node:fs`/`node:path` are untyped without
    `@types/node`, and this is a tree-walk no `.f.js` module's purity rule permits). Recursively
    walk `fjs/` (via `readdirSync`/`statSync`, skipping `node_modules` and any vendored
    `functionalscript` submodule path), read every `*.f.js` file's text, and assert NONE
    contains the case-sensitive substring `'magi'` as a SUBSTRING OF AN IDENTIFIER — per
    Decision 3.6, uppercase `MAGI` in prose stays permitted, so the check must not merely
    `includes('magi')` blindly, since a docstring could legitimately contain "MAGI" (uppercase)
    without tripping it; test with a regex matching lowercase `magi` specifically (e.g.
    `/magi/` — case-sensitive by default — applied to the raw file text; `MAGI` in uppercase
    does not match `/magi/`, so no special-casing is needed beyond using a case-SENSITIVE
    pattern). EXCLUDE this gate's own file and any phase-planning `.md` file from the scan (the
    scan targets `fjs/` only, and this file lives at the repo root, so no self-exclusion logic
    is even needed — confirm this with a comment). Report every offending file and line number
    in the assertion failure message, not merely "found a match", so a future violation is
    actionable. Also assert, as a POSITIVE control, that the phrase `'MAGI'` (uppercase) DOES
    appear somewhere in `fjs/` prose (e.g. a docstring naming "MAGI" as a concept) — proving the
    gate does not accidentally forbid the acceptable uppercase usage.
  </action>
  <acceptance_criteria>
    - Test: `grep -rn "magi" fjs/` (run manually) returns nothing, matching the gate's own
      assertion.
    - Behavior: temporarily inserting a lowercase `magi` identifier into a scratch `.f.js` file
      and re-running `node --test magi-gate.test.js` fails with a message naming the file and
      line — then revert the scratch change (mutation-verified, per AGENTS.md's "a proof is not
      known to work until you have watched it fail").
    - Behavior: the positive-control leaf (uppercase `MAGI` in prose) passes.
    - Test: `npm test` (which discovers `magi-gate.test.js` automatically via bare `node --test`)
      is green.
  </acceptance_criteria>
  <verify>
    <automated>`grep -rn "magi" fjs/` (run manually) returns nothing, matching the gate's own assertion.</automated>
  </verify>
  <done>temporarily inserting a lowercase `magi` identifier into a scratch `.f.js` file and re-running `node --test magi-gate.test.js` fails with a message naming the file and line — then revert the scratch change (mutation-verified, per AGENTS....</done>
</task>

<task type="auto">
  <name>Task 2: Correct the ten stale remedy strings</name>
  <files>fjs/return/scope/module.f.js</files>
  <read_first>
    fjs/return/scope/module.f.js (`unmodeledKindRefusals`'s full current text — every one of
    the ten entries this task touches)
  </read_first>
  <action>
    Correct Decision 1.4's five: `householdEmployeeWages`, `medicaidWaiverPayments`,
    `otherEarnedIncome`, `federalTaxWithheldOnOtherForms` each become `remedy: 'no dialect
    models it (no phase yet)'`; `netQualifiedDisasterLoss` becomes `remedy: 'requires Form 4684
    (no phase yet)'`. Correct the five Plan 13-11 surfaced:
    `scheduleOneAdditionalIncome`/`scheduleOneAdjustments` become `remedy: 'this coarse kind
    covers many distinct Schedule 1 line items with no per-line dialect to attribute a real
    amount to any one of them (no phase yet)'`; `scheduleTwoTaxes` becomes `remedy: 'this
    coarse kind covers AMT, self-employment tax and other Schedule 2 items this engine has no
    dialect for (no phase yet)'`; `scheduleThreeNonrefundableCredits`/
    `scheduleThreeRefundableCredits` become the equivalent for Schedule 3's credits. Every one
    of the ten `everyRefusalNamesALineALabelAndARemedy`-checked fields must stay non-empty and
    line-location-prefixed (the existing proof already enforces this structurally — verify it
    still passes, do not weaken it). Add a code comment above `unmodeledKindRefusals`
    explaining that these ten corrections happened in Phase 13 for two different reasons:
    Decision 1.4's five were simply never going to be reached this phase; the other five ARE
    reached (Schedule 1/2/3 modules exist and are wired) but remain structurally uncomputable
    because the frozen `kindVocabulary` cannot attribute a coarse declaration to a specific
    line item.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "Phase 13" fjs/return/scope/module.f.js` returns `0` (every stale
      "(Phase 13)"/"(TAX-XX, Phase 13)" reference in `unmodeledKindRefusals` is gone).
    - Behavior: `everyRefusalNamesALineALabelAndARemedy` and
      `everyUnmodeledKindRefusesNamingItsOwnLineAndLabel` (the existing proofs) both still pass
      unchanged in structure.
    - Test: `npm test` clean.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean.</automated>
  </verify>
  <done>`grep -c "Phase 13" fjs/return/scope/module.f.js` returns `0` (every stale "(Phase 13)"/"(TAX-XX, Phase 13)" reference in `unmodeledKindRefusals` is gone).</done>
</task>

<task type="auto">
  <name>Task 3: Fix the 18-vs-19 line count in REQUIREMENTS.md and ROADMAP.md; final phase-wide check</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (TAX-10's own line, currently reading "19-line")
    .planning/ROADMAP.md (Phase 13's criterion 2, currently reading "19-line")
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, correct TAX-10's description from "a 19-line
    near-circular computation" to "an 18-line near-circular computation" (Decision 5.3,
    `[VERIFIED: i1040gi.pdf (2025) p32]`). In `.planning/ROADMAP.md`, correct Phase 13's
    criterion 2 the same way. Do not touch any other prose in either file. As the phase's final
    check, run the full suite and the mechanical gate together and record the resulting
    project-local proof count in this plan's own SUMMARY (the new baseline for Phase 14,
    superseding the 665 this phase started from).
  </action>
  <acceptance_criteria>
    - Source: `grep -c "19-line" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns `0`
      for both files; `grep -c "18-line" .planning/REQUIREMENTS.md .planning/ROADMAP.md`
      returns `1` for each.
    - Test: `npm test` clean; `grep -rn "magi" fjs/` empty; `tsc --noEmit` clean.
    - Behavior: `node --test 2>&1 | grep -c '^✔ import("./fjs/'` is recorded as this phase's
      final project-local proof count, exceeding the 665 baseline.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; `grep -rn "magi" fjs/` empty; `tsc --noEmit` clean.</automated>
  </verify>
  <done>`grep -c "19-line" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns `0` for both files; `grep -c "18-line" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns `1` for each.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `magi-gate.test.js` → `fjs/` source tree | Read-only filesystem scan of this repo's own source, no external/untrusted input |
| corrected remedy strings → taxpayer-facing refusal text | Compile-time constants; correcting them changes what a taxpayer reads on refusal, no runtime attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-17 | Tampering | the MAGI gate itself being evaded by mixed-case obfuscation (`Magi`, `MAGI` in an identifier) | mitigate | The regex is case-sensitive on the lowercase substring specifically, per Decision 3.6's own scoping — mutation-tested in Task 1 by inserting a real lowercase occurrence and watching the gate fail |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `grep -rn "magi" fjs/` returns nothing.
- `grep -c "Phase 13" fjs/return/scope/module.f.js` returns `0`.
- `grep -c "19-line" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns `0` for both.
</verification>

<success_criteria>
The MAGI gate is mechanical, not remembered. Every remedy string this phase's own work made
false is corrected. REQUIREMENTS.md and ROADMAP.md state the true 18-line worksheet count. The
phase's final project-local proof count is recorded as Phase 14's new baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-13-SUMMARY.md`
</output>
