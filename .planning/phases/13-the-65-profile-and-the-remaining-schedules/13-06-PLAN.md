---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 06
type: execute
wave: 3
depends_on: ["13-05"]
files_modified: [fjs/schedule/a/module.f.js, fjs/document/w2/module.f.js, fjs/document/1099r/module.f.js, fjs/tax/boundary/module.f.js]
autonomous: true
requirements: [TAX-13]

must_haves:
  truths:
    - "Schedule A computes all 18 printed lines, with mortgage-interest and charitable-AGI-limitation lines as documented zeros trusting the taxpayer-asserted, already-limited amount"
    - "The SALT worksheet's lines w1/w9 use the flat, non-MFS dollar figures; only w10 halves the result for MFS"
    - "A proof watches whether the taxpayer-asserted line 5a is at least the sum of stored W-2 box 17 and 1099-R box 14 withholding, without treating the stored total as an input"
    - "w2 and 1099r docstrings state that a proof now reads their state/local rows, even though computation still does not"
    - "The SALT cap phase-down start and floor thresholds, for every filing status the parameter carries, are registered in fjs/tax/boundary's generic threshold inventory"
  artifacts:
    - path: "fjs/schedule/a/module.f.js"
      provides: "Schedule A, all 18 lines, SALT worksheet, medical floor, documented zeros"
      min_lines: 150
    - path: "fjs/tax/boundary/module.f.js"
      provides: "allThresholds extended with the 10 SALT-cap phase-down start/floor thresholds; expectedThresholdCount bumped 58 → 68"
      contains: "saltCapPhasedownStart"
  key_links:
    - from: "fjs/schedule/a/module.f.js"
      to: "fjs/tax/params/module.f.js"
      via: "taxParamSet.saltCap, taxParamSet.medicalExpenseFloor"
      pattern: "saltCap"
    - from: "fjs/tax/boundary/module.f.js"
      to: "fjs/tax/params/module.f.js"
      via: "allThresholds reads taxParams2025.saltCap.threshold"
      pattern: "saltCapPhasedownStart"
---

<objective>
Build Schedule A: all 18 printed lines, the SALT cap phase-down worksheet (with its
MFS-halves-only-the-final-line quirk), the 7.5% medical floor, and the `saltCapPhasedownIncome`
named function — plus the Decision 2.2 withholding-drift proof, the w2/1099r docstring
corrections it requires, and this wave's registration of the SALT cap's phase-down thresholds in
`fjs/tax/boundary`'s generic, mechanically-generated threshold inventory.

Purpose: Schedule A's own face performs limitation arithmetic for exactly two of six sections
(medical floor, SALT cap); everything else is a documented, taxpayer-trusted boundary
(13-RESEARCH.md §3) — getting that boundary right is what keeps this module honest rather than
silently under- or over-computing. 13-PATTERNS.md names TWO complementary boundary-proof levels
as both required for every new threshold this phase introduces: a hand-written trio pinning the
SPECIFIC arithmetic (Task 2's own fixtures), AND an entry in `fjs/tax/boundary`'s generated
inventory, which catches a threshold silently dropped later — the second level is this plan's
own responsibility.
Output: `fjs/schedule/a/module.f.js` (new module), `fjs/document/w2/module.f.js` and
`fjs/document/1099r/module.f.js` (docstring amendments only), and `fjs/tax/boundary/module.f.js`
(extended `allThresholds`, bumped `expectedThresholdCount`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-RESEARCH.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
`fjs/schedule/b/module.f.js`, read IN FULL: `ScheduleBInputs` typedef shape, `Stored<T>`,
`BoxSum`, `sumBoxOverDocuments`/`addBoxSums`/`documentLine`/`profileDeclaredZeroLine` (all
reimplemented LOCALLY here, never imported from `fjs/form1040/core`), the
documented-zero-with-boundary-in-docstring paragraph for Form 8815, and the standalone,
not-wired-into-scope-guard module docstring shape.

13-RESEARCH.md §3's full 18-line transcription and the SALT worksheet's w1-w10 arithmetic
(quoted in Plan 13-05's `<interfaces>` and repeated in full in 13-RESEARCH.md — the exact
figures: w1=$40,000 flat, w5 threshold $500k/$250k MFS, w7=30%, w9=$10,000 flat, w10 = smaller
of (half-if-MFS(w9)) or Schedule A line5d).

`fjs/document/w2/module.f.js`'s current docstring (the paragraph to amend), and
`fjs/document/1099r/module.f.js`'s equivalent — both currently state nothing reads
`stateIncomeTax`/`stateTaxWithheld`. `w2`'s field is `stateIncomeTax` (inside the
`box15Through20` array); `1099r`'s is `stateTaxWithheld` (inside `stateLocal`).
`w2Schema.box2FederalIncomeTaxWithheld` / `oneZeroNineNineRSchema.box4FederalIncomeTaxWithheld`
are the FEDERAL withholding fields, unrelated to this proof.

From `fjs/tax/params/module.f.js`'s new `saltCap` (Plan 13-05): `saltCap.threshold` carries FIVE
filing-status keys — `single`, `marriedFilingJointly`, `marriedFilingSeparately`,
`headOfHousehold`, `qualifyingSurvivingSpouse` — `marriedFilingSeparately.amount ===
'250000.00'`, every other status `'500000.00'`. `saltCap.flatCap.amount === '40000.00'`,
`saltCap.floor.amount === '10000.00'`, `saltCap.phasedownRatePercent === 30`. The floor point
(where the phase-down bottoms out at the flat $10,000 floor) is `threshold[status] +
(flatCap − floor) / (phasedownRatePercent / 100)` = `threshold[status] + $100,000.00`, using the
SAME flat, non-halved `$40,000`/`$10,000` figures for every status (Pitfall 2 — only the SALT
worksheet's own `w10` halves the FINAL deduction for MFS; the phase-down's own start and floor
income thresholds are not themselves halved).

From `fjs/tax/boundary/module.f.js` (current, extended once already by Plan 13-03 to 58
thresholds — read the whole file, including Plan 13-03's own `seniorDeductionThresholds`
addition, as the immediate style precedent for a status-keyed `.flatMap` reading a NEW
parameter's stored strings).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Schedule A — all 18 lines, SALT worksheet, medical floor, and register the SALT cap thresholds in fjs/tax/boundary</name>
  <files>fjs/schedule/a/module.f.js, fjs/tax/boundary/module.f.js</files>
  <read_first>
    fjs/schedule/b/module.f.js (full file, per `<interfaces>`)
    fjs/document/itemized_deductions/module.f.js (Plan 13-05's new dialect — the entries array
    this module reads, filtered by `lineTag`)
    fjs/tax/boundary/module.f.js (full file, including Plan 13-03's `seniorDeductionThresholds`
    addition — the exact shape this task's SALT threshold registration must follow)
  </read_first>
  <behavior>
    Test 1 (medical floor): AGI $80,000, medical entries totaling $10,000 → line3 = $6,000.00
    (7.5% of $80,000), line4 = $4,000.00; medical entries totaling $5,000 (below the floor) →
    line4 = $0 (line3 > line1).
    Test 2 (SALT flat-then-halved, Pitfall 2): MFS filer, line5d = $30,000, AGI $600,000 (above
    the $250,000 MFS threshold, triggering the worksheet) → w1=$40,000 (NOT $20,000), w6 =
    600000−250000=350000, w7=350000×30%=105000, w8=40000−105000 → floored at w9=$10,000 (flat,
    NOT $5,000), w10 = smaller(HALF of $10,000 = $5,000, $30,000) = $5,000 → line5e = $5,000.
    Test 3 (SALT below-threshold skip): AGI at or below the threshold → w9=w1=$40,000 flat,
    w10 = smaller(halved-if-MFS, line5d) — for a non-MFS filer this collapses to the ordinary
    $40,000 cap.
    Test 4 (documented zeros): line3 (Form 8815, N/A here — Schedule A has none), line9 (Form
    4952 investment interest — always the taxpayer-asserted itemized entry tagged
    `investmentInterest`, or documented zero if absent AND the `investmentInterestForm4952`
    kind is undeclared), line15 (casualty/Form 4684) are documented zeros citing the profile,
    each with the module's own docstring stating why (mortgage-interest acquisition-debt caps
    and charitable AGI percentage limits are trusted, already-limited, taxpayer-asserted
    amounts per Decision 2.1 — Schedule A's own face performs no limitation arithmetic on
    those lines at all, per 13-RESEARCH.md §3).
    Test 5 (line18 election): the profile's `itemizeEvenThoughLessThanStandardDeduction` flows
    through to a `line18` boolean field on the result, read verbatim (no inference).
    Test 6 (fjs/tax/boundary registration): the generic `allThresholds` inventory contains ten
    new entries — `saltCapPhasedownStart:single` ($500,000.00),
    `saltCapPhasedownStart:marriedFilingJointly` ($500,000.00),
    `saltCapPhasedownStart:marriedFilingSeparately` ($250,000.00),
    `saltCapPhasedownStart:headOfHousehold` ($500,000.00),
    `saltCapPhasedownStart:qualifyingSurvivingSpouse` ($500,000.00),
    `saltCapPhasedownFloor:single` ($600,000.00),
    `saltCapPhasedownFloor:marriedFilingJointly` ($600,000.00),
    `saltCapPhasedownFloor:marriedFilingSeparately` ($350,000.00),
    `saltCapPhasedownFloor:headOfHousehold` ($600,000.00), and
    `saltCapPhasedownFloor:qualifyingSurvivingSpouse` ($600,000.00) — and
    `expectedThresholdCount` is `68`, with `everyThresholdIsCovered` and the generated
    per-threshold proof both green over the extended list.
  </behavior>
  <action>
    Create `fjs/schedule/a/module.f.js` exporting `scheduleA` typed `(taxParamSet: TaxParamSet)
    => (input: ScheduleAInput) => ScheduleA`, `ScheduleAInput` carrying `status:
    IndividualFilingStatus`, `agiCents: bigint`, `itemizedEntries: readonly Stored<{ lineTag:
    string, provider: string, amount: string }>[]` (the `vnd.fjs.itemized_deductions` entries,
    already flattened from the one stored document — the module reads `entries`, filtered by
    `lineTag` per line), `medicalExpenseEntries` similarly from
    `vnd.fjs.medical_expenses`, `profile: Stored<ReturnProfile>` (for `documentLine`/
    `profileDeclaredZeroLine`'s citation and for reading `itemizeEvenThoughLessThanStandardDeduction`).
    Reimplement `sumBoxOverDocuments`/`documentLine`/`profileDeclaredZeroLine`/`totalLine`
    locally (per `schedule/b`'s own pattern — a filtered-by-lineTag reader for the itemized
    entries plays the role `sumBoxOverDocuments` plays for a fixed box). Compute: line1 = sum
    of medical entries minus `reimbursed`; line2 = agiCents; line3 = halfUp(line2 ×
    `taxParamSet.medicalExpenseFloor.ratePercent` / 100); line4 = max(line1−line3, 0); line5a =
    the itemized entry tagged `saltIncomeTax` (or `saltGeneralSalesTax` — either election,
    taxpayer-asserted, never computed) — SEE Task 2 for the withholding-drift proof on this
    line; line5b/5c = itemized entries tagged `realEstateTax`/`personalPropertyTax`; line5d =
    5a+5b+5c; line5e = the SALT worksheet result above, capped against line5d; line6 = itemized
    entry tagged `otherTaxes`; line7 = 5e+6; line8a/8b/8c = itemized entries tagged
    `mortgageInterest1098`/`mortgageInterestNo1098`/`pointsNo1098`; line8e = 8a+8b+8c; line9 =
    documented zero (Form 4952, out of scope, `investmentInterestForm4952` kind); line10 =
    8e+9; line11/12/13 = itemized entries tagged `charitableCash`/`charitableNonCash`/
    `charitableCarryover`; line14 = 11+12+13; line15 = documented zero (Form 4684); line16 =
    itemized entry tagged `otherItemized`; line17 = 4+7+10+14+15+16; line18 = the profile's
    `itemizeEvenThoughLessThanStandardDeduction === true`. Export the named function
    `saltCapPhasedownIncome`, typed `(agiCents: bigint) => bigint`, computing the SAME
    AGI-plus-foreign-add-backs shape as `seniorDeductionPhaseoutIncome` (currently `=
    agiCents`, since every add-back is unmodeled and always zero) — its OWN docstring states
    its own add-back list, and it deliberately does not import or call
    `seniorDeductionPhaseoutIncome` (TAX-15/Decision 5.6, 13-PATTERNS.md's `qssParameters...`
    precedent — identical values, independently stored). Write the module's own docstring
    stating the two documented boundaries (mortgage-interest cap, charitable AGI limits)
    verbatim in the `schedule/b` Form 8815 style, and the SALT-worksheet's own "flat-then-
    halved" quirk in the `schedule/b` "two independent tests" warning style.

    In the SAME task, extend `fjs/tax/boundary/module.f.js`: add a new `saltCapThresholds`
    constant, built the same way Plan 13-03's `seniorDeductionThresholds` is —
    `(['single', 'marriedFilingJointly', 'marriedFilingSeparately', 'headOfHousehold',
    'qualifyingSurvivingSpouse'] as const).flatMap(status => [ { label:
    \`saltCapPhasedownStart:${status}\`, cents:
    centsFromString(taxParams2025.saltCap.threshold[status].amount) }, { label:
    \`saltCapPhasedownFloor:${status}\`, cents:
    centsFromString(taxParams2025.saltCap.threshold[status].amount) + 10000000n } ])` — the
    floor point adds `$100,000.00` (10,000,000 cents) to EVERY status's own threshold, derived
    from `(saltCap.flatCap − saltCap.floor) / (saltCap.phasedownRatePercent / 100)` =
    `($40,000 − $10,000) / 0.30` = `$100,000`, the SAME flat spread for every status per
    Pitfall 2 (only the worksheet's own `w10` halves the FINAL deduction for MFS, not this
    income threshold). Spread `saltCapThresholds` into `allThresholds`'s array literal (after
    Plan 13-03's `seniorDeductionThresholds`). Bump `expectedThresholdCount` from `58` to `68`
    in the same change — the exact count of the ten new entries above.
  </action>
  <acceptance_criteria>
    - Behavior: Test 1's medical-floor fixture: `line3 === 600000n`, `line4 === 400000n`.
    - Behavior: Test 2's MFS SALT fixture: `line5e === 500000n` (cents).
    - Behavior: Test 4's documented-zero lines each cite `profile.value.declaredKinds`.
    - Behavior: `line18 === true` when the profile field is set, `false` otherwise.
    - Source: `fjs/tax/boundary/module.f.js`'s `allThresholds.length === 68` and
      `expectedThresholdCount === 68`, both against the bumped hand-typed constant.
    - Behavior: `fjs/tax/boundary/module.f.js`'s generated per-threshold proof includes a green
      leaf for each of the ten `saltCapPhasedownStart:*`/`saltCapPhasedownFloor:*` labels listed
      in Test 6, including the MFS-specific `$250,000.00`/`$350,000.00` pair (distinct from the
      other four statuses' `$500,000.00`/`$600,000.00` pair).
    - Test: `npm test` clean; project-local proof count rises further.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises further.</automated>
  </verify>
  <done>Test 1's medical-floor fixture: `line3 === 600000n`, `line4 === 400000n`; `fjs/tax/boundary/module.f.js`'s `expectedThresholdCount === 68` with all ten new SALT-cap threshold leaves green.</done>
</task>

<task type="auto">
  <name>Task 2: Decision 2.2's withholding-drift proof, and the w2/1099r docstring amendments</name>
  <files>fjs/schedule/a/module.f.js, fjs/document/w2/module.f.js, fjs/document/1099r/module.f.js</files>
  <read_first>
    fjs/document/w2/module.f.js (the current "boxes 15-20 ... never computed on" docstring
    paragraph to amend)
    fjs/document/1099r/module.f.js (the equivalent paragraph for `stateLocal`)
  </read_first>
  <action>
    In `fjs/schedule/a/module.f.js`, add a PROOF (not production logic — this never changes
    `line5a`'s VALUE, which stays taxpayer-asserted) that, when the itemized-deductions
    entries' income-tax election is in force (i.e., `line5a` is the `saltIncomeTax`-tagged
    entry, not `saltGeneralSalesTax`), asserts `centsFromString(line5a-entry.amount) >=`
    the sum of every stored W-2's `stateIncomeTax` (across each W-2's `box15Through20` array)
    plus every stored 1099-R's `stateTaxWithheld` (across each 1099-R's `stateLocal` array) —
    withheld tax is necessarily paid, so the asserted line 5a can never honestly be less. This
    is a PROOF watching for drift (12.1 Decision 2.4's shape, cited by CONTEXT.md Decision 2.2),
    never an input Schedule A reads to COMPUTE line5a. Write it as its own named proof leaf
    (e.g. `assertedSaltIncomeTaxIsAtLeastStoredWithholding`) plus a CONTROL fixture where the
    asserted amount is deliberately lower than stored withholding and the proof's own assertion
    (not Schedule A itself) fails loudly — demonstrate this by wrapping the assertion in the
    project's `refuses`-style helper so the control proves the check is load-bearing. Then
    amend `fjs/document/w2/module.f.js`'s docstring paragraph (the one stating "nothing here
    reads them") to add: a PROOF in `fjs/schedule/a` now reads `stateIncomeTax` to watch the
    asserted Schedule A line 5a for drift — computation still never uses it; the two claims are
    different and both are now true. Make the identical amendment to
    `fjs/document/1099r/module.f.js`'s docstring, naming `stateTaxWithheld` precisely (not
    "box 14" — the array shape means there is no single schema key by that name).
  </action>
  <acceptance_criteria>
    - Behavior: a fixture with W-2 `stateIncomeTax: '4200.00'` and asserted `saltIncomeTax`
      entry `'5000.00'` passes the drift proof; the SAME fixture with the asserted entry
      lowered to `'1000.00'` (below the stored $4,200) fails it, and the control confirms the
      failure names the drift, not merely "something happened".
    - Source: `grep -n "proof.*reads" fjs/document/w2/module.f.js` (or equivalent phrasing)
      shows the amended paragraph distinguishes "a proof reads" from "computation reads".
    - Source: the 1099-R docstring amendment names `stateTaxWithheld`, not "box 14".
    - Test: `npm test` clean; project-local proof count rises further.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises further.</automated>
  </verify>
  <done>a fixture with W-2 `stateIncomeTax: '4200.00'` and asserted `saltIncomeTax` entry `'5000.00'` passes the drift proof; the SAME fixture with the asserted entry lowered to `'1000.00'` (below the stored $4,200) fails it, and the control con...</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| taxpayer-asserted itemized entries → Schedule A computation | Already-validated dialect entries; Schedule A trusts amounts as already Pub-936/Pub-526-limited per Decision 2.1 |
| stored W-2/1099-R state withholding → the drift proof | Read-only cross-check, never an input to computation; a mismatch is a data-quality signal, not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-09 | Tampering | SALT worksheet's flat-vs-halved figures | mitigate | Cent-exact boundary fixtures at the MFS/non-MFS split, per Pitfall 2, now enforced at BOTH the module-local hand-written level and `fjs/tax/boundary`'s generic drift-detection level |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Task 1's ending count.
- `fjs/tax/boundary/module.f.js`'s `expectedThresholdCount === 68`.
</verification>

<success_criteria>
Schedule A computes all 18 lines correctly, including the SALT worksheet's flat-then-halved
quirk and the medical floor. The withholding-drift proof watches stored W-2/1099-R state
withholding without treating it as an input. Both docstrings state the true, narrower claim.
`fjs/tax/boundary`'s generic threshold inventory grows from 58 to 68, covering this wave's new
SALT cap thresholds mechanically.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-06-SUMMARY.md`
</output>
</output>
