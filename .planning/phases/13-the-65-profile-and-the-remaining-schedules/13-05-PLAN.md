---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 05
type: execute
wave: 3
depends_on: ["13-04"]
files_modified: [fjs/tax/params/module.f.js, fjs/document/itemized_deductions/module.f.js, fjs/return/profile/module.f.js]
autonomous: true
requirements: [TAX-13]

must_haves:
  truths:
    - "vnd.fjs.itemized_deductions stores line-tagged entries with no stored total and no formRevision, exactly like vnd.fjs.medical_expenses"
    - "The SALT cap ($40,000/$10,000 floor/$500k threshold/30% rate) and the 7.5% medical floor are stored with honest citations, neither claiming Rev. Proc. 2025-32"
    - "A taxpayer can elect to itemize even though less than the standard deduction (Schedule A line 18) via a new profile field"
  artifacts:
    - path: "fjs/document/itemized_deductions/module.f.js"
      provides: "vnd.fjs.itemized_deductions dialect, line-tagged entries, no total"
      contains: "vnd.fjs.itemized_deductions"
    - path: "fjs/tax/params/module.f.js"
      provides: "saltCap and medicalExpenseFloor parameters"
      contains: "saltCap"
  key_links:
    - from: "fjs/document/itemized_deductions/module.f.js"
      to: "fjs/document/base/module.f.js"
      via: "base(dialect)"
      pattern: "base(dialect)"
---

<objective>
Lay Wave 3's foundation for the itemizing slice (TAX-13): create the taxpayer-asserted
`vnd.fjs.itemized_deductions` dialect (verbatim to `vnd.fjs.medical_expenses`'s design), store
the SALT cap and 7.5% medical floor parameters with citations that do NOT claim Rev. Proc.
2025-32 (only the CTC figure is in it), and add the Schedule A line 18 itemize-anyway election
to the return profile.

Purpose: itemizing does not automatically win above the standard deduction — Schedule A must
compute from real, taxpayer-asserted numbers before any comparison can be trusted (CONTEXT.md
Decision 2.1).
Output: `fjs/document/itemized_deductions/module.f.js` (new dialect),
`fjs/tax/params/module.f.js` (new `saltCap`/`medicalExpenseFloor` parameters),
`fjs/return/profile/module.f.js` (`itemizeEvenThoughLessThanStandardDeduction` field).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-RESEARCH.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
`fjs/document/medical_expenses/module.f.js`, read IN FULL as the verbatim precedent
(13-PATTERNS.md quotes its exact shape): `dialect`/`mediaType` from `base`/`mediaTypeOf`, a
`entryEntry` local const typed via `array(...)`, `recipientTin: string, taxYear: number,
corrected: option(true), entries: array(entryEntry)` — NO `formRevision`, NO total field,
`checkReferences` loops entries validating each money field via `moneyFieldError`, and a
`noTotalIsStored` proof asserting `Object.keys(v).includes('total') === false`.

13-RESEARCH.md §3's SALT cap worksheet, exact figures:
```
w1 = $40,000 (flat, NOT $20,000 for MFS)
w5 threshold = $500,000 ($250,000 MFS)
w7 rate = 30% (continuous, no $1,000 stepping)
w9 floor = $10,000 (flat, NOT $5,000 for MFS — only w10 halves the FINAL result for MFS)
```
Medical floor: 7.5%, IRC §213(a), unmodified by OBBBA.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SALT cap and medical floor parameters</name>
  <files>fjs/tax/params/module.f.js</files>
  <read_first>
    fjs/tax/params/module.f.js (the `seniorDeduction` entry Plan 13-03 added, as the immediate
    style precedent for a `kind: 'publicLaw'` parameter)
  </read_first>
  <action>
    Add exported constant `saltCap` shaped `{ readonly flatCap: AmountWithCitation, readonly
    floor: AmountWithCitation, readonly phasedownRatePercent: number, readonly threshold: {
    readonly single: AmountWithCitation, readonly marriedFilingJointly: AmountWithCitation,
    readonly marriedFilingSeparately: AmountWithCitation, readonly headOfHousehold:
    AmountWithCitation, readonly qualifyingSurvivingSpouse: AmountWithCitation } }` with
    `flatCap.amount = '40000.00'`, `floor.amount = '10000.00'`, `phasedownRatePercent = 30`,
    `threshold.marriedFilingSeparately.amount = '250000.00'`, every other status
    `'500000.00'` — each citation `{ kind: 'publicLaw', publicLaw: '119-21', section:
    '§70120', effectiveDate: '2025-01-01' }` (OBBBA, 13-RESEARCH.md §3/Pitfall 5). Add a
    second exported constant `medicalExpenseFloor` shaped `AmountWithCitation`-like but with a
    RATE, not a dollar amount: `{ readonly ratePercent: number, readonly citation: Citation }`,
    `ratePercent: 7.5`, citation `{ kind: 'code', section: '§213(a)', effectiveDate:
    '2025-01-01' }` (long-standing, unmodified by OBBBA — 13-RESEARCH.md assumption A2).
    `ratePercent` is a plain number crossing no money boundary, matching `ratePercent` on
    `Bracket` — do NOT add it to `everyDollarStringField`'s round-trip list (it is not a
    dollar-string field). Extend `taxParamsByYear[2025]` with both new fields, and extend
    `everyDollarStringField` with `saltCap`'s five dollar-string fields (`flatCap.amount`,
    `floor.amount`, and the five threshold amounts).
  </action>
  <acceptance_criteria>
    - Source: `saltCap.flatCap.amount === '40000.00'`, `.floor.amount === '10000.00'`,
      `.threshold.marriedFilingSeparately.amount === '250000.00'`.
    - Source: `medicalExpenseFloor.ratePercent === 7.5`, `.citation.kind === 'code'`.
    - Behavior: a proof leaf asserts `saltCap`'s citations are ALL `kind: 'publicLaw'` and
      `publicLaw === '119-21'` — never `'revProc'` (Pitfall 5's exact trap).
    - Test: `npm test` clean; project-local proof count rises.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises.</automated>
  </verify>
  <done>`saltCap.flatCap.amount === '40000.00'`, `.floor.amount === '10000.00'`, `.threshold.marriedFilingSeparately.amount === '250000.00'`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create vnd.fjs.itemized_deductions; add the Schedule A line 18 election</name>
  <files>fjs/document/itemized_deductions/module.f.js, fjs/return/profile/module.f.js</files>
  <read_first>
    fjs/document/medical_expenses/module.f.js (the WHOLE file — verbatim precedent)
    fjs/return/profile/module.f.js (returnProfileSchema's `hadForeignFinancialAccount`-style
    additive-field precedent)
  </read_first>
  <behavior>
    Test 1: an entry `{ lineTag: 'saltIncomeTax', provider: 'State of California', amount:
    '4200.00' }` round-trips; `entries: []` validates (a taxpayer with no itemizable expenses
    recorded yet is a real, distinct state).
    Test 2: `Object.keys(validatedValue).includes('total') === false` — `noTotalIsStored`,
    copied verbatim from `medical_expenses`.
    Test 3: a comma-grouped `amount: '4,200.00'` is refused via `moneyFieldError`.
    Test 4: the dialect discriminant rejects a `vnd.fjs.medical_expenses` blob structurally
    (DOC-00, `crossDialect`-style leaf).
  </behavior>
  <action>
    Create `fjs/document/itemized_deductions/module.f.js`, following
    `fjs/document/medical_expenses/module.f.js` verbatim in shape: `dialect =
    'vnd.fjs.itemized_deductions'`, `mediaType = mediaTypeOf(dialect)`, an `itemizedEntry`
    local const `{ lineTag: string, provider: string, amount: string }` — NO
    `reimbursed`-equivalent field (this dialect has no analog to medical's reimbursement
    concept). Schema: `{ ...base(dialect), recipientTin: string, taxYear: number, corrected:
    option(true), entries: array(itemizedEntry) }` — NO `formRevision` (DOC-10 does not apply;
    no printed form). `lineTag` is a free string (Decision 2.1 — enumerating Publication 526's
    allowed categories is deduction logic, this dialect stores; do not add an enum or a
    restricted literal union). `checkReferences` loops entries validating `amount` via
    `moneyFieldError`, mirroring `medical_expenses`'s loop exactly (no date field, so no
    `isoDate` check is needed here — this dialect has no `datePaid` analog). Write the
    module's own docstring's three-bullet argument (no formRevision / no total / free-string
    category) nearly verbatim from `medical_expenses`'s, substituting Schedule A for medical
    (13-PATTERNS.md gives the exact substituted text). `formSubject` cardinality: `''` for
    payer and account, one record per taxpayer per year (same as medical_expenses). Separately,
    in `fjs/return/profile/module.f.js`, add `itemizeEvenThoughLessThanStandardDeduction:
    option(true)` to `returnProfileSchema` (Decision 2.5 — an election no document reports,
    exactly like `hadForeignFinancialAccount`), with a docstring bullet stating what it is.
  </action>
  <acceptance_criteria>
    - Behavior: `validate({ dialect, recipientTin: '222-22-2222', taxYear: 2025, entries: [] })`
      returns `ok`.
    - Behavior: `noTotalIsStored`-equivalent leaf passes.
    - Behavior: a comma-grouped amount is refused.
    - Behavior: `returnProfileSchema` structurally accepts
      `itemizeEvenThoughLessThanStandardDeduction: true` and rejects `: false` (DOC-12).
    - Test: `npm test` clean; project-local proof count rises further.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises further.</automated>
  </verify>
  <done>`validate({ dialect, recipientTin: '222-22-2222', taxYear: 2025, entries: [] })` returns `ok`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| taxpayer-asserted itemized-deduction blob → dialect validation | Untrusted JSON input crosses here (same shape as every other dialect); mitigated by rtti structural validation + `checkReferences` semantic checks, identical pattern to `medical_expenses` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-07 | Tampering | `vnd.fjs.itemized_deductions` malformed money fields | mitigate | `moneyFieldError` exactness check, reused verbatim from `medical_expenses` |
| T-13-08 | Tampering | new `saltCap`/`medicalExpenseFloor` params | accept | Compile-time constants, not attacker-reachable |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Wave 2's ending count.
</verification>

<success_criteria>
`vnd.fjs.itemized_deductions` validates round-trip entries with no stored total. `saltCap` and
`medicalExpenseFloor` are stored with honest, non-Rev.-Proc. citations. The Schedule A line 18
election field exists on the return profile.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-05-SUMMARY.md`
</output>
