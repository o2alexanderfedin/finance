---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 10
type: execute
wave: 4
depends_on: ["13-09"]
files_modified: [fjs/form1040/core/module.f.js, fjs/return/scope/module.f.js]
autonomous: true
requirements: [TAX-12]

must_haves:
  truths:
    - "A return with declared dependents gets a real, non-zero 1040 line 19 (CTC/ODC) and line 28 (ACTC)"
    - "A return with 3+ qualifying children refuses the whole report, naming Part II-B, rather than silently computing a wrong ACTC"
    - "childTaxCreditOrOtherDependents and additionalChildTaxCredit are modeled kinds, not refused ones"
  artifacts:
    - path: "fjs/form1040/core/module.f.js"
      provides: "lines 19 and 28 computed from fjs/form8812's output"
      contains: "form8812"
    - path: "fjs/return/scope/module.f.js"
      provides: "modeledKinds grown from 18 to 20; unmodeledKindRefusals shrunk from 32 to 30; verified no hand-typed proof fixture still references childTaxCreditOrOtherDependents or additionalChildTaxCredit as a refused-kind example"
      contains: "childTaxCreditOrOtherDependents"
---

<objective>
Complete Wave 4 (TAX-12): wire Schedule 8812's both halves into 1040 lines 19 and 28, thread its
Part II-B refusal as a document-data-sufficiency error arm, reclassify
`childTaxCreditOrOtherDependents` and `additionalChildTaxCredit` atomically, and prove end to
end that a return with dependents gets its credits.

Purpose: shipping half the form (line 19 without line 28) would leave line 28 refusing on a
return that has a completed Schedule 8812 sitting in front of it — an outcome no taxpayer could
interpret (CONTEXT.md Decision 4.3).
Output: lines 19 and 28 compute from real dependents; `childTaxCreditOrOtherDependents` and
`additionalChildTaxCredit` move to `modeledKinds` (20 total; 30 refused).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
From Plan 13-09's `fjs/form8812/module.f.js`:
```
export const scheduleEightEightOneTwo (or form8812) = taxParamSet => input => Form8812Outcome
// Form8812Outcome = { kind: 'ok', line1..line14, line16a, line16b, line17, line18a, line18b, line19, line20, line27 } | { kind: 'error', message, unmodeled: [] }
```
From `fjs/form1040/core/module.f.js` (current — `line19` and `line28` are EXISTING
`declaredZero` fields; `line18` already exists as `totalLine('1040 line 18')([line16, line17])`,
the exact input Schedule 8812's Credit Limit Worksheet needs):
```
const line19 = declaredZero('1040 line 19')  // child tax credit, Schedule 8812
const line28 = declaredZero('1040 line 28')  // additional child tax credit
```
The Schedule-D absent-basis error-arm threading pattern (lines ~412-433) is the model to copy
for Part II-B's refusal — this is the THIRD such early-return guard in this file (Schedule D's,
Wave 1's `iraDeductionDeclared`, and now this one).

**This wave's kinds — `childTaxCreditOrOtherDependents`/`additionalChildTaxCredit` — are the
SAME two kinds Plan 13-04's Wave 2 repoint deliberately avoided reusing (that repoint chose
`householdEmployeeWages`/`unreportedTips` precisely because those two stay refused through this
wave and beyond).** A full-file read of `fjs/return/scope/module.f.js` confirms no hand-typed
proof leaf uses `childTaxCreditOrOtherDependents` or `additionalChildTaxCredit` as its own
"still refused" example fixture — the only occurrences are the two kinds' own table rows. Task
2 must VERIFY this explicitly (never assume it) as part of the atomic reclassification, and must
also confirm Plan 13-04's `householdEmployeeWages`/`unreportedTips` repoint still passes
unchanged, since this wave does not touch either kind.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire lines 19 and 28 (kinds still refused — inert)</name>
  <files>fjs/form1040/core/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js (line19/line28's current placeholders, line18's existing
    computation, and the Schedule-D error-arm guard at lines ~412-433 to mirror)
  </read_first>
  <action>
    Import `form8812` (Plan 13-09's export) from `../../form8812/module.f.js`. In
    `form1040TaxAndPaymentLines` (line19/line28 live here, alongside line18), AFTER line18 is
    computed, call `form8812(taxParamSet)({ status, agiCents: income.line11b.value,
    dependents: (profile.value.dependents ?? []).map(d => ({ relationship: d.relationship,
    ssnValidForEmployment: d.ssnValidForEmployment === true, ageAtYearEnd: d.ageAtYearEnd,
    livedWithTaxpayer: d.livedWithTaxpayer === true })), line18Cents: line18.value,
    earnedIncomeCents: profile.value.earnedIncome === undefined ? 0n :
    centsFromString(profile.value.earnedIncome), nontaxableCombatPayCents: 0n })` — thread the
    OUTCOME's error arm exactly like the Schedule D guard: `if (outcome.kind === 'error') {
    return { kind: 'error', message: outcome.message, unmodeled: [] } }` (this is the THIRD
    document-data-sufficiency refusal in this file — Part II-B's 3+-qualifying-children case,
    `unmodeled: []` since it names no `fjs/return/scope` kind). On the `ok` arm: `line19 = {
    value: outcome.line14, sources: unionSources([income.line11b]), rule: '1040 line 19' }`;
    `line28 = { value: outcome.line27, sources: unionSources([income.line11b, line18]),
    rule: '1040 line 28' }`. Note: `form1040TaxAndPaymentLines`'s OWN return type already
    unions with `Form1040Error` (it already threads `line16Outcome`'s error arm the same way),
    so this new guard slots into the existing union without a typedef change.
  </action>
  <acceptance_criteria>
    - Behavior: a fixture with two qualifying children (age 10, SSN valid) and AGI below the
      phase-out produces `line19.value === 440000n` (2 × $2,200) when no other credits apply.
    - Behavior: a fixture with 3 qualifying children returns `kind: 'error'` naming "Part II-B".
    - Behavior: with `childTaxCreditOrOtherDependents`/`additionalChildTaxCredit` still in
      `unmodeledKindRefusals`, a profile declaring either kind still refuses at
      `classifyScope`, before this wiring is reached (inert until Task 2).
    - Test: `npm test` clean; project-local proof count rises.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises.</automated>
  </verify>
  <done>a fixture with two qualifying children (age 10, SSN valid) and AGI below the phase-out produces `line19.value === 440000n` (2 × $2,200) when no other credits apply.</done>
</task>

<task type="auto">
  <name>Task 2: The atomic transition — reclassify both kinds, and verify no proof fixture still depends on either</name>
  <files>fjs/return/scope/module.f.js</files>
  <read_first>
    fjs/return/scope/module.f.js (now at 18/32, the same two hand-typed constants and
    docstring-paragraph pattern reused for the fourth time this phase). Specifically confirm, by
    reading the whole file, that no hand-typed proof leaf uses `childTaxCreditOrOtherDependents`
    or `additionalChildTaxCredit` as its own "still refused" example fixture — Plan 13-04's
    `twoUnmodeledKindsRefuseNamingBothInFormOrder` and
    `unmodeledFollowsFormOrderNotDeclarationOrder` already moved to
    `householdEmployeeWages`/`unreportedTips` for exactly this reason (see this plan's
    `<interfaces>`).
  </read_first>
  <action>
    In one commit: delete `childTaxCreditOrOtherDependents` and `additionalChildTaxCredit`'s
    entries from `unmodeledKindRefusals`; append both to `modeledKinds` (in `kindVocabulary`
    order). Bump `expectedModeledKindCount` 18 → 20 and `expectedUnmodeledKindCount` 32 → 30.
    Extend the docstring's reclassification history with this sixth/seventh pair.

    Add a mechanical verification step (not a code change unless it finds a problem): run
    `grep -n "childTaxCreditOrOtherDependents\|additionalChildTaxCredit"
    fjs/return/scope/module.f.js` and confirm the only remaining occurrences after this edit are
    each kind's new `modeledKinds` entry and the docstring's reclassification-history paragraph
    — no orphaned proof leaf anywhere in the file still declares either kind expecting a
    refusal. Also re-run `twoUnmodeledKindsRefuseNamingBothInFormOrder` and
    `unmodeledFollowsFormOrderNotDeclarationOrder` (Plan 13-04's repoint, using
    `householdEmployeeWages`/`unreportedTips`) and confirm both still pass unmodified.
  </action>
  <acceptance_criteria>
    - Source: `modeledKinds.length === 20`, `unmodeledKindRefusals.length === 30`.
    - Type: `tsc --noEmit` clean.
    - Behavior: `classifyScope(['childTaxCreditOrOtherDependents', 'additionalChildTaxCredit'])`
      returns `{ kind: 'ok' }`.
    - Source: `grep -n "childTaxCreditOrOtherDependents\|additionalChildTaxCredit"
      fjs/return/scope/module.f.js | wc -l` equals exactly the count of each kind's own
      `modeledKinds` entry plus docstring mentions — no leftover proof fixture treating either
      kind as refused.
    - Behavior: `twoUnmodeledKindsRefuseNamingBothInFormOrder` and
      `unmodeledFollowsFormOrderNotDeclarationOrder` (Plan 13-04's repoint) still pass, byte-for-
      byte unchanged, after this wave's reclassification.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>`modeledKinds.length === 20`, `unmodeledKindRefusals.length === 30`; no orphaned proof fixture references either reclassified kind as refused.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end proof — a return with dependents gets its CTC and ACTC</name>
  <files>fjs/form1040/core/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js (Waves 1-3's own proof blocks, the style to extend)
  </read_first>
  <action>
    Add a proof leaf (e.g. `wave4Dependents`) calling `form1040Report` for a filer declaring
    `['wages', 'childTaxCreditOrOtherDependents', 'additionalChildTaxCredit']` with
    `dependentCount: 2` and a matching `dependents` array (both qualifying children, low
    earned income so the ACTC's line19/line20 arithmetic is exercised too) — assert both
    `line19` and `line28` are non-zero and match hand-computed cents figures. Add a second leaf
    exercising the CTC/ODC phase-out cliff at the whole-report level: AGI one cent over
    $400,000 (MFJ) reduces `line19` by exactly $50.00 versus AGI at exactly $400,000.
  </action>
  <acceptance_criteria>
    - Behavior: the dependents fixture's `line19` and `line28` are both non-zero, matching
      hand-computed values.
    - Behavior: the phase-out-cliff fixture shows a $50.00 drop in `line19` between AGI
      $400,000.00 and $400,000.01.
    - Test: `npm test` clean; project-local proof count rises above Wave 3's ending count.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises above Wave 3's ending count.</automated>
  </verify>
  <done>the dependents fixture's `line19` and `line28` are both non-zero, matching hand-computed values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Schedule 8812's Part II-B refusal → whole-report error | A document-data-sufficiency refusal, not a `fjs/return/scope` kind; threaded the same way Schedule D's absent-basis refusal already is |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-14 | Repudiation | scope reclassification | mitigate | `_EveryKindIsEitherModeledOrRefused` type-checks the partition after this fourth reclassification |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Plan 13-09's ending count.
</verification>

<success_criteria>
A return with declared dependents computes real CTC/ODC (line 19) and ACTC (line 28). 3+
qualifying children refuses honestly. `modeledKinds` is 20; `unmodeledKindRefusals` is 30; both
sum to 50. Verified: no proof fixture references `childTaxCreditOrOtherDependents` or
`additionalChildTaxCredit` as refused, and Wave 2's `householdEmployeeWages`/`unreportedTips`
repoint still passes unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-10-SUMMARY.md`
</output>
</output>
