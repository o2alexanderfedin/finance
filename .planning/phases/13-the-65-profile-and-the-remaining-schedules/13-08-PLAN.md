---
phase: 13-the-65-profile-and-the-remaining-schedules
plan: 08
type: execute
wave: 4
depends_on: ["13-07"]
files_modified: [fjs/return/profile/module.f.js, fjs/tax/params/module.f.js]
autonomous: true
requirements: [TAX-12]

must_haves:
  truths:
    - "vnd.fjs.return_profile carries a dependents array whose length always equals dependentCount"
    - "Each dependent entry carries exactly the four facts Schedule 8812 itself keys its classification on: relationship, SSN validity, age at year end, lived-with-taxpayer"
    - "CTC ($2,200), ODC ($500), the ACTC cap ($1,700), and the $400k/$200k phase-out are stored with citations, and the CTC citation is the ONLY one backed by Rev. Proc. 2025-32"
  artifacts:
    - path: "fjs/return/profile/module.f.js"
      provides: "dependents array + cross-field length check"
      contains: "dependents"
    - path: "fjs/tax/params/module.f.js"
      provides: "childTaxCredit parameter set (CTC/ODC/ACTC/phase-out)"
      contains: "childTaxCredit"
---

<objective>
Lay Wave 4's foundation for the dependents/Schedule 8812 slice (TAX-12): add the `dependents`
array to `vnd.fjs.return_profile` (kept alongside `dependentCount`, with a cross-field length
proof), and store the CTC/ODC/ACTC/phase-out parameters — being careful that only the CTC
figure is backed by Rev. Proc. 2025-32.

Purpose: Schedule 8812 cannot tell a qualifying child from an other dependent from a bare
count, and the two credits differ in both amount and refundability (CONTEXT.md Decision 4.1).
Output: `fjs/return/profile/module.f.js` (`dependents` array), `fjs/tax/params/module.f.js`
(`childTaxCredit` parameter set).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-CONTEXT.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-RESEARCH.md
@.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-PATTERNS.md
@AGENTS.md

<interfaces>
From `fjs/return/profile/module.f.js` (current):
```
dependentCount: number,
declaredKinds: array(string),
...
// checkReferences check 7 (the nearest cross-field-check precedent):
if (r.line26EstimatedTaxPayments !== undefined && !r.declaredKinds.includes('estimatedTaxPayments')) {
    return error(...)
}
```
13-RESEARCH.md §4's exact CTC/ODC/ACTC figures:
```
CTC = $2,200/child [VERIFIED: f1040s8.pdf p1 line5; rp-25-32.pdf §2.03]
ODC = $500 (unchanged)
ACTC cap = $1,700/child [VERIFIED: f1040s8.pdf p2 line16b]
phase-out: $400,000 MFJ / $200,000 other, 5% rate, round EXCESS up to next $1,000 (a true cliff)
```
Rev. Proc. 2025-32 backs ONLY the $2,200 CTC figure (Pitfall 5) — ODC/ACTC-cap/phase-out are
NOT in it; research did not verify their exact U.S. Code subsection (LOW confidence on section
number only, per 13-RESEARCH.md's Confidence breakdown).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dependents array to vnd.fjs.return_profile</name>
  <files>fjs/return/profile/module.f.js</files>
  <read_first>
    fjs/return/profile/module.f.js (the full schema, `checkReferences`'s numbered checks, and
    the `hadForeignFinancialAccount`-style additive-field docstring bullets)
  </read_first>
  <behavior>
    Test 1: `dependents` array length matching `dependentCount` validates.
    Test 2: `dependents` array length NOT matching `dependentCount` is refused, naming both
    numbers.
    Test 3: `dependents` omitted entirely still validates (a return declaring `dependentCount:
    0` and no array is a real, valid state — the array is present only when there is something
    to itemize).
    Test 4: a dependent entry round-trips all four fields: `relationship`,
    `ssnValidForEmployment`, `ageAtYearEnd`, `livedWithTaxpayer`.
  </behavior>
  <action>
    Add a local `dependentEntrySchema` const: `{ relationship: string, ssnValidForEmployment:
    option(true), ageAtYearEnd: number, livedWithTaxpayer: option(true) }` (Decision 4.1 — DOC-12's
    option(true) convention extended to these taxpayer-asserted facts, matching every other
    boolean field in this dialect; ABSENT means "not asserted true", the conservative default
    for a credit-eligibility fact). Add `dependents: option(array(dependentEntrySchema))` to
    `returnProfileSchema`. In `checkReferences`, add a new numbered check (after the existing
    check 7): if `r.dependents !== undefined && r.dependents.length !== r.dependentCount`,
    `return error(\`dependents array length (${r.dependents.length}) does not match
    dependentCount (${r.dependentCount})\`)` — mirror the `line26EstimatedTaxPayments`
    cross-field check's shape exactly. Add a docstring bullet in the "schema decisions worth
    stating" section: `dependents` is additive and citizenship/resident-alien status is
    deliberately NOT one of its fields — Schedule 8812's own docstring documents that trust
    boundary (Decision 5.7), not this one.
  </action>
  <acceptance_criteria>
    - Behavior: Test 1-4 all pass as specified.
    - Test: `npm test` clean; project-local proof count rises.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises.</automated>
  </verify>
  <done>Test 1-4 all pass as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Add CTC/ODC/ACTC/phase-out parameters</name>
  <files>fjs/tax/params/module.f.js</files>
  <read_first>
    fjs/tax/params/module.f.js (the `seniorDeduction`/`saltCap` entries added in Waves 2-3, the
    immediate style precedent, and this file's own citation-per-parameter discipline)
  </read_first>
  <action>
    Add exported constant `childTaxCredit` shaped `{ readonly ctcAmount: AmountWithCitation,
    readonly odcAmount: AmountWithCitation, readonly actcCap: AmountWithCitation, readonly
    phaseoutThreshold: { readonly marriedFilingJointly: AmountWithCitation, readonly other:
    AmountWithCitation }, readonly phaseoutRatePercent: number }` with `ctcAmount.amount =
    '2200.00'`, citation `{ kind: 'revProc', revProc: '2025-32', section: '§2.03',
    effectiveDate: '2025-01-01' }` (the ONLY figure in this group backed by a Rev. Proc.);
    `odcAmount.amount = '500.00'`, citation `{ kind: 'code', section: '§24(h)', effectiveDate:
    '2025-01-01' }`; `actcCap.amount = '1700.00'`, same citation shape; `phaseoutThreshold.
    marriedFilingJointly.amount = '400000.00'`, `.other.amount = '200000.00'`, same citation
    shape; `phaseoutRatePercent = 5`. State in a code comment that the ODC/ACTC-cap/phase-out
    `section: '§24(h)'` citation is research's best available identification (13-RESEARCH.md's
    own "LOW confidence on exact U.S. Code section numbers for OBBBA provisions" caveat) — the
    DOLLAR figures themselves are HIGH confidence, read directly off the published 2025 form.
    Extend `taxParamsByYear[2025]` and `everyDollarStringField` with the four new dollar
    amounts.
  </action>
  <acceptance_criteria>
    - Source: `childTaxCredit.ctcAmount.amount === '2200.00'`,
      `.ctcAmount.citation.kind === 'revProc'`, `.ctcAmount.citation.revProc === '2025-32'`.
    - Source: `childTaxCredit.odcAmount.citation.kind === 'code'` (NOT `'revProc'` — the exact
      trap Pitfall 5 names).
    - Behavior: a proof leaf asserts the CTC citation and ONLY the CTC citation among this
      group is `kind: 'revProc'`.
    - Test: `npm test` clean; project-local proof count rises further.
  </acceptance_criteria>
  <verify>
    <automated>`npm test` clean; project-local proof count rises further.</automated>
  </verify>
  <done>`childTaxCredit.ctcAmount.amount === '2200.00'`, `.ctcAmount.citation.kind === 'revProc'`, `.ctcAmount.citation.revProc === '2025-32'`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| taxpayer-declared `dependents` array → CTC/ODC classification | Taxpayer assertion, same trust level as every other declared fact in `vnd.fjs.return_profile`; a false assertion is a taxpayer's own liability, not an engine vulnerability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-11 | Tampering | a crafted `dependents` entry with an out-of-range age or malformed SSN-validity flag | mitigate | `checkReferences`-style semantic validation follows in Wave 4's later plans when the array is consumed; this plan's own structural rtti schema already rejects a non-number `ageAtYearEnd` or a non-`option(true)` boolean field |
</threat_model>

<verification>
- `npm test` (`tsc && node --test`) is green.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises from Wave 3's ending count.
</verification>

<success_criteria>
`vnd.fjs.return_profile` carries a `dependents` array whose length is checked against
`dependentCount`. CTC/ODC/ACTC/phase-out parameters are stored, with only the CTC citation
backed by Rev. Proc. 2025-32.
</success_criteria>

<output>
After completion, create `.planning/phases/13-the-65-profile-and-the-remaining-schedules/13-08-SUMMARY.md`
</output>
