---
phase: 12
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fjs/return/profile/module.f.js
autonomous: true
requirements: [TAX-07]

must_haves:
  truths:
    - "vnd.fjs.return_profile validates every existing fixture unchanged after this plan — the edit is additive only"
    - "the four foreign-account fields are option(...), never a two-valued primitive — an unchecked box is the key's ABSENCE"
    - "a profile declaring hadForeignFinancialAccount without requiredToFileFinCen114 still validates (the two are independent questions)"
    - "no edit in this plan touches fjs/return/scope/module.f.js's modeled/unmodeled partition"
  artifacts:
    - path: "fjs/return/profile/module.f.js"
      provides: "returnProfileSchema with four new additive foreign-account fields (Schedule B Part III)"
      contains: "hadForeignFinancialAccount"
  key_links:
    - from: "fjs/schedule/b/module.f.js (Plan 12-04)"
      to: "fjs/return/profile/module.f.js's four new foreign-account fields"
      via: "reads Stored<ReturnProfile>.value directly, verbatim, never inferred from any document"
      pattern: "hadForeignFinancialAccount|requiredToFileFinCen114|foreignAccountCountries|receivedForeignTrustDistributionOrWasGrantorOrTransferor"
---

<objective>
Add four ADDITIVE fields to `vnd.fjs.return_profile` so Schedule B's Part III (foreign accounts and
trusts) has a taxpayer-declared source to read from (TAX-07, per 12-CONTEXT.md's decision: "Schedule
B's foreign-account answers are taxpayer-DECLARED, never inferred... They live on
`vnd.fjs.return_profile`").

This is the ONE deliberately-granted exception to "do not touch `fjs/return/`" in 12-CONTEXT.md's
AMENDED section: adding fields that model what the taxpayer declares is document work, not
computation, and it must NOT touch `fjs/return/scope/module.f.js`'s modeled/unmodeled partition —
that partition is Phase 12.1's to change, atomically, with its own paired deletion. This plan
touches exactly one file, and it is not that one.

Purpose: Schedule B (Plan 12-04) needs a real, validated place to read "did you have a foreign
account" from, without ever inferring the answer from a stored 1099 (no IRS information return
reports that question).
Output: `fjs/return/profile/module.f.js`, modified additively; every existing proof leaf in the
file continues to pass unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-CONTEXT.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md
@AGENTS.md

<interfaces>
<!-- fjs/return/profile/module.f.js's CURRENT returnProfileSchema (read in full before editing) —
     the schema this plan extends. Every field already there stays untouched: taxYear, filingStatus,
     claimedAsDependent, spouseItemizes, dualStatusAlien, taxpayerBornBeforeJan2_1961,
     taxpayerIsBlind, spouseBornBeforeJan2_1961, spouseIsBlind,
     spouseHadNoIncomeIsNotFilingAndIsNotADependent, dependentCount, declaredKinds,
     wholeDollarElection, earnedIncome, line26EstimatedTaxPayments, line35aRefundRequested,
     line36AppliedToNextYear. `moneyBoxFields` currently names exactly 4 fields with
     `expectedMoneyBoxFieldCount = 4` — this plan adds NO new money field, so that constant and
     array are UNCHANGED by this plan. -->

<!-- Source, fetched and read directly, 12-RESEARCH.md Q4 — https://www.irs.gov/pub/irs-pdf/f1040sb.pdf
     (2025 revision, "Created 4/23/25"), Part III, Line 7a/7b/8, verbatim:
     Line 7a: "did you have a financial interest in or signature authority over a financial account
       ... located in a foreign country?" [Yes/No] — AND, same line group, a SECOND sub-question:
       "If 'Yes,' are you required to file FinCEN Form 114 ... ?" [Yes/No]. These are TWO
       independent Yes/No sub-questions under one printed line label.
     Line 7b: "If you are required to file FinCEN Form 114, list the name(s) of the foreign
       country(-ies) where the financial account(s) is (are) located" — free text, plural.
     Line 8: "did you receive a distribution from, or were you the grantor of, or transferor to,
       a foreign trust?" [Yes/No] -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive foreign-account fields on `vnd.fjs.return_profile` (TAX-07, Schedule B Part III)</name>
  <files>fjs/return/profile/module.f.js</files>
  <read_first>
    fjs/return/profile/module.f.js (the WHOLE file — read every existing field, the `checkReferences`
    seven-step order, and the existing proof suite structure before adding to it; this plan's edit
    must not disturb any of the seven existing semantic checks)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-CONTEXT.md AMENDED section
    (the "one exception... deliberately granted" paragraph — read it in full, it is the authorization
    for touching this file at all)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md Q4's "Return-
    profile support for foreign-account declared facts" subsection (the four recommended fields,
    verbatim, and why each is `option(...)`)
  </read_first>
  <behavior>
    - A profile validating today (every existing fixture in this file's own `proof` object) still
      validates identically after this plan — additive means no existing behavior changes.
    - `hadForeignFinancialAccount: option(true)` — declaring it `false` is rejected structurally
      (same DOC-12 discipline as every other checkbox on this dialect).
    - `requiredToFileFinCen114: option(true)` may be declared `true` WITHOUT
      `hadForeignFinancialAccount` being declared — these are stored as two independent fields
      because the two printed sub-questions are independent facts; this dialect does not enforce a
      cross-field ordering between them (Schedule B, not this dialect, decides what to do with the
      combination).
    - `foreignAccountCountries: option(array(string))` accepts a list of country names and round-trips
      more than one entry without merging or deduplicating them.
    - `receivedForeignTrustDistributionOrWasGrantorOrTransferor: option(true)` — same `option(true)`
      discipline.
  </behavior>
  <action>
    Add four fields to `returnProfileSchema`, appended AFTER `line36AppliedToNextYear` (the schema's
    current last field) — do not reorder any existing field, since reordering an RTTI struct changes
    which field a structural mismatch reports first (DOC-00's discriminant-ordering concern, restated
    at the level of THIS schema's own fields): `hadForeignFinancialAccount: option(true)`,
    `requiredToFileFinCen114: option(true)`, `foreignAccountCountries: option(array(string))`,
    `receivedForeignTrustDistributionOrWasGrantorOrTransferor: option(true)`. Import `array` from
    `functionalscript/fjs/types/rtti/module.f.js` if not already imported (the file's existing import
    line already imports `array, number, option, string` — reuse it, do not re-import).

    Do NOT add a `checkReferences` cross-field rule linking these four fields to each other or to
    `declaredKinds` — 12-RESEARCH.md's recommendation lists no such rule, and inventing one here
    would be scope creep past what TAX-07 and this dialect's own "taxpayer-declared, never inferred"
    philosophy calls for. The four fields are structurally independent, exactly like
    `claimedAsDependent`/`spouseItemizes`/`dualStatusAlien` already are.

    Extend the existing `proof` object (do not replace it) with a new top-level group
    `foreignAccountFields`: `allFourPresentValidate` (a fixture with all four populated, including
    `foreignAccountCountries: ['France', 'Germany']`, validates `ok` and both countries survive in
    order); `eachCheckboxRejectsFalse` (mirrors the existing `falseFlagsRejected`-style leaves —
    `hadForeignFinancialAccount: false` and `receivedForeignTrustDistributionOrWasGrantorOrTransferor: false`
    each rejected); `requiredToFileWithoutHadAccountValidates` (a fixture declaring
    `requiredToFileFinCen114: true` alone, WITHOUT `hadForeignFinancialAccount`, validates `ok` —
    pins the "no forced ordering" behavior claim above as a proof, not just a docstring assertion);
    `multipleCountriesRoundTripInOrder` (two distinct country strings survive as two array entries in
    the order given). Also add one leaf to the file's EXISTING `fullyPopulatedValidates` fixture (or a
    sibling leaf, if editing the existing one risks disturbing its current assertions) confirming the
    fully-populated profile ALSO accepts all four new fields simultaneously alongside every
    already-existing field.

    Update the module's own docstring's "Three schema decisions worth stating" section (or add a
    fourth bullet) noting: the four foreign-account fields are additive, taxpayer-declared per
    Schedule B Part III, and deliberately carry no cross-field validation — citing 12-CONTEXT.md's
    "taxpayer-DECLARED, never inferred" decision by name.
  </action>
  <acceptance_criteria>
    - `grep -n "hadForeignFinancialAccount: option(true)" fjs/return/profile/module.f.js`,
      `grep -n "requiredToFileFinCen114: option(true)" fjs/return/profile/module.f.js`,
      `grep -n "foreignAccountCountries: option(array(string))" fjs/return/profile/module.f.js`, and
      `grep -n "receivedForeignTrustDistributionOrWasGrantorOrTransferor: option(true)" fjs/return/profile/module.f.js`
      each match exactly once in `returnProfileSchema`.
    - `grep -n "expectedMoneyBoxFieldCount = 4" fjs/return/profile/module.f.js` still matches
      UNCHANGED — this plan adds no money box.
    - `grep -c "assertEq(t, 'ok')\|assertEq(t, 'error')" fjs/return/profile/module.f.js` is HIGHER
      after this plan than before (new proof leaves added, none removed).
    - `grep -n "fjs/return/scope" fjs/return/profile/module.f.js` returns NOTHING — this file does
      not import or reference the scope guard.
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; the project-local proof count (baseline 544) increases; every pre-existing proof leaf in this file still passes (no leaf removed or weakened); every acceptance criterion above is confirmed by the grep commands shown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Taxpayer declaration -> `vnd.fjs.return_profile` `validate()` | The taxpayer's own self-reported answers (never derived from a transcribed document) cross into the typed store here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Repudiation | `hadForeignFinancialAccount`, `receivedForeignTrustDistributionOrWasGrantorOrTransferor`: `option(true)` | accept | `false` is structurally unrepresentable — mirrors the dialect's own existing checkbox convention (`claimedAsDependent`, `spouseItemizes`, etc.); no new risk class introduced. |
| T-12-03-02 | Spoofing / Repudiation | A taxpayer declares "no foreign account" (by omission) while one genuinely exists | accept | This is inherent to a self-declared, non-inferred fact — the SAME accepted risk `declaredKinds` already carries for every other 1040 income kind. 12-CONTEXT.md's own decision states the answers are taxpayer-declared BY DESIGN ("no IRS information return reports 'do you have a foreign account'"), so no document-based cross-check can exist without violating that decision. |
| T-12-03-03 | Tampering | `foreignAccountCountries` array entries | accept | Free-text country names, never parsed or validated against an ISO list — consistent with every other free-text label field in this codebase (`payerName`, `box8ForeignCountryOrUsPossession`). No computation reads these values structurally, so a malformed entry cannot corrupt a downstream figure. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly increases (baseline 544).
- Every proof leaf that existed in `fjs/return/profile/module.f.js` before this plan still passes —
  confirmed by re-running the full suite, not merely the new leaves.
- `npm test` (full suite) green after this plan lands.
</verification>

<success_criteria>
- `vnd.fjs.return_profile` carries four new additive `option(...)` fields for Schedule B Part III,
  none of which forces a cross-field ordering on the other.
- Every field previously on this dialect, and every proof leaf previously in this file, is
  unchanged.
- No line in this file references `fjs/return/scope`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-03-SUMMARY.md`
</output>
