---
phase: 12
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fjs/document/1099b/module.f.js
autonomous: true
requirements: [DOC-07]

must_haves:
  truths:
    - "vnd.fjs.1099b documents round-trip through validate() with every printed box independently option-absent-able, including box 1e and box 12"
    - "sourceArtifactHash is REQUIRED (not option) and rejected unless it decodes as a real cBase32 hash (isHash)"
    - "a genuinely absent box 1e is provably distinguishable from box 1e = '0.00' by a proof exhibiting a CHANGED gain, not merely by optionality"
    - "boxes 8-11 (profit/loss on contracts) accept negative money values"
    - "treating a blank box 1e as '0.00' in the DOC-07 consequence proof's own fixture turns that proof RED (Mutation Gate M3)"
  artifacts:
    - path: "fjs/document/1099b/module.f.js"
      provides: "vnd.fjs.1099b dialect tag, mediaType, oneZeroNineNineBSchema, checkReferences, validate, proof"
      min_lines: 380
  key_links:
    - from: "fjs/document/1099b/module.f.js's checkReferences"
      to: "fjs/document/money_field/module.f.js's moneyFieldError"
      via: "import + per-box loop over moneyBoxFields and the stateLocal array's stateTaxWithheld field"
      pattern: "moneyFieldError\\("
    - from: "fjs/document/1099b/module.f.js's checkReferences"
      to: "functionalscript/fjs/media/revision/module.f.js's isHash"
      via: "import, applied to sourceArtifactHash"
      pattern: "isHash\\("
---

<objective>
Add `vnd.fjs.1099b` (DOC-07): the full printed TY2025-revision 1099-B box list, the box
1e/box 12 basis-reported distinction with its required consequence proof, plus the new
`sourceArtifactHash` provenance field DOC-13 needs. Follows the same four-stage
`fjs/media/revision` template every dialect in this codebase follows
(`fjs/document/1099r/module.f.js` is the closest, most recent analog).

This is a STORAGE dialect only. It does not compute Form 8949 categories, Schedule D, or any
gain/loss figure that feeds a return — that chain is explicitly Phase 12.1's (12-CONTEXT.md,
12-RESEARCH.md's Anti-Patterns). DOC-07's required "changes the gain" consequence proof is a
dialect-level demonstration using a small, local, NON-exported helper function — never a
production computation.

Purpose: every 1099-B a declared brokerage profile produces must be representable, validated, and
must preserve the specific fact (basis reported vs. not vs. genuinely absent) that Phase 12.1's
Form 8949 categorization will depend on.
Output: `fjs/document/1099b/module.f.js`, a complete dialect module with its own `proof`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-CONTEXT.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md
@.planning/phases/11-wage-retirement-and-benefit-documents/11-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- base(dialect) / mediaTypeOf(dialect) — fjs/document/base/module.f.js, read in full -->
<!-- export const base = dialect => ({ dialect }) -- spread FIRST in every schema literal -->
<!-- export const mediaTypeOf = dialect => `application/${dialect}+json` -->

<!-- moneyFieldError — fjs/document/money_field/module.f.js -->
<!-- export const moneyFieldError = label => printed => string | undefined -->
<!-- Already accepts negatives (proven by its own negativeAccepted leaf) — boxes 8-11 need this. -->

<!-- isHash — functionalscript/fjs/media/revision/module.f.js -->
<!-- export const isHash = (s) => cBase32ToVec(s) !== null -->
<!-- Any lowercase alphanumeric string from '0123456789abcdefghjkmnpqrstvwxyz' (case-insensitive,
     i/l/o normalized) that does not decode to all-zero passes. A string containing '-' does NOT.
     Use the literal 'deadbeef00112233445566778899aabbccddeeff0011223344556677889900' for
     sourceArtifactHash fixtures in this file — the SAME literal 12-01 and 12-05's DOC-13 proof
     reuse, so a shared-provenance comparison across dialects is a plain `===` on identical
     literals. -->

<!-- centsFromString — fjs/exact/module.f.js -->
<!-- export const centsFromString = parse(centsScale) -- (printed: string) => bigint -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `vnd.fjs.1099b` — the full TY2025 box inventory plus the box 1e/box 12 consequence proof (DOC-07)</name>
  <files>fjs/document/1099b/module.f.js</files>
  <read_first>
    fjs/document/1099r/module.f.js (the closest, most recent four-stage-template analog — copy its
    exact shape and its generated-proof + independently-hand-typed-count idiom for money boxes)
    fjs/document/base/module.f.js
    fjs/document/money_field/module.f.js (note `negativeAccepted` — boxes 8-11 need this, unlike
    every other money box in this codebase so far)
    node_modules/functionalscript/fjs/media/revision/module.f.js (isHash, read directly)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md Q2 in full
    (complete TY2025 box inventory table; the box 1e / box 12 / box 5 three-way analysis; the
    Anti-Patterns section on the Form 8949 checkbox; the "Code Examples" section's illustrative
    `naiveGain` shape — read it as a MODEL for the proof, not literal code to paste unchanged)
  </read_first>
  <behavior>
    - `validate` accepts an instance with `box1eCostOrOtherBasis` present and an exact decimal
      string; rejects a comma-grouped or non-canonical value in that same box, exactly like every
      other money box.
    - `validate` accepts an instance with `box1eCostOrOtherBasis` OMITTED entirely (DOC-11: genuine
      absence, never a sentinel).
    - A local (non-exported) `naiveGain` helper applied to the SAME `box1dProceeds` with (a) a
      present `box1eCostOrOtherBasis` and (b) a genuinely absent one produces two DIFFERENT dollar
      figures — the consequence DOC-07 requires, not merely "the field can be omitted".
    - Boxes 8, 9, 10, 11 accept a NEGATIVE decimal string (`'-500.00'`) without refusal — these are
      profit-OR-loss boxes, unlike every scalar money box modeled in this codebase before now.
    - Every checkbox field (`corrected`, `fatcaFilingRequirement`, `box2ShortTermGainOrLoss`,
      `box2LongTermGainOrLoss`, `box2Ordinary`, `box3CollectiblesProceeds`, `box3QofProceeds`,
      `box5NoncoveredSecurity`, `box6ReportedToIrsGrossProceeds`, `box6ReportedToIrsNetProceeds`,
      `box7LossNotAllowedBasedOnAmountIn1d`, `box12BasisReportedToIrs`) rejects `false` structurally.
  </behavior>
  <action>
    Create `fjs/document/1099b/module.f.js` following `fjs/document/1099r/module.f.js`'s exact
    six-stage shape. `dialect = 'vnd.fjs.1099b'`.

    Module docstring MUST record BOTH URLs and the drift finding: the canonical
    `https://www.irs.gov/pub/irs-pdf/f1099b.pdf` returns HTTP 404 as of this session — 1099-B has NO
    stable un-suffixed URL, unlike 1099-DIV/1099-INT — so the ONLY valid source for TY2025 is
    `https://www.irs.gov/pub/irs-pdf/f1099b--2025.pdf` (the year-suffixed, TY2025-specific revision).
    State plainly that `sourceArtifactHash` is required (DOC-13), never `option`, for the same
    reason as `vnd.fjs.1099div` (12-RESEARCH Assumption A3) — do NOT widen `base()`.

    Schema fields, base spread first, in this exact order: `payerTin: string`,
    `recipientTin: string`, `accountNumber: string`, `taxYear: number`, `formRevision: string`,
    `corrected: option(true)`, `sourceArtifactHash: string` (REQUIRED, validated via `isHash`),
    `cusipNumber: option(string)`, `fatcaFilingRequirement: option(true)`,
    `applicableCheckboxOnForm8949: option(string)` (store the payer-printed letter A-F VERBATIM —
    12-RESEARCH.md's Anti-Patterns section forbids deriving it from boxes 2/5/12; that derivation is
    Phase 12.1's Form 8949 categorization job, not this dialect's), `box1aDescriptionOfProperty:
    option(string)`, `box1bDateAcquired: option(string)` (free text — no date primitive in this
    project; may be blank if box 5 is checked, per the form's own note), `box1cDateSoldOrDisposed:
    option(string)`, `box1dProceeds: option(string)`, `box1eCostOrOtherBasis: option(string)` (**THE
    LOAD-BEARING BOX** — see the consequence-proof requirement below), `box1fAccruedMarketDiscount:
    option(string)`, `box1gWashSaleLossDisallowed: option(string)`, `box2ShortTermGainOrLoss:
    option(true)`, `box2LongTermGainOrLoss: option(true)`, `box2Ordinary: option(true)` (box 2 prints
    THREE separate checkboxes, not one three-way enum — model all three independently, each may be
    blank if box 5 is checked), `box3CollectiblesProceeds: option(true)`, `box3QofProceeds:
    option(true)` (box 3 prints TWO separate checkboxes), `box4FederalIncomeTaxWithheld:
    option(string)`, `box5NoncoveredSecurity: option(true)`, `box6ReportedToIrsGrossProceeds:
    option(true)`, `box6ReportedToIrsNetProceeds: option(true)` (box 6 prints TWO separate
    checkboxes), `box7LossNotAllowedBasedOnAmountIn1d: option(true)`, `box8ProfitOrLossRealized:
    option(string)` (CAN be negative), `box9UnrealizedProfitOrLossPriorYearEnd: option(string)` (CAN
    be negative), `box10UnrealizedProfitOrLossCurrentYearEnd: option(string)` (CAN be negative),
    `box11AggregateProfitOrLoss: option(string)` (CAN be negative), `box12BasisReportedToIrs:
    option(true)` (**the other half of the box 1e story** — model independently, never derive one
    from the other's absence), `box13Bartering: option(string)`, `stateLocal:
    option(array(stateEntry))`, `payerName: option(string)`, `recipientName: option(string)`. The
    printed "2nd TIN not." checkbox is filer-only and NOT modeled — same precedent as every existing
    dialect.

    `stateEntry` (module-scope const, IDENTICAL shape to `vnd.fjs.1099div`'s): `state: string`,
    `stateIdNumber: option(string)`, `stateTaxWithheld: option(string)` — boxes 14-16, no locality.

    `moneyBoxFields` (scalar, exactly 10, in this order): `box1dProceeds`, `box1eCostOrOtherBasis`,
    `box1fAccruedMarketDiscount`, `box1gWashSaleLossDisallowed`, `box4FederalIncomeTaxWithheld`,
    `box8ProfitOrLossRealized`, `box9UnrealizedProfitOrLossPriorYearEnd`,
    `box10UnrealizedProfitOrLossCurrentYearEnd`, `box11AggregateProfitOrLoss`, `box13Bartering`.
    `expectedMoneyBoxFieldCount = 10`, hand-typed, same "deliberately NOT `.length`" docstring
    idiom. `box1aDescriptionOfProperty`, `box1bDateAcquired`, `box1cDateSoldOrDisposed`,
    `cusipNumber`, `applicableCheckboxOnForm8949` stay plain `option(string)` with NO
    numeric-exactness check — none is money.

    `stateLocalMoneyFields` (exactly 1): `stateTaxWithheld`. `expectedStateLocalMoneyFieldCount = 1`,
    hand-typed, same idiom.

    `checkReferences`, in this order: (1) `formRevision.trim() === ''` rejected; (2)
    `sourceArtifactHash` checked via `isHash`; (3) loop `moneyBoxFields` through `moneyFieldError`
    (which already accepts negatives — do not add a separate positive-only check for boxes 8-11);
    (4) loop `r.stateLocal ?? []` through `stateLocalMoneyFields` via `moneyFieldError`.

    Proof suite, mirroring `1099r`'s structure: `dialectAndMediaType`;
    `validate.fullyPopulatedValidates`; `validate.blankBoxesOmittedValidates`;
    `validate.falseFlagsRejected` (all twelve checkbox fields named in the `<behavior>` block above);
    `validate.wrongDialectRejected`; `checkReferences.emptyFormRevisionRejected`;
    `checkReferences.sourceArtifactHashRejectedWhenNotAHash`; `checkReferences.sourceArtifactHashRequired`
    (structural `ValidationError`, path names `sourceArtifactHash`); `checkReferences.moneyBoxExactness`
    (generated + hand-typed-count pair against `expectedMoneyBoxFieldCount`);
    `checkReferences.stateLocalMoneyExactness` (same pair against `expectedStateLocalMoneyFieldCount`);
    `checkReferences.negativeProfitOrLossBoxesAccepted` (a leaf validating an instance with
    `box8ProfitOrLossRealized: '-500.00'`, `box9UnrealizedProfitOrLossPriorYearEnd: '-1200.00'`,
    `box10UnrealizedProfitOrLossCurrentYearEnd: '-1200.00'`, `box11AggregateProfitOrLoss: '-500.00'`
    all validating `ok`); `checkReferences.nonMoneyFreeTextFieldsAcceptAnyStringWithoutExactnessCheck`
    (a leaf proving `box1aDescriptionOfProperty`/`box1bDateAcquired`/`applicableCheckboxOnForm8949`
    accept arbitrary non-numeric strings).

    **DOC-07 consequence proof (`proof.criterion2GainConsequence`, the requirement's actual test):**
    Define a proof-file-local (NOT exported — this phase does not compute Form 8949/Schedule D)
    helper `naiveGain`, typed `(proceedsPrinted: string) => (basisPrinted: string | undefined) =>
    bigint`, computing `centsFromString(proceedsPrinted) - centsFromString(basisPrinted ?? '0.00')`.
    Validate `withBasis` (`box1dProceeds: '10000.00'`, `box1eCostOrOtherBasis: '6000.00'`) and
    `withoutBasis` (`box1dProceeds: '10000.00'`, box 1e OMITTED — genuine absence). Assert
    `withoutBasis`'s validated `box1eCostOrOtherBasis === undefined` (the dialect genuinely models
    absence, not a string `'0'`). Assert `naiveGain('10000.00')(withBasis value's box1e)` equals
    `400000n` and `naiveGain('10000.00')(withoutBasis value's box1e)` equals `1000000n`, then assert
    these two results are NOT equal (`assert(withBasisGain !== withoutBasisGain, [...])`) — this
    inequality IS the "changes the gain" consequence DOC-07 requires: the identical naive formula
    applied to a present-vs-absent box 1e yields two different dollar figures, so silently
    conflating "absent" with "reported as zero" would silently change what a return reports. Do NOT
    reduce this to only asserting the field can be omitted — that proves optionality, not the
    consequence, and is explicitly the failure mode 12-VALIDATION.md warns against.

    For the fixture's `sourceArtifactHash`, use the literal
    `'deadbeef00112233445566778899aabbccddeeff0011223344556677889900'` (same literal as 12-01 and
    12-05, per the interfaces block above).
  </action>
  <acceptance_criteria>
    - `fjs/document/1099b/module.f.js` exports `dialect === 'vnd.fjs.1099b'`,
      `mediaType === 'application/vnd.fjs.1099b+json'`, `oneZeroNineNineBSchema`, `checkReferences`,
      `validate`, `proof`.
    - `grep -n "irs-pdf/f1099b--2025.pdf" fjs/document/1099b/module.f.js` matches, and
      `grep -n "irs-pdf/f1099b.pdf" fjs/document/1099b/module.f.js` (the 404'd canonical URL) also
      appears, cited as the URL that no longer resolves.
    - `grep -n "expectedMoneyBoxFieldCount = 10" fjs/document/1099b/module.f.js` and
      `grep -n "expectedStateLocalMoneyFieldCount = 1" fjs/document/1099b/module.f.js` both match.
    - `grep -n "isHash(" fjs/document/1099b/module.f.js` matches; `sourceArtifactHash` is plain
      `string`, not `option(string)`.
    - `grep -n "naiveGain" fjs/document/1099b/module.f.js` matches, and `export const naiveGain`
      does NOT appear — the helper is proof-local, not a production export.
    - `grep -n "dispatchLine16\|Form8949\|scheduleD\|fjs/tax/line16" fjs/document/1099b/module.f.js`
      returns NOTHING — no computation, no wiring.
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; the project-local proof count (baseline 544) increases; every acceptance criterion above is confirmed by the grep commands shown.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mutation Gate M3 — prove the box-1e-absence consequence proof is load-bearing (BLOCKING)</name>
  <files>fjs/document/1099b/module.f.js</files>
  <read_first>
    fjs/document/1099b/module.f.js (as written by Task 1 — the `criterion2GainConsequence` proof
    group's `withoutBasis` fixture and the inequality assertion between the two `naiveGain` results)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-VALIDATION.md "Mutation
    Gate" section, row M3 (expected outcome: "Treat a blank box 1e as `\"0\"` instead of absent" ->
    "The DOC-07 gain proof goes RED" — "If the proof survives the mutation, it proves the field is
    optional but not that the distinction MATTERS")
    AGENTS.md "A proof is not known to work until you have watched it fail" section
  </read_first>
  <action>
    Since this phase's production code has no code path that ever coerces a blank box 1e to `'0'`
    (there is no such branch to mutate in `checkReferences` — `option(string)` never defaults), the
    mutation is applied to the PROOF'S OWN FIXTURE, exactly simulating "a blank box 1e treated as
    zero": if the fixture meant to model genuine absence instead supplies an explicit `'0.00'`, the
    proof that distinguishes absence from presence must fail to distinguish them — proving the real
    assertion is checking the fact that matters, not merely that the key is optional.

    **Mutation Gate M3 (BLOCKING — run this now, recorded, before considering this plan done):**
    1. In `fjs/document/1099b/module.f.js`'s `criterion2GainConsequence` proof, temporarily ADD
       `box1eCostOrOtherBasis: '0.00'` to the `withoutBasis` fixture (which previously omitted the
       key entirely) — i.e., simulate "a blank box 1e treated as zero" by literally supplying `'0'`
       where genuine absence was modeled.
    2. Run `node --test 2>&1 | grep -c '^✔ import("./fjs/'` and confirm the count DROPS relative to
       the post-Task-1 count. Confirm specifically that either the
       `withoutBasis.value.box1eCostOrOtherBasis === undefined` assertion, or the inequality
       assertion between `naiveGain` on `withBasis` vs. `withoutBasis`, now fails (the two naive
       gains become equal, or the absence assertion no longer holds). Record the failing assertion
       and the exact terminal output in the SUMMARY — not "verified".
    3. Revert the fixture EXACTLY to genuinely omitting `box1eCostOrOtherBasis` (diff against the
       committed Task 1 state, do not retype from memory). Re-run
       `node --test 2>&1 | grep -c '^✔ import("./fjs/'` and confirm the count returns to (at least)
       the post-Task-1 count.
    4. Run `git status --porcelain` and confirm it is empty.
    5. Record every command's actual output in `12-02-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - Mutation Gate M3 was actually run: the SUMMARY records which assertion failed while the
      `withoutBasis` fixture carried an explicit `'0.00'`, and the final `git status --porcelain`
      clean confirmation after reverting.
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` after this task, with the fixture reverted,
      is at least equal to the count recorded at the end of Task 1.
    - `grep -n "box1eCostOrOtherBasis" fjs/document/1099b/module.f.js` shows the `withoutBasis`
      fixture genuinely omits the key (no stray `box1eCostOrOtherBasis: '0.00'` survives in that
      fixture literal).
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/' && git status --porcelain</automated>
  </verify>
  <done>Mutation Gate M3's five steps are all recorded in `12-02-SUMMARY.md` with actual command output (not paraphrased); the fixture is reverted exactly; `git status --porcelain` is empty; `npx tsc --noEmit` exits 0; the project-local proof count is at least the count recorded at the end of Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ingestion -> `vnd.fjs.1099b` `validate()` | An OCR/agent-assembled document object (potentially malformed, mis-scaled, wrong-dialect, or carrying a forged provenance claim) crosses into this project's typed store here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | `checkReferences`'s money-box loop, including negative-accepting boxes 8-11 | mitigate | Every present money box is re-validated through `moneyFieldError`; a comma-grouped or non-canonical amount is refused. Proven by generated-leaf-per-field + hand-typed count, and a dedicated negative-acceptance leaf for boxes 8-11. |
| T-12-02-02 | Tampering | `sourceArtifactHash` | mitigate | Validated via `isHash`; a malformed provenance claim is refused. Required, never `option`. |
| T-12-02-03 | Tampering | A blank `box1eCostOrOtherBasis` silently treated as zero DOWNSTREAM (Phase 12.1's future Form 8949 input) | mitigate | `option(string)`, never defaulted in this codebase's production code today. This phase's own consequence proof exists specifically to catch a FUTURE regression of this kind — and Mutation Gate M3 confirms that proof is load-bearing, not decorative. |
| T-12-02-04 | Tampering | `applicableCheckboxOnForm8949` | accept | Stored verbatim as the payer-printed letter, never derived or re-validated against boxes 2/5/12 — a malformed or nonsensical letter here is a downstream (Phase 12.1) concern, not a structural integrity concern for storage. |
| T-12-02-05 | Repudiation | Twelve `option(true)` checkbox fields | accept | `false` is structurally unrepresentable — mirrors DOC-12's existing precedent across every dialect. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after each task.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly increases after Task 1 (baseline 544).
- Mutation Gate M3 watched RED, then reverted GREEN, with `git status --porcelain` empty — recorded
  in the SUMMARY, not merely asserted.
- `npm test` (full suite) green after both tasks land.
</verification>

<success_criteria>
- `fjs/document/1099b/module.f.js` exists, exports a complete six-stage dialect, and every printed
  box is independently `option`-absent-able except the five identity/key fields and the new
  required `sourceArtifactHash`.
- Box 1e and box 12 are modeled independently, never one derived from the other's absence.
- A proof exhibits the actual CONSEQUENCE of conflating "basis absent" with "basis reported as
  zero" — two different naive-gain dollar figures from the identical formula — not merely that the
  field can be omitted.
- Boxes 8-11 provably accept negative values.
- Mutation Gate M3 was run and its outcome recorded: simulating "blank box 1e treated as zero" in
  the proof's own fixture reddens the consequence assertion; reverting returns the suite to green
  with a clean `git status`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-02-SUMMARY.md`
</output>
