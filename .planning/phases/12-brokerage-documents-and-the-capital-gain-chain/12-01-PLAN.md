---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fjs/document/1099div/module.f.js
autonomous: true
requirements: [DOC-06]

must_haves:
  truths:
    - "vnd.fjs.1099div documents round-trip through validate() with every printed box independently option-absent-able, including box 1b"
    - "sourceArtifactHash is REQUIRED (not option) and rejected unless it decodes as a real cBase32 hash (isHash)"
    - "box 1b, once validated, converts through the same centsFromString path QDCGT's QdcgtInput.line2Cents expects — a shape proof, never a call into dispatchLine16 or fjs/return/scope"
    - "box 8 (foreign country/possession) is free text and is never routed through moneyFieldError"
    - "removing an entry from moneyBoxFields while leaving expectedMoneyBoxFieldCount unchanged turns the coverage proof RED (Mutation Gate M1)"
  artifacts:
    - path: "fjs/document/1099div/module.f.js"
      provides: "vnd.fjs.1099div dialect tag, mediaType, oneZeroNineNineDivSchema, checkReferences, validate, proof"
      min_lines: 350
  key_links:
    - from: "fjs/document/1099div/module.f.js's checkReferences"
      to: "fjs/document/money_field/module.f.js's moneyFieldError"
      via: "import + per-box loop over moneyBoxFields and the stateLocal array's stateTaxWithheld field"
      pattern: "moneyFieldError\\("
    - from: "fjs/document/1099div/module.f.js's checkReferences"
      to: "functionalscript/fjs/media/revision/module.f.js's isHash"
      via: "import, applied to sourceArtifactHash"
      pattern: "isHash\\("
    - from: "both new schemas (this plan and 12-02)"
      to: "fjs/document/base/module.f.js's base(dialect)"
      via: "spread first in the schema object literal"
      pattern: "\\.\\.\\.base\\(dialect\\)"
---

<objective>
Add `vnd.fjs.1099div` (DOC-06): the full printed 1099-DIV box list, plus the new
`sourceArtifactHash` provenance field DOC-13 needs. Follows the same four-stage
`fjs/media/revision` template every dialect in this codebase follows
(`fjs/document/1099r/module.f.js` is the closest, most recent analog).

This plan proves DOC-06's stated shape requirement — "a proof demonstrates the stored box 1b
value has exactly the shape the already-shipped QDCGT worksheet consumes" — and stops there.
It does **not** import `dispatchLine16`, does **not** call into `fjs/return/scope` or
`fjs/form1040/core`, and does **not** wire anything: 12-CONTEXT.md's AMENDED resolution
(2026-08-07) keeps `qualifiedDividends`/`ordinaryDividends` refused by the scope guard until
Phase 12.1 reclassifies them AND wires Form 1040 lines 3a/3b in the same commit. Reclassifying
without wiring would report a confident zero for real dividend income instead of refusing —
worse than today's refusal, and the exact failure TAX-16 exists to prevent. This plan proves the
data is *shaped* correctly for that future wiring; it must not attempt the wiring itself.

Purpose: every 1099-DIV a declared brokerage profile produces must be representable, validated,
and provably compatible with the worksheet it will eventually feed — before Phase 12.1 exists to
consume it.
Output: `fjs/document/1099div/module.f.js`, a complete dialect module with its own `proof`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-CONTEXT.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md
@.planning/phases/11-wage-retirement-and-benefit-documents/11-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- base(dialect) / mediaTypeOf(dialect) — fjs/document/base/module.f.js, read in full -->
<!-- export const base = dialect => ({ dialect }) -- spread FIRST in every schema literal -->
<!-- export const mediaTypeOf = dialect => `application/${dialect}+json` -->

<!-- moneyFieldError — fjs/document/money_field/module.f.js -->
<!-- export const moneyFieldError = label => printed => string | undefined -->
<!-- accepts negatives; do NOT call on box 8 (free-text country name, not money) -->

<!-- isHash — functionalscript/fjs/media/revision/module.f.js -->
<!-- export const isHash = (s) => cBase32ToVec(s) !== null -->
<!-- Any lowercase alphanumeric string from '0123456789abcdefghjkmnpqrstvwxyz' (case-insensitive,
     i/l/o normalized) that does not decode to all-zero passes. Verified live this session:
     'deadbeef00112233445566778899aabbccddeeff0011223344556677889900' passes; a string containing
     '-' does NOT (e.g. 'consolidated-artifact-hash-01' fails). Use ONLY alphanumeric literals for
     sourceArtifactHash fixtures in this plan and in 12-02/12-05. -->

<!-- centsFromString — fjs/exact/module.f.js -->
<!-- export const centsFromString = parse(centsScale) -- (printed: string) => bigint -->

<!-- QdcgtInput (type-only reference; DO NOT import any runtime value from this module) —
     fjs/tax/line16/qdcgt/module.f.js, lines ~94-102:
     { status, line1Cents: bigint, line2Cents: bigint, filingScheduleD: boolean,
       scheduleD15Cents: bigint, scheduleD16Cents: bigint, line7aCents: bigint }
     line2Cents is qualified dividends, as exact cents. This plan's shape proof imports ONLY
     the type via `@import { QdcgtInput } from '../../tax/line16/qdcgt/module.f.js'` (a JSDoc
     type import compiles to nothing at runtime) — never `qdcgt` itself, never `dispatchLine16`. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `vnd.fjs.1099div` — the full printed box inventory plus sourceArtifactHash (DOC-06)</name>
  <files>fjs/document/1099div/module.f.js</files>
  <read_first>
    fjs/document/1099r/module.f.js (the closest, most recent four-stage-template analog — copy its
    exact shape: dialect -> mediaType -> schema -> validateShape -> checkReferences -> validate ->
    proof, including its generated-proof + independently-hand-typed-count idiom for money boxes)
    fjs/document/base/module.f.js
    fjs/document/money_field/module.f.js
    node_modules/functionalscript/fjs/media/revision/module.f.js (isHash, read directly — lines
    ~60-63)
    fjs/tax/line16/qdcgt/module.f.js lines ~90-110 (QdcgtInput typedef — READ ONLY, do not import
    any runtime export)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md Q1 (complete
    box inventory table) and Q3 (the sourceArtifactHash provenance mechanism, and why it is
    required-not-option — Assumption A3)
  </read_first>
  <action>
    Create `fjs/document/1099div/module.f.js` following `fjs/document/1099r/module.f.js`'s exact
    six-stage shape. `dialect = 'vnd.fjs.1099div'`.

    Module docstring MUST record the exact source URL and revision:
    `https://www.irs.gov/pub/irs-pdf/f1099div.pdf` (Rev. January 2024 — 12-RESEARCH.md Q1 confirms
    this form was converted to CONTINUOUS USE, i.e. still current for TY2025, no annual-revision
    drift risk unlike 1099-B). State plainly that `sourceArtifactHash` is a NEW field this phase
    introduces (DOC-13) and is required, not `option`, because every real document is extracted from
    some artifact and there is no legacy stored instance to be backward-compatible with (12-RESEARCH
    Assumption A3) — do NOT widen the shared `base()` helper for this (12-RESEARCH's "Alternatives
    Considered": widening `base()` would force the field onto every already-shipped dialect).

    Schema fields, base spread first, in this exact order: `payerTin: string`,
    `recipientTin: string`, `accountNumber: string`, `taxYear: number`, `formRevision: string`,
    `corrected: option(true)`, `sourceArtifactHash: string` (REQUIRED — validated via `isHash` in
    `checkReferences`, never `option`), `box1aTotalOrdinaryDividends: option(string)`,
    `box1bQualifiedDividends: option(string)`, `box2aTotalCapitalGainDistr: option(string)`,
    `box2bUnrecapSec1250Gain: option(string)`, `box2cSection1202Gain: option(string)`,
    `box2dCollectibles28PercentGain: option(string)`,
    `box2eSection897OrdinaryDividends: option(string)`, `box2fSection897CapitalGain: option(string)`,
    `box3NondividendDistributions: option(string)`, `box4FederalIncomeTaxWithheld: option(string)`,
    `box5Section199ADividends: option(string)`, `box6InvestmentExpenses: option(string)`,
    `box7ForeignTaxPaid: option(string)`, `box8ForeignCountryOrUsPossession: option(string)`,
    `box9CashLiquidationDistributions: option(string)`,
    `box10NoncashLiquidationDistributions: option(string)`,
    `box11FatcaFilingRequirement: option(true)`, `box12ExemptInterestDividends: option(string)`,
    `box13SpecifiedPrivateActivityBondInterestDividends: option(string)`,
    `stateLocal: option(array(stateEntry))`, `payerName: option(string)`,
    `recipientName: option(string)`. The printed "2nd TIN not." checkbox and VOID are filer-only and
    NOT modeled — same precedent as `1099r`/every existing dialect.

    `stateEntry` (module-scope const): `state: string`, `stateIdNumber: option(string)`,
    `stateTaxWithheld: option(string)` — boxes 14-16. 1099-DIV has NO locality boxes (unlike
    `1099r`'s fuller `stateLocalEntry`) — document this explicitly, it is a real difference between
    the two printed forms, not an omission.

    `moneyBoxFields` (scalar, exactly 17, in this order): `box1aTotalOrdinaryDividends`,
    `box1bQualifiedDividends`, `box2aTotalCapitalGainDistr`, `box2bUnrecapSec1250Gain`,
    `box2cSection1202Gain`, `box2dCollectibles28PercentGain`, `box2eSection897OrdinaryDividends`,
    `box2fSection897CapitalGain`, `box3NondividendDistributions`, `box4FederalIncomeTaxWithheld`,
    `box5Section199ADividends`, `box6InvestmentExpenses`, `box7ForeignTaxPaid`,
    `box9CashLiquidationDistributions`, `box10NoncashLiquidationDistributions`,
    `box12ExemptInterestDividends`, `box13SpecifiedPrivateActivityBondInterestDividends`.
    `expectedMoneyBoxFieldCount = 17`, hand-typed, same "deliberately NOT `.length`" docstring
    `1099r`/`w2` use. `box8ForeignCountryOrUsPossession` is a country NAME (free text) — never run
    through `moneyFieldError`; prove this exemption the same way `1099r` proves its percentage-box
    exemption (a leaf supplying an arbitrary non-numeric string to box 8 and asserting `ok`).

    `stateLocalMoneyFields` (exactly 1): `stateTaxWithheld`. `expectedStateLocalMoneyFieldCount = 1`,
    hand-typed, same idiom (a one-element generated+hand-typed pair is still the correct shape —
    do not skip the hand-typed count because the list is short).

    `checkReferences`, in this order: (1) `formRevision.trim() === ''` rejected, same message shape
    as every existing dialect; (2) `sourceArtifactHash` checked via `isHash` — a failing check
    returns `error('sourceArtifactHash is not a valid hash: ' + r.sourceArtifactHash)`; (3) loop
    `moneyBoxFields` through `moneyFieldError`; (4) loop `r.stateLocal ?? []`, and within each entry
    loop `stateLocalMoneyFields` through `moneyFieldError` labeled `entry.state + ' ' + field`.

    Proof suite (`export const proof = {...}`), mirroring `1099r`'s proof structure exactly:
    `dialectAndMediaType`; `validate.fullyPopulatedValidates` (every box populated, two `stateLocal`
    entries including two for the SAME state); `validate.blankBoxesOmittedValidates`;
    `validate.falseFlagsRejected` (`corrected`, `box11FatcaFilingRequirement`);
    `validate.wrongDialectRejected` (`path[0] === 'dialect'`);
    `checkReferences.emptyFormRevisionRejected`; `checkReferences.sourceArtifactHashRejectedWhenNotAHash`
    (a value containing `-`, e.g. `'not-a-hash'`, refused); `checkReferences.sourceArtifactHashRequired`
    (omitting the field entirely fails STRUCTURAL validation, not `checkReferences` — assert the
    error is a `ValidationError` naming `sourceArtifactHash`, not a semantic string); `checkReferences.moneyBoxExactness`
    (generated leaf per `moneyBoxFields` entry + `everyMoneyBoxIsCovered` against
    `expectedMoneyBoxFieldCount`); `checkReferences.stateLocalMoneyExactness` (same idiom against
    `expectedStateLocalMoneyFieldCount`); `checkReferences.box8AcceptsNonNumericTextWithoutExactnessCheck`
    (proves the country-name exemption, mirroring `1099r`'s percentage-box exemption leaf).

    DOC-06 shape proof (`proof.qdcgtInputShape`): validate an instance with
    `box1bQualifiedDividends: '500.00'`. Bind `const line2Cents = centsFromString(v.box1bQualifiedDividends ?? '0.00')`
    and annotate it `/** @type {QdcgtInput['line2Cents']} */` — a `tsc`-enforced compile-time
    assertion that the converted value's TYPE is exactly what `QdcgtInput.line2Cents` expects, with
    zero runtime import of `qdcgt`/`dispatchLine16`/`fjs/return/scope`. Then `assertEq(line2Cents, 50000n)`
    at runtime. This is the whole of DOC-06's "exactly the shape" requirement — do not add a second
    proof that calls into `dispatchLine16` (12-RESEARCH.md Pitfall 1 — that would prove Phase 10's
    worksheet arithmetic again, not this phase's new claim, and would be Phase 12.1's job, not this
    plan's).

    For the fixture's `sourceArtifactHash`, use the literal
    `'deadbeef00112233445566778899aabbccddeeff0011223344556677889900'` throughout this file's
    `minimal` fixture — the SAME literal 12-02 and 12-05's DOC-13 proof reuse, so a shared-provenance
    comparison across dialects is a plain `===` on identical literals, not a coincidence to explain.
  </action>
  <acceptance_criteria>
    - `fjs/document/1099div/module.f.js` exports `dialect === 'vnd.fjs.1099div'`,
      `mediaType === 'application/vnd.fjs.1099div+json'`, `oneZeroNineNineDivSchema`,
      `checkReferences`, `validate`, `proof`.
    - `grep -c "moneyFieldError(" fjs/document/1099div/module.f.js` is greater than 0, and
      `box8ForeignCountryOrUsPossession` never appears on the same line as `moneyFieldError`.
    - `grep -n "expectedMoneyBoxFieldCount = 17" fjs/document/1099div/module.f.js` and
      `grep -n "expectedStateLocalMoneyFieldCount = 1" fjs/document/1099div/module.f.js` both match.
    - `grep -n "isHash(" fjs/document/1099div/module.f.js` matches, and `sourceArtifactHash` is
      declared as plain `string` (not `option(string)`) in the schema.
    - `grep -n "irs-pdf/f1099div.pdf" fjs/document/1099div/module.f.js` matches — the docstring names
      the sourced URL.
    - `grep -n "QdcgtInput" fjs/document/1099div/module.f.js` matches, confirming the type-only
      shape assertion exists; `grep -n "dispatchLine16\|fjs/return/scope\|fjs/form1040/core" fjs/document/1099div/module.f.js`
      returns NOTHING — no wiring, no runtime import of any of these three.
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; the project-local proof count (baseline 544, measured immediately before this task) increases; every acceptance criterion above is confirmed by the grep commands shown, not by inspection alone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mutation Gate M1 — prove the hand-typed money-box count is load-bearing (BLOCKING)</name>
  <files>fjs/document/1099div/module.f.js</files>
  <read_first>
    fjs/document/1099div/module.f.js (as written by Task 1 — the `moneyBoxFields` array, the
    `expectedMoneyBoxFieldCount` constant, and the `checkReferences.moneyBoxExactness` proof group's
    `everyMoneyBoxIsCovered` leaf)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-VALIDATION.md "Mutation
    Gate" section, row M1 (exact expected outcome: schema-generated coverage grows automatically
    when a box is ADDED, so only the independent hand-typed count can catch a box REMOVED)
    .planning/phases/11-wage-retirement-and-benefit-documents/11-01-PLAN.md Task 3 (the numbered,
    recorded-output format this gate mirrors verbatim)
    AGENTS.md "A proof is not known to work until you have watched it fail" section
  </read_first>
  <action>
    **Mutation Gate M1 (BLOCKING — run this now, recorded, before considering this plan done):**
    1. Temporarily remove the entry `box6InvestmentExpenses` from the `moneyBoxFields` array in
       `fjs/document/1099div/module.f.js`. Leave `expectedMoneyBoxFieldCount = 17` UNCHANGED — that
       hand-typed constant is exactly what this gate proves is load-bearing.
    2. Run `node --test 2>&1 | grep -c '^✔ import("./fjs/'` and confirm the count DROPS relative to
       the post-Task-1 count, and specifically that `checkReferences.moneyBoxExactness`'s
       `everyMoneyBoxIsCovered` leaf (or the plan's actual leaf name, if it differs after Task 1's
       implementation) fails. Record the failing leaf name and the actual command output in the
       SUMMARY — not "verified".
    3. Restore the removed entry EXACTLY (re-add `box6InvestmentExpenses` at its original array
       position — do not retype the file from memory; diff against the committed Task 1 state).
       Re-run `node --test 2>&1 | grep -c '^✔ import("./fjs/'` and confirm the count returns to (at
       least) the post-Task-1 count.
    4. Run `git status --porcelain` and confirm it is empty — no stray leftover edits from the
       remove/restore cycle.
    5. Record every command's actual output in `12-01-SUMMARY.md` — the real terminal output, not a
       paraphrase.
  </action>
  <acceptance_criteria>
    - Mutation Gate M1 was actually run: the SUMMARY records the RED leaf name obtained while
      `box6InvestmentExpenses` was removed from `moneyBoxFields`, and the final
      `git status --porcelain` clean confirmation after restoring it.
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` after this task, with the field restored, is
      at least equal to the count recorded at the end of Task 1.
    - `grep -n "expectedMoneyBoxFieldCount = 17" fjs/document/1099div/module.f.js` still matches
      (the hand-typed constant itself was never touched by this gate, only the array it checks
      against).
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/' && git status --porcelain</automated>
  </verify>
  <done>Mutation Gate M1's five steps are all recorded in `12-01-SUMMARY.md` with actual command output (not paraphrased); the removed field is restored exactly; `git status --porcelain` is empty; `npx tsc --noEmit` exits 0; the project-local proof count is at least the count recorded at the end of Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ingestion -> `vnd.fjs.1099div` `validate()` | An OCR/agent-assembled document object (potentially malformed, mis-scaled, wrong-dialect, or carrying a forged provenance claim) crosses into this project's typed store here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | `checkReferences`'s money-box loop | mitigate | Every present money box is re-validated through `moneyFieldError` (exact decimal, safe magnitude); a comma-grouped or non-canonical amount is refused, never silently coerced. Proven by the generated-leaf-per-field + independently hand-typed count idiom, and the hand-typed count's load-bearing role is confirmed by a watched-red mutation (Mutation Gate M1). |
| T-12-01-02 | Tampering | `sourceArtifactHash` | mitigate | Validated via `isHash` in `checkReferences`; a non-cBase32 string (e.g. a spoofed or malformed provenance claim) is refused, never accepted as a shared-artifact link. Required (not `option`) so a document cannot silently omit provenance entirely. |
| T-12-01-03 | Tampering | `box8ForeignCountryOrUsPossession` | mitigate | Explicitly EXCLUDED from `moneyFieldError` (it is a country name, not money) — the exclusion is itself proven by a dedicated leaf, so the gap is a documented, tested design choice, not an unnoticed hole. |
| T-12-01-04 | Repudiation | `corrected`/`box11FatcaFilingRequirement`: `option(true)` | accept | `false` is structurally unrepresentable (rejected by the RTTI type itself) — mirrors DOC-12's existing precedent; no new risk introduced. |
| T-12-01-05 | Spoofing | `sourceArtifactHash` shared across two unrelated documents | accept | This phase validates hash FORMAT, not cryptographic proof that two documents were genuinely co-derived from one artifact — that verification belongs to the ingestion pipeline (Phase 16, "The Orphan Ingestion Island"), which is explicitly out of scope here per DOC-13's "modeling and subject derivation only" boundary. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after each task.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly increases after Task 1 (baseline 544 before this plan).
- Mutation Gate M1 watched RED, then restored GREEN, with `git status --porcelain` empty — recorded in the SUMMARY, not merely asserted.
- `npm test` (full suite) green after both tasks land.
</verification>

<success_criteria>
- `fjs/document/1099div/module.f.js` exists, exports a complete six-stage dialect (dialect,
  mediaType, schema, validate, checkReferences, proof), and every printed box is independently
  `option`-absent-able except the five identity/key fields and the new required `sourceArtifactHash`.
- Every money box is proven exact-decimal via the generated + hand-typed-count idiom; box 8 is
  proven exempt from that check.
- `sourceArtifactHash` is required, validated via `isHash`, and rejects malformed values.
- A compile-time and runtime proof demonstrates box 1b's converted value has exactly the type and
  numeric shape `QdcgtInput.line2Cents` expects — with zero runtime coupling to `fjs/tax/`,
  `fjs/return/`, or `fjs/form1040/`.
- Mutation Gate M1 was run and its outcome recorded: removing a box from `moneyBoxFields` reddens
  the hand-typed count's coverage assertion; restoring it returns the suite to green with a clean
  `git status`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-01-SUMMARY.md`
</output>
