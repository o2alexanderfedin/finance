---
phase: 12
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-03"]
files_modified:
  - fjs/schedule/b/module.f.js
autonomous: true
requirements: [TAX-07]

must_haves:
  truths:
    - "Schedule B applies the $1,500 threshold SEPARATELY to interest and to dividends — two independent tests, never a combined sum"
    - "Schedule B's foreign-account answers are read verbatim from vnd.fjs.return_profile, never inferred from any stored 1099"
    - "Schedule B reads stored vnd.fjs.1099int and vnd.fjs.1099div documents to sum interest and ordinary dividends"
    - "a taxpayer with $1,000 interest and $1,000 dividends (combined $2,000, neither individually over $1,500) does NOT trigger the threshold half of Part III"
  artifacts:
    - path: "fjs/schedule/b/module.f.js"
      provides: "scheduleB pure function, ReportLine-shaped Part I/II totals, threshold booleans, Part III echo, proof"
      min_lines: 200
  key_links:
    - from: "fjs/schedule/b/module.f.js"
      to: "fjs/document/1099int/module.f.js's box1InterestIncome/box3UsSavingsBondsAndTreasuryInterest"
      via: "sum over Stored<OneZeroNineNineInt>[]"
      pattern: "box1InterestIncome|box3UsSavingsBondsAndTreasuryInterest"
    - from: "fjs/schedule/b/module.f.js"
      to: "fjs/document/1099div/module.f.js's box1aTotalOrdinaryDividends"
      via: "sum over Stored<OneZeroNineNineDiv>[]"
      pattern: "box1aTotalOrdinaryDividends"
    - from: "fjs/schedule/b/module.f.js"
      to: "fjs/return/profile/module.f.js's four foreign-account fields"
      via: "verbatim read of profile.value, never a document lookup"
      pattern: "hadForeignFinancialAccount|requiredToFileFinCen114|foreignAccountCountries|receivedForeignTrustDistributionOrWasGrantorOrTransferor"
---

<objective>
Build Schedule B (TAX-07): Part I (interest total), Part II (ordinary dividends total), and Part III
(the $1,500 threshold test — applied SEPARATELY to each — plus the foreign-account/trust
questions), reading stored `vnd.fjs.1099int` and `vnd.fjs.1099div` documents and the return
profile's declared foreign-account facts. New root `fjs/schedule/b/` (12-RESEARCH.md's own
recommendation: a fresh root sidesteps the tension between "computes Schedule B" and CONTEXT's
"touches `fjs/tax/` only to verify the existing QDCGT path").

This module computes real totals from real stored documents, but it is NOT wired into
`fjs/form1040/core`'s `form1040IncomeLines`/`form1040TaxAndPaymentLines`, and it does not consult
`fjs/return/scope`'s `classifyScope` — it is a standalone, independently callable schedule, exactly
the same relationship `fjs/tax/line16/qdcgt` had to `vnd.fjs.1099div` before this phase existed.
Wiring Schedule B's own totals into Form 1040 lines 2b/3b (which today read directly from documents,
bypassing Schedule B entirely) is not named by any of this phase's four success criteria and is left
alone.

Purpose: a declared brokerage-and-interest profile needs its Schedule B threshold and foreign-account
questions answered from real stored data, not left unbuilt because the capital-gain chain is not
finished yet.
Output: `fjs/schedule/b/module.f.js`, a new pure module with its own `proof`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-CONTEXT.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md
@AGENTS.md
fjs/document/1099int/module.f.js
fjs/document/1099div/module.f.js
fjs/return/profile/module.f.js
fjs/report/line/module.f.js

<interfaces>
<!-- ReportLine / Source — fjs/report/line/module.f.js, read in full:
     ReportLine = { readonly value: bigint, readonly sources: readonly [Source, ...Source[]], readonly rule: string }
     Source = { readonly documentHash: string, readonly boxPath: string, readonly value: string }
     `sources` is a NON-EMPTY tuple enforced at tsc level — a legitimately-zero line still cites
     something (fjs/form1040/core's own `profileDeclaredZeroLine` convention, reused below). -->

<!-- fjs/form1040/core/module.f.js's box-sum idiom (PRIVATE consts, NOT exported — reimplement the
     SAME idiom locally in this new file; do not invent a different one):
     sumBoxOverDocuments = documents => boxPath => read => { sources: one Source per PRESENT box,
       value: exact cents sum } -- an absent box contributes nothing and cites nothing (DOC-11).
     documentLine = profile => rule => sum => sum.sources is empty
       ? profileDeclaredZeroLine(profile)(rule) : { value: sum.value, sources: sum.sources, rule }
     profileDeclaredZeroLine = profile => rule => ({ value: 0n, sources: [{ documentHash:
       profile.documentHash, boxPath: 'declaredKinds', value: JSON.stringify(profile.value.declaredKinds) }], rule })
     addBoxSums = a => b => ({ value: a.value + b.value, sources: [...a.sources, ...b.sources] })
     These four are reimplemented locally in fjs/schedule/b/module.f.js because form1040/core does
     not export them — the SAME pattern this codebase already accepts when finance_documents_list
     reimplements idioms it cannot import (11-PATTERNS.md). -->

<!-- vnd.fjs.1099int fields this module reads (fjs/document/1099int/module.f.js): box1InterestIncome:
     option(string), box3UsSavingsBondsAndTreasuryInterest: option(string) — Form 1040 line 2b sums
     BOTH; Schedule B line 2 sums the same two boxes, per the printed form's own instructions. -->

<!-- vnd.fjs.1099div field this module reads (fjs/document/1099div/module.f.js, Plan 12-01):
     box1aTotalOrdinaryDividends: option(string) -->

<!-- vnd.fjs.return_profile fields this module reads verbatim (fjs/return/profile/module.f.js,
     Plan 12-03): hadForeignFinancialAccount: option(true), requiredToFileFinCen114: option(true),
     foreignAccountCountries: option(array(string)),
     receivedForeignTrustDistributionOrWasGrantorOrTransferor: option(true) -->

<!-- centsFromString — fjs/exact/module.f.js -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schedule B — Part I/II totals, the two independent $1,500 tests, and the Part III echo (TAX-07)</name>
  <files>fjs/schedule/b/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js lines ~154-251 (`sumBoxOverDocuments`, `addBoxSums`,
    `documentLine`, `profileDeclaredZeroLine`, `totalLine` — the exact idiom to reimplement locally;
    also lines ~374-384 showing how line 2b already sums 1099-INT boxes 1+3, the precedent Schedule
    B line 2 mirrors)
    fjs/report/line/module.f.js (ReportLine, Source — the type this module's Part I/II lines return)
    fjs/document/1099int/module.f.js (OneZeroNineNineInt type — box1InterestIncome,
    box3UsSavingsBondsAndTreasuryInterest)
    fjs/document/1099div/module.f.js (OneZeroNineNineDiv type — box1aTotalOrdinaryDividends; this
    plan depends on 12-01 for this file to exist)
    fjs/return/profile/module.f.js (ReturnProfile type — the four foreign-account fields; this plan
    depends on 12-03 for these fields to exist)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md Q4 in full
    (the exact printed line structure, the two separate Notes that establish the SEPARATE threshold
    tests — this is the section naming the "common error" to avoid)
  </read_first>
  <behavior>
    - `scheduleB` given two 1099-INT documents summing to $2,000.00 interest and zero 1099-DIV
      documents produces `interestOverThreshold: true` and `dividendsOverThreshold: false`.
    - `scheduleB` given zero 1099-INT documents and one 1099-DIV document with
      `box1aTotalOrdinaryDividends: '2000.00'` produces `interestOverThreshold: false` and
      `dividendsOverThreshold: true`.
    - `scheduleB` given ONE 1099-INT document with `box1InterestIncome: '1000.00'` and ONE 1099-DIV
      document with `box1aTotalOrdinaryDividends: '1000.00'` (combined $2,000, each individually
      under $1,500) produces BOTH `interestOverThreshold: false` AND `dividendsOverThreshold: false`
      — the two tests never combine into one $1,500 comparison.
    - `line4.value` equals exactly `$1,500.00` (`150000n`) produces `interestOverThreshold: false` —
      the form says "over $1,500", not "at least $1,500" (a boundary proof at the cent below, at,
      and above the threshold).
    - `partThreeRequired` is `true` when EITHER threshold is exceeded, OR
      `hadForeignFinancialAccount === true`, OR
      `receivedForeignTrustDistributionOrWasGrantorOrTransferor === true` — and `false` when none of
      the four hold.
    - The foreign-account fields in the returned result are read VERBATIM from
      `profile.value.hadForeignFinancialAccount` etc. — a proof asserts that a profile declaring
      `hadForeignFinancialAccount: true` with ZERO stored 1099-INT/1099-DIV documents still reports
      `hadForeignFinancialAccount: true` (never derived from document presence/absence).
    - Zero stored interest AND zero stored dividend documents still produce valid `ReportLine`s for
      `line2`/`line4`/`line6`, each citing the profile's `declaredKinds` box (mirroring
      `profileDeclaredZeroLine`) rather than failing to construct a `ReportLine` at all.
  </behavior>
  <action>
    Create `fjs/schedule/b/module.f.js`. Module docstring MUST record the exact source URL and
    revision: `https://www.irs.gov/pub/irs-pdf/f1040sb.pdf` (2025 revision, "Created 4/23/25"), and
    state explicitly that Line 3 (Form 8815 excludable EE/I savings bond interest) is NOT modeled
    this phase — Form 8815 does not exist in this codebase — so Line 4 equals Line 2 exactly; record
    this as a documented, deliberate scope boundary (not a silent gap) alongside the equally
    deliberate boundary that Lines 1 and 5 (the per-payer name+amount listing) are not separately
    materialized as structured output — every contributing document is already cited via each
    `ReportLine`'s `sources` tuple (documentHash + boxPath + value per document), which is a
    STRONGER provenance record than a bare payer-name list, and TAX-07's stated success criterion is
    the threshold and foreign-account questions, not a payer-grouped listing.

    Locally reimplement (private, module-scope consts — NOT exported, and NOT imported from
    `fjs/form1040/core` since that module does not export them) the four helpers named in this
    plan's `<interfaces>` block: `sumBoxOverDocuments`, `addBoxSums`, `documentLine`,
    `profileDeclaredZeroLine`. Define a local `Stored<T>` typedef identical in shape to
    `fjs/form1040/core`'s own (`{ readonly documentHash: string, readonly value: T }`) — duplicated
    because it is a type, not a rule, and `fjs/form1040/core` does not export it either.

    Define `export const scheduleBThresholdCents = 150000n` ($1,500.00), with a docstring citing
    both printed Notes ("If line 4 is over $1,500, you must complete Part III" / "If line 6 is over
    $1,500, you must complete Part III") and stating explicitly that the comparison is `>`, never
    `>=` — "over $1,500" is strict.

    Define the exported entry point:
    `export const scheduleB = inputs => { ... }` where `inputs` is
    `{ readonly interestForms: readonly Stored<OneZeroNineNineInt>[], readonly dividendForms:
    readonly Stored<OneZeroNineNineDiv>[], readonly profile: Stored<ReturnProfile> }`. Compute:
    - `line2`: `documentLine(profile)('Schedule B line 2')` of `addBoxSums` over
      `sumBoxOverDocuments(interestForms)('box1InterestIncome')(...)` and
      `sumBoxOverDocuments(interestForms)('box3UsSavingsBondsAndTreasuryInterest')(...)` — the SAME
      two-box sum `fjs/form1040/core`'s line 2b already uses, applied here under Schedule B's own
      printed line number.
    - `line4`: identical VALUE and SOURCES to `line2`, `rule: 'Schedule B line 4'` (Form 8815
      exclusion unmodeled, per the docstring above — do not silently drop this line, construct it
      explicitly equal to line 2 and say why in the docstring).
    - `line6`: `documentLine(profile)('Schedule B line 6')` of
      `sumBoxOverDocuments(dividendForms)('box1aTotalOrdinaryDividends')(...)`.
    - `interestOverThreshold: line4.value > scheduleBThresholdCents`.
    - `dividendsOverThreshold: line6.value > scheduleBThresholdCents`.
    - `hadForeignFinancialAccount: profile.value.hadForeignFinancialAccount === true`,
      `requiredToFileFinCen114: profile.value.requiredToFileFinCen114 === true`,
      `foreignAccountCountries: profile.value.foreignAccountCountries` (pass through, possibly
      `undefined`), `receivedForeignTrustDistributionOrWasGrantorOrTransferor:
      profile.value.receivedForeignTrustDistributionOrWasGrantorOrTransferor === true` — read
      VERBATIM, no document is consulted for any of these four.
    - `partThreeRequired: interestOverThreshold || dividendsOverThreshold ||
      hadForeignFinancialAccount || receivedForeignTrustDistributionOrWasGrantorOrTransferor`.

    Return the record `{ line2, line4, line6, interestOverThreshold, dividendsOverThreshold,
    hadForeignFinancialAccount, requiredToFileFinCen114, foreignAccountCountries,
    receivedForeignTrustDistributionOrWasGrantorOrTransferor, partThreeRequired }`.

    Proof suite covering every `<behavior>` bullet above, plus: `dialectIndependence` — this module
    imports NO `dialect` constant of its own (it is a computed schedule, not a stored document) and
    is NOT registered in `finance_schema`'s `dialectSchemas` (a `grep`-checkable negative);
    `thresholdBoundary` — three leaves at `$1,499.99`/`$1,500.00`/`$1,500.01` for EACH of interest
    and dividends independently (six leaves total, or a generated set over both parts), asserting
    the boundary is exclusive.
  </action>
  <acceptance_criteria>
    - `fjs/schedule/b/module.f.js` exports `scheduleBThresholdCents === 150000n` and `scheduleB`.
    - `grep -n "irs-pdf/f1040sb.pdf" fjs/schedule/b/module.f.js` matches.
    - `grep -n "line4.value > scheduleBThresholdCents\|interestOverThreshold" fjs/schedule/b/module.f.js`
      and `grep -n "line6.value > scheduleBThresholdCents\|dividendsOverThreshold" fjs/schedule/b/module.f.js`
      both match, on SEPARATE comparisons (not a single combined `line4.value + line6.value` sum
      anywhere in the file — confirm with `grep -n "line4.value + line6.value\|line6.value + line4.value" fjs/schedule/b/module.f.js`
      returning NOTHING).
    - `grep -n "hadForeignFinancialAccount\|requiredToFileFinCen114\|foreignAccountCountries\|receivedForeignTrustDistributionOrWasGrantorOrTransferor" fjs/schedule/b/module.f.js`
      matches, and `grep -n "box1InterestIncome\|box1aTotalOrdinaryDividends" fjs/schedule/b/module.f.js`
      never appears on the same line as any of the four foreign-account field names (the two data
      sources are never conflated).
    - `grep -n "classifyScope\|dispatchLine16\|form1040IncomeLines\|form1040TaxAndPaymentLines" fjs/schedule/b/module.f.js`
      returns NOTHING — this module is standalone, not wired into Form 1040.
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; the project-local proof count (baseline 544) increases; every acceptance criterion above is confirmed by the grep commands shown; the six-leaf (or generated-equivalent) threshold-boundary proof demonstrates strict `>` on both Part I and Part II independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored, already-validated documents -> Schedule B computation | This module reads ONLY `Stored<T>` values whose `.value` already passed its own dialect's `validate` — it never re-validates or accepts raw untrusted JSON directly (mirrors `fjs/form1040/core`'s own "nothing here re-validates" convention) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering | A caller passes an object shaped like `Stored<OneZeroNineNineInt>` whose `.value` never actually passed `vnd.fjs.1099int`'s `validate` | accept | Same accepted architecture as `fjs/form1040/core`'s `Form1040Inputs` — the `Stored<T>` type documents the invariant it relies on rather than re-enforcing it at a second layer (AGENTS.md: "one rule, one place"). Enforcement lives at the ingestion boundary, not here. |
| T-12-04-02 | Tampering / Information Disclosure | Combining the $1,500 interest and dividend thresholds into one test, silently changing when Part III is legally required | mitigate | Two structurally independent boolean comparisons (`interestOverThreshold`, `dividendsOverThreshold`), each proven at its own boundary; a generated negative-case proof confirms a combined-but-individually-under total does NOT trigger either flag. |
| T-12-04-03 | Spoofing | Foreign-account answers inferred from a document instead of read from the declared profile | mitigate | `hadForeignFinancialAccount`/etc. are read EXCLUSIVELY from `profile.value` — proven by a leaf constructing a profile that declares a foreign account while ZERO 1099-INT/1099-DIV documents are stored, and asserting the declared answer still surfaces unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly increases (baseline 544).
- `npm test` (full suite) green after this plan lands.
</verification>

<success_criteria>
- `fjs/schedule/b/module.f.js` exists, exports `scheduleB` and `scheduleBThresholdCents`, and
  computes Part I/II totals plus the Part III echo from real stored documents and the declared
  profile.
- The $1,500 threshold is proven as two independent, strict (`>`) comparisons — never a combined sum.
- Foreign-account answers are proven to come exclusively from the profile, never from any document.
- The module is not wired into `fjs/form1040/core` or `fjs/return/scope` — standalone and callable
  on its own.
</success_criteria>

<output>
After completion, create `.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-04-SUMMARY.md`
</output>
