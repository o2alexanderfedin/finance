---
context: phase
phase: 12-brokerage-documents-and-the-capital-gain-chain
task: null
total_tasks: null
status: complete_and_merged
last_updated: 2026-08-08T10:33:22.917Z
---

# Continue Here — paused 2026-08-08 03:33 PDT

**Branch:** `main` @ `674ebb4` — CI green, 0 open PRs, `develop` identical
**State:** **Nothing is in progress.** Phases 11 and 12 both shipped and merged this session.
**Blocked on:** nothing. Two items wait on a person (below).

Measured at pause, not quoted: `tsc` clean · **629 project-local proofs** · working tree clean.

---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| **Reclassify-without-wiring** | Marking `qualifiedDividends`/`ordinaryDividends` as *modeled* in `fjs/return/scope` without simultaneously wiring Form 1040 lines 3a/3b makes the engine report a confident **zero** for dividend income where it currently refuses honestly. Strictly worse than the refusal it replaces, and the exact failure TAX-16 exists to prevent. | **blocking** | Phase 12.1's ROADMAP criterion 2a requires both edits in ONE atomic change. Any plan that touches `fjs/return/scope`'s partition without also touching `fjs/form1040/core`'s lines 3a/3b and its hardcoded `qualifiedDividendsCents: 0n` is wrong and must be rejected at plan-check. |
| **Planning text that contradicts shipped code** | Happened TWICE this session, both caught by research reading source rather than trusting documents. Phase 11's CONTEXT asserted archived revisions were invisible to report programs "by construction" — they were not. Phase 12's roadmap scheduled building a QDCGT worksheet that already existed (634 lines, Phase 10), and its criterion 1 required an edit the same document forbade. | **blocking** | Every phase marked `Research: YES` must have its research read the actual modules its criteria name, and report contradictions as findings rather than planning around them. Both were found before any code was written; neither would have been caught by executing the plan as written. |
| **A mutation gate that tests the fixture, not the consequence** | Phase 12's M3 mutated the 1099-B *fixture* and tripped an `assertEq(fixture.box1e, undefined)` assertion **before** the gain comparison it appeared to guard ever ran. The proof is honest, but proof-local — no production change could have reddened it, because production gain computation does not exist until 12.1. | advisory | When a mutation reddens, check WHICH leaf and WHICH assertion. Recorded as ROADMAP criterion 3a on Phase 12.1: DOC-07's consequence must be re-proven against real gain computation. |
| **Agent worktrees cannot build this repo** | `node_modules/` is gitignored and every `fjs/**` module imports `functionalscript/...` as a bare specifier, so a fresh `git worktree` has no dependency tree and cannot run `tsc` or a single proof. | advisory | Already structural: `workflow.use_worktrees=false` in `.planning/config.json`, committed as `f941014`. Executors run sequentially on the main tree. Do not re-enable. |
| **Hand-maintained counts in prose rot** | `ROADMAP.md`'s header said "15 phases / 79 requirements" while sitting directly beside its own "Recompute rather than trust prose" note. Stale twice: once for TEST-01..04 + MAINT, once for the Phase 12 split. | advisory | Fixed in PR #55; the note now distrusts itself and carries both recompute commands inline. Contrast with `expectedKnownDialectCount`-style guards, which are *deliberately* independent second sources **because a proof can watch them drift**. Prose has no proof. |

**Note the asymmetry in the last row** — it is the session's most transferable idea. A second source
of truth is good when something can watch it drift, and bad when nothing can.

<current_state>
Between phases, cleanly. Phase 12 merged as PR #54; a follow-up docs correction merged as PR #55.
`main` = `develop` = `origin` = `674ebb4`. No branch, no worktree, no file is mid-edit.

Phase 12.1 has **no directory, no CONTEXT, no research, no plans** — it exists only as ROADMAP text.
</current_state>

<completed_work>

## This session

**Phase 11 — Wage, Retirement, and Benefit Documents** (merged, PR #53, `e3f405e`) — 492 → 544 proofs
- `vnd.fjs.1099r`, `vnd.fjs.ssa1099`, `finance_documents_list`, and the DOC-15 retraction story.
- **Found and closed a real hole in shipped Phase 7 code**: `buildRunSnapshot` resolved archived
  subjects' heads *and* every blob from `cas.list()`, so a guest could walk
  `evoList('true') → evoHead → evoRevision → casRead` into a retracted document. The CONTEXT's claim
  that this was impossible "by construction" was false when the phase began.
- Seven mutation gates, every one watched failing. Code review found the first fix conflated
  *archived* with *failed to decode* — which would have silently hidden an **active** document.

**Phase 12 — Brokerage Documents** (merged, PR #54, `5cdf30d`) — 544 → 629 proofs
- `vnd.fjs.1099div`, `vnd.fjs.1099b`, DOC-13 consolidated provenance, Schedule B.
- Split from the original Phase 12 with the phase owner's approval; TAX-08 closed as already
  delivered in Phase 10 rather than rebuilt.
- The phase boundary held, measured not asserted: `git diff` across the whole phase on
  `fjs/return/scope`, `fjs/form1040/core` and `fjs/tax/**` is **empty**.

**PR #55** — corrected ROADMAP totals stale since the split (19 phases, 93 requirements).
</completed_work>

<remaining_work>

Per the phase owner's decision on 2026-08-07, the approved autonomous run is:

**12.1 → 13 → 15 → 16 → 17 → 18**, with **14 skipped**.

| Phase | Status |
|---|---|
| **12.1 The Capital-Gain Chain** (TAX-11, TAX-15) | next; nothing started |
| 13 The 65+ Profile and Remaining Schedules | not started |
| 14 Acceptance | **SKIPPED — not autonomous-executable**; needs the taxpayer's real filed return and real documents |
| 15 Realism Polish and Upstream | not started |
| 16 The Orphan Ingestion Island | not started |
| 17 Documentation Truth Pass | not started |
| 18 Dependency and Duplication Debt | not started |
</remaining_work>

<decisions_made>

- **Phase 12 split into 12 (documents) and 12.1 (chain)** — together they exceeded Phase 10, the
  largest phase shipped so far. Splitting means a failure in the chain does not strand the dialects.
- **TAX-08 marked complete, not rebuilt** — `fjs/tax/line16/qdcgt/module.f.js` already exists at 634
  lines, already imports `baseTaxForAmount`, and its proofs already assert `method22 === 'taxTable'`.
- **All dividend wiring deferred to 12.1, atomically** — see the blocking anti-pattern above.
- **One narrow exception granted** to "Phase 12 must not touch `fjs/return/`": additive
  foreign-account fields on `vnd.fjs.return_profile` for Schedule B, because no IRS information
  return reports "do you have a foreign account" — it can only be taxpayer-declared. Verified by an
  empty `git diff` on `fjs/return/scope/module.f.js`.
- **Phase 14 marked NOT AUTONOMOUS-EXECUTABLE in ROADMAP.md itself**, with the reason, so a future
  run cannot silently attempt it.
- **`workflow.use_worktrees=false`** — see anti-patterns.
- **`/gsd-secure-phase` deliberately not run.** No phase in this project has ever produced a
  `SECURITY.md`; Phases 3 and 6 were far more security-critical and shipped without one. Running it
  for Phase 11 alone would be inconsistent. Recorded as a standing project-wide gap, not a phase gap.
</decisions_made>

<blockers>
None technical.
</blockers>

## Human actions pending

1. **Public vs private — needs Sergey.** One decision covering both repo visibility and demo
   hosting, because a demo that runs the real engine in a public page publishes the real engine
   (the browser must download all 37 `fjs/**` modules to execute them). Not blocking any phase.
2. **Three claims never verified against paper** — now formally tracked as verification debt:
   `10-VERIFICATION.md` carries `status: human_needed` with 3 items. The 2025 Form 1040 face's
   56-line inventory, the 19 standard-deduction chart amounts, and research assumption A2 (whether
   the Tax Computation Worksheet is cent-exact; exactly one recorded value moves if wrong —
   QDCGT case (iv) line 24, `$184,094.50` vs `$184,095.00`). **No agent can close these.** Phases 11
   and 12 both deliberately avoided adding a fourth.
3. **`REQUIREMENTS.md` TAX-10 says the Social Security Benefits Worksheet is 19 lines; it is 18.**
   Deliberately left open for Phase 13, which builds that worksheet and can fix it against the
   printed form rather than swapping one recalled number for another. Rationale recorded in
   `CHANGELOG.md`.

## Required Reading (in order)

1. `AGENTS.md` — authoritative for this repo; overrides generic practice. Read it fully.
2. `.planning/ROADMAP.md` §"Phase 12.1: The Capital-Gain Chain" — especially criteria **2a** (the
   atomic wiring) and **3a** (re-prove DOC-07 against real gain computation).
3. `.planning/phases/12-.../12-CONTEXT.md` §"AMENDED 2026-08-07" — why the wiring moved to 12.1.
4. `.planning/phases/12-.../12-RESEARCH.md` — **note its top banner**: the box lists and findings
   stand; its recommendation that the wiring belongs in Phase 12 was overruled.
5. `fjs/return/scope/module.f.js` — read the docstring section *"Why `qualifiedDividends` is NOT
   modeled, even in the phase that ships QDCGT"*. It states the principle 12.1 must honour:
   *"an engine that can compute a worksheet is not thereby an engine that can read the form feeding it."*

## Infrastructure State

- **No background processes.** No server running. The demo (`./demo/serve.sh`) is not running.
- **`node_modules` is current** — `functionalscript@0.43.1`, matching the lockfile, in the main
  checkout. Worktrees do NOT have one (see anti-patterns).
- **Two stale worktrees exist**, both at `6c4bd21` (pre-Phase-11), on old feature branches
  `feature/phase-10-...` and `feature/phase-6-...`. They are harmless but out of date and have their
  own (stale) `node_modules`. Prune them if they get in the way.
- **Sergey's `origin/wtf` and `origin/revert-14-feature/planning-requirements-roadmap`** are the only
  unmerged remote branches. Leave them alone.

## Standing hazards (unchanged, all still live)

- **Never gate on `npm test`'s raw total** — it includes ~2,100 vendored submodule proofs and moves
  with submodule checkout state. Honest metric: `node --test 2>&1 | grep -c '^✔ import("./fjs/'`.
- **`node --test <some .f.js file>` is a documented FAKE PASS.** Only root discovery registers proofs.
- **Never `cd` into `./functionalscript`** — a submodule, and a bare `cd` persists into later
  commands. **Never `git add`** that path; it silently discards a staged gitlink.
- **`main` and `develop` are protected** — the pre-commit hook refuses direct commits. It caught me
  once this session, correctly.
- **Verify commits with `git ls-tree HEAD` AFTER committing**, never `git diff --cached` before.

<context>
The session's through-line, stated plainly because it decided the outcome twice:

**The planning documents were wrong, and only reading code caught it.** Phase 11's CONTEXT asserted a
security property the shipped code did not have. Phase 12's roadmap scheduled building something that
already existed and demanded an edit it simultaneously forbade. Neither would have been caught by
executing the plans faithfully — both plans would have passed their own gates and shipped something
subtly false.

The corollary shaped every dispatch afterward: subagents were told to try to *falsify* the phase's own
premises, and told that finding the premise unimplementable would be the most valuable thing they
could return. That is what produced both findings.

The second theme is that a green mutation gate is not automatically a meaningful one. M1 reddened a
real production assertion. M3 reddened a fixture assertion that sat in front of the thing it appeared
to guard. Both "passed"; only one bought coverage. The distinction is recorded as ROADMAP criterion 3a
rather than left implicit.
</context>

<next_action>
Start with: `/gsd-autonomous --from 12.1`

That resumes the approved run (12.1 → 13 → 15 → 16 → 17 → 18, skipping 14). Phase 12.1 is
`Research: YES` and genuinely unknown in two respects — the Schedule D Tax Worksheet's line structure,
and whether it is cent-exact or whole-dollar (the same open assumption A2 that the Tax Computation
Worksheet carries).

Before any plan for 12.1 is accepted, confirm it treats the scope reclassification and the lines-3a/3b
wiring as ONE atomic change. That is the blocking constraint at the top of this file.
</next_action>
