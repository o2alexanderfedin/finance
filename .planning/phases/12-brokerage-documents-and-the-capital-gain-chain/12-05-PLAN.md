---
phase: 12
plan: 05
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - fjs/document/consolidated_provenance/module.f.js
  - fjs/server/finance_schema/module.f.js
autonomous: true
requirements: [DOC-13]

must_haves:
  truths:
    - "one consolidated artifact's 1099-DIV section and 1099-B section resolve to two DISTINCT Evo form subjects"
    - "those same two documents can carry an identical, recorded sourceArtifactHash — the shared provenance DOC-13 requires"
    - "finance_schema serves both vnd.fjs.1099div and vnd.fjs.1099b's JSON Schema by name"
    - "finance_schema's dialect count is provably exactly 9 — a dialect quietly deregistered turns the count leaf RED (Mutation Gate M2)"
  artifacts:
    - path: "fjs/document/consolidated_provenance/module.f.js"
      provides: "a proof-only module demonstrating DOC-13's N-subjects-one-provenance property across the two new dialects"
      min_lines: 60
    - path: "fjs/server/finance_schema/module.f.js"
      provides: "dialectSchemas registering vnd.fjs.1099div and vnd.fjs.1099b (9 total), expectedKnownDialectCount = 9, two new *Resolves leaves"
      contains: "expectedKnownDialectCount = 9"
  key_links:
    - from: "fjs/document/consolidated_provenance/module.f.js"
      to: "fjs/document/subject/module.f.js's formSubject"
      via: "import, called once per dialect's key fields with formType set to that dialect's own tag"
      pattern: "formSubject\\("
    - from: "fjs/server/finance_schema/module.f.js's dialectSchemas"
      to: "fjs/document/1099div/module.f.js and fjs/document/1099b/module.f.js's schema exports"
      via: "map entries keyed by each dialect's own dialect constant"
      pattern: "oneZeroNineNineDivSchema|oneZeroNineNineBSchema"
---

<objective>
Close out DOC-13 with a proof (not new production machinery — 12-CONTEXT.md: "DOC-13 is modeling
and subject derivation ONLY... no new ingestion wiring") demonstrating that one consolidated
brokerage artifact's two extracted forms yield two distinct Evo subjects while sharing one recorded
`sourceArtifactHash`, and — in the SAME atomic edit 12-RESEARCH.md's Q3/Q5 findings and this
project's own `finance_schema` docstring require — register both new dialects in `finance_schema`
so an agent can read their field names before authoring a program against them (MCP-06's original
purpose, extended to the two dialects this phase ships).

Depends on both 12-01 and 12-02 because it needs both dialects' schema exports to exist.

Purpose: prove DOC-13's N-subjects/one-provenance property with the REAL dialects (not a synthetic
stand-in), and make both new dialects reachable through the same schema-discovery tool every other
dialect already uses — in one atomic three-part edit, per this phase's non-negotiable constraint 1.
Output: `fjs/document/consolidated_provenance/module.f.js` (new, proof-only) and
`fjs/server/finance_schema/module.f.js` (modified, 7 -> 9 dialects).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-CONTEXT.md
@.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md
@.planning/phases/11-wage-retirement-and-benefit-documents/11-PATTERNS.md
@AGENTS.md
fjs/document/subject/module.f.js
fjs/server/finance_schema/module.f.js

<interfaces>
<!-- formSubject — fjs/document/subject/module.f.js, read in full:
     export const formSubject = ({ payerTin, recipientTin, accountNumber, taxYear, formType }) =>
       jsonText([formType, String(taxYear), payerTin, recipientTin, accountNumber])
     Dialect-independent, already proven deterministic and collision-resistant. ZERO changes needed
     to this file for DOC-13 (12-RESEARCH.md Q3) — this plan only imports it, never edits it. -->

<!-- fjs/server/finance_schema/module.f.js's CURRENT state (read in full before editing) —
     dialectSchemas keys 7 dialect tags today: vnd.fjs.1099int, vnd.fjs.ocr, vnd.fjs.w2,
     vnd.fjs.medical_expenses, vnd.fjs.return_profile, vnd.fjs.1099r, vnd.fjs.ssa1099.
     expectedKnownDialectCount = 7. Each dialect has its own `<name>Resolves` proof leaf comparing
     the tool's live output against `toJsonSchema(<schema>)` called directly — never a hand-written
     JSON literal. -->

<!-- The sourceArtifactHash literal both 12-01 and 12-02 use in their own minimal fixtures:
     'deadbeef00112233445566778899aabbccddeeff0011223344556677889900' — reuse the SAME literal here
     so the "shared provenance" assertion is a plain `===` on identical strings, not a coincidence
     to explain. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DOC-13 provenance proof — one artifact, two dialects, two subjects, one shared hash</name>
  <files>fjs/document/consolidated_provenance/module.f.js</files>
  <read_first>
    fjs/document/subject/module.f.js (whole file — formSubject, FormKey, and its own
    `formSubjectIsDeterministic`/`changingOneField.formType` proofs, which this new proof exercises
    with the two REAL dialects instead of a synthetic FormKey)
    fjs/document/1099div/module.f.js (from Plan 12-01 — dialect tag, validate, minimal fixture shape)
    fjs/document/1099b/module.f.js (from Plan 12-02 — dialect tag, validate, minimal fixture shape)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-RESEARCH.md Q3 in full
    (the exact worked example: two subject strings differing only by `formType`, and why Evo's
    `parents` mechanism structurally CANNOT carry this provenance — `validateParentSubjects` rejects
    a cross-subject parent, verified against
    `node_modules/functionalscript/fjs/cas/evo/module.f.js` lines ~226-239)
  </read_first>
  <action>
    Create `fjs/document/consolidated_provenance/module.f.js` — a PROOF-ONLY module: it exports
    `proof` and NOTHING else (no dialect, no schema, no production function). Module docstring MUST
    state this plainly and cite DOC-13 and 12-CONTEXT.md's "modeling and subject derivation ONLY —
    no new ingestion wiring" boundary as the reason no production code lives here: this file exists
    to exercise `formSubject` (already dialect-independent and already proven, per
    `fjs/document/subject/module.f.js`'s own docstring: "later dialect modules depend on this one,
    never the reverse") against the two REAL dialects this phase ships, not to add a new mechanism.

    Import `formSubject` from `../subject/module.f.js`. Import `dialect as oneZeroNineNineDivDialect,
    validate as validateOneZeroNineNineDiv` from `../1099div/module.f.js`. Import
    `dialect as oneZeroNineNineBDialect, validate as validateOneZeroNineNineB` from `../1099b/module.f.js`.

    Build one shared key set representing ONE consolidated brokerage statement: `payerTin:
    '11-1111111'`, `recipientTin: '222-22-2222'`, `accountNumber: 'ACC-CONSOLIDATED-01'`,
    `taxYear: 2025`, and the shared hash literal
    `'deadbeef00112233445566778899aabbccddeeff0011223344556677889900'`.

    `proof.oneArtifactYieldsTwoSubjectsSharingProvenance`: (1) validate a 1099-DIV instance built
    from the shared key fields plus `sourceArtifactHash` set to the shared hash literal, and a
    1099-B instance built the same way — both must validate `ok`. (2) Compute
    `divSubject = formSubject({ ...sharedKeyFields, formType: oneZeroNineNineDivDialect })` and
    `bSubject = formSubject({ ...sharedKeyFields, formType: oneZeroNineNineBDialect })`. (3) Assert
    `divSubject !== bSubject` — the SAME artifact's two extracted forms are two distinct subjects,
    because `formType` is part of the key. (4) Assert
    `divInstance.value.sourceArtifactHash === bInstance.value.sourceArtifactHash` — the shared
    provenance DOC-13 requires. Both assertions live in ONE proof leaf so the property reads as a
    single fact ("N subjects, one shared hash"), not two disconnected checks.

    Add a second leaf, `differentArtifactsDoNotShareProvenance`: build a THIRD validated instance
    (either dialect) with a DIFFERENT `sourceArtifactHash` literal (e.g.
    `'0000111122223333444455556666777788889999aaaabbbbccccddddeeeeff'` — verify this decodes via
    `isHash` before using it as a fixture) and assert its hash is NOT equal to the shared literal —
    a control proving the equality assertion above is checking something real, not a tautology (two
    fixtures both defaulting to the same value would pass step 4 above vacuously).
  </action>
  <acceptance_criteria>
    - `fjs/document/consolidated_provenance/module.f.js` exports `proof` and exports NOTHING else —
      `grep -n "^export const" fjs/document/consolidated_provenance/module.f.js` shows only `proof`.
    - `grep -n "formSubject(" fjs/document/consolidated_provenance/module.f.js` matches at least
      twice (once per dialect).
    - `grep -n "divSubject !== bSubject\|assertEq(divSubject === bSubject, false)" fjs/document/consolidated_provenance/module.f.js`
      matches (the distinct-subjects assertion, in either equivalent form).
    - `grep -n "sourceArtifactHash" fjs/document/consolidated_provenance/module.f.js` matches at
      least three times (the shared literal used on both instances, plus the differing control).
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; the project-local proof count (baseline 544, or the count after 12-01/12-02 land, whichever is current) increases; every acceptance criterion above is confirmed by the grep commands shown.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `finance_schema` atomic registration — 7 to 9 dialects, ONE commit (DOC-06/DOC-07 reachability)</name>
  <files>fjs/server/finance_schema/module.f.js</files>
  <read_first>
    fjs/server/finance_schema/module.f.js (the WHOLE file — the `dialectSchemas` map, the
    `expectedKnownDialectCount` constant and its docstring, and the `*Resolves`/`unknownDialectRefused`/
    `everyRegisteredDialectIsCounted` proof leaves, exactly as they exist today at count 7)
    .planning/phases/11-wage-retirement-and-benefit-documents/11-PATTERNS.md section
    "fjs/server/finance_schema/module.f.js (MODIFIED — dialectSchemas 5 -> 7)" (the VERBATIM pattern
    this task repeats one register-count higher: import block, map extension,
    `expectedKnownDialectCount` docstring, `*Resolves` leaf shape)
    fjs/document/1099div/module.f.js, fjs/document/1099b/module.f.js (the two schema exports being
    registered — this task depends on both 12-01 and 12-02)
  </read_first>
  <action>
    This is ONE atomic edit — all three parts land in this single task/commit, never split, per this
    phase's non-negotiable constraint 1 (two of the three ships a false green; Phase 11 confirmed the
    count leaf reddens independently of `unknownDialectRefused`):

    1. Import `dialect as oneZeroNineNineDivDialect, oneZeroNineNineDivSchema` from
       `'../../document/1099div/module.f.js'` and `dialect as oneZeroNineNineBDialect,
       oneZeroNineNineBSchema` from `'../../document/1099b/module.f.js'`, alongside the file's
       existing seven dialect imports.
    2. Add both to `dialectSchemas`: `[oneZeroNineNineDivDialect]: oneZeroNineNineDivSchema,
       [oneZeroNineNineBDialect]: oneZeroNineNineBSchema,` — append after the existing seven entries.
    3. Bump `expectedKnownDialectCount` from `7` to `9`. Update its docstring's worked example
       ("This commit registers the SIXTH AND SEVENTH dialects... 5 to 7") to instead say "the EIGHTH
       AND NINTH dialects (`vnd.fjs.1099div`, `vnd.fjs.1099b`), so the count moves from 7 to 9 in one
       step" — keep the surrounding warning prose about `knownDialects.length` unchanged, only the
       specific numbers/names move.
    4. Add two new proof leaves, `oneZeroNineNineDivResolves` and `oneZeroNineNineBResolves`, each
       comparing the tool's live JSON-parsed output against `toJsonSchema(<respective schema>)`
       called DIRECTLY — never a hand-written JSON literal — mirroring `oneZeroNineNineRResolves`'s
       exact shape.

    `unknownDialectRefused` needs NO code change (it already loops over `knownDialects`, which
    includes the two new tags automatically once `dialectSchemas` is extended) — do not touch it.
  </action>
  <acceptance_criteria>
    - `grep -n "expectedKnownDialectCount = 9" fjs/server/finance_schema/module.f.js` matches.
    - `grep -n "oneZeroNineNineDivResolves\|oneZeroNineNineBResolves" fjs/server/finance_schema/module.f.js`
      matches both.
    - `grep -c "\[.*Dialect\]: .*Schema," fjs/server/finance_schema/module.f.js` shows 9 entries in
      `dialectSchemas` (count the map's own entries directly, do not infer).
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` rises by at least 2 relative to the count
      recorded at the end of Task 1 (the two new `*Resolves` leaves, at minimum).
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; the project-local proof count rises by at least 2 over Task 1's ending count; `finance_schema` serves both new dialects' JSON Schema by name, verified by the grep commands above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mutation Gate M2 — prove the hand-typed dialect count is load-bearing (BLOCKING)</name>
  <files>fjs/server/finance_schema/module.f.js</files>
  <read_first>
    fjs/server/finance_schema/module.f.js (as written by Task 2 — `expectedKnownDialectCount`,
    `everyRegisteredDialectIsCounted`, `unknownDialectRefused`)
    .planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-VALIDATION.md "Mutation
    Gate" section, row M2 (expected outcome: set `expectedKnownDialectCount` to 8 (wrong) after
    registering both dialects -> the dialect-count leaf goes RED; that file's own docstring records
    a newly registered dialect is NOT covered for free by `unknownDialectRefused` — Phase 11
    confirmed this exact asymmetry)
    AGENTS.md "A proof is not known to work until you have watched it fail" section
  </read_first>
  <action>
    **Mutation Gate M2 (BLOCKING — run this now, recorded, before considering this plan done):**
    1. Temporarily set `expectedKnownDialectCount` to `8` in `fjs/server/finance_schema/module.f.js`
       (the WRONG value — `dialectSchemas` genuinely has 9 entries after Task 2; leave the map
       itself untouched).
    2. Run `node --test 2>&1 | grep -c '^✔ import("./fjs/'` and confirm the count DROPS relative to
       the post-Task-2 count, and specifically that `everyRegisteredDialectIsCounted` fails while
       `unknownDialectRefused` STAYS GREEN (the same asymmetry Phase 11 documented and this gate
       exists to re-confirm at count 9). Record both leaves' actual pass/fail status and the exact
       terminal output in the SUMMARY.
    3. Restore `expectedKnownDialectCount` to `9` exactly. Re-run
       `node --test 2>&1 | grep -c '^✔ import("./fjs/'` and confirm the count returns to (at least)
       the post-Task-2 count.
    4. Run `git status --porcelain` and confirm it is empty.
    5. Record every command's actual output in `12-05-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - Mutation Gate M2 was actually run: the SUMMARY records `everyRegisteredDialectIsCounted`
      failing AND `unknownDialectRefused` passing simultaneously while `expectedKnownDialectCount`
      was `8`, plus the final `git status --porcelain` clean confirmation after restoring `9`.
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` after this task, with the constant restored,
      is at least equal to the count recorded at the end of Task 2.
    - `grep -n "expectedKnownDialectCount = 9" fjs/server/finance_schema/module.f.js` matches.
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/' && git status --porcelain</automated>
  </verify>
  <done>Mutation Gate M2's five steps are all recorded in `12-05-SUMMARY.md` with actual command output (not paraphrased); the constant is restored to 9 exactly; `git status --porcelain` is empty; `npx tsc --noEmit` exits 0; the project-local proof count is at least the count recorded at the end of Task 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client request -> `finance_schema` tool | An arbitrary `dialect` string arrives from an MCP client and must never crash the process or leak an internal error regardless of whether it names a real dialect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | `dialectSchemas`'s hand-typed `expectedKnownDialectCount` | mitigate | A dialect silently deregistered (or a registration silently dropped in a future merge) is caught ONLY by this independently hand-typed count, never by the map's own `.length` — confirmed load-bearing by Mutation Gate M2, which reproduces the exact asymmetry AGENTS.md's case study describes (the count leaf reddens while `unknownDialectRefused` stays green). |
| T-12-05-02 | Denial of Service | `finance_schema` tool handler, unknown `dialect` argument | accept (pre-existing, unchanged) | Already returns `pure(errorResult(...))`, never throws — this plan adds two registry entries and does not touch that handler's control flow. |
| T-12-05-03 | Spoofing | DOC-13's `sourceArtifactHash` equality asserted between two documents that are not actually co-derived from one physical artifact | accept | This proof module validates the MECHANISM (two dialects, one shared literal, distinct subjects) — it does not, and cannot, cryptographically prove two real stored documents came from the same physical PDF. That verification belongs to the ingestion pipeline (Phase 16, explicitly out of scope here per DOC-13's "modeling and subject derivation only" boundary). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after each task.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly increases after Task 1 and Task 2.
- Mutation Gate M2 watched RED (specifically: `everyRegisteredDialectIsCounted` fails while
  `unknownDialectRefused` passes), then restored GREEN, with `git status --porcelain` empty —
  recorded in the SUMMARY, not merely asserted.
- `npm test` (full suite) green after all three tasks land.
</verification>

<success_criteria>
- DOC-13's N-subjects/one-shared-provenance property is proven against the two REAL new dialects,
  with a control proving the equality check is non-vacuous.
- `finance_schema` serves `vnd.fjs.1099div` and `vnd.fjs.1099b` by name; `dialectSchemas` has
  exactly 9 entries; `expectedKnownDialectCount = 9`.
- Mutation Gate M2 was run and its outcome recorded: a wrong hand-typed count reddens
  `everyRegisteredDialectIsCounted` specifically, while `unknownDialectRefused` stays green — the
  documented asymmetry — and restoring the correct value returns the suite to green with a clean
  `git status`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-brokerage-documents-and-the-capital-gain-chain/12-05-SUMMARY.md`
</output>
