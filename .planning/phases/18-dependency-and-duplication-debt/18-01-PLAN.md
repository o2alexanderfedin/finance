---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - fjs/todo/upstream-mcp-protocol-version-negotiation.md
  - fjs/todo/upstream-node-spawn-effect.md
  - fjs/todo/upstream-total-match-dispatch.md
  - fjs/todo/upstream-json-parse-split.md
autonomous: true
requirements: [MAINT-06]

must_haves:
  truths:
    - "functionalscript is pinned to ^0.44.0 in package.json, and 0.44.0 is what package-lock.json and node_modules actually resolve"
    - "each of the three still-open upstream-*.md notes reflects what is actually true of the installed 0.44.0 code, not a stale earlier version"
    - "the already-retired upstream-json-parse-split.md note is gone, unless re-checking finds its own claim is no longer true"
  artifacts:
    - path: "package.json"
      provides: "functionalscript ^0.44.0 dependency declaration"
      contains: "\"functionalscript\": \"^0.44.0\""
    - path: "package-lock.json"
      provides: "resolved 0.44.0 lockfile entry"
      contains: "functionalscript-0.44.0.tgz"
    - path: "fjs/todo/upstream-mcp-protocol-version-negotiation.md"
      provides: "a dated re-check against 0.44.0"
    - path: "fjs/todo/upstream-node-spawn-effect.md"
      provides: "a dated re-check against 0.44.0"
    - path: "fjs/todo/upstream-total-match-dispatch.md"
      provides: "a dated re-check against 0.44.0"
  key_links:
    - from: "package.json"
      to: "node_modules/functionalscript/package.json"
      via: "npm install resolution"
      pattern: "0\\.44\\.0"
---

<objective>
Bump `functionalscript` from `^0.43.1` to `^0.44.0`, alone, in its own commit — then, as a
separate doc-only commit, re-check all four `fjs/todo/upstream-*.md` notes against the newly
installed version and delete the one whose own text already says to delete it once adopted.

This is Wave 1 and every other Phase 18 plan depends on it (directly or transitively), which is
the mechanism that guarantees the bump lands first and alone: `git bisect` must be able to isolate
a moved figure to either "the dependency bump" or "a refactor," never both at once.

Purpose: keep the vendored dependency current (ROADMAP's corrected Success Criterion 1) without
mixing that change with anything that could itself move a computed figure.
Output: `package.json`/`package-lock.json` on `^0.44.0`; three upstream notes carrying a fresh,
dated 0.44.0 finding; `upstream-json-parse-split.md` deleted (or explicitly not, with a stated
reason).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/18-dependency-and-duplication-debt/18-CONTEXT.md
@.planning/phases/18-dependency-and-duplication-debt/18-VALIDATION.md
@package.json

Baseline measured 2026-08-14 on `6d6d8b8` (this branch's parent): `npm test` 6314/6314, exit 0;
project-local de-duplicated proof count **916**; installed `functionalscript` is `0.43.1`
(`^0.43.1` in both `package.json` and `package-lock.json`, no drift between them today). Registry
confirms `0.44.0` exists (`npm view functionalscript versions --json` lists it as the newest
release).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump functionalscript to ^0.44.0, alone</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    package.json (the `dependencies.functionalscript` line)
    AGENTS.md's "Two references to one dependency drift apart" section — move `package.json` and
    `package-lock.json` together; the submodule pin is a separate, deeper reference this task does
    not touch (CONTEXT.md: kept separate by owner decision).
  </read_first>
  <action>
    Run `npm install functionalscript@0.44.0`. Confirm the resulting `package.json` entry reads
    exactly `"functionalscript": "^0.44.0"` (caret kept, per CONTEXT.md's "keep the caret range"
    decision — exact-pinning was considered and explicitly deferred). If npm writes anything other
    than a caret range (e.g. an exact `0.44.0` because of a local `.npmrc` save-exact setting),
    hand-edit `package.json`'s `dependencies.functionalscript` back to `"^0.44.0"` and re-run
    `npm install` so the lockfile regenerates consistent with that declared range. Do not touch the
    `functionalscript` git submodule pin (`git submodule status`) — CONTEXT.md keeps it a separate,
    deeper guarantee, out of scope for this bump. Do not touch any file under `fjs/` in this task.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').dependencies.functionalscript"` prints `^0.44.0`.
    - `node -p "require('./package-lock.json').packages['node_modules/functionalscript'].version"`
      prints `0.44.0`.
    - `node -p "require('./node_modules/functionalscript/package.json').version"` prints `0.44.0`.
    - `git diff --stat` (before committing) shows only `package.json` and `package-lock.json`
      changed — no `fjs/` file, no submodule gitlink change.
    - `npm test` exits `0`.
    - `node --test 2>&1 | grep '^✔ import("./fjs/' | sed 's/ ([0-9.]*ms)$//' | sort -u | wc -l`
      reports a count `>= 916` (this task neither adds nor removes a proof leaf — expect exactly
      916 unless the bump itself surfaces an unrelated change, which would be a defect to stop and
      report, not silently absorb).
  </acceptance_criteria>
  <done>package.json and package-lock.json both resolve functionalscript to 0.44.0 under a caret
  range, npm test is green, and the diff touches nothing outside those two files.</done>
</task>

<task type="auto">
  <name>Task 2: Re-check the four upstream notes against 0.44.0</name>
  <files>
    fjs/todo/upstream-mcp-protocol-version-negotiation.md,
    fjs/todo/upstream-node-spawn-effect.md,
    fjs/todo/upstream-total-match-dispatch.md,
    fjs/todo/upstream-json-parse-split.md
  </files>
  <read_first>
    fjs/todo/upstream-mcp-protocol-version-negotiation.md (in full — the gap is in `mcpStep`'s
    `initialize` branch: `pr.protocolVersion`, the validated client request, is bound but never
    read again; `result` always echoes the closed-over `protocolVersion`)
    fjs/todo/upstream-node-spawn-effect.md (in full — the gap is that `fjs/effects/node` has `Exec`
    but no long-lived streaming `Spawn`/`SpawnWrite`/`SpawnRead` sibling)
    fjs/todo/upstream-total-match-dispatch.md (in full — the gap is that `match` throws via
    `assert` on an unhandled command rather than offering a total `tryMatch` sibling)
    fjs/todo/upstream-json-parse-split.md (in full — already reads "landed upstream and adopted
    here ... delete once adopted")
  </read_first>
  <action>
    Against the 0.44.0 copy installed by Task 1, re-run the exact falsifiability check each note
    already names, and record a dated finding in the note itself:
    1. `sed -n '1,260p' node_modules/functionalscript/fjs/protocol/mcp/module.f.js | grep -n "initialize" -A 25` —
       locate the `initialize` branch. Confirm whether the validated client request
       (`pr.protocolVersion` or equivalent) is now compared against what the server supports, or
       still discarded in favor of unconditionally echoing the closed-over `protocolVersion`.
       Append a new dated (`2026-08-14`, or the actual run date) paragraph under the note's
       `Status` section stating exactly what was found, citing the file and line range read —
       either "still open, confirmed at 0.44.0" or "closed by <mechanism>, upstream". Do not
       delete this note under any finding — CONTEXT.md's deletion instruction names only the
       already-retired json-parse note.
    2. `grep -n "'exec'\|'spawn'\|export const exec\|export const spawn" node_modules/functionalscript/fjs/effects/node/module.f.js` —
       confirm whether a `spawn`-shaped long-lived effect now exists beside `exec`. Append the same
       kind of dated finding to `upstream-node-spawn-effect.md`.
    3. `grep -n "tryMatch\|export const match" node_modules/functionalscript/fjs/effects/module.f.js` —
       confirm whether a total `tryMatch` sibling now exists beside the throwing `match`. Append the
       same kind of dated finding to `upstream-total-match-dispatch.md`.
    4. Re-confirm `fjs/media/json/module.f.js`'s (this repo's own binding module, `fjs/json/module.f.js`)
       `parse` is still total and this repo's production code still routes through it — the same
       class of check the note's own "What we do when we bump" section already describes. If the
       note's claim ("parse is total as of 0.42.0, adopted here") still holds, delete
       `fjs/todo/upstream-json-parse-split.md` — its own text mandates this. If re-checking finds
       the adoption incomplete or the claim false, do NOT delete it; instead append a dated
       correction explaining what changed.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "0.44.0\|Re-checked" fjs/todo/upstream-mcp-protocol-version-negotiation.md` is
      `>= 1` and the appended text names the specific `initialize`-branch evidence read.
    - Same for `fjs/todo/upstream-node-spawn-effect.md` and `fjs/todo/upstream-total-match-dispatch.md`.
    - `ls fjs/todo/upstream-json-parse-split.md` reports "No such file or directory" UNLESS the
      re-check found the adoption incomplete, in which case the file exists with an updated
      `Status` section explaining exactly why it was not deleted.
    - `npm test` exits `0`; proof count `>= 916` (doc-only edits, no proof added or removed).
    - `git diff --stat` for this task's changes touches only files under `fjs/todo/` — no
      production code.
  </acceptance_criteria>
  <done>Each of the three open notes carries a dated 0.44.0 finding with concrete file/line
  evidence; the already-retired note is deleted unless re-checking disproves its own claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> node_modules | Supply-chain boundary: `npm install` pulls and pins a new third-party release into the dependency tree this project deeply couples to. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `package.json`/`package-lock.json` functionalscript entry | mitigate | `package-lock.json` pins the exact resolved `0.44.0` tarball and its integrity hash from `npm install`; the diff is reviewed (Task 1's own acceptance criteria: `git diff --stat` touches only these two files) before commit; the git submodule pin is deliberately left unmoved this task, per CONTEXT.md, so it remains a second, independent reference to audit later. |
| T-18-02 | Information Disclosure | `fjs/todo/upstream-*.md` notes | accept | Doc-only edits; no runtime code path, no PII, no secret. |
</threat_model>

<verification>
Full `npm test` after each task (Task 1 alone, then Task 2 alone) — both green, proof count
`>= 916` both times. Confirm via `git log` that Task 1's commit touches only `package.json` and
`package-lock.json`, with no other commit landing between it and the start of this plan's work.
</verification>

<success_criteria>
- `functionalscript` is `0.44.0` in `package.json` (caret), `package-lock.json`, and
  `node_modules`.
- The bump is isolated: its own commit, no other file touched.
- All four upstream notes reflect 0.44.0's actual, re-verified state; the retired one is gone
  unless disproven.
- `npm test` green; proof count `>= 916` after both tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/18-dependency-and-duplication-debt/18-01-SUMMARY.md`
</output>
