---
phase: 18
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - fjs/document/form_revision/module.f.js
  - fjs/document/1099int/module.f.js
  - fjs/document/1099r/module.f.js
  - fjs/document/w2/module.f.js
  - fjs/document/ssa1099/module.f.js
  - fjs/document/1099div/module.f.js
  - fjs/document/1099b/module.f.js
autonomous: true
requirements: [MAINT-08]

must_haves:
  truths:
    - "the formRevision emptiness/whitespace check is defined in exactly one place"
    - "all six dialects still refuse an empty or whitespace-only formRevision through their own validate"
    - "mutating the shared check reddens a leaf in all six dialects; mutating one dialect's call site reddens only that dialect"
  artifacts:
    - path: "fjs/document/form_revision/module.f.js"
      provides: "formRevisionError, the single shared definition, following moneyFieldError's precedent"
      exports: ["formRevisionError"]
      min_lines: 30
  key_links:
    - from: "fjs/document/1099int/module.f.js"
      to: "fjs/document/form_revision/module.f.js"
      via: "import { formRevisionError } from '../form_revision/module.f.js'"
      pattern: "formRevisionError"
    - from: "fjs/document/ssa1099/module.f.js"
      to: "fjs/document/form_revision/module.f.js"
      via: "import, called SECOND in checkReferences (after the payerTin check)"
      pattern: "formRevisionError"
---

<objective>
Collapse the byte-identical `formRevision must not be empty or whitespace-only` check, currently
written out six times (`1099int:130`, `1099r:232`, `w2:196`, `ssa1099:130`, `1099div:234`,
`1099b:264`), into one shared `formRevisionError` helper following `moneyFieldError`'s exact
precedent (`fjs/document/money_field/module.f.js:42`) — then prove the sharing is real with the
matched-pair mutation gates M1 (the shared helper reaches all six) and M2 (each dialect is
genuinely wired, not riding on one shared proof).

Purpose: MAINT-08 / ROADMAP's corrected Success Criterion 3 — the check must exist once, shared
the way `moneyFieldError` already is.
Output: `fjs/document/form_revision/module.f.js` (new); all six dialect files call it instead of
carrying their own copy of the literal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/18-dependency-and-duplication-debt/18-CONTEXT.md
@.planning/phases/18-dependency-and-duplication-debt/18-PATTERNS.md
@.planning/phases/18-dependency-and-duplication-debt/18-VALIDATION.md

Population re-derived 2026-08-14 (matches PATTERNS.md exactly, re-confirmed independently during
planning): `grep -rn "formRevision must not be empty" fjs` returns exactly six lines —
`fjs/document/{1099int:130, 1099r:232, w2:196, ssa1099:130, 1099div:234, 1099b:264}/module.f.js`.
No second byte-identical duplication exists among those six files' other checks (`payerTin`,
`sourceArtifactHash`, `box12`, `stateLocal` loops are each genuinely dialect-specific).

**The ordering exception, confirmed by direct read:** in five of the six dialects, the
`formRevision` check is the FIRST statement in `checkReferences`. In `ssa1099` only
(`fjs/document/ssa1099/module.f.js:125-131`), a `payerTin` emptiness check runs first and
`formRevision` is the second statement:

```js
export const checkReferences = r => {
    if (r.payerTin !== '') {
        return error(`payerTin must be the empty string for vnd.fjs.ssa1099 (SSA-1099 prints no payer TIN)`)
    }
    if (r.formRevision.trim() === '') {
        return error(`formRevision must not be empty or whitespace-only`)
    }
    ...
```

This ordering must be preserved exactly — do not make `ssa1099` check `formRevision` first.
</context>

<interfaces>
<!-- moneyFieldError's full shape — the precedent formRevisionError must follow, minus the
     currying (formRevision has no variable field name, unlike a money box). -->

From `fjs/document/money_field/module.f.js:1-47`:
```js
/**
 * ... Extracted when the third dialect needed it ... not written speculatively for the first.
 * @module
 */
import { assert, assertEq } from 'functionalscript/fjs/asserts/module.f.js'
import { centsToString, tryCentsFromString } from '../../exact/module.f.js'

/** @type {(label: string) => (printed: string) => string | undefined} */
export const moneyFieldError = label => printed => {
    const [t, v] = tryCentsFromString(printed)
    if (t === 'error') { return `${label} is not an exact decimal: ${v}` }
    const magnitude = v < 0n ? -v : v
    return magnitude > maxSafeCents ? `${label} exceeds safe integer magnitude: ${printed}` : undefined
}
```

Every dialect's existing call site (`fjs/document/1099int/module.f.js:48,128-131`), the pattern to
replace:
```js
import { moneyFieldError } from '../money_field/module.f.js'
...
export const checkReferences = r => {
    if (r.formRevision.trim() === '') {
        return error(`formRevision must not be empty or whitespace-only`)
    }
    for (const field of moneyBoxFields) { ... }
    return ok(r)
}
```

Each dialect's existing proof leaves to PRESERVE, unmodified (`fjs/document/1099int/module.f.js:269-276`;
identical leaf names present in all six — `1099r:462`, `w2:440`, `ssa1099:269`, `1099div:416`,
`1099b:477` — `1099int` alone additionally has `whitespaceFormRevisionRejected`):
```js
checkReferences: {
    emptyFormRevisionRejected: () => {
        const [t] = validate({ ...minimal, formRevision: '' })
        assertEq(t, 'error')
    },
    // 1099int only:
    whitespaceFormRevisionRejected: () => {
        const [t] = validate({ ...minimal, formRevision: '   ' })
        assertEq(t, 'error')
    },
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared formRevisionError helper</name>
  <files>fjs/document/form_revision/module.f.js</files>
  <read_first>
    fjs/document/money_field/module.f.js (in full — the exact shape to follow)
    fjs/document/1099int/module.f.js:1-40 (module docstring style), :128-131 (a call site)
  </read_first>
  <behavior>
    - `formRevisionError('2024')` returns `undefined` (a real revision string is accepted).
    - `formRevisionError('')` returns the exact literal
      `` `formRevision must not be empty or whitespace-only` `` (the ONLY place this literal may
      live after Task 2).
    - `formRevisionError('   ')` (whitespace-only) returns the same message — `.trim() === ''`
      catches both.
    - The returned message, when defined, equals the literal exactly — asserted as a distinct
      leaf, not merely "is a non-empty string" — since VALIDATION.md's own sign-off check
      (`grep -rn "formRevision must not be empty" fjs` returns exactly one line) depends on this
      literal existing nowhere else once Task 2 lands.
  </behavior>
  <action>
    Create `fjs/document/form_revision/module.f.js`, mirroring `moneyFieldError`'s module docstring
    idiom ("extracted when the Nth caller needed it" — here, six). Export
    `formRevisionError: (printed: string) => string | undefined` — no label currying, since the
    field name is always `formRevision`, unlike `moneyFieldError`'s variable box label. Import only
    `assert`/`assertEq` from `functionalscript/fjs/asserts/module.f.js` — no dialect-specific
    imports, so later dialect modules depend on this one, never the reverse (matching `subject`'s
    and `money_field`'s own independence). Add a colocated `proof` export whose leaves realize the
    `<behavior>` block above, plus one leaf asserting the returned message's exact text.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const formRevisionError" fjs/document/form_revision/module.f.js` returns
      exactly 1 line, typed `(printed: string) => string | undefined`.
    - `grep -c "formRevision must not be empty or whitespace-only" fjs/document/form_revision/module.f.js`
      returns exactly 1 (the literal's only home right now — Task 2 has not run yet).
    - `git diff --stat` for this task shows only the new file — no dialect file touched.
    - `npm test` exits `0`; proof count strictly `> 916` (new leaves added, nothing yet removed).
  </acceptance_criteria>
  <done>fjs/document/form_revision/module.f.js exists, exports formRevisionError matching
  moneyFieldError's precedent, and its own proof covers empty/whitespace/valid/message-content.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all six dialects to the shared helper</name>
  <files>
    fjs/document/1099int/module.f.js, fjs/document/1099r/module.f.js, fjs/document/w2/module.f.js,
    fjs/document/ssa1099/module.f.js, fjs/document/1099div/module.f.js, fjs/document/1099b/module.f.js
  </files>
  <read_first>
    Each of the six files' import block (the `moneyFieldError` import line: `1099int:48`,
    `1099r:103`, `w2:57`, `ssa1099:53`, `1099div:84`, `1099b:114`) and `checkReferences` opening
    (`1099int:128`, `1099r:230`, `w2:194`, `ssa1099:125`, `1099div:232`, `1099b:262`) — line numbers
    reconfirmed 2026-08-14 during planning, matching PATTERNS.md exactly.
  </read_first>
  <action>
    In each of the six files, add `import { formRevisionError } from '../form_revision/module.f.js'`
    next to the existing `moneyFieldError` import. Replace the inline
    `if (r.formRevision.trim() === '') { return error(\`formRevision must not be empty or whitespace-only\`) }`
    block with a call to `formRevisionError(r.formRevision)`, bound to a local, routed into the SAME
    `error(...)` return the dialect already uses when the result is not `undefined`. In `ssa1099`
    ONLY, this replacement is the SECOND statement in `checkReferences` — the existing `payerTin`
    check stays first, unmoved. Touch nothing else in any `checkReferences` body (money-box loops,
    `sourceArtifactHash`/`box12` checks, `stateLocal` loops stay exactly as they are). Do not modify
    or remove any dialect's existing `emptyFormRevisionRejected` / `whitespaceFormRevisionRejected`
    proof leaf — they stay, unchanged, still calling that dialect's own `validate`, and now prove
    the dialect is genuinely wired to the shared helper rather than merely that the helper works in
    isolation.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "formRevision must not be empty" fjs` returns EXACTLY ONE line, inside
      `fjs/document/form_revision/module.f.js` — VALIDATION.md's own sign-off check.
    - For each of the six dialect files: `grep -c "import { formRevisionError } from '../form_revision/module.f.js'"`
      returns exactly `1`.
    - `sed -n '/export const checkReferences = r =>/,/^}/p' fjs/document/ssa1099/module.f.js | head -8`
      shows the `payerTin` refusal textually BEFORE the `formRevisionError` call.
    - All six dialects' `emptyFormRevisionRejected` leaves (and `1099int`'s
      `whitespaceFormRevisionRejected`) are present, byte-identical to before this task, and still
      pass.
    - `npm test` exits `0`; proof count `>= 916` (Task 1's new leaves are the only net change so
      far this plan).
  </acceptance_criteria>
  <done>All six dialects call formRevisionError instead of carrying their own copy of the check;
  ssa1099's payerTin-then-formRevision ordering is preserved; every existing proof still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation Gates M1 + M2 (matched pair)</name>
  <files>fjs/document/form_revision/module.f.js, fjs/document/1099int/module.f.js</files>
  <read_first>
    18-VALIDATION.md's mutation gate table (M1, M2 rows) — read in full already via 18-VALIDATION.md
    AGENTS.md's "A proof is not known to work until you have watched it fail" section
  </read_first>
  <action>
    M1 — Temporarily change `formRevisionError` in `fjs/document/form_revision/module.f.js` to
    unconditionally `return undefined` (accept every input, including empty/whitespace). Run
    `npm test`. Record the failing leaf names. Predicted: a leaf in EACH of the six dialects
    (`emptyFormRevisionRejected` in all six, plus `1099int`'s `whitespaceFormRevisionRejected`, plus
    the new module's own `proof` leaves) goes red — proving the shared helper reaches everywhere.
    If the actual red set differs from this prediction, record the actual set and the reason (per
    AGENTS.md: "a mutation's predicted red set is itself a claim, and it is often wrong"). Restore
    `fjs/document/form_revision/module.f.js` byte-identical (e.g. `git checkout --
    fjs/document/form_revision/module.f.js`) and confirm `git status --short` on that file is empty.

    M2 — With the shared helper restored and green, temporarily remove ONLY `1099int`'s call to
    `formRevisionError` from its `checkReferences` (delete the two lines that call it and return its
    error, leaving `formRevision` unchecked in that one dialect's `checkReferences`). Run
    `npm test`. Predicted: ONLY `1099int`'s `emptyFormRevisionRejected` and
    `whitespaceFormRevisionRejected` leaves go red; the other five dialects' leaves and the shared
    module's own proof stay green — proving `1099int` is genuinely wired to the helper, not that one
    shared proof carries all six. Restore `fjs/document/1099int/module.f.js` byte-identical and
    confirm `git status --short` on that file is empty.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - M1's `npm test` run shows a failing leaf named `emptyFormRevisionRejected` (or its dialect's
      equivalently-named check) under each of the six dialects' `checkReferences` group.
    - M2's `npm test` run shows ONLY `1099int`'s two `*FormRevisionRejected` leaves failing; the
      other five dialects' equivalent leaves and `fjs/document/form_revision/module.f.js`'s own
      proof stay green.
    - After both restores: `git status --short fjs/document/form_revision/module.f.js
      fjs/document/1099int/module.f.js` is empty.
    - Final `npm test` (post-restore) exits `0`; proof count `>= 916`.
  </acceptance_criteria>
  <done>M1 shows the shared helper reaches all six dialects; M2 shows one dialect is genuinely,
  independently wired to it; both mutations are restored byte-identical and the tree is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCR/agent-submitted document fields -> `checkReferences` | A stored document's `formRevision` field (originating from OCR conversion or an agent-authored `cas_add` payload) crosses into this semantic validation gate before it is trusted as a stored revision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | `fjs/document/form_revision/module.f.js` `formRevisionError`, consumed at six `checkReferences` call sites | mitigate | The shared helper still returns a message string consumed by each dialect's existing `error(...)` path — no exception, no new control flow. Mutation Gates M1+M2 prove the wiring is genuine at all six sites, so a future refactor cannot silently drop the refusal at one dialect without the suite catching it. |
| T-18-04 | Repudiation | none new | accept | No change to what is persisted or audited — this is a pure validation-path consolidation. |
</threat_model>

<verification>
Full `npm test` after every task. Proof count re-measured after every task, must stay `>= 916`
throughout (Task 1 raises it; Tasks 2 and 3 must not lower it below that floor). M1/M2 both watched
failing, then restored byte-identical with `git status --short` confirmed clean before moving on.
</verification>

<success_criteria>
- `grep -rn "formRevision must not be empty" fjs` returns exactly one line.
- All six dialects still refuse an empty/whitespace-only `formRevision`, proven by their own
  unchanged proof leaves.
- M1 and M2 both demonstrated and restored.
- `npm test` green; proof count `>= 916`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-dependency-and-duplication-debt/18-02-SUMMARY.md`
</output>
