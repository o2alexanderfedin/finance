---
phase: 01-planning-document-corrections-and-the-upstream-report
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DOCC-07]

must_haves:
  truths:
    - "REQUIREMENTS.md's EXACT-05 text specifies money as a string, not a JSON number, at the storage boundary."
    - "ROADMAP.md's Phase 4 success criterion 4 and Phase 5 depends-on line both specify money as a string, not integer-cents-as-a-JSON-number, at the storage boundary."
    - "REQUIREMENTS.md carries a DOCC-07 requirement entry, a traceability table row, and an updated Phase 1 coverage count of 8."
    - "Exact rationals inside computation and decimal strings on the MCP wire are unchanged by this correction — only the storage layer flips."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected EXACT-05 text, new DOCC-07 bullet, new traceability row, updated Phase 1 coverage count"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 4 success criterion 4, corrected Phase 5 depends-on line"
  key_links:
    - from: ".planning/REQUIREMENTS.md EXACT-05"
      to: "AGENTS.md's absolute money-as-string rule"
      via: "storage-boundary wording matching 'string, never a JSON number'"
      pattern: "string"
---

<objective>
Reconcile three places that still specify integer cents as a JSON *number* at the storage
boundary with PR #17's absolute AGENTS.md rule that money in a stored JSON document is a
string, never a JSON number: `REQUIREMENTS.md`'s EXACT-05 text, and `ROADMAP.md`'s Phase 4
success criterion 4 and Phase 5 depends-on line. Add the DOCC-07 correction itself as a
requirement entry in REQUIREMENTS.md.

Purpose: Phase 4 and Phase 5 both read these exact lines when they are planned; an
uncorrected storage-boundary claim would plan those phases against text that contradicts
AGENTS.md.
Output: `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` with corrected prose, plus a
new DOCC-07 entry. No code, no behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planning-document-corrections-and-the-upstream-report/01-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct EXACT-05 and add the DOCC-07 requirement entry to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (the "### Documentation Corrections (DOC-C)" section around
    line 61, the EXACT-05 bullet at line 249, the Traceability table around line 402-407, and
    the "Coverage by phase" table row for Phase 1 at line 486), AGENTS.md's no-floating-point
    rule (for exact house wording: "money in a stored JSON document is a string, never a JSON
    number... the rtti field is string; exactness is enforced in the semantic check")
  </read_first>
  <action>
    Four edits, all in `.planning/REQUIREMENTS.md`:

    1. The EXACT-05 bullet currently reads: "**EXACT-05** *(T1)*: Layering fixed
       deliberately: integer cents in JSON at the storage boundary (`isSafeInteger`-guarded),
       rationals inside computation, decimal **strings** on the MCP wire — fjs's JSON
       `Primitive` has no `bigint`, so a bigint schema cannot cross JSON-RPC." Replace "integer
       cents in JSON at the storage boundary (`isSafeInteger`-guarded)" with "money as a
       decimal **string** in JSON at the storage boundary (never a JSON number — decoded to
       exact cents by the semantic check, per AGENTS.md)". Leave "rationals inside
       computation, decimal **strings** on the MCP wire" and the rest of the sentence
       unchanged — those two layers are correct as written.

    2. In "### Documentation Corrections (DOC-C)", after the DOCC-06 bullet, add a new
       bullet: "**DOCC-07** *(T0)*: Reconcile the storage-layer money representation with
       AGENTS.md's absolute rule that money in a stored JSON document is a string, never a
       JSON number. Correct EXACT-05's text above and `ROADMAP.md`'s Phase 4 success criterion
       4 and Phase 5 depends-on line, all of which still specified integer cents as a JSON
       *number* at the storage boundary. Only the storage layer flips — exact rationals inside
       computation and decimal strings on the MCP wire are unaffected."

    3. In the Traceability table, insert a new row immediately after the existing
       "| DOCC-06 | T0 | Phase 1 - Planning-Document Corrections | Week 0 | Pending |" row:
       "| DOCC-07 | T0 | Phase 1 - Planning-Document Corrections | Week 0 | Pending |".

    4. In "### Coverage by phase", the Phase 1 row currently reads "| 1.
       Planning-Document Corrections | Week 0 | DOCC-01, DOCC-02, DOCC-03, DOCC-04, DOCC-05,
       DOCC-06, SEC-04 | 7 | T0 |". Add `DOCC-07` to the requirement list (after DOCC-06,
       before SEC-04) and change the count from `7` to `8`.

    Do not change the "**All 79 v1 requirements...**" sentence near the end of the file, or
    any other total-count claim outside REQUIREMENTS.md's own Phase-1 coverage row — those are
    out of this correction's scope per `01-CONTEXT.md`.
  </action>
  <verify>
    <automated>grep -n "DOCC-07" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DOCC-07" .planning/REQUIREMENTS.md` returns >= 3 (bullet, traceability row,
      coverage-by-phase cell)
    - `grep -n "integer cents in JSON at the storage boundary" .planning/REQUIREMENTS.md`
      returns nothing
    - `grep -n "storage boundary" .planning/REQUIREMENTS.md` still returns a match, now
      describing a string, not integer cents
    - `grep -n "DOCC-06, DOCC-07, SEC-04 | 8 | T0" .planning/REQUIREMENTS.md` returns a match
    - `grep -c "rationals inside computation" .planning/REQUIREMENTS.md` returns >= 1
      (unchanged layer survives)
  </acceptance_criteria>
  <done>EXACT-05 states a string at the storage boundary; DOCC-07 exists as a requirement
  entry, a traceability row, and the Phase 1 coverage count is 8.</done>
</task>

<task type="auto">
  <name>Task 2: Correct Phase 4 success criterion 4 and the Phase 5 depends-on line in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md lines 204-231 (Phase 4 and Phase 5 sections)</read_first>
  <action>
    Two edits, both in `.planning/ROADMAP.md`:

    1. Phase 4's success criterion 4 currently reads: "A proof demonstrates all three layers
       on one value: `isSafeInteger`-guarded integer cents in JSON at the storage boundary,
       exact rationals inside computation, decimal **strings** on the MCP wire (fjs's JSON
       `Primitive` has no `bigint`)." Replace "`isSafeInteger`-guarded integer cents in JSON
       at the storage boundary" with "a decimal **string** in JSON at the storage boundary
       (never a JSON number — a JSON number is an IEEE 754 double before any arithmetic
       happens)". Leave "exact rationals inside computation, decimal **strings** on the MCP
       wire (fjs's JSON `Primitive` has no `bigint`)" unchanged.

    2. Phase 5's "**Depends on**" line currently reads: "Phase 2 (project-local store), Phase
       4 (cents at the storage boundary) — **runs concurrently with Phase 3**". Replace
       "(cents at the storage boundary)" with "(money as a string at the storage boundary)".
  </action>
  <verify>
    <automated>grep -n "cents at the storage boundary" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cents at the storage boundary" .planning/ROADMAP.md` returns nothing
    - `grep -n "isSafeInteger.*storage boundary\|storage boundary.*isSafeInteger" .planning/ROADMAP.md` returns nothing
    - `grep -n "string.*storage boundary\|storage boundary.*string" .planning/ROADMAP.md`
      returns matches for both the Phase 4 criterion and the Phase 5 depends-on line
  </acceptance_criteria>
  <done>Phase 4 criterion 4 and Phase 5's depends-on line both describe a string, not
  integer cents, at the storage boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Register DOCC-07 in ROADMAP.md's own two bookkeeping lines</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md line 162 (Phase 1 Requirements line) and lines 419-430 (the Coverage table)</read_first>
  <action>
    Task 1 adds DOCC-07 to REQUIREMENTS.md. ROADMAP.md carries the same two facts
    independently and would otherwise disagree with it. Two edits, both in
    `.planning/ROADMAP.md`:

    1. Phase 1's requirements line currently reads: "**Requirements**: DOCC-01, DOCC-02,
       DOCC-03, DOCC-04, DOCC-05, DOCC-06, SEC-04". Insert `DOCC-07` after `DOCC-06` so it
       reads "**Requirements**: DOCC-01, DOCC-02, DOCC-03, DOCC-04, DOCC-05, DOCC-06,
       DOCC-07, SEC-04".

    2. In the "## Coverage" table, the row `| DOCC (documentation corrections) | 6 | 1 |`
       has a stale count. Change the count from `6` to `7`. The Phases cell stays `1`.

    Change nothing else in the Coverage table — the other eight category rows are correct.
  </action>
  <verify>
    <automated>grep -n "DOCC-06, DOCC-07, SEC-04" .planning/ROADMAP.md; grep -n "DOCC (documentation corrections)" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DOCC-06, DOCC-07, SEC-04" .planning/ROADMAP.md` returns 1
    - `grep -c "| DOCC (documentation corrections) | 7 | 1 |" .planning/ROADMAP.md` returns 1
    - `grep -c "| DOCC (documentation corrections) | 6 | 1 |" .planning/ROADMAP.md` returns 0
    - `grep -c "| MCP (server and tools) | 9 |" .planning/ROADMAP.md` returns 1 (proves the
      rest of the Coverage table was left alone)
  </acceptance_criteria>
  <done>ROADMAP.md's Phase 1 requirements line and Coverage count both include DOCC-07,
  agreeing with REQUIREMENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan edits committed Markdown only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-E01 | Information Disclosure | .planning/REQUIREMENTS.md, .planning/ROADMAP.md | accept | Text-only edits to already-committed planning docs; no new attack surface. |
</threat_model>

<verification>
1. `grep -n "integer cents in JSON at the storage boundary" .planning/REQUIREMENTS.md`
   returns nothing.
2. `grep -n "cents at the storage boundary" .planning/ROADMAP.md` returns nothing.
3. `grep -c "DOCC-07" .planning/REQUIREMENTS.md` returns >= 3.
4. `npm test` still reports 1 pass / 0 fail.
5. `npx tsc --noEmit` remains clean.
</verification>

<success_criteria>
- REQUIREMENTS.md and ROADMAP.md both describe money as a string, not integer cents, at the
  storage boundary.
- DOCC-07 exists in REQUIREMENTS.md as a requirement, a traceability row, and an updated
  Phase 1 coverage count.
</success_criteria>

<output>
After completion, create `.planning/phases/01-planning-document-corrections-and-the-upstream-report/01-05-SUMMARY.md`
</output>
