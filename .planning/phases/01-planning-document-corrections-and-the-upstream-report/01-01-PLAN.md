---
phase: 01-planning-document-corrections-and-the-upstream-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - README.md
  - todo/plan.md
  - fjs/todo/implement-mcp-server.md
autonomous: true
requirements: [DOCC-01, DOCC-02, DOCC-03, DOCC-04, DOCC-05, DOCC-06]

must_haves:
  truths:
    - "No document proposes djs/parser as a validation remedy for import()."
    - "Every claim that an operation not in the map cannot happen states the Object.hasOwn + null-prototype guard condition."
    - "PROJECT.md and README.md name Claude Code / Claude Desktop as the demonstration client; remote transport is recorded as v2."
    - "The import() deferral rests on schedule grounds with named compensating controls (SEC-01/02/03), not on user trust."
    - "fjs/todo/implement-mcp-server.md no longer implies fjs_run cannot be proof-tested."
    - "The TY2025 parameter-sourcing rule (Rev. Proc. 2024-40 as modified by Rev. Proc. 2025-32) is recorded in PROJECT.md and todo/plan.md, naming the original 2025 release as wrong."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Corrected mechanism, trust, audience, and parameter-sourcing claims"
    - path: "README.md"
      provides: "Corrected client claim"
    - path: "todo/plan.md"
      provides: "Corrected trust claim, parameter-sourcing note"
    - path: "fjs/todo/implement-mcp-server.md"
      provides: "Corrected guard claim, trust claim, proof-testability claim"
  key_links: []
---

<objective>
Delete six false or misleading claims, each already verified false against the current repo
text: the unguarded-operation-map soundness claim (DOCC-02), the `djs/parser`-as-validator
claim (DOCC-01), the "sole user is trusted and local" rationale for the `import()` hole
(DOCC-05), the ChatGPT-as-demonstration-client claim (DOCC-03), the `fjs_run`
cannot-be-proof-tested implication (DOCC-04), and the missing TY2025 parameter-sourcing
citation (DOCC-06).

Purpose: these documents are read directly by later phases; a false claim left standing
means a later phase is planned against text that dissolves on contact.
Output: corrected prose in four files. No code, no behavior change, no new sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planning-document-corrections-and-the-upstream-report/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROJECT.md and README.md</name>
  <files>.planning/PROJECT.md, README.md</files>
  <read_first>.planning/PROJECT.md (full file), README.md (full file, 18 lines)</read_first>
  <action>
    In `.planning/PROJECT.md`:

    1. Constraints, `**Execution boundary**` bullet: it ends "...a program asking for `fetch`
       or `readFile` finds no entry, so nothing needs intercepting or patching." This is only
       true if the lookup guard is `Object.hasOwn` against a null-prototype map — a naive
       `in` or `!== undefined` guard admits `Object.prototype` members
       (`constructor`/`toString`/`valueOf`/`__defineGetter__`), and `__defineGetter__` against
       such a guard installs an attacker-controlled getter on the whitelist object itself, a
       reproduced full escape. State that condition in the sentence.

    2. Key Decisions row "Executed programs get CAS/Evo effects only": append the same guard
       condition to its rationale cell.

    3. Constraints, `**\`import()\` is outside that boundary...**` bullet: replace "Accepted
       for Week 1 **only** because the sole user is trusted and local; `todo/plan.md` Week 5
       revisits it, most plausibly by parsing the source with `djs/parser` before importing."
       with: accepted for v1 on schedule grounds — the untrusted party is the document, not
       the user; compensating controls are `--permission` (SEC-01), an import-specifier
       allow-list (SEC-02), and content-hash-derived filenames (SEC-03). `djs/parser` cannot
       validate a program — it is a data-only language with no function node.

    4. Key Decisions row "Unrestricted Node `import()` accepted for v1...": rewrite its
       rationale cell to match point 3, and change its outcome cell from "— Pending, revisit
       Week 5" to "— Pending, revisit v2".

    5. "What This Is" opening paragraph: replace "ChatGPT (or any other MCP client)" with
       "Claude Code or Claude Desktop (or any other MCP client that supports local stdio
       servers)"; add one sentence noting remote transport (HTTPS + OAuth) — required for a
       browser-hosted client such as ChatGPT — is a v2 milestone. Do not touch the historical
       `> The main target is...` blockquote later in the file.

    6. Success Criterion 2: replace "Point an agent at the MCP server" with "Point Claude Code
       or Claude Desktop at the MCP server".

    7. Active requirements bullet "Tax-year parameters (brackets, standard deduction,
       thresholds) stored as data, keyed by year — never hardcoded in the engine": append that
       TY2025 values come from Rev. Proc. 2024-40 as modified by Rev. Proc. 2025-32, not the
       original 2025 inflation-adjustment release.

    In `README.md`: replace "ChatGPT (or any other MCP client)" with the same wording as
    point 5, plus the same v2 remote-transport sentence.
  </action>
  <verify>
    <automated>grep -c "Object.hasOwn" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sole user is trusted and local" .planning/PROJECT.md` returns nothing
    - `grep -c "Object.hasOwn" .planning/PROJECT.md` returns >= 2
    - `grep -n "djs/parser" .planning/PROJECT.md` — any remaining match states it cannot
      validate a program, never proposes it as a remedy
    - `grep -c "Claude Code" .planning/PROJECT.md README.md` combined returns >= 3
    - `grep -c "Rev. Proc. 2025-32" .planning/PROJECT.md` returns >= 1
  </acceptance_criteria>
  <done>PROJECT.md and README.md contain none of the six false claims.</done>
</task>

<task type="auto">
  <name>Task 2: todo/plan.md</name>
  <files>todo/plan.md</files>
  <read_first>todo/plan.md (full file, 96 lines)</read_first>
  <action>
    1. Settled Decisions row "Import-time safety" ends "Tolerable only because the sole user
       is trusted and local." Replace: accepted on schedule grounds, not trust grounds — the
       untrusted party is the document, not the user; compensating controls are `--permission`
       (SEC-01), an import-specifier allow-list (SEC-02), content-hash-derived filenames
       (SEC-03).

    2. Week 5 bullet "Revisit execution safety: validating source as genuine FunctionalScript
       before `import()`, and/or Worker isolation with hard limits. Week 1 deliberately
       defers both." Replace: `djs/parser` cannot validate an agent-authored program (data-only
       language, no function node); a genuine source validator, if ever wanted, is v2 work, not
       Week 5. Worker/child-process isolation is the other v2 candidate.

    3. In "## Week 2", after the Goal line, add one sentence: TY2025 figures (standard
       deduction, brackets, thresholds) must be sourced from Rev. Proc. 2024-40 as modified
       by Rev. Proc. 2025-32, never the original 2025 inflation-adjustment release.
  </action>
  <verify>
    <automated>grep -n "sole user is trusted and local" todo/plan.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sole user is trusted and local" todo/plan.md` returns nothing
    - `grep -c "Rev. Proc. 2025-32" todo/plan.md` returns >= 1
  </acceptance_criteria>
  <done>todo/plan.md contains neither the trust claim nor an implication that djs/parser
  validates programs, and Week 2 states the correct parameter source.</done>
</task>

<task type="auto">
  <name>Task 3: fjs/todo/implement-mcp-server.md</name>
  <files>fjs/todo/implement-mcp-server.md</files>
  <read_first>fjs/todo/implement-mcp-server.md (full file, 204 lines)</read_first>
  <action>
    1. "### The restricted runner" ends "...Nothing needs to be intercepted or patched — an
       operation that is not in the map simply cannot happen." Replace the last sentence: true
       only if the lookup guard is `Object.hasOwn` against a null-prototype map; a naive `in`
       or `!== undefined` guard admits `Object.prototype` members, and `__defineGetter__`
       against such a guard installs an attacker-controlled getter on the whitelist object
       itself — a reproduced full escape (see `upstream-match-partial-operation-map.md`).

    2. "### Known limitation: import-time execution": replace "Accepted for Week 1 because the
       only user is trusted and local. Week 5 revisits it — most plausibly by parsing the
       source with FunctionalScript's own `djs/parser` before importing, which would enforce
       the language subset rather than assume it." with: accepted for v1 on schedule grounds —
       the untrusted party is the document, not the user; compensating controls are
       `--permission` (SEC-01), an import-specifier allow-list (SEC-02), content-hash-derived
       filenames (SEC-03). `djs/parser` cannot close this — it is a data-only language with no
       function node. A genuine source validator, if ever wanted, is v2 work, not Week 5.

    3. "## Testing" opens "The server itself cannot be proof-tested directly — `casMcpServer`
       has the same problem..." Scope this to the stdio server *process*, and add: this does
       not extend to `fjs_run` — `import()` is reached through the `import_` effect, which
       `fjs/effects/node/virtual` interprets in-memory, so the whole materialize-and-run path
       is fully proof-testable with no real filesystem.
  </action>
  <verify>
    <automated>grep -n "simply cannot happen" fjs/todo/implement-mcp-server.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "simply cannot happen" fjs/todo/implement-mcp-server.md` returns nothing
    - `grep -n "the only user is trusted and local" fjs/todo/implement-mcp-server.md` returns
      nothing
    - `grep -n "fjs_run" fjs/todo/implement-mcp-server.md | grep -i "cannot be proof"` returns
      nothing
    - `grep -c "Object.hasOwn" fjs/todo/implement-mcp-server.md` returns >= 1
  </acceptance_criteria>
  <done>fjs/todo/implement-mcp-server.md contains none of the three false claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Markdown-only edits to already-committed planning docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-A01 | Information Disclosure | PROJECT.md, README.md, todo/plan.md, fjs/todo/implement-mcp-server.md | accept | Text-only edits; no secrets, no new attack surface. |
</threat_model>

<verification>
1. `grep -rn "djs/parser" .planning todo fjs README.md` returns no sentence proposing it as a
   validation remedy.
2. `grep -rn "sole user is trusted and local\|the only user is trusted and local" .planning todo fjs` returns nothing.
3. `npm test` still reports 1 pass / 0 fail; `npx tsc --noEmit` remains clean.
</verification>

<success_criteria>
All six claims corrected in place; nothing else in the four files changed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-planning-document-corrections-and-the-upstream-report/01-01-SUMMARY.md`
</output>
