---
phase: 01-planning-document-corrections-and-the-upstream-report
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fjs/todo/upstream-match-partial-operation-map.md
  - .planning/phases/01-planning-document-corrections-and-the-upstream-report/01-UPSTREAM-ISSUE.md
autonomous: false
requirements: [DOCC-02, SEC-04]

must_haves:
  truths:
    - "fjs/todo/upstream-match-partial-operation-map.md's soundness claim states the Object.hasOwn + null-prototype guard condition."
    - "Its Local workaround section warns that a command in map guard is unsafe, with the reproduced evidence."
    - "Its three numbered upstream fix sketches are byte-identical to their current text."
    - "A public FunctionalScript issue exists for the match prototype-dispatch hole, with the full __defineGetter__ reproduction attached, and its URL is recorded in the repo."
  artifacts:
    - path: "fjs/todo/upstream-match-partial-operation-map.md"
      provides: "Corrected soundness claim, warned workaround section, recorded issue URL"
    - path: ".planning/phases/01-planning-document-corrections-and-the-upstream-report/01-UPSTREAM-ISSUE.md"
      provides: "The exact posted issue body"
  key_links:
    - from: "fjs/todo/upstream-match-partial-operation-map.md Status line"
      to: "github.com/functionalscript/functionalscript issues"
      via: "recorded URL"
      pattern: "github.com/functionalscript/functionalscript/issues/"
---

<objective>
Correct the soundness claim in Sergey's upstream todo file and warn its unsafe workaround
proposal (DOCC-02, differentiated treatment — the three fix sketches are untouched), then
file the public FunctionalScript issue for the `match` prototype-dispatch hole and record its
URL (SEC-04). Filing is human-gated: this publishes a working exploit against shipped 0.40.0
before a fix exists, an explicitly accepted risk per `01-CONTEXT.md`, but the exact text is
reviewed before it goes public.

Output: corrected `fjs/todo/upstream-match-partial-operation-map.md`, a posted GitHub issue,
and its URL recorded in the repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planning-document-corrections-and-the-upstream-report/01-CONTEXT.md
@.planning/phases/01-planning-document-corrections-and-the-upstream-report/guard-check.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the file and draft the issue</name>
  <files>fjs/todo/upstream-match-partial-operation-map.md, .planning/phases/01-planning-document-corrections-and-the-upstream-report/01-UPSTREAM-ISSUE.md</files>
  <read_first>
    fjs/todo/upstream-match-partial-operation-map.md (full file, 65 lines),
    .planning/phases/01-planning-document-corrections-and-the-upstream-report/guard-check.mjs
  </read_first>
  <action>
    In `fjs/todo/upstream-match-partial-operation-map.md` (do not touch "## What the upstream
    fix should look like" — leave its three numbered sketches byte-identical):

    1. "## Why it matters here": "That design is sound — an operation not in the map
       genuinely cannot happen — but its *failure mode* is a raw `TypeError`." Replace: sound
       only if the lookup guard is `Object.hasOwn` against a null-prototype map. A naive
       `command in map` or `map[command] !== undefined` guard is not sound — both return
       `true` for `constructor`, `toString`, `valueOf`, `__defineGetter__`, and
       `__defineGetter__` against such a guard installs an attacker-controlled getter on the
       whitelist object itself, a reproduced full escape. Even with a sound guard, the failure
       mode is still a raw `TypeError`.

    2. "## Local workaround": before the existing "*None yet.*" text, add: a `command in map`
       guard is not safe (`in` admits `constructor`/`__defineGetter__`/`toString`/`valueOf`);
       the correct guard is `Object.hasOwn(map, command)` against a null-prototype map
       (`{ __proto__: null, ... }`).

    Create `01-UPSTREAM-ISSUE.md` in the phase directory: the exact body to file against
    `github.com/functionalscript/functionalscript`. Include: a title naming the hole; the
    `guard-check.mjs` source verbatim in a fenced `js` block; the reproduced table (six probed
    commands against `in`/`!== undefined`/`Object.hasOwn`, per `01-CONTEXT.md`); the concrete
    impact (`__defineGetter__` under an `in` guard installs a getter on the whitelist object,
    reading it runs arbitrary code — verified output `ARBITRARY CODE RAN`); and
    `Object.hasOwn(map, command)` against a null-prototype map named as the fix.
  </action>
  <verify>
    <automated>grep -c "Object.hasOwn" fjs/todo/upstream-match-partial-operation-map.md .planning/phases/01-planning-document-corrections-and-the-upstream-report/01-UPSTREAM-ISSUE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "genuinely cannot happen" fjs/todo/upstream-match-partial-operation-map.md`
      returns nothing unqualified
    - The "## What the upstream fix should look like" section is byte-identical to its
      pre-edit text
    - `01-UPSTREAM-ISSUE.md` exists and contains `Object.hasOwn`, `__defineGetter__`, and
      `ARBITRARY CODE RAN`
  </acceptance_criteria>
  <done>The file states its guard condition and warns the unsafe workaround; the issue draft
  is ready for review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    `01-UPSTREAM-ISSUE.md` — the exact text to be filed as a public GitHub issue against
    `github.com/functionalscript/functionalscript`, disclosing a working prototype-dispatch
    escape (`__defineGetter__`) against shipped `functionalscript@0.40.0`, with the fix named.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/01-planning-document-corrections-and-the-upstream-report/01-UPSTREAM-ISSUE.md`
    and read it in full. Confirm the title and body are exactly what you want posted publicly —
    this is the point where the disclosure becomes irreversible.
  </how-to-verify>
  <resume-signal>Type "approved" to post as-is, or describe the edit needed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Post the issue and record its URL</name>
  <files>fjs/todo/upstream-match-partial-operation-map.md</files>
  <read_first>.planning/phases/01-planning-document-corrections-and-the-upstream-report/01-UPSTREAM-ISSUE.md (post-approval content), fjs/todo/upstream-match-partial-operation-map.md (current Status line)</read_first>
  <action>
    Run `gh issue create --repo functionalscript/functionalscript --title "<title from
    01-UPSTREAM-ISSUE.md>" --body-file .planning/phases/01-planning-document-corrections-and-the-upstream-report/01-UPSTREAM-ISSUE.md`.
    Capture the returned issue URL. Edit `fjs/todo/upstream-match-partial-operation-map.md`'s
    top `Status:` line to record it (e.g. append "Reported upstream: <URL>.").
  </action>
  <verify>
    <automated>grep -n "github.com/functionalscript/functionalscript/issues/" fjs/todo/upstream-match-partial-operation-map.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "github.com/functionalscript/functionalscript/issues/" fjs/todo/upstream-match-partial-operation-map.md` returns >= 1
  </acceptance_criteria>
  <done>The issue is filed and its URL is recorded in the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Repo → public GitHub issue tracker | A working exploit reproduction against shipped functionalscript 0.40.0 crosses from this private planning corpus to a public third-party tracker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-C01 | Information Disclosure | 01-UPSTREAM-ISSUE.md, posted publicly | accept | Explicitly accepted in `01-CONTEXT.md`: publishing before a fix exists is a deliberate choice, weighed against shipping the fix with the report. The human-verify checkpoint (task 2) is the mitigation for posting the *wrong* text, not for the disclosure itself. |
| T-01-C02 | Tampering | The three upstream fix sketches (Sergey's API design) | mitigate | Task 1's acceptance criteria require a byte-identical diff of that section. |
</threat_model>

<verification>
1. `grep -n "genuinely cannot happen" fjs/todo/upstream-match-partial-operation-map.md`
   returns nothing unqualified.
2. `grep -c "github.com/functionalscript/functionalscript/issues/" fjs/todo/upstream-match-partial-operation-map.md` returns >= 1.
3. `npm test` still reports 1 pass / 0 fail; `npx tsc --noEmit` remains clean.
</verification>

<success_criteria>
The soundness claim is corrected, the workaround section is warned, the fix sketches are
unmodified, and a filed public issue's URL is recorded in the repo.
</success_criteria>

<output>
After completion, create `.planning/phases/01-planning-document-corrections-and-the-upstream-report/01-03-SUMMARY.md`
</output>
