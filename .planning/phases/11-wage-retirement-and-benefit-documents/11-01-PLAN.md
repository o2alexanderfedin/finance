---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fjs/document/1099r/module.f.js
  - fjs/document/ssa1099/module.f.js
autonomous: true
requirements: [DOC-08, DOC-09]

must_haves:
  truths:
    - "vnd.fjs.1099r documents round-trip through validate() with every box independently option-absent-able"
    - "vnd.fjs.ssa1099 documents round-trip through validate() with payerTin always the empty string, documented as a deliberate deviation"
    - "1099-R box 7a stores a LIST of distribution codes (option(array(string))), never a (code, amount) pair — distinct from W-2 box 12"
    - "1099-R boxes 14-19 (stateLocal) repeat faithfully as an array, including two entries for the same state, nothing merged or summed"
    - "Every checkbox in both dialects is option(true); false is rejected structurally"
    - "Percentage boxes (1099-R 8b, 9a) are never run through moneyFieldError"
  artifacts:
    - path: "fjs/document/1099r/module.f.js"
      provides: "vnd.fjs.1099r dialect tag, mediaType, oneZeroNineNineRSchema, checkReferences, validate, proof"
      min_lines: 400
    - path: "fjs/document/ssa1099/module.f.js"
      provides: "vnd.fjs.ssa1099 dialect tag, mediaType, ssa1099Schema, checkReferences, validate, proof"
      min_lines: 180
  key_links:
    - from: "fjs/document/1099r/module.f.js's checkReferences"
      to: "fjs/document/money_field/module.f.js's moneyFieldError"
      via: "import + per-box loop over moneyBoxFields and stateLocal's stateLocalMoneyFields"
      pattern: "moneyFieldError\\("
    - from: "fjs/document/ssa1099/module.f.js's checkReferences"
      to: "fjs/document/money_field/module.f.js's moneyFieldError"
      via: "import + per-box loop over moneyBoxFields"
      pattern: "moneyFieldError\\("
    - from: "both new schemas"
      to: "fjs/document/base/module.f.js's base(dialect)"
      via: "spread first in the schema object literal"
      pattern: "\\.\\.\\.base\\(dialect\\)"
---

<objective>
Add the two new document dialects the 65+ profile needs: `vnd.fjs.1099r` (DOC-09) and
`vnd.fjs.ssa1099` (DOC-08). Both follow the existing `fjs/media/revision` four-stage template
(`fjs/document/1099int/module.f.js` is the template; `fjs/document/w2/module.f.js` supplies the
repeating-array and list-of-codes shapes 1099-R needs). This phase STORES AND READS ONLY — nothing
here computes a tax figure, and nothing here touches `fjs/tax/` or `fjs/return/`.

Purpose: every non-brokerage document the declared taxpayer profile produces must be
representable and validated before it can be listed (Plan 11-03) or fed into Phase 13's
computation.
Output: `fjs/document/1099r/module.f.js`, `fjs/document/ssa1099/module.f.js`, each a complete
dialect module with its own `proof`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-wage-retirement-and-benefit-documents/11-CONTEXT.md
@.planning/phases/11-wage-retirement-and-benefit-documents/11-RESEARCH.md
@.planning/phases/11-wage-retirement-and-benefit-documents/11-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- base(dialect) / mediaTypeOf(dialect) — fjs/document/base/module.f.js, read in full -->
<!-- export const base = dialect => ({ dialect })  -- spread FIRST in every schema literal -->
<!-- export const mediaTypeOf = dialect => `application/${dialect}+json` -->

<!-- moneyFieldError — fjs/document/money_field/module.f.js -->
<!-- export const moneyFieldError = label => printed => string | undefined -->
<!-- Do NOT call this on a percentage box (1099-R 8b, 9a) — it enforces the cents-scale exact-decimal rule, not percentages. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `vnd.fjs.1099r` — the full printed box inventory (DOC-09)</name>
  <files>fjs/document/1099r/module.f.js</files>
  <read_first>
    fjs/document/1099int/module.f.js (the four-stage template, verbatim shape to copy)
    fjs/document/w2/module.f.js (repeating-array `stateLocalEntry` shape at lines 65-77/125/200-211/392-421; the `option(true)` checkbox convention at lines 100/112-114/348-356; the generated-proof + hand-typed-count idiom at lines 133-159/262-306)
    fjs/document/base/module.f.js
    fjs/document/money_field/module.f.js
    .planning/phases/11-wage-retirement-and-benefit-documents/11-RESEARCH.md Q1 (complete box inventory table, the TY2025-vs-TY2026 revision diff, the formRevision "Created MM/DD/YY only on Copy A" finding)
    .planning/phases/11-wage-retirement-and-benefit-documents/11-PATTERNS.md section "fjs/document/1099r/module.f.js" (the worked schema block and the box7a/pair-list deviation note)
  </read_first>
  <action>
    Create `fjs/document/1099r/module.f.js` following `fjs/document/1099int/module.f.js`'s exact
    six-stage shape (dialect -> mediaType -> schema `oneZeroNineNineRSchema` -> `validateShape` ->
    `checkReferences` -> composed `validate`). `dialect = 'vnd.fjs.1099r'`.

    Schema fields, in this exact order (base spread first): `payerTin: string`,
    `recipientTin: string`, `accountNumber: string`, `taxYear: number`, `formRevision: string`,
    `corrected: option(true)`, `box1GrossDistribution: option(string)`,
    `box2aTaxableAmount: option(string)`, `box2bTaxableAmountNotDetermined: option(true)`,
    `box2bTotalDistribution: option(true)`, `box3CapitalGain: option(string)`,
    `box4FederalIncomeTaxWithheld: option(string)`,
    `box5EmployeeContribOrInsurancePremiums: option(string)`,
    `box6NuaInEmployerSecurities: option(string)`, `box7aDistributionCodes: option(array(string))`,
    `box7bIraSepSimple: option(true)`, `box7cTrumpAccount: option(true)`,
    `box7dEarningsOnExcessContrib: option(string)`, `box8aOther: option(string)`,
    `box8bPercentageOfAnnuityContract: option(string)`,
    `box9aPercentageOfTotalDistribution: option(string)`, `box9bTotalEmployeeContrib: option(string)`,
    `box10AmountAllocableToIrrWithin5Years: option(string)`,
    `box11FirstYearOfDesigRothContrib: option(string)`, `box12FatcaFilingRequirement: option(true)`,
    `box13DateOfPayment: option(string)`, `stateLocal: option(array(stateLocalEntry))`,
    `payerName: option(string)`, `recipientName: option(string)`.

    `stateLocalEntry` (module-scope const, mirrors `w2Schema`'s `stateLocalEntry`): `state: string`,
    `payerStateNo: option(string)`, `stateTaxWithheld: option(string)`,
    `stateDistribution: option(string)`, `localTaxWithheld: option(string)`,
    `localityName: option(string)`, `localDistribution: option(string)`.

    `moneyBoxFields` (scalar, exactly 10, in this order): `box1GrossDistribution`,
    `box2aTaxableAmount`, `box3CapitalGain`, `box4FederalIncomeTaxWithheld`,
    `box5EmployeeContribOrInsurancePremiums`, `box6NuaInEmployerSecurities`,
    `box7dEarningsOnExcessContrib`, `box8aOther`, `box9bTotalEmployeeContrib`,
    `box10AmountAllocableToIrrWithin5Years`. `expectedMoneyBoxFieldCount = 10`, hand-typed, with the
    same "deliberately NOT `moneyBoxFields.length`" docstring w2/1099int use.

    `stateLocalMoneyFields` (exactly 4): `stateTaxWithheld`, `stateDistribution`,
    `localTaxWithheld`, `localDistribution`. `expectedStateLocalMoneyFieldCount = 4`, hand-typed,
    same idiom.

    Do NOT run `moneyFieldError` on `box8bPercentageOfAnnuityContract`,
    `box9aPercentageOfTotalDistribution` (percentages, not money — RESEARCH.md Pitfall 3), on
    `box11FirstYearOfDesigRothContrib` (a year, not money), or on `box13DateOfPayment` (a date, free
    text) — these four stay plain `option(string)` with no numeric-exactness check this phase.

    `checkReferences`: (1) `formRevision.trim() === ''` rejected, same message shape as
    `1099int`/`w2`; (2) loop `moneyBoxFields` through `moneyFieldError`, exactly like `w2`'s scalar
    loop; (3) loop `r.stateLocal ?? []`, and within each entry loop `stateLocalMoneyFields` through
    `moneyFieldError(`${entry.state} ${field}`)`, exactly like `w2`'s `box15Through20` loop. No
    additional per-code check is required on `box7aDistributionCodes` (unlike w2's box12, there is
    no attached amount to make an empty code meaningless).

    Module docstring MUST name both sourced URLs
    (`https://www.irs.gov/pub/irs-pdf/f1099r.pdf` — 2026 revision, modeled here;
    `https://www.irs.gov/pub/irs-prior/f1099r--2025.pdf` — 2025/TY2025 revision, diffed against
    it) and state explicitly: box 7c/7d and the 8a/8b split are 2026-only additions, absent on
    every real TY2025 document (harmless — `option`-typed); and that `formRevision` on a
    recipient's actual copy (Copy B/C/2) carries no "Created" date string, only the tax-year
    watermark, so the ingestion pass records whatever revision text is ACTUALLY printed rather than
    deriving `formRevision` from `taxYear` in code (DOC-10 forbids the derivation).

    Proof suite (`export const proof = {...}`), mirroring `w2`'s proof structure: `dialectAndMediaType`;
    `validate.fullyPopulatedValidates` (every box populated, including two `box7aDistributionCodes`
    entries e.g. `['7','G']`, and a `stateLocal` array with two entries for the SAME state);
    `validate.blankBoxesOmittedValidates`; `validate.falseFlagsRejected` (all six `option(true)`
    fields: `corrected`, `box2bTaxableAmountNotDetermined`, `box2bTotalDistribution`,
    `box7bIraSepSimple`, `box7cTrumpAccount`, `box12FatcaFilingRequirement`);
    `validate.wrongDialectRejected` (asserts `path[0] === 'dialect'`); `box7a.repeatedCodeIsPreserved`
    (two codes survive as two array entries, order preserved); `stateLocal.multipleRowsIncludingRepeatedStateRoundTrip`
    (three rows, two sharing `state: 'CA'`, asserting `v.stateLocal?.length === 3` and neither CA row
    is merged/summed); `stateLocal.inexactAmountRejected`; `checkReferences.emptyFormRevisionRejected`;
    `checkReferences.moneyBoxExactness` (generated leaf per `moneyBoxFields` entry + `everyMoneyBoxIsCovered`
    against `expectedMoneyBoxFieldCount`); `checkReferences.stateLocalMoneyExactness` (generated leaf
    per `stateLocalMoneyFields` entry + `everyFieldIsCovered` against `expectedStateLocalMoneyFieldCount`);
    `checkReferences.percentageBoxesAcceptAnyStringWithoutExactnessCheck` (a percentage box carrying
    e.g. `'12.345'` — more than two decimal places — still validates ok, proving it is genuinely
    exempt from `moneyFieldError`, not merely untested).
  </action>
  <acceptance_criteria>
    - `fjs/document/1099r/module.f.js` exports `dialect === 'vnd.fjs.1099r'`, `mediaType === 'application/vnd.fjs.1099r+json'`, `oneZeroNineNineRSchema`, `checkReferences`, `validate`, `proof`.
    - `grep -c 'option(' fjs/document/1099r/module.f.js` reports every box field as `option(...)` (DOC-11) except the five required identity/key fields (`payerTin`, `recipientTin`, `accountNumber`, `taxYear`, `formRevision`).
    - `grep -c "moneyFieldError(" fjs/document/1099r/module.f.js` is greater than 0 and the two percentage box names never appear on the same line as `moneyFieldError`.
    - `grep -n "expectedMoneyBoxFieldCount = 10" fjs/document/1099r/module.f.js` and `grep -n "expectedStateLocalMoneyFieldCount = 4" fjs/document/1099r/module.f.js` both match.
    - Module docstring (`grep -n "irs-prior/f1099r--2025.pdf" fjs/document/1099r/module.f.js`) names the 2025 revision URL.
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; the project-local proof count (baseline 492, measured immediately before this task) increases; every acceptance criterion above is confirmed by the grep commands shown, not by inspection alone.</done>
</task>

<task type="auto">
  <name>Task 2: `vnd.fjs.ssa1099` — sourced from Pub 915, not an IRS form (DOC-08)</name>
  <files>fjs/document/ssa1099/module.f.js</files>
  <read_first>
    fjs/document/1099int/module.f.js (the four-stage template — same shape, much smaller field set)
    fjs/document/base/module.f.js
    fjs/document/money_field/module.f.js
    .planning/phases/11-wage-retirement-and-benefit-documents/11-RESEARCH.md Q2 (complete box
    inventory from IRS Pub 915 (2025) Appendix pp.20-22, the "this is a SAMPLE, not a blank form"
    caution, the payerTin-has-no-printed-value finding)
    .planning/phases/11-wage-retirement-and-benefit-documents/11-PATTERNS.md section
    "fjs/document/ssa1099/module.f.js"
  </read_first>
  <action>
    Create `fjs/document/ssa1099/module.f.js`, `dialect = 'vnd.fjs.ssa1099'`, same six-stage shape.

    Schema fields, in this order (base spread first): `payerTin: string` (ALWAYS stored as `''` —
    SSA-1099 prints no payer TIN anywhere; document this as a deliberate deviation, not an
    oversight — do not invent a fake SSA EIN), `recipientTin: string` (Box 2, beneficiary's SSN),
    `accountNumber: string` (Box 8, the Claim Number — Pub 915 directs using this number for any
    SSA correspondence, the natural fit for this project's subject-key slot), `taxYear: number`,
    `formRevision: string`, `corrected: option(true)` (kept for structural consistency with every
    other dialect; document LOW confidence it is ever populated — the Pub 915 sample shows no
    CORRECTED box), `box3BenefitsPaid: option(string)` (Box 3, money), `box3Description:
    option(string)` (free text, "Description of Amount in Box 3" — never parsed into line items),
    `box4BenefitsRepaid: option(string)` (Box 4, money), `box4Description: option(string)` (free
    text), `box5NetBenefits: option(string)` (Box 5, money — Box 3 minus Box 4, printed on the form,
    never computed here), `box6VoluntaryFederalIncomeTaxWithheld: option(string)` (Box 6, money),
    `recipientName: option(string)` (Box 1, "Name"). Do NOT model Box 7 (Address — PII with no
    computational use, mirrors the W-2 box 9/14 omission precedent).

    `moneyBoxFields` (exactly 4, in this order): `box3BenefitsPaid`, `box4BenefitsRepaid`,
    `box5NetBenefits`, `box6VoluntaryFederalIncomeTaxWithheld`. `expectedMoneyBoxFieldCount = 4`,
    hand-typed, same "deliberately NOT `.length`" docstring idiom.

    `checkReferences`: (1) `formRevision.trim() === ''` rejected; (2) loop `moneyBoxFields` through
    `moneyFieldError`. `box3Description`/`box4Description`/`recipientName` are plain optional
    strings, never validated beyond structural `option(string)` — they are never computed on.

    Module docstring MUST state plainly, with the exact source: SSA-1099 is issued by the Social
    Security Administration, NOT the IRS — there is no `irs.gov/pub/irs-pdf/` blank form for it.
    Source: IRS Publication 915 (2025), Appendix, pages 20-22,
    `https://www.irs.gov/pub/irs-pdf/p915.pdf` — an illustrated SAMPLE the publication itself
    labels as such ("Caution: The illustrated versions... are proof copies"), not a guaranteed-
    current blank form. Also document the `payerTin: ''` deviation and the `accountNumber` ->
    Box 8 Claim Number mapping, each with its own one-line rationale.

    Proof suite: `dialectAndMediaType`; `validate.fullyPopulatedValidates` (every box populated,
    including both description fields); `validate.blankBoxesOmittedValidates`;
    `validate.correctedFalseRejected`; `validate.wrongDialectRejected`
    (`path[0] === 'dialect'`); `validate.payerTinEmptyStringRoundTrips` (a minimal fixture with
    `payerTin: ''` validates ok and reads back `''`, pinning the deliberate-deviation convention as
    a proof, not merely a docstring claim); `checkReferences.emptyFormRevisionRejected`;
    `checkReferences.moneyBoxExactness` (generated leaf per `moneyBoxFields` entry +
    `everyMoneyBoxIsCovered` against `expectedMoneyBoxFieldCount`); `checkReferences.descriptionFieldsStoreVerbatim`
    (a `box3Description`/`box4Description` string containing arbitrary punctuation round-trips
    unchanged — proves "never parsed", not merely "accepted").
  </action>
  <acceptance_criteria>
    - `fjs/document/ssa1099/module.f.js` exports `dialect === 'vnd.fjs.ssa1099'`, `mediaType === 'application/vnd.fjs.ssa1099+json'`, `ssa1099Schema`, `checkReferences`, `validate`, `proof`.
    - `grep -n "irs-pdf/p915.pdf" fjs/document/ssa1099/module.f.js` matches — the docstring names the real source.
    - `grep -n "expectedMoneyBoxFieldCount = 4" fjs/document/ssa1099/module.f.js` matches.
    - A fixture with `payerTin: ''` validates `'ok'` (proven by `validate.payerTinEmptyStringRoundTrips`, not merely asserted in prose).
    - `grep -n "box7\|Address" fjs/document/ssa1099/module.f.js` — Box 7 (address) is absent from the schema, confirmed by its absence from the field list.
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>`npx tsc --noEmit` exits 0; the project-local proof count increases again over Task 1's post-task count; every acceptance criterion above is confirmed by the grep/proof commands shown.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation Gate M2 — prove the hand-typed box-count constants are load-bearing (BLOCKING)</name>
  <files>fjs/document/1099r/module.f.js</files>
  <read_first>
    fjs/document/1099r/module.f.js (as written by Task 1 — the `moneyBoxFields` array, the
    `expectedMoneyBoxFieldCount` constant, and the `checkReferences.moneyBoxExactness` proof
    group's `everyMoneyBoxIsCovered` leaf)
    .planning/phases/11-wage-retirement-and-benefit-documents/11-VALIDATION.md "Mutation Gate"
    section (M2's exact expected outcome: "Remove one box from a new dialect's schema" ->
    "The hand-typed count assertion turns RED")
    AGENTS.md "A proof is not known to work until you have watched it fail" section
    .planning/phases/11-wage-retirement-and-benefit-documents/11-02-PLAN.md Task 2's Mutation Gate
    M1 treatment (the rigor and numbered-step format mirrored verbatim below)
  </read_first>
  <action>
    Schema-GENERATED coverage (the per-field leaf built by iterating `moneyBoxFields` itself) grows
    automatically when a box is added, so it structurally cannot detect a box REMOVED — only the
    independent hand-typed `expectedMoneyBoxFieldCount` constant can. The mutation must therefore be
    a removal, run against Task 1's `vnd.fjs.1099r` dialect (the idiom is identical in
    `vnd.fjs.ssa1099`; one watched mutation of the idiom is sufficient per `11-VALIDATION.md`'s M2
    row, which names "a new dialect's schema" generically rather than requiring one run per dialect).

    **Mutation Gate M2 (BLOCKING — run this now, recorded, before considering this plan done):**
    1. Temporarily remove the entry `box6NuaInEmployerSecurities` from the `moneyBoxFields` array in
       `fjs/document/1099r/module.f.js`. Leave `expectedMoneyBoxFieldCount = 10` UNCHANGED — that
       hand-typed constant is exactly what this gate proves is load-bearing.
    2. Run `node --test 2>&1 | grep -c '^✔ import("./fjs/'` and confirm the count DROPS relative to
       the post-Task-2 count, and specifically that `checkReferences.moneyBoxExactness`'s
       `everyMoneyBoxIsCovered` leaf (or the plan's actual leaf name, if it differs after Task 1's
       implementation) fails. Record the failing leaf name and the actual command output in the
       SUMMARY — not "verified".
    3. Restore the removed entry EXACTLY (re-add `box6NuaInEmployerSecurities` at its original array
       position — do not retype the file from memory; use editor undo or a fresh diff against the
       committed Task 1 state). Re-run `node --test 2>&1 | grep -c '^✔ import("./fjs/'` and confirm
       the count returns to (at least) the post-Task-2 count.
    4. Run `git status --porcelain` and confirm it is empty — no stray leftover edits from the
       remove/restore cycle.
    5. Record every command's actual output in `11-01-SUMMARY.md` — the real terminal output, not a
       paraphrase.
  </action>
  <acceptance_criteria>
    - Mutation Gate M2 was actually run: the SUMMARY records the RED leaf name obtained while
      `box6NuaInEmployerSecurities` was removed from `moneyBoxFields`, and the final
      `git status --porcelain` clean confirmation after restoring it.
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` after this task, with the field restored, is
      at least equal to the count recorded at the end of Task 2.
    - `grep -n "expectedMoneyBoxFieldCount = 10" fjs/document/1099r/module.f.js` still matches
      (the hand-typed constant itself was never touched by this gate, only the array it checks
      against).
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/' && git status --porcelain</automated>
  </verify>
  <done>Mutation Gate M2's five steps are all recorded in `11-01-SUMMARY.md` with actual command output (not paraphrased); the removed field is restored exactly; `git status --porcelain` is empty; `npx tsc --noEmit` exits 0; the project-local proof count is at least the count recorded at the end of Task 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ingestion -> document dialect `validate()` | An OCR/agent-assembled document object (potentially malformed, mis-scaled, or wrong-dialect) crosses into this project's typed store here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | `fjs/document/1099r/module.f.js`, `fjs/document/ssa1099/module.f.js` `checkReferences` | mitigate | Every present money box is re-validated through `moneyFieldError` (exact decimal, safe magnitude); a comma-grouped or non-canonical amount is refused, never silently coerced. Proven by the generated-leaf-per-field + independently hand-typed count idiom, not merely by one hand-written example — and the hand-typed count's load-bearing role is confirmed by a watched-red mutation (Mutation Gate M2). |
| T-11-01-02 | Tampering | Percentage boxes (1099-R 8b, 9a) | mitigate | These are explicitly EXCLUDED from `moneyFieldError` (a cents-scale check would wrongly reject or misrepresent a legitimate percentage) — the exclusion is itself proven by `percentageBoxesAcceptAnyStringWithoutExactnessCheck`, so the gap is a documented, tested design choice, not an unnoticed hole. |
| T-11-01-03 | Repudiation | Both dialects' `corrected: option(true)` | accept | `false` is structurally unrepresentable (rejected by the RTTI type itself, not a runtime check) — mirrors DOC-12's existing precedent; no new risk introduced. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after each task.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly increases after Task 1 and Task 2 (baseline 492 before this plan).
- Mutation Gate M2 watched RED, then restored GREEN, with `git status --porcelain` empty — recorded in the SUMMARY, not merely asserted.
- `npm test` (full suite) green after all tasks land.
</verification>

<success_criteria>
- `fjs/document/1099r/module.f.js` and `fjs/document/ssa1099/module.f.js` exist, export a complete
  four/six-stage dialect (dialect, mediaType, schema, validate, checkReferences, proof), and every
  box is independently `option`-absent-able.
- Every money box in both dialects is proven exact-decimal via the generated + hand-typed-count
  idiom; no percentage box is misrouted through `moneyFieldError`.
- 1099-R box 7a is a list of code strings (never a `(code, amount)` pair); boxes 14-19 repeat as a
  faithful array including two entries for the same state.
- Mutation Gate M2 was run and its outcome recorded: removing a box from `moneyBoxFields` reddens
  the hand-typed count's coverage assertion; restoring it returns the suite to green with a clean
  `git status`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-wage-retirement-and-benefit-documents/11-01-SUMMARY.md`
</output>
