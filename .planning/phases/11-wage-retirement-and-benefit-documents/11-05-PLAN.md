---
phase: 11
plan: 05
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - fjs/server/module.f.js
  - fjs-run-integration.test.js
autonomous: true
requirements: [MCP-08, TEST-03]

must_haves:
  truths:
    - "finance_documents_list is advertised via tools/list and is actually callable in the same real, separate-process session"
    - "fjs-run-integration.test.js's toolsCalled set equals advertisedTools after finance_documents_list is registered — no tool is advertised without real-process coverage"
  artifacts:
    - path: "fjs/server/module.f.js"
      provides: "financeMcpHandlers including financeDocumentsListTool"
      contains: "financeDocumentsListTool"
    - path: "fjs-run-integration.test.js"
      provides: "a call('finance_documents_list', ...) invocation, landed in the SAME commit as the registry change"
      contains: "finance_documents_list"
  key_links:
    - from: "fjs/server/module.f.js's financeMcpHandlers"
      to: "fjs/server/finance_documents_list/module.f.js's financeDocumentsListTool"
      via: "import + array entry between financeTaxParamsTool and fjsRunTool"
      pattern: "financeDocumentsListTool"
    - from: "fjs-run-integration.test.js's toolsCalled"
      to: "the assert.equal(...) against advertisedTools at the end of the test"
      via: "call('finance_documents_list', ...) added in the SAME commit as the registry entry"
      pattern: "finance_documents_list"
---

<objective>
Register `finance_documents_list` (Plan 11-03's tool) into `financeMcpHandlers`, and add its
real-process integration coverage to `fjs-run-integration.test.js`, IN THE SAME COMMIT. This is a
non-negotiable ordering constraint (Phase 08-04's own precedent, re-verified in
`11-RESEARCH.md` Q4): `fjs-run-integration.test.js` derives its advertised/called tool sets from a
LIVE `tools/list` response and every `call(...)` actually made, then asserts the two sets are
equal. The moment `financeMcpHandlers` advertises `finance_documents_list`, that assertion fails
unless this SAME test file also calls it — a registry-only commit breaks `npm test` immediately.

Purpose: TEST-03's standing rule — a tool is not "done" when its virtual proofs are green, only
when a real, separate-process client has actually called it.
Output: `fjs/server/module.f.js` (registers the tool, extends
`proof.session.toolsListEnumeratesComposedRegistry`) and `fjs-run-integration.test.js` (adds the
real-process call), landed together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@fjs/server/module.f.js
@fjs/server/finance_documents_list/module.f.js
@fjs-run-integration.test.js

<interfaces>
<!-- From fjs/server/finance_documents_list/module.f.js (Plan 11-03's output):
     export const financeDocumentsListTool = evo => cas => toolEntry('finance_documents_list', ...) -->
<!-- fjs/server/module.f.js's CURRENT financeMcpHandlers (before this plan's edit):
     export const financeMcpHandlers = home => cacheKey => fromRegistry([
       ...casToolRegistry(home)(cacheKey),
       ...evoToolRegistry(evo(fileCas(sha256)(home))(cacheKey)),
       casRefreshTool(fileCas(sha256)(home))(cacheKey),
       financeSchemaTool,
       financeTaxParamsTool,
       fjsRunTool(home)(fileCas(sha256)(home))(evo(fileCas(sha256)(home))(cacheKey)),
     ]) -->
<!-- fjs-run-integration.test.js's toolsCalled/advertisedTools mechanism (lines ~197-230, ~484-492):
     const toolsCalled = new Set()
     const call = async (name, args) => { ...; toolsCalled.add(name); return waitForId(id) }
     const advertisedTools = new Set(listResponse.result.tools.map(t => t.name))
     assert.equal([...toolsCalled].sort().join(','), [...advertisedTools].sort().join(','), ...) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register `finance_documents_list` AND add its real-process call — ONE commit</name>
  <files>fjs/server/module.f.js, fjs-run-integration.test.js</files>
  <read_first>
    fjs/server/module.f.js (the whole file — already read in planning; `financeMcpHandlers` at
    lines 141-148, the import block at lines 75-83, `proof.session.toolsListEnumeratesComposedRegistry`
    at lines 369-378)
    fjs-run-integration.test.js (already read in planning: the `docAHash`/`docBHash`/`docCHash` and
    `subjectA`/`subjectB`/`subjectC` seeding at lines ~237-280; the `finance_schema`/
    `finance_tax_params` call blocks at lines ~285-304, the exact style to copy; the
    `toolsCalled`/`advertisedTools` equality assertion at lines ~484-492 — the hard ordering
    constraint this task exists to satisfy)
    fjs/server/finance_documents_list/module.f.js (Plan 11-03's output — the exact tool name and
    handler shape)
    .planning/phases/11-wage-retirement-and-benefit-documents/11-RESEARCH.md Q4, "The same-commit
    ordering constraint" (re-verified against the actual test file, quoting the exact assertion)
  </read_first>
  <action>
    In `fjs/server/module.f.js`: add `import { financeDocumentsListTool } from
    './finance_documents_list/module.f.js'` alongside the existing `financeSchemaTool`/
    `financeTaxParamsTool` imports (lines 75-76). In `financeMcpHandlers`, insert
    `financeDocumentsListTool(evo(fileCas(sha256)(home))(cacheKey))(fileCas(sha256)(home))` between
    `financeTaxParamsTool` and `fjsRunTool(...)` in the array literal — reusing the SAME
    `evo(fileCas(sha256)(home))(cacheKey)` and `fileCas(sha256)(home)` expressions the surrounding
    lines already construct, never re-derived differently. Extend
    `proof.session.toolsListEnumeratesComposedRegistry` with
    `assert(tools.some(t => t.name === 'finance_documents_list'))`, mirroring the existing
    `finance_schema`/`finance_tax_params` assertions on the same lines — this is the in-process
    (virtual) half of registry coverage; the real-process half is the second file below.

    In `fjs-run-integration.test.js`, add a `call('finance_documents_list', {})` (or `{ archived:
    true }`) invocation, placed anywhere AFTER `subjectA`/`subjectB`/`subjectC` and
    `docAHash`/`docBHash`/`docCHash` are already in scope (they are seeded starting at line ~258,
    well before the tool-coverage section at the end) and BEFORE the final `toolsCalled`/
    `advertisedTools` equality assertion. Mirror the `finance_tax_params` call block's exact style:
    `const documentsListResponse = await call('finance_documents_list', {})`, then
    `assert.ok(!documentsListResponse.result.isError, ...)`, then a CONCRETE assertion about the
    response shape — parse `documentsListResponse.result.content[0].text` as JSON and assert it is
    an array that includes an entry whose `subject` is one of `subjectA`/`subjectB`/`subjectC`
    (already-seeded, real subjects from earlier in this SAME session) — never merely "the call did
    not error".

    Both edits land in ONE commit. Do not commit `fjs/server/module.f.js`'s registry change without
    this test file's new call in the same commit — per the non-negotiable ordering constraint, a
    registry-only commit breaks `npm test` immediately via the `toolsCalled`/`advertisedTools`
    equality assertion.
  </action>
  <acceptance_criteria>
    - `grep -n "financeDocumentsListTool" fjs/server/module.f.js` shows both the import and its use inside `financeMcpHandlers`'s array literal.
    - `grep -n "finance_documents_list" fjs/server/module.f.js` also shows it inside `proof.session.toolsListEnumeratesComposedRegistry`'s assertions.
    - `grep -n "call('finance_documents_list'" fjs-run-integration.test.js` matches, and the surrounding assertion parses the response and checks a concrete field (e.g. a known subject), not merely `!isError`.
    - `git status --porcelain -- fjs/server/module.f.js fjs-run-integration.test.js` shows both files modified before the commit, and `git ls-tree HEAD -- fjs/server/module.f.js fjs-run-integration.test.js` (run AFTER committing) shows both files' new blob hashes — confirming neither was silently dropped from the commit (AGENTS.md's "verify with `git ls-tree HEAD` AFTER committing, never `git diff --cached` before" rule).
    - `npm test` passes (the real-process integration suite, which the fast per-task grep-count metric does not exercise).
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Both files are modified and committed together (verified via `git ls-tree HEAD` after the commit, not `git diff --cached` before); `npm test` passes, including the `toolsCalled`/`advertisedTools` equality assertion in `fjs-run-integration.test.js`; the virtual `proof.session.toolsListEnumeratesComposedRegistry` also names `finance_documents_list`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (real, separate process) -> `financeMcpHandlers`'s composed registry | The point where an actually-advertised tool must be actually reachable, over the real stdio transport, not merely inside a virtual harness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Denial of Service / broken contract (a tool advertised but unreachable, or shipped without integration coverage) | `financeMcpHandlers`, `fjs-run-integration.test.js` | mitigate | The registry entry and the integration test's `call('finance_documents_list', ...)` land in the SAME commit, enforced mechanically by the test's own `toolsCalled`/`advertisedTools` equality assertion — a registry-only commit fails `npm test` immediately, which is the intended fail-fast behavior (TEST-03), not a bug to work around. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test` (full suite, `tsc && node --test`, including the real-process integration test) passes.
- `git ls-tree HEAD -- fjs/server/module.f.js fjs-run-integration.test.js`, run AFTER committing, shows both files' updated blobs.
</verification>

<success_criteria>
- `finance_documents_list` is advertised via `tools/list` and successfully called in the same real,
  separate-process session `fjs-run-integration.test.js` drives.
- The `toolsCalled`/`advertisedTools` equality assertion passes with the new tool included on both
  sides.
</success_criteria>

<output>
After completion, create `.planning/phases/11-wage-retirement-and-benefit-documents/11-05-SUMMARY.md`
</output>
