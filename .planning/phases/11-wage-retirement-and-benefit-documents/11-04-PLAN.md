---
phase: 11
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - fjs/server/finance_schema/module.f.js
autonomous: true
requirements: [DOC-08, DOC-09]

must_haves:
  truths:
    - "finance_schema resolves vnd.fjs.1099r and returns oneZeroNineNineRSchema's own toJsonSchema output"
    - "finance_schema resolves vnd.fjs.ssa1099 and returns ssa1099Schema's own toJsonSchema output"
    - "Deleting either new dialect's entry from dialectSchemas is caught by the independently hand-typed count, not merely by its own now-missing *Resolves leaf"
  artifacts:
    - path: "fjs/server/finance_schema/module.f.js"
      provides: "dialectSchemas registering 7 dialects; expectedKnownDialectCount === 7"
      contains: "expectedKnownDialectCount = 7"
  key_links:
    - from: "dialectSchemas"
      to: "fjs/document/1099r/module.f.js's oneZeroNineNineRSchema and fjs/document/ssa1099/module.f.js's ssa1099Schema"
      via: "import + map entry, keyed by each module's own exported dialect tag"
      pattern: "oneZeroNineNineRSchema|ssa1099Schema"
---

<objective>
Register the two new dialects (from Plan 11-01) into `finance_schema`'s `dialectSchemas` lookup
map, so an agent can read `vnd.fjs.1099r`'s and `vnd.fjs.ssa1099`'s field names through the same
MCP-06 tool every other dialect already uses — never guessing them.

Purpose: a new dialect that is not discoverable through `finance_schema` is not usable the way
every prior dialect is; this closes that gap for DOC-08/DOC-09 the moment their schema files exist.
Output: `fjs/server/finance_schema/module.f.js`, modified — `dialectSchemas` grows from 5 to 7
entries, in ONE atomic edit alongside its own hand-typed count and per-dialect resolve proof (this
file's own docstring documents that a newly registered dialect is NOT covered "for free" by doing
only one of the three — see Task 1's action for why this is a single non-negotiable task).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@fjs/server/finance_schema/module.f.js
@fjs/document/1099r/module.f.js
@fjs/document/ssa1099/module.f.js

<interfaces>
<!-- From fjs/document/1099r/module.f.js (Plan 11-01's output):
     export const dialect = 'vnd.fjs.1099r'
     export const oneZeroNineNineRSchema = /** @type {const} */ ({ ...base(dialect), ... }) -->
<!-- From fjs/document/ssa1099/module.f.js (Plan 11-01's output):
     export const dialect = 'vnd.fjs.ssa1099'
     export const ssa1099Schema = /** @type {const} */ ({ ...base(dialect), ... }) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The atomic three-part `dialectSchemas` bump (register + count + resolve proofs)</name>
  <files>fjs/server/finance_schema/module.f.js</files>
  <read_first>
    fjs/server/finance_schema/module.f.js (the whole file — already read in planning; re-read now,
    especially `expectedKnownDialectCount`'s own docstring at lines 76-97, which names the EXACT
    mutation-verified trap this task exists to avoid: "`unknownDialectRefused` loops over
    `knownDialects` — `Object.keys(dialectSchemas)`, the code under test — so deleting an entry
    deletes it from the loop's own iteration set at the same instant, and that proof stays green
    over a dialect that quietly stopped being served")
    fjs/document/1099r/module.f.js, fjs/document/ssa1099/module.f.js (Plan 11-01's output — read
    the exact `dialect`/schema-const export names to import)
  </read_first>
  <action>
    This is ONE task and MUST be one commit — the file's own docstring documents that doing only
    one or two of the following three edits ships a false green (this exact defect class has
    shipped in this repo's history via `finance_schema`'s own `unknownDialectRefused`/
    `returnProfileResolves` pair, per AGENTS.md's mutation-sweep case study). Do all three:

    1. **Register**: import `dialect as oneZeroNineNineRDialect, oneZeroNineNineRSchema` from
       `'../../document/1099r/module.f.js'` and `dialect as ssa1099Dialect, ssa1099Schema` from
       `'../../document/ssa1099/module.f.js'`, alongside the existing five-dialect import block
       (lines 44-48). Add `[oneZeroNineNineRDialect]: oneZeroNineNineRSchema` and
       `[ssa1099Dialect]: ssa1099Schema` as two new entries in the `dialectSchemas` object literal
       (lines 65-71).
    2. **Bump the hand-typed count**: change `const expectedKnownDialectCount = 5` to `= 7`. Update
       the docstring's own worked example (currently says "Raise it in the same commit that
       registers a sixth dialect" — this commit registers the SIXTH AND SEVENTH; adjust the prose
       to say so, do not leave it describing only one of the two).
    3. **Add a `*Resolves` proof leaf per new dialect**: `oneZeroNineNineRResolves` and
       `ssa1099Resolves`, each following the EXACT existing shape (e.g.
       `oneZeroNineNineIntResolves`, lines 161-168): `call('vnd.fjs.1099r')` /
       `call('vnd.fjs.ssa1099')`, assert `result.isError === undefined`, assert
       `JSON.stringify(JSON.parse(textOf(result))) === JSON.stringify(toJsonSchema(<the imported
       schema const, called DIRECTLY>))` — never a hand-written JSON literal (that reintroduces the
       second-source-of-truth MCP-06 exists to eliminate, per this file's own header).

    The `unknownDialectRefused` leaf needs NO change — it already loops over `knownDialects`
    (`Object.keys(dialectSchemas)`), which picks up the two new tags automatically once step 1
    lands. Confirm this by inspection, do not add a redundant assertion for it.
  </action>
  <acceptance_criteria>
    - `grep -n "expectedKnownDialectCount = 7" fjs/server/finance_schema/module.f.js` matches.
    - `grep -n "oneZeroNineNineRResolves\|ssa1099Resolves" fjs/server/finance_schema/module.f.js` shows both new proof leaf names.
    - `grep -c "\\[.*Dialect\\]:" fjs/server/finance_schema/module.f.js` (the `dialectSchemas` entries) reports 7.
    - Neither new `*Resolves` leaf's expected side is a hand-written JSON string literal — each calls `toJsonSchema` on the imported schema const directly (grep for `toJsonSchema(oneZeroNineNineRSchema)` and `toJsonSchema(ssa1099Schema)`).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/jobs4alex/sergey-shandar/finance && npx tsc --noEmit && node --test 2>&1 | grep -c '^✔ import("./fjs/'</automated>
  </verify>
  <done>All three edits land together; `expectedKnownDialectCount === 7`; both new `*Resolves` leaves pass; `unknownDialectRefused` still passes unchanged; the project-local proof count increases by at least 2 over the pre-task baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `finance_schema`'s request-supplied `dialect` string -> `dialectSchemas` lookup | An arbitrary, untrusted string names which schema to serve; already mitigated upstream by the existing open-string-keyed-map + `errorResult` refusal pattern this task extends, not replaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering (of the test suite's own coverage) | `dialectSchemas`, `expectedKnownDialectCount` | mitigate | A dialect silently dropped from `dialectSchemas` (now or in a future edit) is caught by the independently hand-typed `expectedKnownDialectCount`, which is NOT derived from `dialectSchemas`/`knownDialects` and therefore cannot shrink in lockstep with an accidental deletion — per this file's own documented mutation-verified trap and AGENTS.md's four-times-shipped defect class. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly increases (baseline: post-Wave-1 count).
- `npm test` (full suite) green.
</verification>

<success_criteria>
- `dialectSchemas` registers all 7 dialects; `expectedKnownDialectCount === 7`; both new dialects
  have their own `*Resolves` proof leaf comparing against `toJsonSchema` called directly on their
  own schema const.
</success_criteria>

<output>
After completion, create `.planning/phases/11-wage-retirement-and-benefit-documents/11-04-SUMMARY.md`
</output>
