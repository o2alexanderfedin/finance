---
phase: 15
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - year-genericity-gate.test.js
  - fjs/document/prior_year_capital_loss/module.f.js
  - fjs/tax/carryover/module.f.js
autonomous: true
requirements: [TAX-17]

must_haves:
  truths:
    - "A prior-year capital loss carryover is recorded as a transcribed fact from a filed return (four figures), never as a supplied carryover total."
    - "A carryover document that exists but has a missing or inconsistent required field is refused, naming the field — TAX-16's honest-refusal rule applied to this new dialect."
    - "The Capital Loss Carryover Worksheet's short-term and long-term outputs are computed from the four figures alone, matching the printed IRS worksheet's 13 lines exactly, verified against independently hand-computed worked examples."
    - "No bare year-comparison (a hardcoded 2025 branch) exists anywhere in the tax/schedule/form computation paths, mechanically enforced with a positive control."
  artifacts:
    - path: "fjs/document/prior_year_capital_loss/module.f.js"
      provides: "the vnd.fjs.prior_year_capital_loss document dialect: four required signed-decimal figures"
      min_lines: 60
    - path: "fjs/tax/carryover/module.f.js"
      provides: "capitalLossCarryoverWorksheet — the 13-line IRS worksheet, pure bigint arithmetic"
      min_lines: 60
    - path: "year-genericity-gate.test.js"
      provides: "mechanical gate: no bare year-comparison branch in fjs/, with a positive control"
  key_links:
    - from: "fjs/tax/carryover/module.f.js"
      to: "fjs/document/prior_year_capital_loss/module.f.js"
      via: "the worksheet's four cents inputs, derived via centsFromString from the dialect's four fields"
      pattern: "centsFromString"
    - from: "year-genericity-gate.test.js"
      to: "fjs/tax/params/module.f.js, fjs/server/finance_tax_params/module.f.js"
      via: "explicit acknowledgment that these two sites legitimately hold a 2025 map key"
      pattern: "taxParamsByYear|taxParamsResponses"
---

<objective>
Lay the two foundational, independently-verifiable pieces of TAX-17: the new
`vnd.fjs.prior_year_capital_loss` document dialect (a transcribed prior-year fact, never an
asserted carryover total) and the Capital Loss Carryover Worksheet itself (pure arithmetic over
exactly the four prior-year figures the printed IRS worksheet consumes — no tax-year parameters,
no TY2024 transcription). Also land the no-bare-2025 mechanical gate, scoped to actual defect
shapes rather than a blanket text scan, so every later wave's carryover-wiring work is protected
from the day it lands.

Purpose: prove the carryover computation is correct BEFORE it is wired into Schedule D (Plan
15-05), and prove year-genericity is a structural property this codebase already has, not
something this phase must retrofit.
Output: `fjs/document/prior_year_capital_loss/module.f.js`, `fjs/tax/carryover/module.f.js`,
`year-genericity-gate.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-realism-polish-and-upstream/15-CONTEXT.md
@.planning/phases/15-realism-polish-and-upstream/15-RESEARCH.md
@AGENTS.md
@magi-gate.test.js
@fjs/document/1099b/module.f.js
@fjs/document/medical_expenses/module.f.js
@fjs/document/money_field/module.f.js
@fjs/document/base/module.f.js
@fjs/schedule/d/module.f.js
</context>

<interfaces>
<!-- Hand-computed worked examples for the worksheet — independent of the code under test, per
     AGENTS.md. All three are derived directly from the IRS's own "Capital Loss Carryover
     Worksheet—Lines 6 and 14" (2025 Instructions for Schedule D, p.10, fetched directly). -->

The worksheet's 13 printed lines, transcribed verbatim (i1040sd.pdf, 2025 revision, p.10):
1. Amount from prior-year Form 1040/1040-SR/1040-NR line 15 (enclose in parens if it would have
   been a loss).
2. The loss from prior-year Schedule D line 21, as a positive amount.
3. Combine lines 1 and 2; if zero or less, enter -0-.
4. The smaller of line 2 or line 3.
   (If prior-year Schedule D line 7 is a loss, go to line 5; otherwise enter -0- on line 5 and go
   to line 9.)
5. The loss from prior-year Schedule D line 7, as a positive amount.
6. Any gain from prior-year Schedule D line 15; if a loss, enter -0-.
7. Add lines 4 and 6.
8. Short-term capital loss carryover: subtract line 7 from line 5; if zero or less, enter -0-.
   (If prior-year Schedule D line 15 is a loss, go to line 9; otherwise skip lines 9-13.)
9. The loss from prior-year Schedule D line 15, as a positive amount.
10. Any gain from prior-year Schedule D line 7; if a loss, enter -0-.
11. Subtract line 5 from line 4; if zero or less, enter -0-.
12. Add lines 10 and 11.
13. Long-term capital loss carryover: subtract line 12 from line 9; if zero or less, enter -0-.

Worked Example A (fully absorbed by the $3,000 cap, single filer): prior-year Schedule D line 7 =
-$5,000.00 (ST loss), line 15 = +$2,000.00 (LT gain), line 21 = -$3,000.00, Form 1040 line 15 =
+$50,000.00. Expected: shortTermCarryoverCents = 0, longTermCarryoverCents = 0 (the $3,000 loss
cap exactly absorbs the $3,000 net loss; nothing carries).

Worked Example B (partial ST carryover, single filer): prior-year Schedule D line 7 = -$10,000.00
(ST loss), line 15 = +$1,000.00 (LT gain), line 21 = -$3,000.00, Form 1040 line 15 = +$20,000.00.
Expected: shortTermCarryoverCents = 600000 ($6,000.00), longTermCarryoverCents = 0.

Worked Example C (LT carryover, ST is a gain, single filer): prior-year Schedule D line 7 =
+$1,000.00 (ST gain), line 15 = -$8,000.00 (LT loss), line 21 = -$3,000.00, Form 1040 line 15 =
+$30,000.00. Expected: shortTermCarryoverCents = 0, longTermCarryoverCents = 400000 ($4,000.00).

From `fjs/document/money_field/module.f.js`: `moneyFieldError(label)(printed): string | undefined`
— accepts negative decimal strings (its own `negativeAccepted` leaf proves this).

From `fjs/document/base/module.f.js`: `base(dialect): { readonly dialect: D }`,
`mediaTypeOf(dialect)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The no-bare-2025 gate — scoped to a real branch shape, with a positive control</name>
  <files>year-genericity-gate.test.js</files>
  <read_first>
    magi-gate.test.js
    fjs/tax/params/module.f.js
    fjs/server/finance_tax_params/module.f.js
  </read_first>
  <action>
    Create `year-genericity-gate.test.js` at the repo root, `@ts-nocheck`, mirroring
    `magi-gate.test.js`'s exact structure (module header explaining the AGENTS.md carve-out,
    `listFilesRecursively` skipping `node_modules`/`functionalscript`, scan root `fjs/`). Define
    `yearBranchPattern` matching an identifier ending in `year`/`Year` compared with a 4-digit
    literal via a comparison operator, in EITHER order:
    `/[A-Za-z_$][\w$]*[Yy]ear\s*(?:===|!==|==|!=|<=|>=|<|>)\s*\d{4}|\d{4}\s*(?:===|!==|==|!=|<=|>=|<|>)\s*[A-Za-z_$][\w$]*[Yy]ear/`.
    This deliberately does NOT match `taxParamsByYear[2025]` (index access, no comparison
    operator), `const taxParams2025 = ...` (an identifier, not a comparison), a Rev. Proc.
    citation string `'2025-32'`, or an `assertEq(a, 2025)` proof call (comma-separated arguments,
    not a comparison operator) — verify each of these four legitimate shapes explicitly as
    negative-control assertions against the regex directly (in-memory strings, not a file scan),
    proving the pattern's precision before trusting it against the real tree.

    First test: scan every file under `fjs/` and assert zero matches, naming file/line on
    failure (mirroring `magi-gate.test.js`'s offense-reporting style exactly).

    Second test (positive control, AGENTS.md "a gate needs a control"): assert the SAME pattern
    DOES match two synthetic in-memory strings — `'if (taxYear === 2025) { return refuse() }'`
    and `'return year !== 2025 ? undefined : x'` — never written into real `fjs/` source.
  </action>
  <acceptance_criteria>
    - The real-tree scan test passes with zero offenses against the CURRENT codebase (verified by
      running it — if it fails, the codebase already has a real hardcoded-year branch, which must
      be reported, not silently excluded).
    - The four negative-control assertions (index access, test-fixture identifier, citation
      string, `assertEq` call) each independently confirm the regex does NOT match.
    - The positive-control test fails if the regex is narrowed to require, e.g., only `===` (not
      `!==`) — i.e., it is mutation-checkable.
  </acceptance_criteria>
  <verify>
    <automated>node --test all.test.js 2>&1 | grep -c 'year-genericity-gate'</automated>
  </verify>
  <done>Gate, its four negative controls, and its positive control all pass.</done>
</task>

<task type="auto">
  <name>Task 2: The prior-year capital-loss document dialect</name>
  <files>fjs/document/prior_year_capital_loss/module.f.js</files>
  <read_first>
    fjs/document/1099b/module.f.js
    fjs/document/medical_expenses/module.f.js
    fjs/document/money_field/module.f.js
    fjs/document/base/module.f.js
  </read_first>
  <action>
    Create `fjs/document/prior_year_capital_loss/module.f.js` following `fjs/document/1099b`'s
    four-stage template (dialect -> mediaType -> schema -> structural `validate` -> semantic
    `checkReferences` -> composed `validate`) and `fjs/document/medical_expenses`'s precedent for
    a dialect with no payer (taxpayer-transcribed-from-own-prior-return, not an information
    return): `dialect = 'vnd.fjs.prior_year_capital_loss'`. Schema (via `base(dialect)` spread
    first): `recipientTin: string`, `taxYear: number` (the PRIOR year these four figures are FROM,
    e.g. `2024` for a TY2025 return), `corrected: option(true)`, and four REQUIRED (never
    `option`) signed-decimal-string fields: `priorYearFormLine15` (Form 1040/1040-SR/1040-NR line
    15, as it would have been if negative values were representable — the worksheet's own line 1
    footnote), `priorYearScheduleDLine7`, `priorYearScheduleDLine15`, `priorYearScheduleDLine21`.
    All four are REQUIRED (not `option`) — a document that exists carries every one of the four
    facts the worksheet needs, or it does not exist at all (CONTEXT.md's "absent means zero,
    present-but-broken refuses" — the ABSENCE case is handled by the CALLER never constructing
    this document, not by any field inside it being optional).

    `checkReferences`: validate each of the four fields via `moneyFieldError(fieldName)(value)`
    (accepts negative — every one of these four figures can legitimately be negative). Refuse,
    naming the specific field, on the first invalid one — this IS the "present-but-broken
    refuses, naming the field" behavior TAX-17 requires; no other module implements this half of
    the requirement.

    Write the module docstring documenting: (a) this dialect stores ONLY the four raw prior-year
    facts, never a pre-computed carryover total — the carryover is DERIVED, not asserted, mirroring
    `vnd.fjs.medical_expenses`'s "no stored total" precedent; (b) there is no `formRevision` field
    for the same reason `medical_expenses` has none — this is a taxpayer's own transcription of
    facts off their OWN prior return, not a printed information-return revision; (c) cardinality
    is one running record per taxpayer per prior tax year, subject keyed the same way
    `medical_expenses` is (payer-less).

    Proofs (mirroring `1099b`'s generated-loop idiom): `dialectAndMediaType`; `fullyPopulatedValidates`;
    `wrongDialectRejected`; a generated per-field exactness proof (comma-grouped value rejected)
    over all four money fields with an independently hand-typed `expectedMoneyBoxFieldCount = 4`;
    a `negativeValuesAccepted` leaf (all four fields negative, still validates ok — this is the
    normal case, since a loss year and a loss cap are both negative facts); a
    `presentButInvalidFieldRefusesNamingTheField` leaf asserting the error message from an
    inexact `priorYearScheduleDLine7` names that exact field; and one structural
    `<field>Required` leaf per field (omitting it fails STRUCTURAL validation, distinguishing
    "missing" from "present but malformed", mirroring `1099b`'s own
    `sourceArtifactHashRequired`).
  </action>
  <acceptance_criteria>
    - `validate` rejects a document missing any of the four required fields (structural error,
      not a semantic string), and rejects a present-but-comma-grouped value on each field
      (semantic error naming that field).
    - `validate` accepts a fully-populated document with all four figures negative.
    - No `total`/`carryover`-named field exists anywhere in the schema — grep confirms it.
    - `npx tsc --noEmit` reports zero new errors.
  </acceptance_criteria>
  <verify>
    <automated>node --test all.test.js 2>&1 | grep -c '^✔ import("\./fjs/document/prior_year_capital_loss/'</automated>
  </verify>
  <done>Dialect module exists, validates the four facts exactly, refuses a broken one by name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The Capital Loss Carryover Worksheet — 13 lines, pure bigint arithmetic</name>
  <files>fjs/tax/carryover/module.f.js</files>
  <behavior>
    - Worked Example A (fully absorbed): inputs (ST loss $5,000, LT gain $2,000, line21 -$3,000,
      Form1040 line15 +$50,000) -> `{ shortTermCarryoverCents: 0n, longTermCarryoverCents: 0n }`.
    - Worked Example B (partial ST carryover): inputs (ST loss $10,000, LT gain $1,000, line21
      -$3,000, Form1040 line15 +$20,000) -> `{ shortTermCarryoverCents: 600000n,
      longTermCarryoverCents: 0n }`.
    - Worked Example C (LT carryover, ST is a gain): inputs (ST gain $1,000, LT loss $8,000,
      line21 -$3,000, Form1040 line15 +$30,000) -> `{ shortTermCarryoverCents: 0n,
      longTermCarryoverCents: 400000n }`.
    - Edge case: a taxpayer with NO prior-year loss at all (line7 = +$500, line15 = +$500, line21
      = $0.00, Form1040 line15 = +$40,000) -> both outputs `0n` (the worksheet's own eligibility
      note collapses to zero structurally, with no separate gate needed).
  </behavior>
  <read_first>
    fjs/schedule/d/module.f.js
    fjs/document/prior_year_capital_loss/module.f.js
  </read_first>
  <action>
    Create `fjs/tax/carryover/module.f.js`. Export a typedef `CarryoverWorksheetInputs` with four
    signed `bigint` fields: `priorYearFormLine15Cents`, `priorYearScheduleDLine7Cents`,
    `priorYearScheduleDLine15Cents`, `priorYearScheduleDLine21Cents`. Export
    `capitalLossCarryoverWorksheet: (inputs: CarryoverWorksheetInputs) => { readonly
    shortTermCarryoverCents: bigint, readonly longTermCarryoverCents: bigint }`. Implement all 13
    printed lines as named `const`s in printed order (mirroring `fjs/schedule/d`'s own "every
    const is one printed line" convention), using this exact derivation (verified by hand against
    the three worked examples in this task's `<behavior>` block): `line1 =
    priorYearFormLine15Cents`; `line2 = priorYearScheduleDLine21Cents < 0n ?
    -priorYearScheduleDLine21Cents : 0n`; `line3 = max(0n, line1 + line2)`; `line4 = min(line2,
    line3)`; `line5 = priorYearScheduleDLine7Cents < 0n ? -priorYearScheduleDLine7Cents : 0n`;
    `line6 = priorYearScheduleDLine15Cents > 0n ? priorYearScheduleDLine15Cents : 0n`; `line7 =
    line4 + line6`; `shortTermCarryoverCents = max(0n, line5 - line7)`; `line9 =
    priorYearScheduleDLine15Cents < 0n ? -priorYearScheduleDLine15Cents : 0n`; `line10 =
    priorYearScheduleDLine7Cents > 0n ? priorYearScheduleDLine7Cents : 0n`; `line11 = max(0n, line4
    - line5)`; `line12 = line10 + line11`; `longTermCarryoverCents = max(0n, line9 - line12)`.
    Cite the source page (i1040sd.pdf, 2025 revision, p.10) in the module docstring, and quote the
    13 printed lines verbatim in a comment above the implementation (this plan's `<interfaces>`
    block already has the exact transcription).

    Write RED first: a test file section with the three worked-example proof leaves asserting
    against the hand-typed expected `bigint` literals from `<behavior>` — confirm they fail
    against a stub. Then implement per the formula above (GREEN). Add the edge-case leaf from
    `<behavior>` last.
  </action>
  <acceptance_criteria>
    - All three worked-example proof leaves pass, each asserting against a LITERAL `bigint`
      expected value (never a value computed by a second call to the function under test).
    - The edge-case leaf (no prior-year loss) passes with both outputs `0n`.
    - Mutating `line4 = min(line2, line3)` to `line4 = line3` reddens Worked Example B (predict,
      then verify by mutation, then revert) — record the observed red/green in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node --test all.test.js 2>&1 | grep -c '^✔ import("\./fjs/tax/carryover/'</automated>
  </verify>
  <done>Worksheet module exists; all three worked examples plus the edge case pass; the mutation sweep is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Taxpayer-transcribed prior-year figures -> worksheet | Untrusted magnitude/sign, trusted structure (already schema-validated before this module ever sees it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-15-03 | Tampering | A crafted carryover document asserting a favorable carryover with no derivation | mitigate | Dialect stores only the four raw facts; the carryover total is always DERIVED by the worksheet, never stored/asserted (module docstring + no `total` field, mechanically absent) |
| T-15-04 | Repudiation | A malformed field silently coerced instead of refused | mitigate | `moneyFieldError` per field in `checkReferences`, naming the field; proved by `presentButInvalidFieldRefusesNamingTheField` |
</threat_model>

<verification>
`npm test` green; the three worked examples in `fjs/tax/carryover/module.f.js` pass against
independently hand-computed literals; `year-genericity-gate.test.js` passes with zero real
offenses and a working positive control.
</verification>

<success_criteria>
The new dialect and worksheet exist, are independently correct (verified against hand-computed
examples and a recorded mutation), and the year-genericity gate protects every later wave's
carryover-wiring code from a hardcoded-year regression.
</success_criteria>

<output>
After completion, create `.planning/phases/15-realism-polish-and-upstream/15-02-SUMMARY.md`
</output>
