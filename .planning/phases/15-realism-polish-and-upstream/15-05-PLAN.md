---
phase: 15
plan: 05
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - fjs/schedule/d/module.f.js
  - fjs/form1040/core/module.f.js
autonomous: true
requirements: [TAX-17]

must_haves:
  truths:
    - "A prior-year capital loss, when a valid carryover document is stored, carries into the current year's Schedule D lines 6 and 14 as a negative amount, reachable through the full form1040IncomeLines computation, not merely through scheduleD called in isolation."
    - "Absent a carryover document, a return with brokerage sales computes Schedule D lines 6/14 as a legitimate 0n — it does not refuse."
    - "form1040IncomeLines/scheduleD compute correctly under a SECOND, synthetic TaxParamSet distinct from taxParamsByYear[2025], demonstrating year-genericity is structural, not retrofitted."
  artifacts:
    - path: "fjs/schedule/d/module.f.js"
      provides: "ScheduleDInputs widened with an optional prior-year carryover; lines 6/14 computed via the worksheet, never hardcoded 0n unconditionally"
    - path: "fjs/form1040/core/module.f.js"
      provides: "Form1040Inputs widened with capitalLossCarryoverForms; form1040IncomeLines threads it into scheduleD"
  key_links:
    - from: "fjs/schedule/d/module.f.js"
      to: "fjs/tax/carryover/module.f.js"
      via: "capitalLossCarryoverWorksheet(...) called with the stored document's four cents figures"
      pattern: "capitalLossCarryoverWorksheet"
    - from: "fjs/form1040/core/module.f.js"
      to: "fjs/schedule/d/module.f.js"
      via: "scheduleD({ status, brokerageForms, dividendForms, priorYearCapitalLossCarryover })"
      pattern: "priorYearCapitalLossCarryover"
---

<objective>
Wire the already-built carryover worksheet and document dialect (Plan 15-02) into Schedule D and,
through it, into the full Form 1040 income-line computation — so a prior-year capital loss
genuinely carries into the current year's return through the SAME entry point every other line
uses, not as an isolated unit-tested side path.

Purpose: close TAX-17's "carries over into the current year's Schedule D" criterion end to end,
and demonstrate "any year with parameters and documents computes" structurally, with a synthetic
second `TaxParamSet` — never a second real transcribed tax year.
Output: widened `ScheduleDInputs` (Schedule D) and `Form1040Inputs` (Form 1040 core).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-realism-polish-and-upstream/15-CONTEXT.md
@.planning/phases/15-realism-polish-and-upstream/15-RESEARCH.md
@AGENTS.md
@fjs/schedule/d/module.f.js
@fjs/tax/carryover/module.f.js
@fjs/document/prior_year_capital_loss/module.f.js
</context>

<interfaces>
<!-- Executor should use these directly — no codebase exploration needed. -->

Current `ScheduleDInputs` and the two hardcoded-zero lines this plan changes
(`fjs/schedule/d/module.f.js`):
```
typedef ScheduleDInputs = {
  readonly status: IndividualFilingStatus,
  readonly brokerageForms: readonly Stored<OneZeroNineNineB>[],
  readonly dividendForms: readonly Stored<OneZeroNineNineDiv>[],
}
// inside scheduleD:
const line6 = 0n   // ST capital loss carryover — multi-year, TAX-17/Phase 15
...
const line14 = 0n  // LT capital loss carryover — multi-year, TAX-17/Phase 15
```

From `fjs/tax/carryover/module.f.js` (Plan 15-02):
```
export const capitalLossCarryoverWorksheet: (inputs: {
  readonly priorYearFormLine15Cents: bigint,
  readonly priorYearScheduleDLine7Cents: bigint,
  readonly priorYearScheduleDLine15Cents: bigint,
  readonly priorYearScheduleDLine21Cents: bigint,
}) => { readonly shortTermCarryoverCents: bigint, readonly longTermCarryoverCents: bigint }
```

From `fjs/document/prior_year_capital_loss/module.f.js` (Plan 15-02): `PriorYearCapitalLoss` has
`priorYearFormLine15`, `priorYearScheduleDLine7`, `priorYearScheduleDLine15`,
`priorYearScheduleDLine21` — all `string` (decimal, signed), plus `recipientTin`, `taxYear`.
Convert via `centsFromString` from `fjs/exact/module.f.js` before calling the worksheet.

`Stored<T>` (already defined in both `fjs/schedule/d/module.f.js` and
`fjs/form1040/core/module.f.js`): `{ readonly documentHash: string, readonly value: T }`.

Current `Form1040Inputs` widening precedent and its ASSEMBLY function (`fjs/form1040/core/module.f.js`,
~line 1836 and ~1846) — the EXACT mechanical technique to repeat, per this file's own established
convention ("`npx tsc --noEmit` is what surfaced every call site... per this plan's own
instruction not to enumerate them from memory"):
```
const inputsOf = profile => w2s => interestForms => dividendForms => brokerageForms =>
    retirementForms => socialSecurityForms => itemizedDeductionForms => medicalExpenseForms =>
        ({ profile, w2s, interestForms, dividendForms, brokerageForms,
           retirementForms, socialSecurityForms, itemizedDeductionForms, medicalExpenseForms })
```
There are 63 call sites of `inputsOf(` in this file. Widening `inputsOf`'s signature by one
trailing parameter makes every EXISTING 9-argument call site type-error at `npx tsc --noEmit`
(the function then returns a curried function awaiting the 10th argument, not `Form1040Inputs`,
wherever the result is used) — this is the SAME mechanism Plan 13-07 used to find its own call
sites; do not attempt to enumerate all 63 from this plan text. Use `tsc`'s own error list, and a
scripted `sed`/`perl` pass appending `([])` after the existing 9th argument's application, to fix
them in bulk, then re-run `tsc` to confirm zero remain.

`form1040IncomeLines`'s existing Schedule D call site (~line 428-462):
```
export const form1040IncomeLines = taxParamSet => inputs => {
    const { profile, w2s, interestForms, dividendForms, brokerageForms,
        retirementForms, socialSecurityForms, itemizedDeductionForms, medicalExpenseForms } = inputs
    ...
    const filingScheduleD = declaredKindsOf(profile).includes('capitalGainsOrLosses')
    const scheduleDOutcome = filingScheduleD ? scheduleD({ status, brokerageForms, dividendForms }) : undefined
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Widen ScheduleDInputs — the worksheet drives lines 6 and 14</name>
  <files>fjs/schedule/d/module.f.js</files>
  <read_first>
    fjs/schedule/d/module.f.js
    fjs/tax/carryover/module.f.js
    fjs/document/prior_year_capital_loss/module.f.js
  </read_first>
  <action>
    Add `readonly priorYearCapitalLossCarryover?: Stored<PriorYearCapitalLoss>` to
    `ScheduleDInputs` (optional — CONTEXT.md's "absent means zero" is expressed at the TYPE level
    here, not via an `option`-wrapped value). Add the type-only JSDoc import: `/** @import {
    PriorYearCapitalLoss } from '../../document/prior_year_capital_loss/module.f.js' */`. Import
    `capitalLossCarryoverWorksheet` from `../../tax/carryover/module.f.js` and `centsFromString`
    (already imported in this file).

    Inside `scheduleD`, replace the two hardcoded-zero `const line6 = 0n` / `const line14 = 0n`
    assignments with: if `inputs.priorYearCapitalLossCarryover` is `undefined`, `line6 = 0n` /
    `line14 = 0n` exactly as today (the legitimate-zero case — a first-year filer or someone with
    genuinely no prior-year loss); otherwise call `capitalLossCarryoverWorksheet({
    priorYearFormLine15Cents: centsFromString(carryover.value.priorYearFormLine15),
    priorYearScheduleDLine7Cents: centsFromString(carryover.value.priorYearScheduleDLine7),
    priorYearScheduleDLine15Cents: centsFromString(carryover.value.priorYearScheduleDLine15),
    priorYearScheduleDLine21Cents: centsFromString(carryover.value.priorYearScheduleDLine21) })`
    and set `line6 = -result.shortTermCarryoverCents`, `line14 = -result.longTermCarryoverCents`
    (Schedule D's own printed form prints both lines' entry boxes in parentheses — a carryover
    LOSS reduces the total, so both are entered as negative). Update the module docstring's "No
    prior-year carryover" section to state plainly that this boundary is now closed, citing this
    plan.

    Attach provenance: when `line6 !== 0n`, add one `Source` to this module's own `sources` array
    citing `carryover.documentHash`, `boxPath: 'priorYearCapitalLossCarryoverWorksheet(shortTerm)'`,
    `value: centsToString(result.shortTermCarryoverCents)`; symmetrically for `line14` when
    nonzero. (No source is added when the carryover is absent or contributes zero — an absent
    document has nothing to cite, matching every other absent-box convention in this codebase.)

    Add proofs: (a) absent carryover + brokerage sales present -> lines 6/14 both `0n`, whole
    Schedule D still computes `ok` (the Trap-4 control — a return with sales and NO carryover
    document must NOT refuse); (b) a present, valid carryover document with hand-typed figures
    (reuse Plan 15-02's Worked Example B: ST loss $10,000/LT gain $1,000/line21 -$3,000/Form1040
    line15 +$20,000) -> `line6 === -600000n`, `line14 === 0n`, and the `sources` array contains
    exactly one entry citing the carryover document's hash for the ST line.
  </action>
  <acceptance_criteria>
    - `scheduleD` with no `priorYearCapitalLossCarryover` field at all still computes `kind: 'ok'`
      with `line6 === 0n && line14 === 0n` when brokerage sales are present (Trap 4's control).
    - `scheduleD` with the Worked-Example-B carryover produces `line6 === -600000n` and
      `line14 === 0n`, matching Plan 15-02's independently hand-computed worksheet output exactly.
    - `npx tsc --noEmit` reports zero new errors.
  </acceptance_criteria>
  <verify>
    <automated>node --test all.test.js 2>&1 | grep -c '^✔ import("\./fjs/schedule/d/'</automated>
  </verify>
  <done>ScheduleDInputs widened; lines 6/14 driven by the worksheet when present, legitimately zero when absent.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the carryover through Form1040Inputs; prove reachability and year-genericity</name>
  <files>fjs/form1040/core/module.f.js</files>
  <read_first>
    fjs/form1040/core/module.f.js
    fjs/schedule/d/module.f.js
  </read_first>
  <action>
    Add `readonly capitalLossCarryoverForms: readonly Stored<PriorYearCapitalLoss>[]` to the
    `Form1040Inputs` typedef (a LIST, matching this file's own established convention for a
    "one running record per taxpayer per year" document — see the typedef's own docstring for
    `itemizedDeductionForms`/`medicalExpenseForms`), appended at the END of the field list. Add
    the type-only JSDoc import for `PriorYearCapitalLoss`.

    Widen `inputsOf` by ONE trailing curried parameter (`capitalLossCarryoverForms`), appended
    after `medicalExpenseForms`, mirroring the exact append-at-the-end technique this file's own
    docstring already documents for the two prior widenings. Run `npx tsc --noEmit`; for every
    reported call-site error (up to 63), append `([])` as the new trailing argument application —
    a scripted find/replace across this file is appropriate here, since every site needs the
    IDENTICAL fix.

    In `form1040IncomeLines`, destructure `capitalLossCarryoverForms` out of `inputs` alongside
    the existing fields, and pass `priorYearCapitalLossCarryover: capitalLossCarryoverForms[0]`
    into the existing `scheduleD({ status, brokerageForms, dividendForms, ... })` call (taking the
    first entry if present, `undefined` otherwise — the cardinality-flattening technique this
    file's own docstring already uses for `itemizedDeductionForms`/`medicalExpenseForms`).

    Add a proof: a full return (brokerage sales, `capitalGainsOrLosses` declared, ONE stored
    carryover document with Plan 15-02's Worked Example B figures) computes, through
    `form1040IncomeLines` called at the TOP-LEVEL entry point (not `scheduleD` in isolation), a
    Schedule D outcome whose `line6 === -600000n` — the reachable-path proof this criterion
    requires.

    Add the year-genericity proof: construct a SECOND, clearly-labeled-synthetic `TaxParamSet`
    (e.g. `syntheticTaxParamSetForGenericityProof`, doubling TY2025's standard deduction figures
    and re-labeling every `Citation`'s `kind` as `'code'` with a note identifying it as
    non-real/test-only — never added to `taxParamsByYear`), call `form1040IncomeLines` with it
    directly against an existing fixture return, and assert the standard-deduction-driven line(s)
    reflect the SYNTHETIC figures, not TY2025's — proving `form1040IncomeLines` genuinely
    dispatches on its `taxParamSet` argument rather than a hardcoded year.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors after the full widening (all ~63 call sites fixed).
    - The full-return proof asserts `line6 === -600000n` through `form1040IncomeLines`, not
      through a direct `scheduleD(...)` call.
    - The year-genericity proof's expected value is computed from the SYNTHETIC parameter set's
      own literal figures (hand-typed), never copy-pasted from TY2025's output.
    - `node --test 2>&1 | grep -c '^✔ import("\./fjs/'` shows a strictly higher de-duplicated count
      than before this plan (per STATE.md's de-duplication caveat).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && node --test all.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>Form1040Inputs widened; the carryover is reachable end to end; year-genericity is proven structurally with a synthetic second TaxParamSet.</done>

  <!-- Task-level verify is the proof suite, not `npm test`: the latter is ~8 minutes
       (481s / 6166 tests, measured on e36ef1a) and blows the per-task Nyquist target on every
       commit. 15-VALIDATION.md's Sampling Rate reserves the full suite for the wave gate. -->

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Stored, already-validated carryover document -> scheduleD | Trusted by construction — an invalid document never reaches this layer (Plan 15-02's dialect refuses it at ingest) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-15-09 | Tampering | A return with real capital losses silently reports zero carryover due to a wiring gap (list-to-optional flattening dropping the entry) | mitigate | Explicit `[0]` take-first with a full-return proof asserting the nonzero result reaches Schedule D's own line |
| T-15-10 | Repudiation | The reachable-path proof only exercises `scheduleD` in isolation, silently leaving `form1040IncomeLines` untested | mitigate | Task 2's proof calls `form1040IncomeLines` at the top level, explicitly required as an acceptance criterion |
</threat_model>

<verification>
`npm test` green; Schedule D and Form 1040 proofs both pass with the carryover wired end to end;
year-genericity proven via a synthetic second `TaxParamSet`.
</verification>

<success_criteria>
A prior-year capital loss, when documented, carries into the current year's Schedule D lines
6/14 through the full `form1040IncomeLines` entry point; absence is a legitimate zero; a second,
synthetic `TaxParamSet` computes correctly, proving year-genericity structurally.
</success_criteria>

<output>
After completion, create `.planning/phases/15-realism-polish-and-upstream/15-05-SUMMARY.md`
</output>
