---
phase: 19
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - fjs/run/module.f.js
  - fjs/server/fjs_run/module.f.js
  - fjs/server/module.f.js
  - fjs-run-integration.test.js
  - payer-report-integration.test.js
autonomous: true
requirements: [PROV-04, EXEC-13]

must_haves:
  truths:
    - "Calling fjs_run without taxYear is refused by toolEntry's own RTTI check before the handler ever runs"
    - "Calling fjs_run with an unknown taxYear is refused by name, naming both the offending year and the known set, never a throw"
    - "A successful fjs_run response carries taxYear, paramSetHash and programHash alongside the existing six keys"
    - "The persisted vnd.fjs.run record — both the ok and the error arm — carries the same taxYear/paramSetHash the response reported"
    - "Mutating the pinned derivation's && to || reddens the existing subjectOnlyWithoutParentsPersistsPinnedFalse proof; the mutation is restored byte-identical afterward"
  artifacts:
    - path: fjs/run/module.f.js
      provides: "runSchema widened with required taxYear/paramSetHash fields"
      contains: "taxYear: number"
    - path: fjs/server/fjs_run/module.f.js
      provides: "taxYear threading, paramSetHash computation, envelope widening, unknown-year refusal"
      contains: "paramSetHash(taxParamSet)"
  key_links:
    - from: fjs/server/fjs_run/module.f.js
      to: fjs/report/provenance/module.f.js
      via: "paramSetHash(taxParamSet) call inside fjsRunTool's handler, before executeRun runs"
      pattern: "paramSetHash\\("
    - from: fjs/server/fjs_run/module.f.js
      to: fjs/run/module.f.js
      via: "handleRunOutcome's Run record literal now spreads provenance.taxYear/paramSetHash"
      pattern: "provenance\\.taxYear"
---

<objective>
Wire `taxYear`/`paramSetHash` through `fjs_run`'s schema, its persisted `vnd.fjs.run` record,
and its response envelope — PROV-04's decision text is explicit that the header belongs in
BOTH places, not only the response. Then perform Mutation Gate M2, converting EXEC-13's
"inherited as done" pinned flag into an observed, watched-failing property.

Purpose: this is the atomic core of the phase. Widening `fjsRunInputSchema` with a REQUIRED
field makes `toolEntry`'s RTTI check reject every existing caller that does not supply it — this
plan's Task 1 must update every one of those callers in the SAME commit, or `npm test` goes red
between commits (AGENTS.md's Phase 8/11/12 precedent, cited in this phase's own planning
context). Verified against the live tree rather than assumed: the actual call-site count is
NINE, not the three named in this phase's own planning brief — four inside
`fjs/server/fjs_run/module.f.js`'s own proof (`fjsRunTool(...).handle(...)`), one inside
`fjs/server/module.f.js`'s `weekOneConvergence` proof, three inside `fjs-run-integration.test.js`,
and one inside `payer-report-integration.test.js`. Trusting the "3" figure here would have
shipped a red suite; AGENTS.md's own rule ("verify each literal against the function that will
consume it") is why this count was re-derived by `grep` before writing this plan, not copied.

Output: `fjs_run` accepts and validates `taxYear`, refuses an unknown one, reports and persists
`taxYear`/`paramSetHash`/`programHash` together, and Mutation Gate M2 is performed and recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reproducibility-and-report-provenance/19-CONTEXT.md
@.planning/phases/19-reproducibility-and-report-provenance/19-PATTERNS.md
@.planning/phases/19-reproducibility-and-report-provenance/19-VALIDATION.md
@AGENTS.md
@fjs/report/provenance/module.f.js

<interfaces>
<!-- From Plan 19-01, already shipped and proof-covered when this plan starts. -->
`paramSetHash: (taxParamSet: TaxParamSet) => string` and `reviewedEstimateFraming: string`,
both exported from `fjs/report/provenance/module.f.js`. Import path from
`fjs/server/fjs_run/module.f.js`: `'../../report/provenance/module.f.js'`.

<!-- fjs/server/finance_tax_params/module.f.js's unknown-year refusal shape (COPY this, do not
     invent a new one) -->
```
export const knownYears = Object.keys(taxParamsResponses).map(Number)
...
const response = taxParamsResponses[args.year]
if (response === undefined) {
    return pure(errorResult(`unknown tax year: ${args.year}; known: ${knownYears.join(', ')}`))
}
```
`fjs_run` performs the identical lookup against `taxParamsByYear` (from
`fjs/tax/params/module.f.js`, already `{ readonly [year: number]: TaxParamSet }`) instead of
`taxParamsResponses`, and refuses the SAME way — `errorResult`, never a throw, before
`executeRun` is ever called.

<!-- fjs/run/module.f.js's runSchema (lines 104-115) and its two proof fixtures (lines 201-221),
     the ONLY two places a full Run literal is hand-typed — every other proof leaf spreads one
     of these two. -->
```
export const runSchema = /** @type {const} */ ({
    ...base(dialect),
    programHash: string,
    args: array(string),
    pinned: boolean,
    subject: option(string),
    parents: option(array(string)),
    status: or('ok', 'error'),
    inputs: array(inputEntry),
    resultHash: option(string),
    error: option(string),
})
```
Add `taxYear: number` and `paramSetHash: string` after `args`, before `pinned` — both REQUIRED
(no `option`), because PROV-04 states every run, pinned or not, ok or error, carries this
header. `minimalOk`/`minimalError` (the two fixtures) need the two new fields added; every
other proof leaf in that file spreads `...minimalOk`/`...minimalError` and needs no separate edit.

<!-- fjs/server/fjs_run/module.f.js's handleRunOutcome (lines 350-405) and fjsRunTool (lines
     432-459) — the two functions this plan's Task 1 rewrites. -->
`handleRunOutcome`'s current curried signature: `cas => programHash => programArgs => pinned =>
pinFields => rawOutcome => { ... }`. Insert ONE new parameter, named `provenance` and typed `{
readonly taxYear: number, readonly paramSetHash: string }`, between `pinFields` and `rawOutcome`
— NOT as two separate new curry levels (that would double the edits at every call site for no
benefit). Every one of the fifteen existing calls to `handleRunOutcome(...)` in this file's own
proof section, and the ONE production call site inside `fjsRunTool`, must gain this one new
curried argument.

`fjsRunTool`'s current handler body (inside the `args =>` closure, lines 439-457):
```
const programArgs = args.args ?? []
const pinned = args.subject !== undefined && args.parents !== undefined
const pinFields = /** @type {...} */ (pinned ? { subject: args.subject, parents: args.parents } : {})
return step(
    executeRun(materializeHomeRoot)(cas)(evoApi)({ hash: args.hash, args: programArgs, ...pinFields }),
    outcome => handleRunOutcome(cas)(args.hash)(programArgs)(pinned)(pinFields)(outcome),
)
```
The unknown-year check and `resolvedParamSetHash` computation belong BEFORE the `step(...)`
call (a genuine short-circuit, mirroring `finance_tax_params`'s own early-return shape); the
final line's `handleRunOutcome(...)` call gains the new `provenance` argument between `pinFields`
and `outcome`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire taxYear/paramSetHash through schema, record and response — every call site in one commit</name>
  <files>fjs/run/module.f.js, fjs/server/fjs_run/module.f.js, fjs/server/module.f.js, fjs-run-integration.test.js, payer-report-integration.test.js</files>
  <read_first>
    fjs/run/module.f.js (whole file: `runSchema`, `checkReferences`, `minimalOk`/`minimalError`)
    fjs/server/fjs_run/module.f.js (whole file — this is the file with the most edits; read the
    module docstring, `fjsRunInputSchema`, `handleRunOutcome`, `fjsRunTool`, and every one of its
    own `fjsRunTool(...).handle(...)` and `handleRunOutcome(...)` call sites in its own `proof`)
    fjs/server/finance_tax_params/module.f.js (the unknown-year refusal pattern and the
    `number`-from-rtti import, copied verbatim in shape)
    fjs/tax/params/module.f.js (lines 704-740: `TaxParamSet`, `taxParamsByYear`)
    fjs/report/provenance/module.f.js (this plan's own dependency, from Plan 19-01 — `paramSetHash`,
    `reviewedEstimateFraming`)
    fjs/server/module.f.js (lines ~760-810: `weekOneConvergence`'s own `fjs_run` `tools/call`)
    fjs-run-integration.test.js (lines ~320-500: the three `call('fjs_run', ...)` sites)
    payer-report-integration.test.js (lines ~220-232: the one `call('fjs_run', ...)` site)
  </read_first>
  <action>
    1. `fjs/run/module.f.js`: add `number` to the existing `import { array, boolean, option, or,
       string }` line (from `functionalscript/fjs/types/rtti/module.f.js`). Add `taxYear:
       number` and `paramSetHash: string` to `runSchema`, positioned after `args` and before
       `pinned`. Extend `minimalOk` with `taxYear: 2025, paramSetHash: 'sha256-paramset1'` and
       `minimalError` with `taxYear: 2025, paramSetHash: 'sha256-paramset1'` — no other edit
       needed in this file; every other proof leaf spreads one of these two fixtures.

    2. `fjs/server/fjs_run/module.f.js`:
       - Add `number` to the existing `import { option, array, string } from
         'functionalscript/fjs/types/rtti/module.f.js'` line.
       - Import `taxParamsByYear` from `'../../tax/params/module.f.js'`.
       - Import `paramSetHash, reviewedEstimateFraming` from `'../../report/provenance/module.f.js'`.
       - Add `taxYear: number` to `fjsRunInputSchema` (alongside `hash`, `args`, `subject`,
         `parents`).
       - Widen `handleRunOutcome`'s curried signature: insert one new parameter `provenance`
         (typed `{ readonly taxYear: number, readonly paramSetHash: string }`) between the
         existing `pinFields` and `rawOutcome` parameters. Update its own `@type` JSDoc line to
         match.
       - Inside `handleRunOutcome`'s body, add `taxYear: provenance.taxYear, paramSetHash:
         provenance.paramSetHash` to BOTH the ok-arm and the error-arm `Run` record literals
         (the two `/** @type {Run} */ const record = {...}` blocks).
       - Inside `handleRunOutcome`'s ok-arm response literal (the object passed to `jsonText(...)`
         inside `pure(okResult(...))`, currently the six keys `resultHash, runHash, preview,
         truncated, readCount, literalCount`), add `taxYear: provenance.taxYear, paramSetHash:
         provenance.paramSetHash, programHash` (the existing curried parameter, already in
         scope — reference it directly, do not re-derive it) `, reviewedEstimateFraming` (the
         imported constant, referenced directly).
       - Inside `fjsRunTool`'s handler body, BEFORE the existing `return step(executeRun(...),
         ...)` line: look up `const taxParamSet = taxParamsByYear[args.taxYear]`; if
         `taxParamSet === undefined`, return `pure(errorResult(...))` naming `args.taxYear` and
         `Object.keys(taxParamsByYear).map(Number).join(', ')`, mirroring
         `finance_tax_params`'s `unknownYearRefused` text shape exactly (never a throw, and
         never calling `executeRun` first). When defined, compute `const resolvedParamSetHash =
         paramSetHash(taxParamSet)` once. Change the final `handleRunOutcome(...)` call to
         supply the new argument: `handleRunOutcome(cas)(args.hash)(programArgs)(pinned)(pinFields)({
         taxYear: args.taxYear, paramSetHash: resolvedParamSetHash })(outcome)`.
       - Update the FOUR existing `fjsRunTool(...).handle({...})` calls in this file's own
         `proof` (verify current line numbers via `grep -n "fjsRunTool(" fjs/server/fjs_run/module.f.js`
         — last verified at approximately lines 1111, 1161, 1386, 1432) to add `taxYear: 2025`
         to each argument object.
       - Update EVERY existing `handleRunOutcome(...)` call in this file's own `proof` (verify
         via `grep -n "handleRunOutcome(" fjs/server/fjs_run/module.f.js` — last verified at
         approximately lines 456 [the production call, already handled above], 677, 847, 894,
         943, 986, 1008, 1033, 1073, 1219, 1266, 1302, 1357, 1467, 1687) by inserting one more
         curried application, `({ taxYear: 2025, paramSetHash: 'sha256-paramset-test' })`,
         immediately after the existing `pinFields`-shaped argument (the literal `{}`, `pinFields`,
         or similarly-named argument at that position) and before the final `(outcome)` /
         `(outcome2)` argument. Do this for every matched line; none are exempt — each one goes
         through `toolEntry`'s RTTI-free direct call, but the record it builds is now invalid
         without the new fields regardless.

    3. `fjs/server/module.f.js`: in `weekOneConvergence`'s `fjs_run` `tools/call` message
       (`grep -n "name: 'fjs_run'" fjs/server/module.f.js` — last verified at approximately line
       800), add `taxYear: 2025` to that call's `arguments` object.

    4. `fjs-run-integration.test.js`: add `taxYear: 2025` to the THREE existing `call('fjs_run',
       {...})` invocations (`grep -n "call('fjs_run'" fjs-run-integration.test.js` — last
       verified at approximately lines 326, 433, 482).

    5. `payer-report-integration.test.js`: add `taxYear: 2025` to the ONE existing
       `call('fjs_run', {...})` invocation (last verified at approximately line 228).

    Also add two new proof leaves to `fjs/server/fjs_run/module.f.js`'s own `proof` (a new
    top-level group, e.g. `taxYearHandling`), each independently named:
    - `missingTaxYearRejectedByToolEntry`: `fjsRunTool(home)(cas)(e).handle({ hash: someHash })`
      WITHOUT `taxYear` returns `isError: true` with text including `'invalid arguments'` —
      proves `toolEntry`'s own RTTI check, not this module's own code, is what refuses it.
    - `unknownTaxYearRefused`: `fjsRunTool(home)(cas)(e).handle({ hash: someHash, taxYear:
      1999 })` returns `isError: true`, and the text names both `'1999'` and `'2025'` —
      mirroring `finance_tax_params`'s own `unknownYearRefused` leaf.
  </action>
  <acceptance_criteria>
    - `grep -n "handleRunOutcome(" fjs/server/fjs_run/module.f.js` — every matched line's
      argument list now has one more curried application than before this task, ending in
      `(outcome)` or `(outcome2)` as before.
    - `grep -n "fjsRunTool(" fjs/server/fjs_run/module.f.js` — every matched `.handle({...})`
      call's argument object includes `taxYear`.
    - `grep -n "call('fjs_run'" fjs-run-integration.test.js payer-report-integration.test.js`
      and `grep -n "name: 'fjs_run'" fjs/server/module.f.js` — every matched call's arguments
      object includes `taxYear: 2025`.
    - A real `fjs_run` call missing `taxYear` returns `isError: true` naming "invalid arguments"
      (new proof leaf `missingTaxYearRejectedByToolEntry`).
    - A real `fjs_run` call with `taxYear: 1999` returns `isError: true` naming both "1999" and
      "2025" (new proof leaf `unknownTaxYearRefused`).
    - A successful `fjs_run` response's parsed JSON has `taxYear`, `paramSetHash` (a non-empty
      string), and `programHash` (equal to the hash of the program that was run) alongside the
      existing six keys.
    - `npm test 2>&1 | tail -20` reports `tsc` clean, all tests passing, exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>Schema widened in fjs/run/module.f.js; fjs_run refuses a missing or unknown taxYear;
  every one of the nine pre-existing call sites plus fifteen internal handleRunOutcome call
  sites gained the new argument(s); response and persisted record both carry
  taxYear/paramSetHash/programHash; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation Gate M2 — pinned derivation's && mutated to ||</name>
  <files>fjs/server/fjs_run/module.f.js</files>
  <read_first>
    fjs/server/fjs_run/module.f.js (the line `const pinned = args.subject !== undefined &&
    args.parents !== undefined` inside `fjsRunTool`'s handler, and the
    `pinIntegrity.subjectOnlyWithoutParentsPersistsPinnedFalse` proof leaf that already names
    this exact mutation in its own comment)
    fjs/report/provenance/module.f.js (`countsTowardReproducibilityAcceptance`, from Plan 19-01
    — check independently whether this mutation reddens its proof too, rather than assuming
    19-VALIDATION.md's prediction is exactly right)
    .planning/phases/19-reproducibility-and-report-provenance/19-VALIDATION.md (M2's own
    definition and predicted red set)
    AGENTS.md ("A mutation's predicted red set is itself a claim, and it is often wrong" section
    — record the ACTUAL observed red set, not the predicted one)
  </read_first>
  <action>
    Change `const pinned = args.subject !== undefined && args.parents !== undefined` to use
    `||` instead of `&&`. Run `npm test 2>&1 | tail -40` and record every failing leaf verbatim
    — expect at minimum `fjsRunTool.pinIntegrity.subjectOnlyWithoutParentsPersistsPinnedFalse`
    to fail (its own comment already names this exact mutation as the reason
    `assertEq(record.pinned, false)` exists). Separately check, and record honestly either way,
    whether `fjs/report/provenance/module.f.js`'s own
    `countsTowardReproducibilityAcceptance.rejectsAnUnpinnedRun`/`acceptsAPinnedRun` leaves also
    redden — they operate on a hand-built `Run` literal and never call `fjsRunTool`, so the
    prediction is that they do NOT redden; if they do, that is new information about a coupling
    nobody had written down, and must be recorded as such rather than silently treated as
    expected. After recording the full observed red set, revert the single `||` back to `&&`
    exactly and re-run `npm test 2>&1 | tail -20`, confirming `exit 0` and that `git status
    --porcelain -- fjs/server/fjs_run/module.f.js` reports nothing.
  </action>
  <acceptance_criteria>
    - With the mutation applied: `npm test` reports at least one failing test, and
      `fjsRunTool.pinIntegrity.subjectOnlyWithoutParentsPersistsPinnedFalse` is among the
      failures.
    - The full observed red set is recorded (in the plan's own execution notes / SUMMARY),
      including an explicit statement of whether the acceptance-predicate leaves reddened or
      stayed green.
    - After restoring `&&`: `npm test 2>&1 | tail -20` reports exit 0, and `git status
      --porcelain -- fjs/server/fjs_run/module.f.js` is empty.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -40</automated>
  </verify>
  <done>Mutation performed, full red set observed and recorded, code restored byte-identical,
  suite green, git status clean for the touched file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| MCP client → `fjs_run` tool | `taxYear` is a new, untrusted, request-supplied numeric argument crossing this boundary for the first time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Denial of Service | `fjsRunTool`'s `taxYear` lookup | mitigate | An unknown `taxYear` is refused via `errorResult` naming the offending year and the known set, before `executeRun` is ever called — never a throw, never a crashed connection (mirrors `finance_tax_params`'s own precedent). |
| T-19-02-02 | Repudiation | `pinned`'s `&&`/`||` derivation | mitigate | Mutation Gate M2 (Task 2) converts EXEC-13's "inherited as done" claim into an observed, watched-failing-then-restored property. |
| T-19-02-03 | Information Disclosure | `paramSetHash`/`taxYear` in the response and persisted record | accept | No PII; this exposure is PROV-04's entire purpose — deliberate provenance disclosure to the same trusted local single user this project's REQUIREMENTS.md Accepted Risks already cover. |
| T-19-02-04 | Tampering | a caller supplying a `taxYear` that does not match the program's actual embedded parameters | accept | Same accepted-risk shape as every other `fjs_run` argument in this single-user, locally-trusted tool; `paramSetHash`'s job is to make the mismatch VISIBLE in the output, not to prevent the caller from supplying one. |
</threat_model>

<verification>
`npm test 2>&1 | tail -20` after Task 1 (full suite, all nine external call sites plus fifteen
internal ones updated, green). `npm test 2>&1 | tail -40` twice during Task 2 (once mutated,
expected red; once restored, expected green), with the observed red set recorded either way.
</verification>

<success_criteria>
`fjs_run` requires and validates `taxYear`, refuses an unknown one by name, reports and persists
`taxYear`/`paramSetHash`/`programHash` together on every successful run; Mutation Gate M2 is
performed, its actual (not merely predicted) red set recorded, and the code restored
byte-identical; `npm test` green.
</success_criteria>

<output>
After completion, create `.planning/phases/19-reproducibility-and-report-provenance/19-02-SUMMARY.md`
</output>
