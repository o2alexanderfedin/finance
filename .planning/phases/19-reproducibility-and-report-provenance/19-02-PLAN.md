---
phase: 19
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - fjs/run/module.f.js
  - fjs/report/amend/module.f.js
  - fjs/media/dialects/module.f.js
  - fjs/server/fjs_run/module.f.js
  - fjs/server/module.f.js
  - fjs-run-integration.test.js
  - payer-report-integration.test.js
autonomous: true
requirements: [PROV-04, EXEC-13]

must_haves:
  truths:
    - "Calling fjs_run without taxYear is refused by toolEntry's own RTTI check before the handler ever runs"
    - "Calling fjs_run with an unknown taxYear is refused by name, naming both the offending year and the known set, never a throw"
    - "A successful fjs_run response carries taxYear, paramSetHash and programHash alongside the existing six keys"
    - "The persisted vnd.fjs.run record — both the ok and the error arm — carries the same taxYear/paramSetHash the response reported"
    - "Every Run-literal construction site in the repo (fjs/run/module.f.js's own fixtures AND fjs/report/amend/module.f.js's okRun/errorRun fixtures) supplies taxYear/paramSetHash once the fields become required, so npm test never goes red mid-wave"
    - "Mutating the pinned derivation's && to || reddens the existing subjectOnlyWithoutParentsPersistsPinnedFalse proof, and leaves the acceptance predicate's own proof green (confirmed structurally unreachable, not merely observed); the mutation is restored byte-identical afterward"
  artifacts:
    - path: fjs/run/module.f.js
      provides: "runSchema widened with required taxYear/paramSetHash fields"
      contains: "taxYear: number"
    - path: fjs/server/fjs_run/module.f.js
      provides: "taxYear threading, paramSetHash computation, envelope widening, unknown-year refusal"
      contains: "paramSetHash(taxParamSet)"
  key_links:
    - from: fjs/server/fjs_run/module.f.js
      to: fjs/report/provenance/module.f.js
      via: "paramSetHash(taxParamSet) call inside fjsRunTool's handler, before executeRun runs"
      pattern: "paramSetHash\\("
    - from: fjs/server/fjs_run/module.f.js
      to: fjs/run/module.f.js
      via: "handleRunOutcome's Run record literal now spreads provenance.taxYear/paramSetHash"
      pattern: "provenance\\.taxYear"
    - from: fjs/report/amend/module.f.js
      to: fjs/run/module.f.js
      via: "okRun/errorRun test fixtures construct Run literals directly and must satisfy the same widened runSchema"
      pattern: "taxYear: 2025"
---

<objective>
Wire `taxYear`/`paramSetHash` through `fjs_run`'s schema, its persisted `vnd.fjs.run` record,
and its response envelope — PROV-04's decision text is explicit that the header belongs in
BOTH places, not only the response. Then perform Mutation Gate M2, converting EXEC-13's
"inherited as done" pinned flag into an observed, watched-failing property.

Purpose: this is the atomic core of the phase. Widening `runSchema`/`Run` with REQUIRED fields
makes EVERY existing construction of a `Run`-shaped literal a potential `tsc` error, and
widening `fjsRunInputSchema` with a REQUIRED field makes `toolEntry`'s RTTI check reject every
existing caller that omits it. Both populations must be updated in this SAME commit, or `npm
test` goes red between commits (AGENTS.md's Phase 8/11/12 precedent).

**Two independent populations, both closed here — do not conflate them:**
1. **Runtime `fjs_run` tool-call sites** (9, `taxYear` missing from the call breaks the RTTI
   check): 4 inside `fjs/server/fjs_run/module.f.js`'s own proof (`fjsRunTool(...).handle(...)`),
   1 inside `fjs/server/module.f.js`'s `weekOneConvergence`, 3 inside
   `fjs-run-integration.test.js`, 1 inside `payer-report-integration.test.js`.
2. **`Run`-literal construction sites** (type-level — `taxYear`/`paramSetHash` missing breaks
   `tsc`, independent of whether anything ever calls `fjs_run` at runtime): `fjs/run/module.f.js`'s
   own `minimalOk`/`minimalError` fixtures, `fjs/server/fjs_run/module.f.js`'s
   `handleRunOutcome` (both the ok-arm and error-arm record literals — the one PRODUCTION
   construction site), and `fjs/report/amend/module.f.js`'s `okRun`/`errorRun` TEST-FIXTURE-ONLY
   helpers (found via `grep -rln "@import.*\bRun\b" fjs *.js`, which returns exactly these three
   files: `fjs/run/module.f.js` defines `Run`, the other two construct it).
   `fjs/server/fjs_run/module.f.js`'s FOURTEEN additional `handleRunOutcome(...)` proof-section
   calls are a THIRD, narrower population: they pass ALREADY-CONSTRUCTED arguments through an
   unchanged call shape, so they need the new curried argument added (population 1's concern:
   call-site arity), not a new `Run` literal (they never build one directly — `handleRunOutcome`
   builds it internally, from the arguments they supply).

Output: `fjs_run` accepts and validates `taxYear`, refuses an unknown one, reports and persists
`taxYear`/`paramSetHash`/`programHash` together; every `Run`-literal construction site and every
runtime call site is updated in this one commit; Mutation Gate M2 is performed and confirmed
against the corrected gate table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reproducibility-and-report-provenance/19-CONTEXT.md
@.planning/phases/19-reproducibility-and-report-provenance/19-PATTERNS.md
@.planning/phases/19-reproducibility-and-report-provenance/19-VALIDATION.md
@AGENTS.md
@fjs/report/provenance/module.f.js

<interfaces>
<!-- From Plan 19-01, already shipped and proof-covered when this plan starts. -->
`paramSetHash: (taxParamSet: TaxParamSet) => string` and `reviewedEstimateFraming: string`,
both exported from `fjs/report/provenance/module.f.js`. Import path from
`fjs/server/fjs_run/module.f.js`: `'../../report/provenance/module.f.js'`.

<!-- fjs/server/finance_tax_params/module.f.js's unknown-year refusal shape (COPY this, do not
     invent a new one) -->
```
export const knownYears = Object.keys(taxParamsResponses).map(Number)
...
const response = taxParamsResponses[args.year]
if (response === undefined) {
    return pure(errorResult(`unknown tax year: ${args.year}; known: ${knownYears.join(', ')}`))
}
```
`fjs_run` performs the identical lookup against `taxParamsByYear` (from
`fjs/tax/params/module.f.js`, already `{ readonly [year: number]: TaxParamSet }`) instead of
`taxParamsResponses`, and refuses the SAME way — `errorResult`, never a throw, before
`executeRun` is ever called.

<!-- fjs/run/module.f.js's runSchema (lines 104-115) and its two proof fixtures (lines 201-221),
     the ONLY two places a full Run literal is hand-typed in THAT file — every other proof leaf
     in it spreads one of these two. -->
```
export const runSchema = /** @type {const} */ ({
    ...base(dialect),
    programHash: string,
    args: array(string),
    pinned: boolean,
    subject: option(string),
    parents: option(array(string)),
    status: or('ok', 'error'),
    inputs: array(inputEntry),
    resultHash: option(string),
    error: option(string),
})
```
Add `taxYear: number` and `paramSetHash: string` after `args`, before `pinned` — both REQUIRED
(no `option`), because PROV-04 states every run, pinned or not, ok or error, carries this
header.

<!-- fjs/report/amend/module.f.js's okRun/errorRun (lines 377-397) — the SECOND, independent
     Run-literal construction site, found by grepping every @import of Run in the repo. Neither
     takes new parameters here: both are hand-typed constant literals, exactly like their
     existing `dialect`/`pinned`/`status`/`inputs` fields, and their four call sites (lines
     ~408, 409, 421, 422, inside runOkDiffFixture/statusErrorDiffFixture) need NO change — the
     curried signature `(programHash) => (args) => (resultHash|message) => Run` is unchanged. -->
```
const okRun = programHash => args => resultHash => ({
    dialect: runDialect,
    programHash,
    args,
    pinned: false,
    status: 'ok',
    inputs: [],
    resultHash,
})
const errorRun = programHash => args => message => ({
    dialect: runDialect,
    programHash,
    args,
    pinned: false,
    status: 'error',
    inputs: [],
    error: message,
})
```
Add `taxYear: 2025, paramSetHash: 'sha256-paramset1'` as two more hand-typed fields to EACH of
these two returned object literals. `amendmentDiff` (the function under test in this file) never
reads `taxYear`/`paramSetHash` — it only compares `programHash`/`args` and diffs the wire
result — so any valid constant satisfies `runSchema`; there is no reason to thread a real value
through.

<!-- fjs/server/fjs_run/module.f.js's handleRunOutcome (lines 350-405) and fjsRunTool (lines
     432-459) — the PRODUCTION Run-literal construction site, and the function this plan's
     Task 1 rewrites the most. -->
`handleRunOutcome`'s current curried signature: `cas => programHash => programArgs => pinned =>
pinFields => rawOutcome => { ... }`. Insert ONE new parameter, named `provenance` and typed `{
readonly taxYear: number, readonly paramSetHash: string }`, between `pinFields` and `rawOutcome`
— NOT as two separate new curry levels (that would double the edits at every call site for no
benefit).

`fjsRunTool`'s current handler body (inside the `args =>` closure, lines 439-457):
```
const programArgs = args.args ?? []
const pinned = args.subject !== undefined && args.parents !== undefined
const pinFields = /** @type {...} */ (pinned ? { subject: args.subject, parents: args.parents } : {})
return step(
    executeRun(materializeHomeRoot)(cas)(evoApi)({ hash: args.hash, args: programArgs, ...pinFields }),
    outcome => handleRunOutcome(cas)(args.hash)(programArgs)(pinned)(pinFields)(outcome),
)
```
The unknown-year check and `resolvedParamSetHash` computation belong BEFORE the `step(...)`
call (a genuine short-circuit, mirroring `finance_tax_params`'s own early-return shape); the
final line's `handleRunOutcome(...)` call gains the new `provenance` argument between `pinFields`
and `outcome`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire taxYear/paramSetHash through every schema, record, response and Run-literal construction site — one commit</name>
  <files>fjs/run/module.f.js, fjs/report/amend/module.f.js, fjs/server/fjs_run/module.f.js, fjs/server/module.f.js, fjs-run-integration.test.js, payer-report-integration.test.js</files>
  <read_first>
    fjs/run/module.f.js (whole file: `runSchema`, `checkReferences`, `minimalOk`/`minimalError`)
    fjs/report/amend/module.f.js (lines 44-63 for import style, lines 377-397 for `okRun`/
    `errorRun`, lines 405-425 for their four call sites — confirm no call site needs editing)
    fjs/server/fjs_run/module.f.js (whole file — this is the file with the most edits; read the
    module docstring, `fjsRunInputSchema`, `handleRunOutcome`, `fjsRunTool`, and every one of its
    own `fjsRunTool(...).handle(...)` and `handleRunOutcome(...)` call sites in its own `proof`)
    fjs/server/finance_tax_params/module.f.js (the unknown-year refusal pattern and the
    `number`-from-rtti import, copied verbatim in shape)
    fjs/tax/params/module.f.js (lines 704-740: `TaxParamSet`, `taxParamsByYear`)
    fjs/report/provenance/module.f.js (this plan's own dependency, from Plan 19-01 — `paramSetHash`,
    `reviewedEstimateFraming`)
    fjs/server/module.f.js (lines ~760-810: `weekOneConvergence`'s own `fjs_run` `tools/call`)
    fjs-run-integration.test.js (lines ~320-500: the three `call('fjs_run', ...)` sites)
    payer-report-integration.test.js (lines ~220-232: the one `call('fjs_run', ...)` site)
  </read_first>
  <action>
    Work through this checklist in order. Every line number is a last-verified reference point —
    confirm the current line with the `grep` command shown before editing, since earlier items in
    this same checklist shift later ones.

    **A. `fjs/run/module.f.js` — the Run type definition (population 2)**
    - [ ] A1. Add `number` to the existing `import { array, boolean, option, or, string }` line
          (from `functionalscript/fjs/types/rtti/module.f.js`).
    - [ ] A2. Add `taxYear: number` and `paramSetHash: string` to `runSchema`, positioned after
          `args` and before `pinned`.
    - [ ] A3. Add `taxYear: 2025, paramSetHash: 'sha256-paramset1'` to the `minimalOk` fixture.
    - [ ] A4. Add `taxYear: 2025, paramSetHash: 'sha256-paramset1'` to the `minimalError` fixture.
    - No other edit in this file — every proof leaf besides the two fixtures spreads one of them.

    **B. `fjs/report/amend/module.f.js` — the second Run-literal construction site (population 2)**
    - [ ] B1. Add `taxYear: 2025, paramSetHash: 'sha256-paramset1'` as two more hand-typed fields
          inside `okRun`'s returned object literal (`grep -n "const okRun" fjs/report/amend/module.f.js`
          — last verified line 378).
    - [ ] B2. Add the SAME two fields inside `errorRun`'s returned object literal (last verified
          line 389).
    - [ ] B3. No change at `okRun`/`errorRun`'s four call sites (`runOkDiffFixture`,
          `statusErrorDiffFixture`, last verified lines 408, 409, 421, 422) — their curried
          call signature (`programHash => args => resultHash|message`) is unchanged; only the
          object each one RETURNS gained two more constant fields.

    **C. `fjs/server/fjs_run/module.f.js` — schema, handler, envelope, and the production Run
    literal (populations 1, 2 and 3 all live in this one file)**
    - [ ] C1. Add `number` to the existing `import { option, array, string } from
          'functionalscript/fjs/types/rtti/module.f.js'` line.
    - [ ] C2. Import `taxParamsByYear` from `'../../tax/params/module.f.js'`.
    - [ ] C3. Import `paramSetHash, reviewedEstimateFraming` from
          `'../../report/provenance/module.f.js'`.
    - [ ] C4. Add `taxYear: number` to `fjsRunInputSchema`.
    - [ ] C5. Widen `handleRunOutcome`'s curried signature: insert a new `provenance` parameter
          (typed `{ readonly taxYear: number, readonly paramSetHash: string }`) between the
          existing `pinFields` and `rawOutcome` parameters; update its own `@type` JSDoc line.
    - [ ] C6. Add `taxYear: provenance.taxYear, paramSetHash: provenance.paramSetHash` to the
          ok-arm `Run` record literal (population 2, production site).
    - [ ] C7. Add the SAME two fields to the error-arm `Run` record literal (population 2,
          production site).
    - [ ] C8. Add `taxYear: provenance.taxYear, paramSetHash: provenance.paramSetHash,
          programHash` (the existing curried parameter, referenced directly) `,
          reviewedEstimateFraming` (the imported constant, referenced directly) to the ok-arm's
          response envelope literal, alongside the existing six keys.
    - [ ] C9. Inside `fjsRunTool`'s handler, BEFORE the existing `return step(executeRun(...),
          ...)` line: look up `const taxParamSet = taxParamsByYear[args.taxYear]`; if
          `undefined`, return `pure(errorResult(...))` naming `args.taxYear` and
          `Object.keys(taxParamsByYear).map(Number).join(', ')` — mirror
          `finance_tax_params`'s `unknownYearRefused` text shape, never a throw, never calling
          `executeRun` first.
    - [ ] C10. When defined, compute `const resolvedParamSetHash = paramSetHash(taxParamSet)`
          once; change the `handleRunOutcome(...)` call to
          `handleRunOutcome(cas)(args.hash)(programArgs)(pinned)(pinFields)({ taxYear:
          args.taxYear, paramSetHash: resolvedParamSetHash })(outcome)` (population 1's
          production call, and population 3's production call, both satisfied by this one edit).
    - [ ] C11-C14. Update the FOUR existing `fjsRunTool(...).handle({...})` calls in this file's
          own `proof` (`grep -n "fjsRunTool(" fjs/server/fjs_run/module.f.js` — last verified
          lines 1111, 1161, 1386, 1432) — add `taxYear: 2025` to each argument object
          (population 1, test sites).
    - [ ] C15-C28. Update the FOURTEEN existing `handleRunOutcome(...)` calls in this file's own
          `proof` section (`grep -n "handleRunOutcome(" fjs/server/fjs_run/module.f.js` — last
          verified lines 677, 847, 894, 943, 986, 1008, 1033, 1073, 1219, 1266, 1302, 1357, 1467,
          1687 — the FIFTEENTH match, line 456, is the production call already handled by C10)
          — insert one more curried application, `({ taxYear: 2025, paramSetHash:
          'sha256-paramset1' })`, immediately after the existing `pinFields`-shaped argument and
          before the final `(outcome)`/`(outcome2)` argument, at every one of these fourteen
          lines (population 3, test sites — they pass through to `handleRunOutcome`'s
          production-shaped record-building, so a valid constant is enough here too).
    - [ ] C29. Add two new proof leaves to a new top-level `proof` group (e.g.
          `taxYearHandling`): `missingTaxYearRejectedByToolEntry` (`fjsRunTool(...).handle({
          hash: someHash })` without `taxYear` returns `isError: true` naming "invalid
          arguments") and `unknownTaxYearRefused` (`.handle({ hash: someHash, taxYear: 1999 })`
          returns `isError: true` naming both "1999" and "2025", mirroring
          `finance_tax_params`'s own `unknownYearRefused` leaf).

    **D. `fjs/server/module.f.js` — one runtime call site (population 1)**
    - [ ] D1. `grep -n "name: 'fjs_run'" fjs/server/module.f.js` (last verified line 800): add
          `taxYear: 2025` to that call's `arguments` object.

    **E. `fjs-run-integration.test.js` — three runtime call sites (population 1)**
    - [ ] E1-E3. `grep -n "call('fjs_run'" fjs-run-integration.test.js` (last verified lines
          326, 433, 482): add `taxYear: 2025` to each.

    **F. `payer-report-integration.test.js` — one runtime call site (population 1)**
    - [ ] F1. Last verified line 228: add `taxYear: 2025` to the `call('fjs_run', {...})`
          invocation.

    **G. `fjs/media/dialects/module.f.js` — a RUNTIME-VALIDATED `Run` fixture (population 4)**

    > **This site is invisible to `tsc` AND to the `@import`-based grep that found population 2.**
    > It was missed by the planner and by the first plan-check pass, and found only by an
    > empirical probe. Read this paragraph before editing, because the reason it hides is the
    > reason a fifth site could hide too.
    >
    > `fixtures` (line 172) is typed `{ readonly [dialect: string]: Readonly<Record<string,
    > unknown>> }`. A `Run`-shaped literal stored under that type is **not** checked against
    > `Run` by the compiler — missing required fields produce no `tsc` error at all. The fixture
    > is instead validated at **runtime**, through `dialectEntry(runSchema, v => checkRun(v)[0]
    > === 'ok')` (lines 93-97), consumed by `detectFinance = detect(financeDialects)`.
    >
    > Verified empirically against the real RTTI validator with the widened schema from A1-A4:
    > the un-updated fixture fails with `{"path":["taxYear"],"message":"unexpected value"}`.
    > `dialectEntry`'s `match` then returns `false`, `detect()` falls through to the non-dialect
    > fallback, and this file's own generated proof leaf
    > `proof.detectFinance['vnd.fjs.run']` (lines 288-296) fails its
    > `assertEq(meta.mime_type, 'application/vnd.fjs.run+json')`.

    - [ ] G1. Lines 242-250, the `[runDialect]` entry of `fixtures`: add `taxYear: 2025,` and
          `paramSetHash: 'sha256-paramset1',` to the literal, matching the values B1-B2 use in
          `fjs/report/amend/module.f.js` so all hand-typed `Run` fixtures in the repo agree.

    **Before declaring this task done, re-derive the population instead of trusting any single
    grep.** Three separate greps have each returned a *subset* of the real answer during this
    phase's planning. The command that actually enumerates every `Run`-shaped literal is:
    `grep -rn "pinned:" fjs *.js | grep -v '^\s*\*'` — it returns exactly four source files
    (`fjs/run`, `fjs/report/amend`, `fjs/media/dialects`, and `fjs/server/fjs_run`'s two
    shorthand `pinned,` sites, which C-section edits cover). If it returns a fifth, that file
    needs the same treatment and this checklist is incomplete.
  </action>
  <acceptance_criteria>
    - `grep -c "taxYear: 2025" fjs/report/amend/module.f.js` reports 2 (`okRun`/`errorRun`).
    - `grep -c "taxYear: 2025" fjs/media/dialects/module.f.js` reports 1 (the `[runDialect]`
      fixture), and `npm test` shows the `detectFinance` leaf asserting
      `application/vnd.fjs.run+json` still passing — this leaf is the ONLY signal that
      population 4 was handled, since `tsc` cannot see it.
    - `grep -rn "pinned:" fjs *.js | grep -v '^\s*\*'` returns literals in exactly four source
      files, all updated: `fjs/run`, `fjs/report/amend`, `fjs/media/dialects`,
      `fjs/server/fjs_run`. A fifth file means the checklist missed a site.
    - `grep -n "handleRunOutcome(" fjs/server/fjs_run/module.f.js` — every one of the fifteen
      matched lines' argument list now has one more curried application than before this task.
    - `grep -n "fjsRunTool(" fjs/server/fjs_run/module.f.js` — every matched `.handle({...})`
      call's argument object includes `taxYear`.
    - `grep -n "call('fjs_run'" fjs-run-integration.test.js payer-report-integration.test.js`
      and `grep -n "name: 'fjs_run'" fjs/server/module.f.js` — every matched call's arguments
      object includes `taxYear: 2025`.
    - A real `fjs_run` call missing `taxYear` returns `isError: true` naming "invalid arguments"
      (new proof leaf `missingTaxYearRejectedByToolEntry`).
    - A real `fjs_run` call with `taxYear: 1999` returns `isError: true` naming both "1999" and
      "2025" (new proof leaf `unknownTaxYearRefused`).
    - A successful `fjs_run` response's parsed JSON has `taxYear`, `paramSetHash` (a non-empty
      string), and `programHash` (equal to the hash of the program that was run) alongside the
      existing six keys.
    - `fjs/report/amend/module.f.js`'s own `amendmentDiff` proof suite is unaffected (its fixtures
      still validate against the widened `runSchema`; no assertion in that file reads
      `taxYear`/`paramSetHash`, so none needed updating beyond the two literal additions).
    - `npm test 2>&1 | tail -20` reports `tsc` clean, all tests passing, exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>Schema widened in fjs/run/module.f.js; both Run-literal construction sites
  (fjs/run/module.f.js's own fixtures and fjs/report/amend/module.f.js's okRun/errorRun) and the
  production construction site (handleRunOutcome) all supply taxYear/paramSetHash; fjs_run
  refuses a missing or unknown taxYear; every one of the nine runtime call sites plus fourteen
  internal handleRunOutcome proof-section call sites gained the new argument(s); response and
  persisted record both carry taxYear/paramSetHash/programHash; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation Gate M2 — pinned derivation's && mutated to ||</name>
  <files>fjs/server/fjs_run/module.f.js</files>
  <read_first>
    fjs/server/fjs_run/module.f.js (the line `const pinned = args.subject !== undefined &&
    args.parents !== undefined` inside `fjsRunTool`'s handler, and the
    `pinIntegrity.subjectOnlyWithoutParentsPersistsPinnedFalse` proof leaf that already names
    this exact mutation in its own comment)
    fjs/run/module.f.js (lines 160-163: `checkReferences`'s both-or-neither rule — this is WHY
    the mixed case can never reach the acceptance predicate as a validated `Run`)
    fjs/report/provenance/module.f.js (`countsTowardReproducibilityAcceptance`, from Plan 19-01
    — its own proof is expected to STAY GREEN under this mutation; see the corrected gate table
    below for why)
    .planning/phases/19-reproducibility-and-report-provenance/19-VALIDATION.md (M2's row and its
    2026-08-12 correction note, immediately below the gate table — read the correction, not just
    the table: the required red set is now exactly ONE leaf, and demanding the predicate's
    rejection arm redden was asking for a structurally unreachable state)
  </read_first>
  <action>
    Change `const pinned = args.subject !== undefined && args.parents !== undefined` to use
    `||` instead of `&&`. Run `npm test 2>&1 | tail -40` and confirm
    `fjsRunTool.pinIntegrity.subjectOnlyWithoutParentsPersistsPinnedFalse` fails — this is the
    ONE required red leaf per 19-VALIDATION.md's corrected gate table: `&&`/`||` diverge only on
    the mixed case (exactly one of `subject`/`parents` supplied), which that mutation turns into
    `pinned: true` with the OTHER field left `undefined` — a shape `fjs/run/module.f.js`'s
    `checkReferences` (lines 160-163) rejects outright, so `handleRunOutcome`'s own
    record-assembly `assert` fires before any `Run` value is ever built. A pinned-without-parents
    `Run` cannot reach `countsTowardReproducibilityAcceptance`'s input at all — confirm its own
    proof (Plan 19-01) stays green under this SAME mutation, as a positive confirmation of that
    fact, not merely an absence of failure to shrug off. Do not require it to redden; requiring
    that was 19-VALIDATION.md's own original error, already corrected. After confirming the one
    required leaf fails and the predicate's leaves stay green, revert `||` back to `&&` exactly
    and re-run `npm test 2>&1 | tail -20`, confirming `exit 0` and that `git status --porcelain
    -- fjs/server/fjs_run/module.f.js` reports nothing.
  </action>
  <acceptance_criteria>
    - With the mutation applied: `npm test` fails, and
      `fjsRunTool.pinIntegrity.subjectOnlyWithoutParentsPersistsPinnedFalse` is among the
      failures (the required red leaf).
    - With the SAME mutation applied: `fjs/report/provenance/module.f.js`'s
      `countsTowardReproducibilityAcceptance.rejectsAnUnpinnedRun`/`acceptsAPinnedRun` leaves
      both stay green, confirming they are structurally unreachable by this mutation.
    - After restoring `&&`: `npm test 2>&1 | tail -20` reports exit 0, and `git status
      --porcelain -- fjs/server/fjs_run/module.f.js` is empty.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -40</automated>
  </verify>
  <done>Mutation performed, the one required red leaf confirmed failing, the acceptance
  predicate's leaves confirmed to stay green, code restored byte-identical, suite green, git
  status clean for the touched file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| MCP client → `fjs_run` tool | `taxYear` is a new, untrusted, request-supplied numeric argument crossing this boundary for the first time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Denial of Service | `fjsRunTool`'s `taxYear` lookup | mitigate | An unknown `taxYear` is refused via `errorResult` naming the offending year and the known set, before `executeRun` is ever called — never a throw, never a crashed connection (mirrors `finance_tax_params`'s own precedent). |
| T-19-02-02 | Repudiation | `pinned`'s `&&`/`||` derivation | mitigate | Mutation Gate M2 (Task 2) is performed against the corrected, achievable red set — a leaf that already existed and already named this exact mutation in its own comment. |
| T-19-02-03 | Information Disclosure | `paramSetHash`/`taxYear` in the response and persisted record | accept | No PII; this exposure is PROV-04's entire purpose — deliberate provenance disclosure to the same trusted local single user this project's REQUIREMENTS.md Accepted Risks already cover. |
| T-19-02-04 | Tampering | a caller supplying a `taxYear` that does not match the program's actual embedded parameters | accept | Same accepted-risk shape as every other `fjs_run` argument in this single-user, locally-trusted tool; `paramSetHash`'s job is to make the mismatch VISIBLE in the output, not to prevent the caller from supplying one. |
</threat_model>

<verification>
`npm test 2>&1 | tail -20` after Task 1 (full suite, all nine runtime call sites, fourteen
internal `handleRunOutcome` sites, and both `Run`-literal construction sites updated, green).
`npm test 2>&1 | tail -40` twice during Task 2 (once mutated, once restored), confirming the
corrected, single-leaf red set.
</verification>

<success_criteria>
`fjs_run` requires and validates `taxYear`, refuses an unknown one by name, reports and persists
`taxYear`/`paramSetHash`/`programHash` together on every successful run; every `Run`-literal
construction site in the repo satisfies the widened schema; Mutation Gate M2 is performed
against its corrected required red set and the code restored byte-identical; `npm test` green.
</success_criteria>

<output>
After completion, create `.planning/phases/19-reproducibility-and-report-provenance/19-02-SUMMARY.md`
</output>
