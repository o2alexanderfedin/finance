---
phase: 19
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - fjs-run-integration.test.js
  - fjs/server/fjs_run/snapshot/module.f.js
autonomous: true
requirements: [PROV-05, PROV-04, EXEC-13]

must_haves:
  truths:
    - "The real fjs_run response over a live MCP session carries taxYear/paramSetHash/programHash together, on the one call this file already proves reaches the real response shape"
    - "The SAME scenario, run unpinned, visibly moves after an amendment lands on the subject — built and observed BEFORE the pinned assertion is trusted"
    - "A pinned rerun after the SAME amendment reproduces byte-identical output: equal resultHash strings AND equal fetched bytes from CAS"
    - "The EXEC-13 acceptance predicate returns true for the pinned run's persisted record and false for the unpinned control's persisted record"
    - "Mutating buildRunSnapshot's pinned branch to resolve the live head reddens the new pinned-reproduction assertion; the mutation is restored byte-identical afterward"
  artifacts:
    - path: fjs-run-integration.test.js
      provides: "PROV-04 header assertions on the decisive call; PROV-05's control-then-pinned reproducibility proof"
      contains: "countsTowardReproducibilityAcceptance"
  key_links:
    - from: fjs-run-integration.test.js
      to: fjs/report/provenance/module.f.js
      via: "import and call of countsTowardReproducibilityAcceptance against persisted run records fetched back from CAS"
      pattern: "countsTowardReproducibilityAcceptance\\("
    - from: fjs-run-integration.test.js
      to: fjs/server/fjs_run/snapshot/module.f.js
      via: "the pinned-reproduction assertion is the decisive target Mutation Gate M1 mutates buildRunSnapshot against"
      pattern: "resultHash"
---

<objective>
Extend the real-process integration test with PROV-04's header assertions and PROV-05's
adversarial, control-first, byte-identical reproduction proof — the phase's central
deliverable. Then perform Mutation Gate M1, the single line the whole requirement rests on.

Purpose: PROV-05 asserts that output did NOT move. An assertion that nothing happened passes
trivially if the mechanism under test never ran at all (this is exactly how Phase 15 shipped two
vacuous proofs). 19-VALIDATION.md therefore makes the unpinned control leg mandatory and ordered
FIRST: this plan builds and observes the control MOVING before it ever asserts the pinned run's
stability, as an explicit sequence within one task, not a comment.

Output: `fjs-run-integration.test.js` proves PROV-04 and PROV-05 against a real, separate
process; Mutation Gate M1 is performed and recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-reproducibility-and-report-provenance/19-CONTEXT.md
@.planning/phases/19-reproducibility-and-report-provenance/19-PATTERNS.md
@.planning/phases/19-reproducibility-and-report-provenance/19-VALIDATION.md
@AGENTS.md
@fjs/report/provenance/module.f.js

<interfaces>
<!-- Names already established in fjs-run-integration.test.js by prior plans/waves — reuse
     these, do not re-seed. -->
`docAHash`, `docBHash`, `docCHash` (~lines 258-260): three already-seeded `vnd.fjs.1099int`
document hashes, `docAHash`/`docBHash` distinct content.
`pinProgramSource = 'export const report = ctx => args => ctx.evoHead(args[0])'` and
`pinProgramHash` (~lines 429-430): an already-materialized program whose report is exactly
"resolve the head of the subject named in args[0]" — the correct shape for both the control and
pinned legs below, since its output is directly the JSON-stringified array of head hashes for
whatever subject the pin/live resolution yields.
`casAdd`, `call` (~lines 198-256): the existing helpers — `call(name, args)` sends a real
`tools/call` and returns the parsed JSON-RPC response; `casAdd(content)` wraps `cas_add` and
returns the new hash.
The existing pin-setup block (~lines 413-464) already shows the exact `evo_add` → pinned
`fjs_run` → `cas_get(hash, content:true)` → compare shape to copy for the pinned leg below;
`assert.notEqual(pinResultMeta.text, JSON.stringify([pinLiveRevHash]))` (~line 454) is this
file's own established idiom for "prove it would have been different."

<!-- fjs/server/fjs_run/snapshot/module.f.js's buildRunSnapshot — Mutation Gate M1's target,
     lines 301-318 of that file. -->
```
return mapStep(
    withHeads,
    state => {
        const publicState = { blobs: ..., activeSubjects: ..., archivedSubjects: ..., heads: ..., revisions: ... }
        return pin === undefined
            ? publicState
            : { ...publicState, heads: { ...publicState.heads, [pin.subject]: pin.parents } }
    },
)
```
The pinned branch's value must change from `pin.parents` to `publicState.heads[pin.subject] ??
[]` — this KEEPS `pin` referenced (via `pin.subject`), so the mutation compiles under
`noUnusedParameters`/`noUnusedLocals`; it changes the override's effect (resolves to whatever
the LIVE store already computed for that subject, i.e. the pin does nothing) without orphaning
any binding — the exact failure mode AGENTS.md's "mutation that deletes the last use of a
binding does not compile" section warns against.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROV-04 header assertions and PROV-05's control-then-pinned reproducibility proof</name>
  <files>fjs-run-integration.test.js</files>
  <read_first>
    fjs-run-integration.test.js (the whole file — in particular the decisive call block ~lines
    323-360 post-19-02 [line numbers shift after Plan 19-02's `taxYear` insertions; re-locate via
    `grep -n "The decisive call"`], the existing pin-setup block ~400-464, the zero-read
    adversary block ~466-499, and the `casAdd`/`call` helpers ~198-256)
    fjs/report/provenance/module.f.js (`paramSetHash`, `countsTowardReproducibilityAcceptance` —
    from Plan 19-01)
    .planning/phases/19-reproducibility-and-report-provenance/19-VALIDATION.md ("The control leaf
    is not optional" section — build and observe the control MOVING before the pinned assertion)
    .planning/phases/19-reproducibility-and-report-provenance/19-CONTEXT.md ("The Adversarial
    Reproduction Proof (PROV-05)" decisions: what changes between runs, the two-way
    byte-identical check)
  </read_first>
  <action>
    Add `import { countsTowardReproducibilityAcceptance } from
    './fjs/report/provenance/module.f.js'` alongside this file's other `.f.js` imports near the
    top.

    PROV-04: immediately after the existing `readCount`/`literalCount` assertions on the
    decisive call's `parsed` object, add three assertions on the SAME `parsed` value:
    `assert.equal(parsed.taxYear, 2025)`; `assert.ok(typeof parsed.paramSetHash === 'string' &&
    parsed.paramSetHash !== '')`; `assert.equal(parsed.programHash, programHash)` (the variable
    already in scope from `casAdd(programSource)` earlier in the same block).

    PROV-05, added as a new section after the existing pin-setup block, in this exact order
    (the control leg's code must appear BEFORE the pinned leg's code in the file, and its
    movement assertion must be observed to pass before the pinned leg is written/trusted):

    1. Control leg (unpinned) — build and observe movement first:
       - `evo_add({ parents: [], subject: 'finance-integration-prov05-control', snapshot:
         docAHash })` → capture the returned revision hash as `controlRev1`.
       - `fjs_run({ hash: pinProgramHash, args: ['finance-integration-prov05-control'], taxYear:
         2025 })` (no `subject`/`parents` — an ordinary unpinned run) → `controlRun1`; fetch its
         `resultHash`'s bytes via `cas_get(hash, content:true)` → `controlBytes1`.
       - `evo_add({ parents: [controlRev1], subject: 'finance-integration-prov05-control',
         snapshot: docBHash })` — the amendment, added between the two control runs.
       - Run the SAME unpinned `fjs_run` call again (identical arguments) → `controlRun2`; fetch
         its bytes → `controlBytes2`.
       - `assert.notEqual(controlBytes1, controlBytes2)` — the control MOVED. If this assertion
         does not pass, STOP: do not write the pinned leg against a scenario that does not
         demonstrate movement at all (19-VALIDATION.md's own instruction).

    2. Pinned leg (only after step 1's assertion passes) — the decisive PROV-05 assertion:
       - `evo_add({ parents: [], subject: 'finance-integration-prov05-pinned', snapshot:
         docAHash })` → `pinnedRev1`.
       - `fjs_run({ hash: pinProgramHash, args: ['finance-integration-prov05-pinned'], subject:
         'finance-integration-prov05-pinned', parents: [pinnedRev1], taxYear: 2025 })` →
         `pinnedRun1`; fetch its `resultHash`'s bytes → `pinnedBytes1`.
       - `evo_add({ parents: [pinnedRev1], subject: 'finance-integration-prov05-pinned',
         snapshot: docBHash })` — the SAME shape of amendment as the control leg, landing on the
         pinned subject BETWEEN the two pinned runs.
       - Run the SAME pinned `fjs_run` call again — SAME `subject`, SAME `parents: [pinnedRev1]`
         (the FIRST revision, never the new one just added) → `pinnedRun2`; fetch its bytes →
         `pinnedBytes2`.
       - `assert.equal(pinnedRun1.resultHash, pinnedRun2.resultHash)` (hash-string equality) AND
         `assert.equal(pinnedBytes1, pinnedBytes2)` (actual-byte equality) — both checks, per
         19-CONTEXT.md's decision that hash equality alone is close to a content-addressing
         tautology.

    3. EXEC-13 consumption: fetch both `pinnedRun1`'s and `controlRun1`'s persisted
       `vnd.fjs.run` records via `cas_get(runHash, content:true)` + `JSON.parse` (mirror the
       existing `pinRunRecord` pattern ~lines 459-464). Assert
       `countsTowardReproducibilityAcceptance(pinnedRunRecord) === true` and
       `countsTowardReproducibilityAcceptance(controlRunRecord) === false`.
  </action>
  <acceptance_criteria>
    - The control leg's `assert.notEqual(controlBytes1, controlBytes2)` appears BEFORE the
      pinned leg's code in the file (source order, not just execution order).
    - `pinnedRun1.resultHash === pinnedRun2.resultHash` and `pinnedBytes1 === pinnedBytes2` both
      hold.
    - `countsTowardReproducibilityAcceptance` is imported and called against two REAL,
      CAS-fetched run records (not hand-built fixtures), returning `true` for the pinned one and
      `false` for the control.
    - The decisive call's `parsed` object carries `taxYear === 2025`, a non-empty
      `paramSetHash`, and `programHash` equal to the run program's own hash.
    - `node --test fjs-run-integration.test.js 2>&1 | tail -30` passes (this root-level file is
      individually runnable per AGENTS.md's stated exception).
    - `npm test 2>&1 | tail -20` reports exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test fjs-run-integration.test.js 2>&1 | tail -30 && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>PROV-04 header assertions added; PROV-05's control-then-pinned reproducibility proof
  passes with the control observed moving first; EXEC-13's predicate consumed against real
  persisted records; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation Gate M1 — buildRunSnapshot's pinned branch resolves the live head</name>
  <files>fjs/server/fjs_run/snapshot/module.f.js</files>
  <read_first>
    fjs/server/fjs_run/snapshot/module.f.js (`buildRunSnapshot`'s final `mapStep` return, lines
    301-318 — the exact ternary Mutation Gate M1 targets)
    fjs-run-integration.test.js (this plan's own Task 1 output — the PROV-05 pinned-reproduction
    assertion is the decisive target this mutation must redden)
    .planning/phases/19-reproducibility-and-report-provenance/19-VALIDATION.md (M1's own
    definition)
    AGENTS.md ("The mutation that deletes the last use of a binding does not compile" and "A
    mutation's predicted red set is itself a claim, and it is often wrong" sections)
  </read_first>
  <action>
    In `buildRunSnapshot`'s final `mapStep` callback, change the pinned branch from `{
    ...publicState, heads: { ...publicState.heads, [pin.subject]: pin.parents } }` to `{
    ...publicState, heads: { ...publicState.heads, [pin.subject]: publicState.heads[pin.subject]
    ?? [] } }` — this drops the override's EFFECT (the pinned subject now resolves to whatever
    the live store already computed) while keeping `pin` referenced via `pin.subject`, so the
    mutation compiles.

    Run `npm test 2>&1 | tail -40` and record the FULL observed red set. Expect at minimum this
    plan's own new pinned-reproduction assertion (Task 1) to fail. Separately check, and record
    honestly either way, whether any of these ALSO redden (this mutation is central and likely
    reddens more than one proof — AGENTS.md requires recording the true set, not only the
    predicted one): `fjs/server/fjs_run/snapshot/module.f.js`'s own
    `buildRunSnapshotResolvesTheStore.pinOverridesTheResolvedHead` proof;
    `fjs/server/fjs_run/module.f.js`'s `executeRun.pinOverridesTheLiveHeadThroughFullExecuteRun`
    proof; and the pre-existing real-process pin proof in this same integration test file
    (`assert.notEqual(pinResultMeta.text, ...)` around the pin-setup block).

    After recording the observed red set, revert the change back to `pin.parents` exactly and
    re-run `npm test 2>&1 | tail -20`, confirming `exit 0` and that `git status --porcelain --
    fjs/server/fjs_run/snapshot/module.f.js` reports nothing.
  </action>
  <acceptance_criteria>
    - With the mutation applied: `npm test` fails, and Task 1's PROV-05 pinned-reproduction
      assertion is among the failures.
    - The full observed red set is recorded (in the SUMMARY), including whether each of the
      three additional proofs named above reddened or stayed green.
    - After restoring `pin.parents`: `npm test 2>&1 | tail -20` reports exit 0, and `git status
      --porcelain -- fjs/server/fjs_run/snapshot/module.f.js` is empty.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -40</automated>
  </verify>
  <done>Mutation performed, full red set observed and recorded, code restored byte-identical,
  suite green, git status clean for the touched file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Real MCP session ↔ separate `node index.js` process | Already an established boundary (Phase 7); this plan adds no new one, only new assertions over the same real-process channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering | `buildRunSnapshot`'s pinned-heads override | mitigate | Mutation Gate M1 (Task 2) is the single line PROV-05 rests on; observed reddening the pinned-reproduction assertion converts "should be pinned" into "watched to be pinned." |
| T-19-03-02 | Repudiation | PROV-05's "output did not move" claim | mitigate | The unpinned control leg is built and its movement observed BEFORE the pinned assertion is trusted, per 19-VALIDATION.md — a proof that something did not happen is otherwise indistinguishable from a proof of nothing. |
| T-19-03-03 | Denial of Service | none new | accept | Test-only file; no new production input surface. |
</threat_model>

<verification>
`node --test fjs-run-integration.test.js 2>&1 | tail -30` then `npm test 2>&1 | tail -20` after
Task 1. `npm test 2>&1 | tail -40` twice during Task 2 (mutated, expected red; restored,
expected green), with the observed red set recorded either way.
</verification>

<success_criteria>
The real-process integration test proves PROV-04's header and PROV-05's adversarial,
control-first, byte-identical reproduction; EXEC-13's predicate is consumed against real
persisted records; Mutation Gate M1 is performed, its actual red set recorded, and the code
restored byte-identical; `npm test` green. This closes the phase: EXEC-13, PROV-04, and PROV-05
are all delivered and mutation-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/19-reproducibility-and-report-provenance/19-03-SUMMARY.md`
</output>
