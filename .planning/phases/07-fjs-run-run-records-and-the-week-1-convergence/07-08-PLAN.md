---
phase: 07-fjs-run-run-records-and-the-week-1-convergence
plan: 08
type: execute
wave: 5
depends_on: ["07-03", "07-06", "07-07"]
files_modified: [fjs/server/module.f.js]
autonomous: true
requirements: [EXEC-08, EXEC-10, EXEC-11, EXEC-12, PROV-03, MCP-06]
tags: [fjs-run, integration, week-1, functionalscript]

must_haves:
  truths:
    - "An agent calls finance_schema('vnd.fjs.1099int'), authors a program against its field names, stores it, calls fjs_run, and gets total interest across every stored 1099-INT back with its result and run-record hashes"
    - "A document with box1InterestIncome absent contributes nothing to the total, proving absent-is-skipped rather than coerced to zero"
    - "The full path runs through the real financeMcpServer/mcpStep/stdioTransport stack, not a bespoke test harness"
  artifacts:
    - path: "fjs/server/module.f.js"
      provides: "financeMcpHandlers concatenates financeSchemaTool and fjsRunTool into the existing registry array"
      contains: "fjsRunTool"
  key_links:
    - from: "fjs/server/module.f.js financeMcpHandlers"
      to: "fjs/server/finance_schema/module.f.js financeSchemaTool"
      via: "array concatenation into fromRegistry([...])"
      pattern: "financeSchemaTool"
    - from: "fjs/server/module.f.js financeMcpHandlers"
      to: "fjs/server/fjs_run/module.f.js fjsRunTool"
      via: "array concatenation into fromRegistry([...])"
      pattern: "fjsRunTool"
---

<objective>
Wire `financeSchemaTool` (Plan 03) and `fjsRunTool` (Plans 05-07) into `financeMcpHandlers`'s
existing registry array, then prove `todo/plan.md`'s Week 1 finish line end to end (Success
Criterion 1) through the REAL `financeMcpServer`/`mcpStep`/`stdioTransport` stack: an agent
calls `finance_schema('vnd.fjs.1099int')`, a program is stored that sums `box1InterestIncome`
across every stored 1099-INT (skipping the one with the field absent, never treating it as
zero), `fjs_run` is called, and the returned total is correct — with `resultHash`/`runHash`
both present and resolvable.

Purpose: this is the phase's own name — "the Week 1 Convergence" — and the single proof every
other plan in this phase exists to make possible.
Output: `fjs/server/module.f.js`'s `financeMcpHandlers` grows two entries; a new end-to-end
proof namespace exercises the whole path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fjs-run-run-records-and-the-week-1-convergence/07-CONTEXT.md
@.planning/phases/07-fjs-run-run-records-and-the-week-1-convergence/07-RESEARCH.md
</context>

<interfaces>
<!-- fjs/server/module.f.js — existing, read in full during planning -->
export const financeMcpHandlers = home => cacheKey => fromRegistry([
    ...casToolRegistry(home)(cacheKey),
    ...evoToolRegistry(evo(fileCas(sha256)(home))(cacheKey)),
    casRefreshTool(fileCas(sha256)(home))(cacheKey),
])
// Module header (lines 26-33) already reserves this exact seam as a comment, naming Phase 6/7's fjs_run tool.
export const financeConfig: McpConfig   // unchanged
export const financeMcpServer: (home: string) => Effect<...>   // unchanged

// Existing proof pattern to extend, NOT replace (casRefresh.seedInvisibleUntilRefreshed, lines ~354-417):
// runBatch(state, messages) threads memoryValues/memoryNext forward across NDJSON batches while
// resetting stdout/stderr per batch — the template for: seed documents -> store program -> call
// finance_schema -> call fjs_run -> decode result, all against ONE financeMcpServer instance.

<!-- Plan 03 -->
export const financeSchemaTool: ToolEntry<never>   // or whatever operation type Plan 03 settles on

<!-- Plan 06/07 -->
export const fjsRunTool: ToolEntry<FileCasOperation | MemOp>

<!-- fjs/document/1099int/module.f.js — the dialect the Week-1 program reads, unchanged -->
export const oneZeroNineNineIntSchema   // ...box1InterestIncome: option(string), payerTin, recipientTin, accountNumber, taxYear, formRevision required
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire financeSchemaTool and fjsRunTool into financeMcpHandlers</name>
  <files>fjs/server/module.f.js</files>
  <read_first>fjs/server/module.f.js (module header seam comment, financeMcpHandlers), fjs/server/finance_schema/module.f.js (post Plan 03), fjs/server/fjs_run/module.f.js (post Plan 07)</read_first>
  <action>
    Import `financeSchemaTool` from `./finance_schema/module.f.js` and `fjsRunTool` from
    `./fjs_run/module.f.js`. Add both as additional entries in `financeMcpHandlers`'s
    `fromRegistry([...])` array, after the existing `casRefreshTool` entry. Update the module
    header's reserved-seam comment (currently describing this as future work) to reflect that
    it is now wired, per this repo's convention of module headers stating what is true rather
    than what was planned. Confirm `financeSchemaTool`'s and `fjsRunTool`'s operation-type
    parameters both unify cleanly into `financeMcpHandlers`'s existing
    `FileCasOperation | MemOp` return type with no cast; if `tsc` disagrees, adjust the
    NARROWEST possible annotation at the call site (never widen `financeMcpHandlers`'s own
    signature to paper over a mismatch — a mismatch there is a signal one of the two tools'
    types needs revisiting, not this file).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `tsc` is clean with both tools concatenated into the registry, no widened types, no casts
    - `tools/list` (proven by the EXISTING `toolsListEnumeratesComposedRegistry` proof, extended
      or duplicated to also assert `finance_schema` and `fjs_run` are present in the returned
      tool names) enumerates both new tool names alongside `evo_list`/`cas_refresh`
  </acceptance_criteria>
  <done>Both tools wired into financeMcpHandlers; tools/list enumerates all of them; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Week-1 finish line, end to end</name>
  <files>fjs/server/module.f.js</files>
  <read_first>fjs/server/module.f.js casRefresh.seedInvisibleUntilRefreshed (the runBatch/state-threading pattern), fjs/exact/module.f.js (centsFromString/centsToString, for the proof's own independent expected-total computation)</read_first>
  <behavior>
    - Seed at least three `vnd.fjs.1099int`-shaped revisions directly into the virtual CAS store
      (bypassing `evo_add`, mirroring `casRefresh`'s own seeding technique), each behind its own
      subject: one with `box1InterestIncome: '1234.56'`, one with `box1InterestIncome: '10.00'`,
      and one with the field ABSENT entirely — the absent case is mandatory and must be
      distinguishable from a `'0.00'` case, proving absent-is-skipped rather than
      coerced-to-zero.
    - Store a guest program's source via `cas.write` (the real content-addressed write an agent
      would perform), and separately seed the SAME content as a `JsModule` at the hash-derived
      materialization path in the virtual `root` (per Plan 05's documented scope note: `virtual`
      cannot execute freshly-written bytes as code, so the established `JsModule` stand-in is
      the correct technique for the EXECUTION half of this proof; the WRITE mechanism itself was
      independently proven in Plan 05). The program's `report` uses `ctx.step`/`ctx.pure` to
      enumerate subjects, resolve each one's head/revision/snapshot, sum every PRESENT
      `box1InterestIncome` via `ctx.centsFromString`, and format the total via
      `ctx.centsToString`.
    - Drive, against ONE threaded virtual `State`: `initialize` -> `notifications/initialized`
      -> `tools/call finance_schema {dialect: 'vnd.fjs.1099int'}` (assert the response contains
      `box1InterestIncome` as a field name) -> `tools/call fjs_run {hash: <program hash>}` ->
      decode the response JSON, assert `resultHash`/`runHash` are both present, decode the
      result blob back out of the virtual CAS store by `resultHash` and assert it equals
      `'1244.56'` (computed independently in the proof via `centsFromString`/`centsToString` on
      the two present values, NEVER as a hardcoded literal matching the seeded data by
      coincidence — this doubles as a lightweight perturbation check even though PROV-07 itself
      is a later phase).
  </behavior>
  <action>
    Add a new proof namespace (e.g. `proof.weekOneConvergence`) to `fjs/server/module.f.js`,
    following the `casRefresh.seedInvisibleUntilRefreshed` multi-batch pattern. Seed the three
    documents and the program as described. Drive the full session through
    `financeMcpServer`/`stdioTransport`/`mcpStep` (or the same `runBatch`-style helper this file
    already defines), asserting each step above.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - The proof's expected total is computed via `centsFromString`/`centsToString` inside the
      proof itself, from the two present seeded values, never as a literal copied from having
      run the program once and observed the answer
    - The absent-field document is seeded and its subject IS enumerated by the program (via
      `evoList`/`evoHead`), proving the program actively skips it rather than the proof simply
      never presenting it
    - `resultHash` and `runHash` both decode successfully back out of the virtual CAS store
    - `npm test`'s final total pass count is strictly greater than 134 (the count on `main`
      before this phase) — the phase-gate condition from 07-VALIDATION.md
  </acceptance_criteria>
  <done>The full Week-1 finish line proven end to end through the real server stack; absent-vs-zero distinction proven; npm test green with a materially higher pass count than 134.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composed registry -> real transport | The full session must behave identically to how a real Claude Code client would drive it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Information Disclosure | tools/list exposing fjs_run/finance_schema | accept | Both are the intended, documented public surface of this server; no new disclosure beyond what MCP-02's registration already exposes |
</threat_model>

<verification>
`npm test` green, total pass count strictly greater than 134. `tools/list` enumerates
`finance_schema` and `fjs_run` alongside the existing tools. Manual: `grep -n "casWrite\|evoAdd"
fjs/guest/module.f.js fjs/guest/materialize/module.f.js` returns no output (final re-check of
Success Criterion 3's named static check, post-wiring). `git check-ignore .fjs-run` confirms the
Plan 05 gitignore entry holds.
</verification>

<success_criteria>
All five phase success criteria hold simultaneously through the real, composed
`financeMcpServer`. `todo/plan.md`'s Week 1 finish line is proven, not asserted.
</success_criteria>

<output>
After completion, create `.planning/phases/07-fjs-run-run-records-and-the-week-1-convergence/07-08-SUMMARY.md`
</output>
