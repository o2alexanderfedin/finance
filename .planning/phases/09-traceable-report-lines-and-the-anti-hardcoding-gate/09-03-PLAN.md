---
phase: 09-traceable-report-lines-and-the-anti-hardcoding-gate
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified: [fjs/server/fjs_run/module.f.js]
autonomous: true
requirements: [PROV-07]
tags: [zero-read-gate, response-envelope, phase-9]

must_haves:
  truths:
    - "A program that dispatches zero CAS/Evo reads is refused as an error run, never returned as an ok result"
    - "A successful run's response envelope names its own read count and numeric-literal count beside resultHash/runHash"
    - "The two new envelope fields cannot push a maximal-preview response anywhere near the 64 KiB size guard or the 128 KiB stdio cap"
  artifacts:
    - path: "fjs/server/fjs_run/module.f.js"
      provides: "The zero-observed-reads kill condition and the six-key response envelope"
      contains: "literalCount"
  key_links:
    - from: "fjs/server/fjs_run/module.f.js executeRun"
      to: "fjs/report/audit/module.f.js countNumericLiterals"
      via: "literalCount = countNumericLiterals(sourceText), computed at the same point checkSpecifiers reads the same string"
      pattern: "countNumericLiterals"
    - from: "fjs/server/fjs_run/module.f.js handleRunOutcome"
      to: "vnd.fjs.run's own inputs[]"
      via: "readCount: inputs.length — the SAME array the run record persists, never a second counter"
      pattern: "inputs.length"
---

<objective>
Wire PROV-07's actual kill mechanism into `fjs_run`: a run whose interpreted program dispatched
zero CAS/Evo reads becomes an ERROR result, never a silent 'ok' — this is what actually defeats
`() => pure({ line16: 9137 })` (09-CONTEXT.md). Surface the read count (derived from the SAME
`inputs[]` the run record already carries — never a second counter) and the numeric-literal count
(Plan 09-02's `countNumericLiterals`, computed at the same point `checkSpecifiers` reads the same
source text) in the response envelope beside `resultHash`/`runHash`.

Purpose: this is the runtime half of the anti-hardcoding gate — the half that actually refuses,
where Plan 09-01's type only enforces shape.
Output: `fjs/server/fjs_run/module.f.js` updated — the zero-read gate in `executeRun` (and its
test-mirror `runExecuteRunViaFixture`), and the six-key response envelope in `handleRunOutcome`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/09-traceable-report-lines-and-the-anti-hardcoding-gate/09-CONTEXT.md
@fjs/server/fjs_run/module.f.js
@fjs/report/audit/module.f.js
@fjs/server/response/module.f.js
@fjs/exec/module.f.js
</context>

<interfaces>
<!-- fjs/report/audit/module.f.js — Plan 09-02's output -->
export const countNumericLiterals: (source: string) => number

<!-- fjs/server/response/module.f.js -->
export const previewBytes: number  // 8192
export const guardBytes: number    // 65536
export const sizeGuard: (guardBytes: number) => (previewBytes: number) => (content: string, hash: string) => { readonly preview: string, readonly truncated: boolean }
// tryUtf8/bit_vec length is how sizeGuard measures bytes — reuse the SAME measurement, never content.length

<!-- fjs/exec/module.f.js -->
// Read = readonly [string, readonly unknown[]] — interpret's OWN observed-dispatch record
// Interpreted<T> = Result<readonly [T, readonly Read[]], string>

<!-- fjs/server/fjs_run/module.f.js — CURRENT shapes this plan modifies -->
// RunOutcome<T> = { kind: 'ok', value: T, reads: readonly Read[] } | { kind: 'error', message: string, reads: readonly Read[] }
// executeRun: (materializeHomeRoot) => (cas) => (evoApi) => (input) => Effect<..., RunOutcome<unknown>>
//   — reads sourceText via `const sourceText = utf8ToString(readResult[1])`, then calls
//     materializeProgram/loadProgram/buildRunSnapshot/interpret in nested step() closures.
// runExecuteRunViaFixture: (home) => (cas) => (evoApi) => (hash) => (source) => (report) => (args) =>
//   (pin) => (state) => readonly [State, RunOutcome<unknown>]
//   — mirrors executeRun's OWN steps for testing (decomposed across two virtual sessions); already
//     receives `source` as a parameter.
// handleRunOutcome: (cas) => (programHash) => (programArgs) => (pinned) => (pinFields) => (outcome)
//   => Effect<..., ToolsCallResult>
//   — on outcome.kind==='ok': builds `inputs` from `outcome.reads`, writes result+record, returns
//     `okResult(JSON.stringify({ resultHash, runHash, preview, truncated }))`.
// EXISTING zero-read fixtures used PURELY for plumbing (never business logic), which this plan's
// new gate would otherwise incorrectly fail:
//   - goodProgramSource = 'export const report = ctx => args => ctx.pure("ok")' / goodReport
//     (used by seedGoodProgram/assertSessionSurvivesAFollowingCall to prove SESSION SURVIVAL)
//   - trivialReport inside fjsRunTool.behavioral.exactlyTwoNewHandlerConstructedHashesAppear
//   - tinyReport inside fjsRunTool.responseShape.fourKeysExactlyAndResultAlwaysResolvable
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: literalCount + the zero-read gate in executeRun and its test-mirror</name>
  <files>fjs/server/fjs_run/module.f.js</files>
  <read_first>
    fjs/report/audit/module.f.js (`countNumericLiterals` — the exact function this task imports and
    calls), 09-CONTEXT.md's "The Anti-Hardcoding Audit" decisions ("Zero observed CAS reads is an
    error result" and "The CAS-read count derives from the run record's existing inputs[]" — the
    two decisions this task implements), fjs/exec/module.f.js's `Read`/`Interpreted` typedefs
    (confirms `reads.length` is exactly `interpret`'s own observed-dispatch count).
  </read_first>
  <action>
    Import `countNumericLiterals` from `../../report/audit/module.f.js`.

    Widen the `RunOutcome` typedef's `'ok'` arm to add `readonly literalCount: number` (the
    `'error'` arm is unchanged — a zero-read outcome is expressed as an `'error'` instead, per the
    next paragraph, so it never needs a `literalCount` field of its own).

    In `executeRun`, immediately after `const sourceText = utf8ToString(readResult[1])` — the exact
    point `checkSpecifiers` will read this SAME string from, moments later, inside `loadProgram` —
    add `const literalCount = countNumericLiterals(sourceText)`. This value is already in lexical
    scope for every later nested `step(...)` callback in this function; thread it forward with no
    new parameter.

    At the point where `interpret` succeeds — `const [value, reads] = v`, immediately before the
    existing `return pure({kind:'ok', value, reads})` — branch on `reads.length`: if it is `0`,
    return `pure({ kind: 'error', message: \`report produced zero observed reads over any stored
    document (source contains ${literalCount} numeric literal(s)) — a computed report must read at
    least one stored document\`, reads: [] })` instead. Otherwise return the WIDENED ok shape:
    `pure({ kind: 'ok', value, reads, literalCount })`.

    Apply the identical two changes — computing `literalCount` from the parameter this function
    already receives, and the same `reads.length === 0` branch at its own `interpret`-success point
    — to `runExecuteRunViaFixture`, which mirrors `executeRun`'s own steps for testing per this
    file's established decomposition pattern. It already takes `source` as a parameter, so compute
    `literalCount = countNumericLiterals(source)` there directly.

    Repair the three EXISTING zero-read fixtures this file already defines for PLUMBING tests
    (never business-logic tests), each of which currently reads nothing and would otherwise be
    incorrectly refused by the new gate: `goodProgramSource`/`goodReport` (used by
    `seedGoodProgram`/`assertSessionSurvivesAFollowingCall` to prove session survival — NOT to test
    the zero-read gate), `trivialReport` inside
    `fjsRunTool.behavioral.exactlyTwoNewHandlerConstructedHashesAppear`, and `tinyReport` inside
    `fjsRunTool.responseShape.fourKeysExactlyAndResultAlwaysResolvable`. For each, wrap the existing
    `ctx.pure(<value>)` body in `ctx.step(ctx.evoList('false'), () => ctx.pure(<value>))` — a real,
    harmless single dispatch (listing active subjects, even against an empty store) giving
    `reads.length === 1` with no seeded document required. Update `goodProgramSource`'s stored TEXT
    the same way for consistency (its actual runtime behavior still comes from the `JsModule`
    fixture, not the parsed text, per this file's established split — keeping the two in sync
    avoids a misleading stored-source comment).
  </action>
  <verify>
    <automated>npx tsc</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc` exits 0
    - `grep -n "literalCount = countNumericLiterals" fjs/server/fjs_run/module.f.js` shows it
      computed in both `executeRun` and `runExecuteRunViaFixture`
    - `grep -c "reads.length === 0" fjs/server/fjs_run/module.f.js` is at least 2
    - `grep -c "ctx.evoList('false')" fjs/server/fjs_run/module.f.js` is at least 4 (the three
      repaired fixtures, plus this file's own pre-existing use in `sumReport`)
  </acceptance_criteria>
  <done>`executeRun` and its test-mirror both compute `literalCount` and refuse a zero-read outcome
  as an error; the file's own pre-existing plumbing fixtures still exercise real 'ok' paths.</done>
</task>

<task type="auto">
  <name>Task 2: The six-key response envelope, the size-guard proof, and the decisive zero-read proof</name>
  <files>fjs/server/fjs_run/module.f.js</files>
  <read_first>
    fjs/server/response/module.f.js (`sizeGuard`/`guardBytes`/`previewBytes`/`tryUtf8` — confirm the
    envelope's own size story, and reuse the SAME byte-length measurement, before adding two integer
    fields beside it), this file's own existing `assertPersistedErrorRunRecord` helper (reused, not
    reimplemented, for the new proof's error-record assertion).
  </read_first>
  <action>
    In `handleRunOutcome`'s `outcome.kind === 'ok'` branch, change the final response JSON from
    `{ resultHash, runHash, preview, truncated }` to `{ resultHash, runHash, preview, truncated,
    readCount: inputs.length, literalCount: outcome.literalCount }` — `inputs.length` (already
    computed at the top of this function from `outcome.reads`) IS the observed CAS-read count
    (09-CONTEXT.md: derive it from the SAME `inputs[]` the run record persists, never a second
    counter).

    Update `responseShape.fourKeysExactlyAndResultAlwaysResolvable`'s key-count assertion from the
    4-key sorted array to the 6-key sorted array `['literalCount', 'preview', 'readCount',
    'resultHash', 'runHash', 'truncated']`; rename the leaf (or add a comment) to reflect six keys.
    Confirm this leaf's `tinyReport` (repaired in Task 1 to dispatch one `evoList` read) now yields
    `parsed['readCount'] === 1` and `parsed['literalCount']` equal to
    `countNumericLiterals` of that report's own stored source text — assert both explicitly rather
    than only checking the key set.

    Add one new proof leaf, `zeroReadGate.zeroReadOutcomeBecomesAnErrorResult`: a purpose-built
    zero-read report distinct from Plan 09-04's own verbatim adversary fixture — e.g. `ctx => () =>
    ctx.pure('unused')`, zero effect dispatch — run through `runExecuteRunViaFixture` then
    `handleRunOutcome`. Assert `callResult.isError === true`, the returned text includes `'zero
    observed reads'`, and (reusing `assertPersistedErrorRunRecord`) the persisted run record has
    `status: 'error'`.

    Add one new proof leaf, `sizeGuard.newFieldsStayWellClearOfTheGuard`: build the full six-key
    JSON envelope with `preview` at its maximum possible length (`previewBytes`, 8192 characters of
    a repeated single-byte character) and assert its UTF-8 byte length — measured via `tryUtf8`
    then `bit_vec`'s `length`, the SAME measurement `sizeGuard` itself uses (import both from
    `functionalscript/fjs/text/module.f.js` and `functionalscript/fjs/types/bit_vec/module.f.js`,
    the same two modules `fjs/server/response/module.f.js` already imports) — is strictly less than
    `guardBytes` (65536), proving the two new integer fields cannot push a maximal-preview response
    past the existing 64 KiB guard.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and reports 0 fail
    - `grep -n "readCount: inputs.length" fjs/server/fjs_run/module.f.js` shows the six-key envelope
    - `grep -c "'literalCount', 'preview', 'readCount', 'resultHash', 'runHash', 'truncated'"
      fjs/server/fjs_run/module.f.js` is at least 1
    - `grep -n "zeroReadOutcomeBecomesAnErrorResult" fjs/server/fjs_run/module.f.js` shows the new
      proof
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` is strictly greater than the count recorded
      after Plan 09-02
  </acceptance_criteria>
  <done>The response envelope carries `readCount`/`literalCount` beside `resultHash`/`runHash`,
  proven to stay well clear of the 64 KiB guard, and a zero-read outcome is proven, for the first
  time, to become an error result rather than a silent 'ok'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `interpret`'s observed reads → the "is this run valid" decision | The zero-read gate is the actual enforcement point; everything upstream (Plan 09-01's type) is shape-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering (bypassing the read-count gate with an unused, decoy read) | A future program calls e.g. `ctx.evoList` but never uses the result, satisfying `reads.length > 0` while still returning a hardcoded figure | accept | 09-CONTEXT.md's own accepted-risk framing: PROV-07's read-count is the cheapest, most direct kill of the STATED adversary (`() => pure({line16: 9137})`), not a claim that every possible decoy is caught. Plan 09-04 establishes the perturbation-gate PATTERN (assert the output moves when a real input changes) as the mitigation a later phase's own report programs must reapply per program. |
| T-09-03-02 | Denial of Service / Information Disclosure (envelope growth pushing a response past the stdio cap) | The two new integer fields | mitigate | Task 2's own byte-length proof against a maximal-length `preview`, well clear of `guardBytes` (64 KiB) and the transport's 128 KiB line cap. |
| T-09-03-03 | Repudiation (a run record's `inputs[]`/counts under- or over-stating what really happened) | `inputs[]`, `literalCount` | mitigate (inherited, not re-solved here) | `inputs[]` already derives exclusively from `interpret`'s own observed dispatch (EXEC-05, Phase 3) and is structurally checked against the frozen four on read-back (PROV-03, Phase 7); this plan adds no new way for a guest program to influence either count. |
</threat_model>

<verification>
`npm test` exits 0 with `tsc` clean. `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly
greater than the count recorded after Plan 09-02.
</verification>

<success_criteria>
A zero-observed-read program is proven to become an error result rather than an 'ok' one; a
successful run's response envelope names its own read count and numeric-literal count beside
`resultHash`/`runHash`; both new fields are proven to stay well clear of the existing size guard.
PROV-07's runtime mechanism (short of the perturbation gate and the verbatim adversary, Plan 09-04)
is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/09-traceable-report-lines-and-the-anti-hardcoding-gate/09-03-SUMMARY.md`
</output>
