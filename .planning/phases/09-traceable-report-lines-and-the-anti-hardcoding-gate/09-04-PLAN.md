---
phase: 09-traceable-report-lines-and-the-anti-hardcoding-gate
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-03"]
files_modified: [fjs/server/fjs_run/module.f.js, fjs-run-integration.test.js]
autonomous: true
requirements: [PROV-07]
tags: [perturbation-gate, adversary-fixture, control-leaf, phase-9]

must_haves:
  truths:
    - "Changing one stored document's box value moves a real, ReportLine-shaped report's output"
    - "The verbatim `() => pure({ line16: 9137 })`-shaped adversary — which satisfies every other stated criterion — fails"
    - "The adversary's failure is invariant to the same document change that moves the real program's output — proving the assertion is about the gate, not about a fixture that would have moved anyway"
    - "The real, separate-process fjs_run response names its own read count and literal count"
  artifacts:
    - path: "fjs/server/fjs_run/module.f.js"
      provides: "The perturbation gate's real leg, the verbatim adversary fixture, and its control leaf"
      contains: "hardcodedAdversaryFailsAndIsInvariantToInputChange"
    - path: "fjs-run-integration.test.js"
      provides: "Real-process assertions on the envelope's readCount/literalCount fields"
  key_links:
    - from: "fjs/server/fjs_run/module.f.js proof.antiHardcodingGate"
      to: "fjs/report/line/module.f.js ReportLine"
      via: "the demonstration program's returned line literal, type-annotated against Plan 09-01's own type"
      pattern: "ReportLine"
    - from: "fjs/server/fjs_run/module.f.js hardcodedAdversaryFailsAndIsInvariantToInputChange"
      to: "fjs/report/audit/module.f.js countNumericLiterals"
      via: "the adversary's own error message's literal count, verified against countNumericLiterals directly, never hand-computed"
      pattern: "countNumericLiterals"
---

<objective>
The decisive proof ROADMAP criterion 4 names explicitly: seed a document, run a minimal
ReportLine-shaped report, change the document, run again, and assert the output moved — paired
with a control proving the exact adversary `() => pure({ line16: 9137 })`, written verbatim, fails
identically whether or not the document changes. Extend the real-process integration test to
assert the new envelope fields it already receives.

Purpose: this is the demonstration ROADMAP asks for — "a minimal program that computes something
trivial from real stored documents" — and the literal adversarial proof: a gate built against an
adversary it is never run against is a claim, not a proof.
Output: two new proof leaves inside `fjs/server/fjs_run/module.f.js` (the perturbation gate's real
leg and its control), plus extended assertions in `fjs-run-integration.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/09-traceable-report-lines-and-the-anti-hardcoding-gate/09-CONTEXT.md
@fjs/server/fjs_run/module.f.js
@fjs/report/line/module.f.js
@fjs-run-integration.test.js
</context>

<interfaces>
<!-- fjs/report/line/module.f.js — Plan 09-01's output -->
export type ReportLine = { readonly value: bigint, readonly sources: readonly [Source, ...(readonly Source[])], readonly rule: string }
export type Source = { readonly documentHash: string, readonly boxPath: string, readonly value: string }

<!-- fjs/server/fjs_run/module.f.js — helpers ALREADY in this file, reused (never reimplemented) -->
seedText: (cas) => (text) => Effect<O, string>
runExecuteRunViaFixture: (home) => (cas) => (evoApi) => (hash) => (source) => (report) => (args) => (pin) => (state) => readonly [State, RunOutcome<unknown>]
handleRunOutcome: (cas) => (programHash) => (programArgs) => (pinned) => (pinFields) => (outcome) => Effect<..., ToolsCallResult>
assertPersistedErrorRunRecord: (cas) => (state) => (errorText) => void
// executeRun's zero-read error message (Plan 09-03): 'report produced zero observed reads over any
// stored document (source contains ${literalCount} numeric literal(s)) — a computed report must
// read at least one stored document'
countNumericLiterals: (source: string) => number   // imported into this file already, Plan 09-03

<!-- fjs/cas/evo/module.f.js — headsOf's own rule, confirmed by reading it directly -->
// A subject's heads = revision hashes that no other revision of the SAME subject names as a
// parent. To make a second revision the SOLE live head, its `parents` array must include the
// first revision's own hash: e.add({ parents: [rev1], subject, snapshot: docHash2 }).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The perturbation gate's real leg — a ReportLine-shaped program whose output moves</name>
  <files>fjs/server/fjs_run/module.f.js</files>
  <read_first>
    fjs/report/line/module.f.js (Plan 09-01's `ReportLine`/`Source` types — the exact shape this
    demonstration program's return value must satisfy), fjs/cas/evo/module.f.js's `headsOf`/
    `addRevisionToCache` (confirms a second revision's `parents` must name the first revision's own
    hash to become the subject's sole live head — read this directly rather than assuming), this
    file's own existing `multiDocumentSumAcrossTwoStoredDocuments` proof (the established
    seed/`evoHead`/`evoRevision`/`casRead` read chain this task's own report program reuses).
  </read_first>
  <action>
    Add a new top-level `proof.antiHardcodingGate` section to this file. Inside it, add
    `realProgramOutputMovesWhenTheInputDocumentChanges`:

    Seed a fresh home/cas/evo cache (mirroring this file's own established pattern:
    `fileCas(sha256)(home)`, `initEvo`, `evo(cas)(cacheKey)`). `seedText(cas)('{"box1InterestIncome":
    "10.00"}')` → `docHash1`. `e.add({ parents: [], subject: 'perturbation-subject', snapshot:
    docHash1 })` → assert `[0] === 'ok'`, capture `rev1 = addResult[1]`. Seed a clean zero-import
    stand-in program source (this file's established convention — the `JsModule` fixture below is
    what actually runs) and its hash via `seedText`.

    Define a `JsModule` fixture `demoReport`, shaped `ctx => () => ctx.step(ctx.evoHead(subject),
    headsJson => ...)`, reading the subject's head → revision → snapshot → `box1InterestIncome`
    (the SAME chain `multiDocumentSumAcrossTwoStoredDocuments` already establishes), then building
    a value locally typed `/** @type {import('../../report/line/module.f.js').ReportLine} */`:
    `{ value: ctx.centsFromString(raw), sources: [{ documentHash: rev.snapshot, boxPath:
    'box1InterestIncome', value: raw }], rule: '1040 line 2b' }` — the smallest possible
    demonstration reading one real stored document. Return `ctx.pure(JSON.stringify({ value:
    ctx.centsToString(line.value), sources: line.sources, rule: line.rule }))` — the JSON-safe wire
    projection; NEVER `JSON.stringify` a `bigint` directly (EXACT-05).

    Run once via `runExecuteRunViaFixture`. Assert `outcome.kind === 'ok'`; parse the returned JSON
    and assert `value === '10.00'`, `sources[0].boxPath === 'box1InterestIncome'`,
    `sources[0].documentHash === docHash1`, `rule === '1040 line 2b'`.

    Perturb: `seedText(cas)('{"box1InterestIncome":"20.00"}')` → `docHash2`; `e.add({ parents:
    [rev1], subject: 'perturbation-subject', snapshot: docHash2 })` → assert `[0] === 'ok'` (this
    makes `docHash2`'s revision the SOLE live head, per `headsOf`'s parents-exclusion rule).

    Run the SAME `demoReport` again, against the updated state. Assert `outcome.kind === 'ok'`;
    parse; assert `value === '20.00'` — strictly different from the first run's `'10.00'` — "the
    output moved" because a real input changed.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and reports 0 fail
    - `grep -n "realProgramOutputMovesWhenTheInputDocumentChanges" fjs/server/fjs_run/module.f.js`
      shows the leaf
    - `grep -n "import('../../report/line/module.f.js').ReportLine" fjs/server/fjs_run/module.f.js`
      shows the demonstration program's line literal is type-annotated against Plan 09-01's own type
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` is strictly greater than the count recorded
      after Plan 09-03
  </acceptance_criteria>
  <done>A minimal, real `ReportLine`-shaped report over one stored document is proven to change its
  output when that document's box value changes.</done>
</task>

<task type="auto">
  <name>Task 2: The verbatim adversary fixture and its control leaf</name>
  <files>fjs/server/fjs_run/module.f.js</files>
  <read_first>
    09-CONTEXT.md's "The Perturbation Gate" decisions (the verbatim-adversary requirement and the
    control-leaf requirement, both quoted verbatim there), Plan 09-03's zero-read error message
    (the exact substring this task asserts against, already wired into `executeRun`).
  </read_first>
  <action>
    Inside `proof.antiHardcodingGate`, add `hardcodedAdversaryFailsAndIsInvariantToInputChange`.

    Seed a fresh home/cas/evo cache and one document/subject exactly as Task 1 does
    (`docHash1 = '{"box1InterestIncome":"10.00"}'`, `rev1` via `e.add({parents: [], subject:
    'adversary-subject', snapshot: docHash1})`) — even though the adversary never reads it, the
    control needs a real document to perturb.

    The adversary's stored source text, written VERBATIM — this is the literal proof fixture, not a
    paraphrase: `export const report = ctx => () => ctx.pure({ line16: 9137 })`. The inner `() =>
    ctx.pure({ line16: 9137 })` IS 09-CONTEXT.md's own quoted phrase, adapted only by the
    unavoidable `ctx.` prefix — a stored program has zero imports (EXEC-07) and cannot reach a bare
    `pure` any other way. The matching `JsModule` fixture is `ctx => () => ctx.pure({ line16: 9137
    })` — a plain object literal return, zero effect dispatch, never touching `ctx.casRead`/
    `evoList`/`evoHead`/`evoRevision`.

    Run it via `runExecuteRunViaFixture` against the state BEFORE perturbation. Assert
    `outcome.kind === 'error'`. Assert `outcome.message` includes `'zero observed reads'`, and
    includes the literal count computed by calling `countNumericLiterals` (already imported into
    this file, Plan 09-03) directly on this exact adversary source string — never hand-computed —
    so the assertion cannot silently drift from the audit's own logic.

    Perturb the SAME document exactly as Task 1 does (`docHash2 = '{"box1InterestIncome":
    "20.00"}'`, `e.add({parents: [rev1], subject: 'adversary-subject', snapshot: docHash2})`).

    Run the SAME adversary fixture again, against the perturbed state. Assert `outcome.kind ===
    'error'` again — and, the control: assert `outcome.message` is IDENTICAL, character for
    character, to the first run's message. The document changed; the adversary's outcome did not,
    because it never read the document at all. This is what makes the assertion about the gate
    rather than about a fixture that would have moved anyway.

    Feed either outcome through `handleRunOutcome` and this file's own `assertPersistedErrorRunRecord`
    helper, confirming a `status: 'error'` run record is still persisted for a zero-read refusal
    (PROV-03: provenance that covers only successes is not provenance).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and reports 0 fail
    - `grep -n "ctx.pure({ line16: 9137 })" fjs/server/fjs_run/module.f.js` shows the verbatim
      adversary text present, unparaphrased
    - `grep -n "hardcodedAdversaryFailsAndIsInvariantToInputChange" fjs/server/fjs_run/module.f.js`
      shows the control leaf
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` is strictly greater than the count recorded
      after Task 1
  </acceptance_criteria>
  <done>The exact adversary ROADMAP criterion 4 names is proven, by execution, to fail — and to
  fail identically regardless of a real document change, which is what makes the failure about the
  gate rather than about this one fixture.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the real-process integration test with the new envelope fields</name>
  <files>fjs-run-integration.test.js</files>
  <read_first>
    fjs-run-integration.test.js itself (the existing decisive `fjs_run` call and its current
    assertions — `resultHash`/`runHash` — this task extends, never replaces), 09-CONTEXT.md's
    "Scope and Wiring" decision (this file's existing call gains assertions; no new MCP tool, so no
    new entry needed in the runtime tool-coverage assertion).
  </read_first>
  <action>
    In the existing `runResponse`/`parsed` block (the decisive `fjs_run` call, already reading
    three seeded 1099-INT documents where one has an absent `box1InterestIncome`), add two
    assertions on the response envelope: `typeof parsed.readCount === 'number' &&
    parsed.readCount > 0` (this run's own program dispatches `evoList` plus, per seeded subject,
    `evoHead`/`evoRevision`/`casRead` — several reads), and `typeof parsed.literalCount ===
    'number' && parsed.literalCount >= 1` (this program's own stored source text contains at least
    the `0n` accumulator literal). Do NOT import `fjs/report/audit/module.f.js` into this
    `@ts-nocheck` root-level file (AGENTS.md's file-conventions note: root-level impure files stay
    minimal) — assert only the type and the lower bound, which needs no cross-import.
  </action>
  <verify>
    <automated>node --test fjs-run-integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test fjs-run-integration.test.js` exits 0 (this ONE file is the documented exception
      to the "`node --test &lt;file&gt;` is a fake pass" rule — it is an ordinary `node:test` file,
      not a `.f.js` proof module)
    - `grep -n "parsed.readCount" fjs-run-integration.test.js` shows the new assertion
    - `grep -n "parsed.literalCount" fjs-run-integration.test.js` shows the new assertion
    - `npm test` still exits 0 (the full suite, `tsc` included, stays green after this file's edit)
  </acceptance_criteria>
  <done>The real, separate-process `fjs_run` call names its own read count and literal count, and
  the whole suite (unit + integration) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this proof suite's own before/after comparison methodology | A perturbation proof that always shows "movement" regardless of whether the gate works is worthless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering (a perturbation proof that would show "movement" for any rerun, gate or no gate) | `realProgramOutputMovesWhenTheInputDocumentChanges` | mitigate | The control leaf (`hardcodedAdversaryFailsAndIsInvariantToInputChange`) proves the SAME before/after methodology yields an IDENTICAL result for a program that reads nothing — directly modeled on the STATE.md Phase-6 lesson that a "which message/did it move" proof cannot distinguish the real property from a spy that never fires, without a leg that must NOT move. |
| T-09-04-02 | Spoofing (a plausible-looking hardcoded figure reaching a user) | The verbatim `9137` adversary | mitigate | Proven to never reach an `'ok'` outcome in either run — `outcome.kind === 'error'` both times — so the figure is never returned, never written to CAS, and never appears in any response envelope. |
| T-09-04-03 | Information Disclosure (a root-level integration test importing production internals it doesn't need) | Task 3's own scope | mitigate | Kept to a numeric lower-bound/`typeof` assertion with no new import into the `@ts-nocheck` root file, per AGENTS.md's minimal-root-file convention. |
</threat_model>

<verification>
`npm test` exits 0 with `tsc` clean. `node --test fjs-run-integration.test.js` exits 0 independently.
`node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly greater than the count recorded after
Plan 09-03.
</verification>

<success_criteria>
ROADMAP Phase 9 Success Criterion 4 is satisfied: a perturbation gate proves a real program's
output moves when an input document changes, and the exact adversary
`() => pure({ line16: 9137 })` — which satisfies every other stated criterion — fails, with a
control proving that failure is invariant to the same document change, not merely a fixture that
would have moved anyway. The real-process integration test names the new envelope fields.
</success_criteria>

<output>
After completion, create `.planning/phases/09-traceable-report-lines-and-the-anti-hardcoding-gate/09-04-SUMMARY.md`
</output>
