---
phase: 09-traceable-report-lines-and-the-anti-hardcoding-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [fjs/report/audit/module.f.js]
autonomous: true
requirements: [PROV-07]
tags: [numeric-literal-audit, anti-hardcoding, phase-9]

must_haves:
  truths:
    - "A numeric literal embedded inside an identifier, a string, a template literal, or a comment is never counted"
    - "The verbatim adversary fixture's one literal (9137) is counted exactly once, and the '16' inside 'line16' is never counted"
    - "The audit is a pure count — it never refuses or throws on a program's own account"
  artifacts:
    - path: "fjs/report/audit/module.f.js"
      provides: "countNumericLiterals — the numeric-literal audit run on a stored program's source text"
      contains: "countNumericLiterals"
---

<objective>
Build the numeric-literal audit (PROV-07, the reported half): a pure count of numeric-literal
tokens in a stored report program's source text, immune to being fooled by a digit embedded in an
identifier, a string, a template literal, or a comment. This count is REPORTED, never refused
(09-CONTEXT.md) — legitimate programs contain numeric literals (a bracket index, a zero, a scale);
the actual anti-hardcoding kill condition is the separate, unconditional zero-observed-CAS-reads
check Plan 09-03 wires into `fjs_run` itself.

Purpose: gives Criterion 3 ("a numeric-literal audit... visible to the user") a concrete,
independently-testable implementation before it is wired into the response envelope.
Output: `fjs/report/audit/module.f.js` — `countNumericLiterals` and its honesty proofs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/09-traceable-report-lines-and-the-anti-hardcoding-gate/09-CONTEXT.md
@fjs/guest/materialize/module.f.js
</context>

<interfaces>
<!-- fjs/guest/materialize/module.f.js — the linear-time regex idiom this module's own
     string-stripping regexes copy, to avoid catastrophic backtracking -->
<!-- specifierPattern uses a negated character class plus an escaped-any alternative per quote
     kind: (?:[^'"]*) style bodies, never a lazy wildcard -->
export const checkSpecifiers: (allowed: readonly string[]) => (source: string) => Result<string, string>
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: countNumericLiterals — strip strings/comments, then count</name>
  <files>fjs/report/audit/module.f.js</files>
  <read_first>
    fjs/guest/materialize/module.f.js (`specifierPattern`'s negated-character-class technique — the
    precedent this module's own string-stripping regexes copy for linear-time safety, and the
    module SEC-02's specifier gate lives in, which 09-CONTEXT.md names as "the same place" this
    audit conceptually runs alongside), 09-CONTEXT.md's "The Anti-Hardcoding Audit" decisions
    (REPORTED never refused; not a parser).
  </read_first>
  <behavior>
    - Empty source: 0.
    - A plain literal (`const x = 42`): 1.
    - Multiple distinct literals in one source (`const a = 1; const b = 2.5; const c = 3n`): 3
      (integer, decimal, and bigint-suffixed literals all count).
  </behavior>
  <action>
    Create `fjs/report/audit/module.f.js` with a module docstring naming PROV-07 and this module's
    two properties: REPORTED never refused, and "not a parser — strings/templates/comments are
    stripped textually first, then every remaining token is counted."

    Define a private `stripStringsAndComments` function `(source: string) => string`: sequentially
    `.replace(...)` away every single-quoted span (pattern shape: a `'`, then a negated-class body
    allowing an escaped character — `(?:[^'\\]|\\.)*` — then a closing `'`; replace the whole match
    with `''`), every double-quoted span (same shape with `"`, replaced with `""`), and every
    backtick-quoted template span (same shape with `` ` ``, replaced with `` `` ``) — each pattern
    mirrors `checkSpecifiers`'s own `specifierPattern` negated-class-plus-escape idiom, which this
    repo already established as free of catastrophic backtracking. Then strip every `//` line
    comment (`\/\/[^\n]*`, replaced with nothing) and every `/* */` block comment
    (`\/\*[\s\S]*?\*\/`, replaced with nothing).

    Define and export `countNumericLiterals`, `(source: string) => number`: run
    `stripStringsAndComments` first, then `.match(/\b\d+(?:\.\d+)?n?\b/g)` against the stripped
    text, returning `0` when `match` returns `null`, else the matched array's `length`. Document
    inline why `\b` alone is sufficient to exclude a digit embedded in an identifier (e.g.
    `box1InterestIncome`'s `1`): a digit immediately preceded by a letter is not a word-boundary
    position at all (both are `\w` characters), so the pattern never even attempts to start a match
    there — no special-casing is needed.

    Add a `proof` export (import `assertEq` from `functionalscript/fjs/asserts/module.f.js`) with
    the three leaves the `<behavior>` block above names, plus `zeroOnEmptySource`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and reports 0 fail
    - `grep -n "export const countNumericLiterals" fjs/report/audit/module.f.js` shows the export
    - `grep -c "Math.round\|toFixed\|parseFloat" fjs/report/audit/module.f.js` is exactly 0 (this
      module reads source TEXT only — it never touches a computed money value)
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` is strictly greater than 240
  </acceptance_criteria>
  <done>`countNumericLiterals` exists, is pure, counts plain and multiple literals correctly, and
  uses the same linear-time regex idiom SEC-02's specifier gate already established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the tokenizer is honest, including the exact verbatim adversary fixture</name>
  <files>fjs/report/audit/module.f.js</files>
  <read_first>
    (Same file as Task 1 — no new source to read.)
  </read_first>
  <behavior>
    - A digit embedded in an identifier (`const box1InterestIncome = ctx.casRead(a)`): 0 — the `1`
      inside `box1InterestIncome` is never counted.
    - A digit inside a single-quoted string (`const s = '2024 tax year'`): 0.
    - A digit inside a template literal (`` const s = `rev ${x} 2024` ``): 0.
    - A digit inside a `//` line comment, alongside a real literal on the same source
      (`// 2024\nconst x = 1`): 1 — only the real literal counts.
    - A digit inside a `/* */` block comment, alongside a real literal
      (`/* 2024 */ const x = 1`): 1.
    - The verbatim adversary fixture text `export const report = ctx => () => ctx.pure({ line16:
      9137 })` (09-CONTEXT.md's own quoted phrase, adapted to this project's `ctx`-based entry
      point — see Plan 09-04, which reuses this exact string): exactly 1 — the `16` inside
      `line16` is never counted (no word boundary between the letter and the digit); only `9137`
      is.
  </behavior>
  <action>
    Add the six proof leaves the `<behavior>` block above describes to `fjs/report/audit/module.f.js`'s
    existing `proof` export, using `assertEq`. Name the adversary-fixture leaf
    `countsTheVerbatimAdversaryFixtureLiteralExactlyOnce` and write the fixture string exactly as
    given above — this exact string is the same one Plan 09-04 later stores and runs for real, so
    keep it byte-for-byte identical (a source comment noting this cross-plan reuse is welcome).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and reports 0 fail
    - `grep -n "countsTheVerbatimAdversaryFixtureLiteralExactlyOnce" fjs/report/audit/module.f.js`
      shows the leaf
    - `grep -c "ctx.pure({ line16: 9137 })" fjs/report/audit/module.f.js` is at least 1 (the
      verbatim text, unparaphrased)
    - `node --test 2>&1 | grep -c '^✔ import("./fjs/'` is strictly greater than the count recorded
      after Task 1
  </acceptance_criteria>
  <done>The tokenizer is proven not to be fooled by a digit inside an identifier, a string, a
  template literal, or a comment, and to count the exact verbatim adversary fixture's one literal
  correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored-program source text → numeric-literal count | The count is informational only; nothing downstream refuses on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Denial of Service (ReDoS) | `stripStringsAndComments`'s per-quote-kind regexes | mitigate | Each uses a negated character class plus an escaped-any alternative (`[^'\\]\|\\.`), the identical linear-time shape `fjs/guest/materialize/module.f.js`'s own `checkSpecifiers`/`specifierPattern` already established and relies on; no nested quantifiers over ambiguous alternation are introduced. |
| T-09-02-02 | Tampering (of the audit's own honesty) | `countNumericLiterals` undercounting a literal legitimately embedded inside a template literal's `${...}` expression, since the whole template span is stripped | accept | Documented limitation in the module's own header. Per 09-CONTEXT.md, this audit is REPORTED, never refused — an undercount here has no security consequence, since the actual anti-hardcoding kill condition is the zero-observed-CAS-reads check (Plan 09-03), computed independently from `interpret`'s own dispatch, not from this text-only heuristic. |
</threat_model>

<verification>
`npm test` exits 0 with `tsc` clean. `node --test 2>&1 | grep -c '^✔ import("./fjs/'` strictly
greater than 240 after this plan completes.
</verification>

<success_criteria>
`countNumericLiterals` correctly reports the numeric-literal count of arbitrary program source
text, proven immune to digits embedded in identifiers, strings, templates, and comments, and
proven correct on the exact verbatim adversary fixture Plan 09-04 later executes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-traceable-report-lines-and-the-anti-hardcoding-gate/09-02-SUMMARY.md`
</output>
