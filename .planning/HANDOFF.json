{
  "version": "1.0",
  "timestamp": "2026-08-17T21:30:00.000Z",
  "phase": "none",
  "phase_name": "milestone-v2-complete-v1.0.0-released",
  "phase_dir": null,
  "plan": 0,
  "task": 0,
  "total_tasks": 0,
  "status": "released",
  "position_note": "All 120 requirements complete, v1.0.0 tagged and pushed. Nothing in flight, no open PRs, no stashes, one worktree, two branches.",
  "git": {
    "branch": "develop",
    "note": "NO SHAs ARE RECORDED HERE ON PURPOSE. The previous version pinned develop=5f75fcd main=66c0873 and both were stale within the hour, because the commit that wrote them moved develop past them. Present-tense git claims are comparisons; run the verify commands.",
    "tag": "v1.0.0",
    "working_tree": "clean apart from the untracked type-level/ orphan from another session",
    "open_prs": 0,
    "worktrees": 1,
    "verify": [
      "git fetch -q origin",
      "test \"$(git rev-parse develop)\" = \"$(git rev-parse origin/develop)\"   # develop is pushed",
      "test \"$(git rev-parse main)\"    = \"$(git rev-parse origin/main)\"      # main is pushed",
      "git merge-base --is-ancestor develop main   # everything on develop is in main. NOT the reverse: main carries a --no-ff merge commit per release, so --is-ancestor main develop is false on a healthy repo.",
      "test -z \"$(git diff main develop)\"          # identical trees -- this is the real equivalence check",
      "git rev-parse -q --verify v1.0.0            # tag exists"
    ]
  },
  "suite": {
    "command": "npm test",
    "exit": 0,
    "measured_on": "fd6702c, the v1.0.0 release point",
    "tests": "2242/2242",
    "project_local_proof_leaves": 2218,
    "tsc": "clean",
    "gate_command": "npm test 2>&1 | grep -c '^✔ import(\"./fjs/'",
    "warning": "NEVER gate on npm test's total (AGENTS.md line 127) -- it includes ~2100 vendored submodule proofs and moves with submodule state. The submodule is deliberately de-initialised."
  },
  "remaining_tasks": [
    {
      "id": 1,
      "name": "upstream-mjs-migration: 0.44/0.45 dropped the .js emit; migration measured at 288 residual errors, each fixable only by a forbidden construct",
      "status": "blocked_upstream_with_specified_fix"
    },
    {
      "id": 2,
      "name": "tax-return-report-source-k1-routes-unexercised: three stored-program TEXT route lines executed by no fixture; the cheap fixture is a fake pass",
      "status": "open_with_recipe"
    },
    {
      "id": 3,
      "name": "schedule-b-omits-k1-interest-and-dividends: K-1 interest can cross the $1,500 threshold invisibly to Schedule B's own line 4",
      "status": "open_with_reasoning"
    },
    {
      "id": 4,
      "name": "Three upstream fjs notes re-verified still open against 0.45.0: upstream-mcp-protocol-version-negotiation, upstream-node-spawn-effect, upstream-total-match-dispatch",
      "status": "open"
    }
  ],
  "blockers": [],
  "human_actions_pending": [],
  "decisions": [
    {
      "decision": "Stay on functionalscript 0.43.1",
      "rationale": "0.44/0.45 dropped the .js emit: 1287/1288 tsc errors. Rewriting all 396 files was MEASURED on a throwaway snapshot and still left 288 errors, because the shipped .d.mts files import the core vocabulary and never re-export it. Every mechanical fix is a cast, an any, or a redeclared type -- all forbidden. The literal requirement (take 0.43.0) is satisfied.",
      "phase": "18"
    },
    {
      "decision": "Remove the orphan OCR conversion rather than wiring it",
      "rationale": "from_ocr was imported by nothing and ocr_amount only by it; PDF/OCR extraction in code is permanently out of scope and the vision pass emits both forms directly. The vnd.fjs.ocr DIALECT stays registered.",
      "phase": "MAINT-01"
    },
    {
      "decision": "The engine reaches guest programs via guestCtx, never a server tool",
      "rationale": "A finance_compute_1040 tool would let the agent stop authoring programs. tools/list has stayed at 13 through twelve phases; that count is the check.",
      "phase": "21"
    },
    {
      "decision": "An asserted fact whose absence must not read as 'no' is two exact strings, not option(true)",
      "rationale": "option(true) makes absence indistinguishable from a denial, and for SSTB and the EIC facts the wrong default is the one that GRANTS money.",
      "phase": "28/32/27"
    }
  ],
  "uncommitted_files": [],
  "next_action": "Nothing required. The milestone is closed and released. If work resumes, the SIX open fjs/todo/ notes are the candidate list (fjs/todo/ holds nine files; three are satisfied specs kept in their original present tense, each with a corrected status line). NOTE: an earlier version of this field said Phase 17 'never ran' and that a defect deferred to it surfaced 'months later'. Both are false. Phase 17 shipped as PR #84 at 03:26 PDT on 2026-08-17 and Phase 18 as PR #87 at 14:10 PDT; the deferral was written at 21:10 PDT, SEVEN HOURS AFTER the phase it defers to had finished. A deferral aimed at a completed phase is collected by nobody.",
  "context_notes": "Two failures in this session are worth more than the features. FIRST: `git push` reported success while doing nothing, three times, because an agent run un-isolated in the main checkout left HEAD on its own branch -- caught only because main's TEST COUNT came back 2185 instead of 2242, not by checking a ref. SECOND: the proof suite had been running TWICE for months, recorded as a 'double-counted metric, severity advisory, remedy: de-duplicate'; it was double-RUN, ~53s per invocation, and the documented workaround is exactly why nobody looked again. A workaround that works is the most expensive kind of bug. The through-line across eleven occurrences this session: only a COMPARISON stops a count from drifting -- naming the hazard in a comment failed three separate times, while the two gates added (theHandTypedListNamesEveryModeledKind, dialect_parity) each caught a real omission within a day."
}