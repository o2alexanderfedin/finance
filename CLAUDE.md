# CLAUDE.md

See [AGENTS.md](./AGENTS.md).

That file is the single source of truth for how to work in this repo — conventions, file
layout, code style, testing, and commands. It is written for any agent, not just Claude.

**Keep this file a pointer.** Do not copy conventions here: two copies drift, and the one
you are reading is the one that would go stale. If something belongs in an agent's
instructions, it belongs in `AGENTS.md`.
